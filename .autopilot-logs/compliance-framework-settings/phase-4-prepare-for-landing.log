All 17 review findings fixed and submitted for review. Here's the summary:

## Prepare for Landing Complete

**Branch:** `feat/live-dashboard-metrics`
**Work session:** ws-8f9e
**All 32 tests passing**

### Findings Fixed: 17/17

**P1 Critical (4):**
- Added `authorized?/1` helper that re-queries membership on every mutating event (toggle, save)
- Nil-guarded membership in mount to prevent `FunctionClauseError`
- Fixed crafted-event test to verify toggle not rendered for non-admins
- Added dirty state tracking tests (disabled/enabled/re-disabled Save button)

**P2 Warning (9):**
- Gated `update_field` handler on `can_edit?` guard clause
- Fixed `maybe_put_integer` to discard invalid strings instead of storing them
- Reset `form_data` in `update/2` when association `updated_at` changes
- Added `@impl true` on all `handle_event` clauses
- Changed sidebar compliance link to `~p` sigil for compile-time route verification
- Added upper bounds: retention_days <= 36,500, verification_cadence_hours <= 8,760, additional_required_fields <= 50
- Simplified `Map.take` to atom keys only in `update_framework_config`
- Added test for `additional_required_fields` comma parsing/trimming/save
- Removed duplicate `iso27001` registry alias, keeping canonical `iso_27001`

**P3 Suggestion (3):**
- Pass association directly in `{:framework_updated, id, assoc}` message to avoid extra DB query
- Flash message assertions addressed (code verified correct; component flash not testable via LiveView test framework)
- Added test for `effective_config/2` with empty `config_overrides`

**P4 Nice-to-have (1):**
- Added test for `count_active_frameworks/1`
