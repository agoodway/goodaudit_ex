Import complete. Here's the summary:

**Epic:** `td-c278a9` — Live Dashboard Metrics

**24 issues created** (1:1 with tasks.md subtasks + 1 review gate):
- **4 backend** — context query functions (`count_logs`, `recent_logs`, `count_active_api_keys`, `count_active_frameworks`)
- **12 frontend** — mount updates, 4 metric card replacements, recent activity rendering, checklist, system status comment, 4 helper functions
- **7 test** — one per context function + 3 LiveView integration tests
- **1 review gate** — blocks until all work is done, then triggers `/review-work`

**Dependencies wired:** backend → frontend (mount) → UI cards/helpers → tests → review gate

All 23 checkboxes in `tasks.md` marked as `[x]` to prevent duplicate imports. Ready for `/takeoff`.
