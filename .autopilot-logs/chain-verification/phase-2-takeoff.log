OpenAI Codex v0.107.0 (research preview)
--------
workdir: /Users/tbrewer/projects/goodway/goodaudit
model: gpt-5.3-codex
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR]
reasoning effort: high
reasoning summaries: none
session id: 019cb58c-f0b9-7db2-890c-fb6f45426046
--------
user
Run /takeoff for the change named "chain-verification" located at openspec/changes/chain-verification/. Do not ask me any questions — use your best judgment for any decisions. IMPORTANT: Do NOT create a new feature branch. You are already on the correct feature branch from a previous change in this pipeline. Stay on the current branch, find the epic, and begin implementation immediately.
mcp: context7 starting
mcp: sequential-thinking starting
mcp: zread starting
mcp: perplexity starting
mcp: playwright starting
mcp: sequential-thinking ready
mcp: perplexity ready
mcp: context7 ready
mcp: playwright ready
2026-03-03T21:13:59.852934Z ERROR rmcp::transport::common::reqwest::streamable_http_client: unexpected content type: None
2026-03-03T21:13:59.852958Z ERROR rmcp::transport::worker: worker quit with fatal: Unexpected content type: None, when send initialized notification
mcp: zread failed: MCP client for `zread` failed to start: MCP startup failed: handshaking with MCP server failed: Send message error Transport [rmcp::transport::worker::WorkerTransport<rmcp::transport::streamable_http_client::StreamableHttpClientWorker<reqwest::async_impl::client::Client>>] error: Transport channel closed, when send initialized notification
mcp startup: ready: sequential-thinking, perplexity, context7, playwright; failed: zread
codex
Using the `takeoff` skill now because you explicitly requested `/takeoff`; I’ll load its workflow, locate the epic for `openspec/changes/chain-verification/`, and start implementation on the current branch without creating a new branch.
exec
/bin/zsh -lc "pwd && ls -la .codex/skills/takeoff && sed -n '1,220p' .codex/skills/takeoff/SKILL.md" in /Users/tbrewer/projects/goodway/goodaudit exited 1 in 52ms:
/Users/tbrewer/projects/goodway/goodaudit
ls: .codex/skills/takeoff: No such file or directory
codex
The skill isn’t in the repo-local `.codex/skills` folder, so I’m loading it from the listed global path at `/Users/tbrewer/.agents/skills/takeoff/SKILL.md` and then executing its steps.
exec
/bin/zsh -lc "ls -la /Users/tbrewer/.agents/skills/takeoff && sed -n '1,260p' /Users/tbrewer/.agents/skills/takeoff/SKILL.md" in /Users/tbrewer/projects/goodway/goodaudit succeeded in 51ms:
total 8
drwxr-xr-x@  3 tbrewer  staff    96 Feb 12 09:01 .
drwxr-xr-x@ 45 tbrewer  staff  1440 Feb 20 13:20 ..
-rw-r--r--@  1 tbrewer  staff  3827 Feb 12 09:01 SKILL.md
---
name: takeoff
description: Start work on a td epic by creating a feature branch, starting a work session, activating child issues, and implementing them. Use when the user says "takeoff", "take off", "start the epic", "begin work on epic", or wants to kick off implementation of an OpenSpec change.
---

# Takeoff

Create a feature branch, start a td work session, and activate all child issues for an epic.

## Workflow

### 1. Identify the epic

Determine which epic to work on. If not specified by the user, check:

```bash
codex
I’ve loaded the takeoff workflow and will now locate the matching `td` epic for `chain-verification`, inspect its tree, and activate the appropriate child issues while keeping you on the current branch.
exec
/bin/zsh -lc 'td epic list' in /Users/tbrewer/projects/goodway/goodaudit succeeded in 51ms:
P2 [open] td-9db2d6: Chain Verification
exec
/bin/zsh -lc 'td tree td-9db2d6' in /Users/tbrewer/projects/goodway/goodaudit succeeded in 52ms:
epic td-9db2d6: Chain Verification
├── task td-1d7eca: Create GA.Audit.Verifier module [open] 
├── task td-46c84b: Add GA.Audit.verify_chain(account_id) delegation [open] 
├── task td-8a9954: Implement verify(account_id) streaming entrypoint [open] 
├── task td-486ab1: Implement do_verify_batches/4 recursive batch loader [open] 
├── task td-5806a7: Implement verify_entry/4 chain and checkpoint checks [open] 
├── task td-7ae373: Create GA.Audit.CheckpointWorker GenServer module [open] 
├── task td-e596e4: Implement CheckpointWorker init/1 scheduling [open] 
├── task td-39f158: Implement checkpoint tick over active accounts [open] 
├── task td-cb684b: Add CheckpointWorker to GA.Application children [open] 
├── task td-bb90d7: Test valid chain verification report [open] 
├── task td-c5e429: Test tampered checksum detection [open] 
├── task td-166913: Test sequence gap detection within account [open] 
├── task td-dbf670: Test checkpoint anchor validation outcomes [open] 
├── task td-c4e66e: Test verification duration tracking [open] 
├── task td-f96191: Test account-scoped verification isolation [open] 
├── task td-54c8dc: Test worker checkpoints all active accounts [open] 
├── task td-e39b5f: Test worker skips suspended accounts [open] 
├── task td-ad7419: Test worker handles accounts with no entries [open] 
└── task td-833971: Review all work for Chain Verification [open] 
exec
/bin/zsh -lc 'git branch --show-current' in /Users/tbrewer/projects/goodway/goodaudit succeeded in 52ms:
main
codex
I found epic `td-9db2d6` and the full task tree; next I’m starting a work session for it and tagging all implementation/test issues except the review gate.
exec
/bin/zsh -lc 'td ws start "Epic: Chain Verification"' in /Users/tbrewer/projects/goodway/goodaudit exited 1 in 73ms:
Error: work session already active
exec
/bin/zsh -lc 'td ws current' in /Users/tbrewer/projects/goodway/goodaudit succeeded in 257ms:
FILES CHANGED:
  .../account-hmac-keys/phase-2-commit.log +1 -0
  .../account-hmac-keys/phase-3-commit.log +1 -0
  .../phase-3-pre-landing-check.log +1 -0
  .../account-hmac-keys/phase-4-commit.log +1 -0
  .../phase-4-prepare-for-landing.log +1 -0
  .../account-hmac-keys/phase-5-commit.log +1 -0
  .../account-hmac-keys/phase-5-land-the-plane.log +1 -0
  .autopilot-logs/audit-context/phase-1-commit.log +1 -0
  .../audit-context/phase-1-prepare-for-takeoff.log +1 -0
  .autopilot-logs/audit-context/phase-2-commit.log +1 -0
  .autopilot-logs/audit-context/phase-2-takeoff.log +1 -0
  .autopilot-logs/audit-context/phase-3-commit.log +1 -0
  .../audit-context/phase-3-pre-landing-check.log +1 -0
  .autopilot-logs/audit-context/phase-4-commit.log +1 -0
  .../audit-context/phase-4-prepare-for-landing.log +1 -0
  .autopilot-logs/audit-context/phase-5-commit.log +1 -0
  .../audit-context/phase-5-land-the-plane.log +1 -0
  .autopilot-logs/audit-schemas/phase-1-commit.log +1 -0
  .../audit-schemas/phase-1-prepare-for-takeoff.log +1 -0
codex
There’s already an active `td` work session from the previous pipeline step, so I’m switching context by tagging the `chain-verification` issues into the active session and starting implementation immediately.
exec
/bin/zsh -lc 'td ws --help' in /Users/tbrewer/projects/goodway/goodaudit succeeded in 52ms:
Manage multi-issue work sessions.

Usage:
  td ws [command]

Aliases:
  ws, worksession

Available Commands:
  current             Show current work session state
  end                 End work session without handoff
  handoff             End work session and generate handoffs for all tagged issues
  list                List recent work sessions
  log                 Log to the work session
  show                Show details of a past work session
  start               Start a named work session
  tag                 Associate issues with the current work session (auto-starts open issues)
  untag               Remove issues from work session

Flags:
exec
/bin/zsh -lc "rg --files | rg '"'^'"\\.td/'" in /Users/tbrewer/projects/goodway/goodaudit exited 1 in 51ms:

exec
/bin/zsh -lc 'ls -la' in /Users/tbrewer/projects/goodway/goodaudit succeeded in 52ms:
total 8
drwxr-xr-x@  12 tbrewer  staff   384 Mar  3 15:39 .
drwxr-xr-x@ 140 tbrewer  staff  4480 Mar  2 08:46 ..
drwxr-xr-x@   9 tbrewer  staff   288 Mar  3 16:08 .autopilot-logs
drwxr-xr-x@   5 tbrewer  staff   160 Mar  2 11:14 .claude
drwxr-xr-x@   4 tbrewer  staff   128 Mar  1 16:53 .codex
drwxr-xr-x@   4 tbrewer  staff   128 Mar  1 16:53 .factory
drwxr-xr-x@  13 tbrewer  staff   416 Mar  3 16:13 .git
-rw-r--r--@   1 tbrewer  staff     8 Mar  3 14:10 .gitignore
drwxr-xr-x@   8 tbrewer  staff   256 Mar  3 08:39 .opencode
drwxr-xr-x@   8 tbrewer  staff   256 Mar  3 16:14 .todos
drwxr-xr-x@  22 tbrewer  staff   704 Mar  3 15:24 app
drwxr-xr-x@   5 tbrewer  staff   160 Mar  1 16:53 openspec
exec
/bin/zsh -lc "find .todos -maxdepth 3 -type f | sed -n '1,200p'" in /Users/tbrewer/projects/goodway/goodaudit succeeded in 52ms:
.todos/agent_errors.jsonl
.todos/config.json
.todos/command_usage.jsonl
.todos/config.json.lock
.todos/issues.db
.todos/db.lock
codex
I’m closing the prior epic’s active work session and creating a fresh `chain-verification` session so the issue activation and logs stay clean for this change.
exec
/bin/zsh -lc 'td ws end' in /Users/tbrewer/projects/goodway/goodaudit succeeded in 52ms:
Warning: No handoff recorded for ws-4b25
Tagged issues remain in_progress: td-a3fc52, td-4e6211, td-e654a8, td-49194f, td-06f49d, td-3a398f, td-313d91, td-0a6c38, td-33d5bf, td-c2e888, td-7ffe38, td-df07dd, td-33aac6, td-88edcc, td-293b6d, td-69e600, td-73877a, td-390cac, td-a952fe, td-a1301a, td-ba1628, td-71fa23, td-97a751, td-50915d, td-31343a, td-bfb568, td-d189ce
WORK SESSION ENDED
exec
/bin/zsh -lc 'td ws start "Epic: Chain Verification"' in /Users/tbrewer/projects/goodway/goodaudit succeeded in 71ms:
WORK SESSION STARTED: ws-e303
Name: Epic: Chain Verification

Tag issues with: td ws tag <issue-ids>
Log progress with: td ws log "message"
exec
/bin/zsh -lc 'td ws tag td-1d7eca td-46c84b td-8a9954 td-486ab1 td-5806a7 td-7ae373 td-e596e4 td-39f158 td-cb684b td-bb90d7 td-c5e429 td-166913 td-dbf670 td-c4e66e td-f96191 td-54c8dc td-e39b5f td-ad7419' in /Users/tbrewer/projects/goodway/goodaudit succeeded in 1.16s:
STARTED td-46c84b (session: ses_cded60)
TAGGED td-8a9954 → ws-e303
STARTED td-8a9954 (session: ses_cded60)
TAGGED td-486ab1 → ws-e303
STARTED td-486ab1 (session: ses_cded60)
TAGGED td-5806a7 → ws-e303
STARTED td-5806a7 (session: ses_cded60)
TAGGED td-7ae373 → ws-e303
STARTED td-7ae373 (session: ses_cded60)
TAGGED td-e596e4 → ws-e303
STARTED td-e596e4 (session: ses_cded60)
TAGGED td-39f158 → ws-e303
STARTED td-39f158 (session: ses_cded60)
TAGGED td-cb684b → ws-e303
STARTED td-cb684b (session: ses_cded60)
TAGGED td-bb90d7 → ws-e303
STARTED td-bb90d7 (session: ses_cded60)
TAGGED td-c5e429 → ws-e303
STARTED td-c5e429 (session: ses_cded60)
TAGGED td-166913 → ws-e303
exec
/bin/zsh -lc 'td ws current' in /Users/tbrewer/projects/goodway/goodaudit succeeded in 51ms:
WORK SESSION: ws-e303 "Epic: Chain Verification"
Started: just now
Git: 0c29eaa → 0c29eaa (+0 commits)

TAGGED ISSUES:
  td-1d7eca  Create GA.Audit.Verifier module  [in_progress]  P2
  td-46c84b  Add GA.Audit.verify_chain(account_id) delegation  [in_progress]  P2
  td-8a9954  Implement verify(account_id) streaming entrypoint  [in_progress]  P2
  td-486ab1  Implement do_verify_batches/4 recursive batch loader  [in_progress]  P2
  td-5806a7  Implement verify_entry/4 chain and checkpoint checks  [in_progress]  P2
  td-7ae373  Create GA.Audit.CheckpointWorker GenServer module  [in_progress]  P2
  td-e596e4  Implement CheckpointWorker init/1 scheduling  [in_progress]  P2
  td-39f158  Implement checkpoint tick over active accounts  [in_progress]  P2
  td-cb684b  Add CheckpointWorker to GA.Application children  [in_progress]  P2
  td-bb90d7  Test valid chain verification report  [in_progress]  P2
  td-c5e429  Test tampered checksum detection  [in_progress]  P2
  td-166913  Test sequence gap detection within account  [in_progress]  P2
  td-dbf670  Test checkpoint anchor validation outcomes  [in_progress]  P2
  td-c4e66e  Test verification duration tracking  [in_progress]  P2
  td-f96191  Test account-scoped verification isolation  [in_progress]  P2
codex
Takeoff setup is complete (`ws-e303`, 18 child issues started, review gate skipped). I’m now pulling the next priority issue and implementing it directly in code.
exec
/bin/zsh -lc 'td next' in /Users/tbrewer/projects/goodway/goodaudit succeeded in 52ms:
td-9db2d6  [P2]  Chain Verification  epic  [open]

Run `td start td-9db2d6` to begin working on this issue.
exec
/bin/zsh -lc 'td critical-path' in /Users/tbrewer/projects/goodway/goodaudit succeeded in 52ms:
CRITICAL PATH SEQUENCE (resolve in order):

  1. td-5806a7  Implement verify_entry/4 chain and checkpoint checks  [in_progress]
     └─▶ unblocks 7
  2. td-46c84b  Add GA.Audit.verify_chain(account_id) delegation  [in_progress]
     └─▶ unblocks 7
  3. td-1d7eca  Create GA.Audit.Verifier module  [in_progress]
     └─▶ unblocks 7
  4. td-8a9954  Implement verify(account_id) streaming entrypoint  [in_progress]
     └─▶ unblocks 7
  5. td-486ab1  Implement do_verify_batches/4 recursive batch loader  [in_progress]
     └─▶ unblocks 7
  6. td-cb684b  Add CheckpointWorker to GA.Application children  [in_progress]
     └─▶ unblocks 4
  7. td-39f158  Implement checkpoint tick over active accounts  [in_progress]
     └─▶ unblocks 4
  8. td-e596e4  Implement CheckpointWorker init/1 scheduling  [in_progress]
     └─▶ unblocks 4
  9. td-7ae373  Create GA.Audit.CheckpointWorker GenServer module  [in_progress]
     └─▶ unblocks 4
exec
/bin/zsh -lc 'td show td-1d7eca' in /Users/tbrewer/projects/goodway/goodaudit succeeded in 52ms:
td-1d7eca: Create GA.Audit.Verifier module
Status: [in_progress]
Type: task | Priority: P2
Labels: backend

Description:
Source task: 1.1 Create `lib/app/audit/verifier.ex` with `GA.Audit.Verifier` module

Artifacts:
- Tasks: openspec/changes/chain-verification/tasks.md
- Proposal: openspec/changes/chain-verification/proposal.md
- Design: openspec/changes/chain-verification/design.md
- Spec: openspec/changes/chain-verification/specs/verification-engine/spec.md

SESSION LOG:
  [16:14] Started (via work session)

GIT STATE:
  Started: 0c29eaa (main) just now
  Current: 0c29eaa (main) +0 commits
codex
I’m loading the change artifacts (tasks, proposal, design, verification spec) so implementation aligns exactly with the OpenSpec requirements before I edit files.
exec
/bin/zsh -lc "sed -n '1,260p' openspec/changes/chain-verification/tasks.md" in /Users/tbrewer/projects/goodway/goodauditexec
/bin/zsh -lc "sed -n '1,260p' openspec/changes/chain-verification/proposal.md" in /Users/tbrewer/projects/goodway/goodauditexec
/bin/zsh -lc "sed -n '1,340p' openspec/changes/chain-verification/design.md" in /Users/tbrewer/projects/goodway/goodauditexec
/bin/zsh -lc "sed -n '1,340p' openspec/changes/chain-verification/specs/verification-engine/spec.md" in /Users/tbrewer/projects/goodway/goodaudit succeeded in 51ms:
## 1. Verification Engine

- [x] 1.1 Create `lib/app/audit/verifier.ex` with `GA.Audit.Verifier` module
- [x] 1.2 Implement `verify(account_id)` — fetches account's HMAC key via `GA.Accounts.get_hmac_key(account_id)`, loads checkpoints for the account into a map, calls `stream_and_verify/3` scoped to account with key, measures duration
- [x] 1.3 Implement `do_verify_batches/4` — recursive batch loading (1000 entries per batch via `WHERE account_id = ? AND sequence_number > ? ORDER BY sequence_number ASC LIMIT 1000`), reduces over `verify_entry/3`
- [x] 1.4 Implement `verify_entry/4` — checks sequence continuity within account, recomputes checksum via `Chain.verify_checksum(hmac_key, entry, previous_checksum)`, checks checkpoint anchors, updates accumulator state
- [x] 1.5 Add `verify_chain(account_id)` to `GA.Audit` context — delegates to `Verifier.verify(account_id)`

## 2. Checkpoint Worker

- [x] 2.1 Create `lib/app/audit/checkpoint_worker.ex` with `GA.Audit.CheckpointWorker` GenServer
- [x] 2.2 Implement `init/1` — reads interval from opts (default 1 hour), schedules first checkpoint run
- [x] 2.3 Implement `handle_info(:create_checkpoints, state)` — iterates all active accounts via `GA.Repo.all(from a in GA.Accounts.Account, where: a.status == :active)`, calls `GA.Audit.create_checkpoint/1` for each, logs results per account, reschedules
- [x] 2.4 Add `GA.Audit.CheckpointWorker` to `GA.Application` children list before `GAWeb.Endpoint`

## 3. Tests

- [x] 3.1 Test valid chain verification for an account — all entries pass, report shows `valid: true`
- [x] 3.2 Test tampered checksum detection — manually inserted entry with wrong checksum produces `valid: false` with `first_failure` details
- [x] 3.3 Test sequence gap detection — entries with non-contiguous sequence numbers within an account produce `sequence_gaps` in report
 succeeded in 51ms:
## Why

The audit chain can now be created and queried per-account, but there's no way to verify its integrity. We need a verification engine that streams through an **account's** entire chain from genesis, recomputing checksums and checking sequence continuity. We also need a periodic worker that creates checkpoints automatically **for all active accounts**, providing regular anchor points for efficient partial verification.

## What Changes

1. **`GA.Audit.Verifier`** — Streaming verification engine that reads an account's chain in batches of 1000, recomputes HMAC checksums (including account_id in payload), detects sequence gaps, validates checkpoint anchors, and returns a detailed report. Always operates on a single account's chain.

2. **`GA.Audit.CheckpointWorker`** — GenServer that creates checkpoints **for all active accounts** on a configurable schedule (default: 1 hour). Iterates `GA.Accounts` to find active accounts, creates a checkpoint for each. Added to the supervision tree.

> **Production evolution note:** This change establishes baseline full verification and a simple worker loop. Default incremental verification is introduced in `incremental-verification`, and worker scale controls are introduced in `checkpoint-worker-scaling`.

3. **`GA.Audit.verify_chain(account_id)`** — Context function that delegates to the verifier for a specific account.

## Capabilities

### New Capabilities
- `verification-engine`: Per-account streaming chain integrity verification with gap detection, checksum validation, and checkpoint anchor checking
- `checkpoint-worker`: Periodic GenServer that creates chain checkpoints for all active accounts on a configurable schedule

 succeeded in 50ms:
## ADDED Requirements

### Requirement: Per-account streaming chain verification

The verifier MUST accept an `account_id`, fetch the account's HMAC key via `GA.Accounts.get_hmac_key(account_id)`, and read that account's audit log entries in batches (default 1000) ordered by sequence number ascending, starting from genesis. For each entry it MUST: check sequence continuity (expected vs actual sequence number within the account), recompute the HMAC checksum using `GA.Audit.Chain.verify_checksum(hmac_key, entry, previous_checksum)` and compare against the stored checksum, and check any checkpoint anchors at that sequence number for the account. The verifier MUST return a report map. Entries from other accounts MUST never be included in verification.

#### Scenario: Valid chain for account
- **WHEN** `verify(account_id)` is called on an intact chain for that account
- **THEN** it returns `%{valid: true, total_entries: N, verified_entries: N, first_failure: nil, sequence_gaps: [], checkpoint_results: [...], duration_ms: M}`

#### Scenario: Tampered checksum detected
- **WHEN** an entry's stored checksum has been altered (e.g., directly in the database with triggers disabled)
- **THEN** the report has `valid: false` and `first_failure` contains `%{type: :checksum_mismatch, sequence_number: N, stored_checksum: ..., expected_checksum: ...}`

#### Scenario: Sequence gap detected within account
- **WHEN** entries exist for an account with sequence numbers 1, 2, 4 (3 is missing)
- **THEN** the report has `valid: false`, `sequence_gaps` contains `%{expected: 3, found: 4, missing: [3]}`

#### Scenario: Account isolation during verification
- **WHEN** `verify(account_a_id)` is called and account B has entries
 succeeded in 50ms:
## Context

Audit entries can be created and queried per-account. Now we need the integrity verification system that proves an account's chain is intact, and automated checkpoint creation across all accounts for regular anchoring.

## Goals / Non-Goals

**Goals:**
- Per-account full chain verification from genesis in streaming batches (memory-efficient for large chains)
- Detect all tamper types: modified checksums, deleted entries (sequence gaps), altered checkpoints — within an account's chain
- Detailed report suitable for API response and compliance auditing
- Automatic periodic checkpoints for all active accounts with zero operator intervention

**Non-Goals:**
- Partial/incremental verification in this change (implemented in `incremental-verification`)
- External anchoring (checksum.dev integration) — separate proposal
- Real-time tamper alerting — verification is on-demand or periodic
- Cross-account verification or aggregation

## Decisions

exec
/bin/zsh -lc "sed -n '1,260p' app/lib/app/audit/verifier.ex" in /Users/tbrewer/projects/goodway/goodauditexec
/bin/zsh -lc "sed -n '1,260p' app/lib/app/audit/checkpoint_worker.ex" in /Users/tbrewer/projects/goodway/goodauditexec
/bin/zsh -lc "sed -n '1,260p' app/lib/app/audit.ex" in /Users/tbrewer/projects/goodway/goodauditexec
/bin/zsh -lc "sed -n '1,260p' app/lib/app/application.ex" in /Users/tbrewer/projects/goodway/goodauditexec
/bin/zsh -lc "sed -n '1,320p' app/test/app/audit/verifier_test.exs" in /Users/tbrewer/projects/goodway/goodauditexec
/bin/zsh -lc "sed -n '1,320p' app/test/app/audit/checkpoint_worker_test.exs" in /Users/tbrewer/projects/goodway/goodaudit exited 1 in 52ms:
sed: app/lib/app/audit/verifier.ex: No such file or directory
 exited 1 in 51ms:
sed: app/lib/app/audit/checkpoint_worker.ex: No such file or directory
 succeeded in 50ms:
defmodule GA.Audit do
  @moduledoc """
  Business logic for account-scoped append-only audit logs and checkpoints.
  """

  import Ecto.Changeset, only: [put_change: 3]
  import Ecto.Query, warn: false

  alias GA.Accounts
  alias GA.Audit.{Chain, Checkpoint, Log}
  alias GA.Repo

  @default_limit 50
  @max_limit 1000

  @doc """
  Creates a new audit log entry with per-account chain fields in a single transaction.
  """
  def create_log_entry(account_id, attrs)
      when is_binary(account_id) and (is_map(attrs) or is_list(attrs)) do
 succeeded in 52ms:
defmodule GA.Application do
  # See https://hexdocs.pm/elixir/Application.html
  # for more information on OTP Applications
  @moduledoc false

  use Application

  @impl true
  def start(_type, _args) do
    children = [
      GAWeb.Telemetry,
      GA.Repo,
      {DNSCluster, query: Application.get_env(:app, :dns_cluster_query) || :ignore},
      {Phoenix.PubSub, name: GA.PubSub},
      # Start a worker by calling: GA.Worker.start_link(arg)
      # {GA.Worker, arg},
      # Start to serve requests, typically the last entry
      GAWeb.Endpoint
    ]

 exited 1 in 51ms:
sed: app/test/app/audit/verifier_test.exs: No such file or directory
 exited 1 in 50ms:
sed: app/test/app/audit/checkpoint_worker_test.exs: No such file or directory
codex
The verifier/worker files and their tests are not present yet, which matches the first tasks. I’m mapping the existing audit/test file layout next so I can add these modules in the correct locations.
exec
/bin/zsh -lc "rg --files app/lib | rg 'audit'" in /Users/tbrewer/projects/goodway/goodaudit succeeded in 51ms:
app/lib/app/audit/log.ex
app/lib/app/audit/checkpoint.ex
app/lib/app/audit/chain.ex
app/lib/app/audit.ex
exec
/bin/zsh -lc "rg --files app/test | rg 'audit'" in /Users/tbrewer/projects/goodway/goodaudit succeeded in 51ms:
app/test/app/audit/schema_migration_test.exs
app/test/app/audit/chain_test.exs
app/test/app/audit/checkpoint_test.exs
app/test/app/audit/log_test.exs
app/test/app/audit/context_test.exs
exec
/bin/zsh -lc "sed -n '260,520p' app/lib/app/audit.ex" in /Users/tbrewer/projects/goodway/goodaudit succeeded in 51ms:
    |> min(@max_limit)
  end

  defp normalize_limit(_), do: @default_limit

  defp paginate(entries, limit) do
    {page_entries, overflow} = Enum.split(entries, limit)

    next_cursor =
      case {page_entries, overflow} do
        {[_ | _], [_ | _]} -> List.last(page_entries).sequence_number
        _ -> nil
      end

    {page_entries, next_cursor}
  end

  defp put_default_timestamp(attrs) do
    if Map.has_key?(attrs, :timestamp) or Map.has_key?(attrs, "timestamp") do
      attrs
exec
/bin/zsh -lc "sed -n '1,280p' app/lib/app/audit/chain.ex" in /Users/tbrewer/projects/goodway/goodauditexec
/bin/zsh -lc "sed -n '1,320p' app/lib/app/accounts.ex" in /Users/tbrewer/projects/goodway/goodauditexec
/bin/zsh -lc "sed -n '1,260p' app/test/app/audit/context_test.exs" in /Users/tbrewer/projects/goodway/goodauditexec
/bin/zsh -lc "sed -n '260,520p' app/test/app/audit/context_test.exs" in /Users/tbrewer/projects/goodway/goodauditexec
/bin/zsh -lc "sed -n '1,280p' app/lib/app/accounts/account.ex" in /Users/tbrewer/projects/goodway/goodaudit succeeded in 52ms:
defmodule GA.Audit.Chain do
  @moduledoc """
  Deterministic HMAC-SHA-256 chain checksum computation utilities.
  """

  @genesis_previous "genesis"
  @checksum_pattern ~r/\A[0-9a-f]{64}\z/
  @payload_fields [
    "account_id",
    "sequence_number",
    "timestamp",
    "user_id",
    "user_role",
    "session_id",
    "action",
    "resource_type",
    "resource_id",
    "outcome",
    "failure_reason",
    "phi_accessed",
 succeeded in 52ms:
defmodule GA.Accounts do
  @moduledoc """
  The Accounts context.
  """

  import Ecto.Query, warn: false
  alias GA.Repo

  alias GA.Accounts.{Account, AccountUser, ApiKey, User, UserToken, UserNotifier}

  ## Database getters

  @doc """
  Gets a user by email.

  ## Examples

      iex> get_user_by_email("foo@example.com")
      %User{}

 succeeded in 51ms:
defmodule GA.Accounts.Account do
  @moduledoc """
  Schema for accounts (organizations/tenants).
  Users can belong to multiple accounts.
  """
  use Ecto.Schema
  import Ecto.Changeset

  @derive {Inspect, except: [:hmac_key]}
  @primary_key {:id, :binary_id, autogenerate: true}
  @foreign_key_type :binary_id

  schema "accounts" do
    field :name, :string
    field :slug, :string
    field :status, Ecto.Enum, values: [:active, :suspended], default: :active
    field :hmac_key, :binary, redact: true, load_in_query: false

    has_many :account_users, GA.Accounts.AccountUser
    has_many :users, through: [:account_users, :user]
 succeeded in 51ms:
defmodule GA.Audit.ContextTest do
  use GA.DataCase, async: false

  import Ecto.Query

  alias GA.Accounts
  alias GA.Audit
  alias GA.Audit.{Chain, Checkpoint, Log}
  alias GA.Repo

  describe "create_log_entry/2" do
    test "creates genesis entry with sequence 1 and nil previous_checksum" do
      account = account_fixture()

      assert {:ok, log} = Audit.create_log_entry(account.id, valid_attrs())
      assert log.account_id == account.id
      assert log.sequence_number == 1
      assert is_nil(log.previous_checksum)

      assert {:ok, hmac_key} = Accounts.get_hmac_key(account.id)
 succeeded in 51ms:
                 valid_attrs(%{
                   user_id: "user-1",
                   action: "update",
                   phi_accessed: true,
                   resource_id: "wrong-action",
                   timestamp: in_window
                 })
               )

      assert {:ok, _} =
               Audit.create_log_entry(
                 account.id,
                 valid_attrs(%{
                   user_id: "user-1",
                   action: "read",
                   phi_accessed: true,
                   resource_id: "wrong-time",
                   timestamp: before_window
                 })
               )
exec
/bin/zsh -lc "rg -n \"get_hmac_key|list_active_accounts|create_account\\(\" app/lib/app/accounts.ex" in /Users/tbrewer/projects/goodway/goodaudit succeeded in 51ms:
309:  def create_account(user, attrs) do
335:  def create_account(attrs) when is_map(attrs) do
342:  def get_hmac_key(account_id) when is_binary(account_id) do
352:  def get_hmac_key(_), do: {:error, :invalid_id}
exec
/bin/zsh -lc "sed -n '300,460p' app/lib/app/accounts.ex" in /Users/tbrewer/projects/goodway/goodaudit succeeded in 52ms:
  # ============================================

  @doc "Get an account by ID."
  def get_account(id), do: Repo.get(Account, id)

  @doc "Get an account by slug."
  def get_account_by_slug(slug), do: Repo.get_by(Account, slug: slug)

  @doc "Create a new account with the given user as owner."
  def create_account(user, attrs) do
    Repo.transaction(fn ->
      with {:ok, account} <- attrs |> account_changeset_with_hmac_key() |> Repo.insert(),
           {:ok, _account_user} <- add_user_to_account(account, user, :owner) do
        account
      else
        {:error, changeset} -> Repo.rollback(changeset)
      end
    end)
  end

exec
/bin/zsh -lc "sed -n '1,260p' app/lib/app/audit/log.ex" in /Users/tbrewer/projects/goodway/goodauditexec
/bin/zsh -lc "sed -n '1,260p' app/lib/app/audit/checkpoint.ex" in /Users/tbrewer/projects/goodway/goodauditexec
/bin/zsh -lc "sed -n '1,240p' app/priv/repo/migrations/20260303153000_create_audit_logs.exs" in /Users/tbrewer/projects/goodway/goodauditexec
/bin/zsh -lc "sed -n '1,240p' app/priv/repo/migrations/20260303153100_create_audit_checkpoints.exs" in /Users/tbrewer/projects/goodway/goodauditexec
/bin/zsh -lc "rg -n \"create table\\(:audit_logs|create table\\(:audit_checkpoints|index\\(:audit_logs\" app/priv/repo/migrations/*.exs" in /Users/tbrewer/projects/goodway/goodaudit succeeded in 52ms:
defmodule GA.Audit.Log do
  @moduledoc """
  Ecto schema for append-only audit log entries.
  """
  use Ecto.Schema
  import Ecto.Changeset

  @primary_key {:id, :binary_id, autogenerate: true}
  @foreign_key_type :binary_id

  @valid_actions ~w(create read update delete export login logout)
  @valid_outcomes ~w(success failure)

  schema "audit_logs" do
    belongs_to(:account, GA.Accounts.Account)

    field(:sequence_number, :integer)
    field(:checksum, :string)
    field(:previous_checksum, :string)
    field(:user_id, :string)
 succeeded in 51ms:
defmodule GA.Audit.Checkpoint do
  @moduledoc """
  Ecto schema for append-only account-scoped audit checkpoints.
  """
  use Ecto.Schema
  import Ecto.Changeset

  @primary_key {:id, :binary_id, autogenerate: true}
  @foreign_key_type :binary_id

  schema "audit_checkpoints" do
    belongs_to(:account, GA.Accounts.Account)

    field(:sequence_number, :integer)
    field(:checksum, :string)
    field(:signature, :string)
    field(:verified_at, :utc_datetime_usec)

    timestamps(type: :utc_datetime_usec)
  end
 succeeded in 51ms:
defmodule GA.Repo.Migrations.CreateAuditCheckpoints do
  use Ecto.Migration

  def up do
    create table(:audit_checkpoints, primary_key: false) do
      add(:id, :binary_id, primary_key: true)

      add(:account_id, references(:accounts, type: :binary_id, on_delete: :nothing), null: false)

      add(:sequence_number, :bigint, null: false)
      add(:checksum, :string, size: 64, null: false)
      add(:signature, :text)
      add(:verified_at, :utc_datetime_usec)

      timestamps(type: :utc_datetime_usec)
    end

    create(unique_index(:audit_checkpoints, [:account_id, :sequence_number]))

    create(
 succeeded in 51ms:
defmodule GA.Repo.Migrations.CreateAuditLogs do
  use Ecto.Migration

  def up do
    create table(:audit_logs, primary_key: false) do
      add(:id, :binary_id, primary_key: true)

      add(:account_id, references(:accounts, type: :binary_id, on_delete: :nothing), null: false)

      add(:sequence_number, :bigint, null: false)
      add(:checksum, :string, size: 64, null: false)
      add(:previous_checksum, :string, size: 64)
      add(:user_id, :string, null: false)
      add(:user_role, :string, null: false)
      add(:session_id, :string)
      add(:action, :string, null: false)
      add(:resource_type, :string, null: false)
      add(:resource_id, :string, null: false)
      add(:timestamp, :utc_datetime_usec, null: false)
      add(:source_ip, :string)
 succeeded in 50ms:
app/priv/repo/migrations/20260303153100_create_audit_checkpoints.exs:5:    create table(:audit_checkpoints, primary_key: false) do
app/priv/repo/migrations/20260303153000_create_audit_logs.exs:5:    create table(:audit_logs, primary_key: false) do
app/priv/repo/migrations/20260303153000_create_audit_logs.exs:30:    create(unique_index(:audit_logs, [:account_id, :sequence_number]))
app/priv/repo/migrations/20260303153000_create_audit_logs.exs:31:    create(index(:audit_logs, [:account_id]))
app/priv/repo/migrations/20260303153000_create_audit_logs.exs:32:    create(index(:audit_logs, [:account_id, :timestamp]))
app/priv/repo/migrations/20260303153000_create_audit_logs.exs:33:    create(index(:audit_logs, [:account_id, :user_id]))
app/priv/repo/migrations/20260303153000_create_audit_logs.exs:34:    create(index(:audit_logs, [:account_id, :resource_type, :resource_id]))
app/priv/repo/migrations/20260303153000_create_audit_logs.exs:35:    create(index(:audit_logs, [:account_id, :action]))
app/priv/repo/migrations/20260303153000_create_audit_logs.exs:36:    create(index(:audit_logs, [:account_id, :phi_accessed], where: "phi_accessed = true"))
codex
I’ve confirmed the supporting schemas/migrations and now I’m implementing the verifier, audit delegation, and checkpoint worker modules plus new tests in one pass so the started issues can be advanced together.
exec
/bin/zsh -lc "rg -n \"CheckpointWorker|verify_chain|audit\" app/config app/lib app/test | sed -n '1,240p'" in /Users/tbrewer/projects/goodway/goodauditexec
/bin/zsh -lc "sed -n '1,240p' app/test/support/fixtures/accounts_fixtures.ex" in /Users/tbrewer/projects/goodway/goodauditexec
/bin/zsh -lc "sed -n '1,260p' app/test/support/data_case.ex" in /Users/tbrewer/projects/goodway/goodauditexec
/bin/zsh -lc "sed -n '1,260p' app/config/test.exs" in /Users/tbrewer/projects/goodway/goodauditexec
/bin/zsh -lc "sed -n '1,260p' app/config/config.exs" in /Users/tbrewer/projects/goodway/goodaudit succeeded in 50ms:
app/lib/app/audit/log.ex:3:  Ecto schema for append-only audit log entries.
app/lib/app/audit/log.ex:14:  schema "audit_logs" do
app/lib/app/audit/log.ex:41:  Validates externally provided audit log attributes.
app/lib/app/audit/log.ex:73:    |> check_constraint(:action, name: :audit_logs_action_valid_check)
app/lib/app/audit/log.ex:74:    |> check_constraint(:outcome, name: :audit_logs_outcome_valid_check)
app/lib/app/audit.ex:3:  Business logic for account-scoped append-only audit logs and checkpoints.
app/lib/app/audit.ex:17:  Creates a new audit log entry with per-account chain fields in a single transaction.
app/lib/app/audit.ex:55:  Lists account-scoped audit entries with cursor pagination and optional filters.
app/lib/app/audit.ex:74:  Fetches a single audit log entry scoped to an account.
app/lib/app/audit.ex:86:  Creates an audit checkpoint at the account's latest log sequence.
app/lib/app/audit/checkpoint.ex:3:  Ecto schema for append-only account-scoped audit checkpoints.
app/lib/app/audit/checkpoint.ex:11:  schema "audit_checkpoints" do
app/lib/app/audit/checkpoint.ex:27:      name: :audit_checkpoints_account_id_sequence_number_index
app/lib/app/audit/checkpoint.ex:29:    |> check_constraint(:checksum, name: :audit_checkpoints_checksum_format_check)
app/lib/app/audit/chain.ex:26:  Computes a lowercase hex-encoded HMAC-SHA-256 checksum for audit attributes.
app/test/app/audit/checkpoint_test.exs:84:  test "append-only triggers reject update and delete for audit_logs and audit_checkpoints" do
app/test/app/audit/checkpoint_test.exs:138:      SQL.query!(Repo, "TRUNCATE TABLE audit_logs", [])
app/test/app/audit/checkpoint_test.exs:142:      SQL.query!(Repo, "TRUNCATE TABLE audit_checkpoints", [])
app/test/app/audit/schema_migration_test.exs:15:  defp insert_audit_log(attrs) do
app/test/app/audit/schema_migration_test.exs:45:      INSERT INTO audit_logs (
 succeeded in 52ms:
defmodule GA.AccountsFixtures do
  @moduledoc """
  This module defines test helpers for creating
  entities via the `GA.Accounts` context.
  """

  import Ecto.Query

  alias GA.Accounts
  alias GA.Accounts.Scope

  def unique_user_email, do: "user#{System.unique_integer()}@example.com"
  def valid_user_password, do: "hello world!"

  def valid_user_attributes(attrs \\ %{}) do
    Enum.into(attrs, %{
      email: unique_user_email()
    })
  end

 succeeded in 51ms:
defmodule GA.DataCase do
  @moduledoc """
  This module defines the setup for tests requiring
  access to the application's data layer.

  You may define functions here to be used as helpers in
  your tests.

  Finally, if the test case interacts with the database,
  we enable the SQL sandbox, so changes done to the database
  are reverted at the end of every test. If you are using
  PostgreSQL, you can even run database tests asynchronously
  by setting `use GA.DataCase, async: true`, although
  this option is not recommended for other databases.
  """

  use ExUnit.CaseTemplate

  using do
    quote do
 succeeded in 51ms:
import Config

# Only in tests, remove the complexity from the password hashing algorithm
config :bcrypt_elixir, :log_rounds, 1

# Configure your database
#
# The MIX_TEST_PARTITION environment variable can be used
# to provide built-in test partitioning in CI environment.
# Run `mix help test` for more information.
config :app, GA.Repo,
  username: "postgres",
  password: "postgres",
  hostname: "localhost",
  database: "app_test#{System.get_env("MIX_TEST_PARTITION")}",
  pool: Ecto.Adapters.SQL.Sandbox,
  pool_size: System.schedulers_online() * 2

# We don't run a server during test. If one is required,
# you can enable the server option below.
 succeeded in 51ms:
# This file is responsible for configuring your application
# and its dependencies with the aid of the Config module.
#
# This configuration file is loaded before any dependency and
# is restricted to this project.

# General application configuration
import Config

config :app, :scopes,
  user: [
    default: true,
    module: GA.Accounts.Scope,
    assign_key: :current_scope,
    access_path: [:user, :id],
    schema_key: :user_id,
    schema_type: :binary_id,
    schema_table: :users,
    test_data_fixture: GA.AccountsFixtures,
    test_setup_helper: :register_and_log_in_user
exec
/bin/zsh -lc "find openspec/changes/chain-verification/specs -maxdepth 3 -type f -name '*.md' -print" in /Users/tbrewer/projects/goodway/goodaudit succeeded in 51ms:
openspec/changes/chain-verification/specs/verification-engine/spec.md
openspec/changes/chain-verification/specs/checkpoint-worker/spec.md
exec
/bin/zsh -lc "sed -n '1,320p' openspec/changes/chain-verification/specs/checkpoint-worker/spec.md" in /Users/tbrewer/projects/goodway/goodaudit succeeded in 51ms:
## ADDED Requirements

### Requirement: Multi-account periodic checkpoint creation

The `GA.Audit.CheckpointWorker` GenServer MUST create chain checkpoints for all active accounts on a configurable interval (default: 1 hour). It MUST be added to the application's supervision tree so it starts automatically. On each tick, it iterates all accounts with `status: :active` and calls `GA.Audit.create_checkpoint/1` for each.

#### Scenario: Scheduled checkpoint for all accounts
- **WHEN** the configured interval elapses
- **THEN** the worker creates a checkpoint for each active account that has audit entries

#### Scenario: Account with no entries yet
- **WHEN** the worker processes an active account with no audit entries
- **THEN** it logs a debug message for that account and continues to the next account

#### Scenario: Checkpoint failure for one account
- **WHEN** `create_checkpoint/1` returns an error for one account
- **THEN** the worker logs an error for that account but continues processing remaining accounts (does not crash)

#### Scenario: Suspended accounts skipped
- **WHEN** the worker fires and some accounts have `status: :suspended`
exec
/bin/zsh -lc 'td context td-1d7eca' in /Users/tbrewer/projects/goodway/goodaudit succeeded in 99ms:
td-1d7eca: Create GA.Audit.Verifier module
Status: [in_progress]
Type: task | Priority: P2
Labels: backend

Description:
Source task: 1.1 Create `lib/app/audit/verifier.ex` with `GA.Audit.Verifier` module

Artifacts:
- Tasks: openspec/changes/chain-verification/tasks.md
- Proposal: openspec/changes/chain-verification/proposal.md
- Design: openspec/changes/chain-verification/design.md
- Spec: openspec/changes/chain-verification/specs/verification-engine/spec.md

SESSION LOG:
  [16:14] Started (via work session)

GIT STATE:
  Started: 0c29eaa (main) 2m ago
  Current: 0c29eaa (main) +0 commits
codex
Implementation plan for this takeoff pass:

1. Add `GA.Audit.Verifier` with:
- `verify/1` entrypoint
- per-account checkpoint preload
- batched `audit_logs` streaming (`1000` rows, `sequence_number` cursor)
- `verify_entry/4` checks for sequence continuity, checksum integrity, and checkpoint anchor validation
- report with `valid`, counts, failure details, gap details, checkpoint results, and `duration_ms`

2. Extend `GA.Audit` with `verify_chain/1` delegating to verifier.

3. Add `GA.Audit.CheckpointWorker` GenServer with:
- configurable interval (default 1 hour)
- periodic tick that processes only active accounts
- resilient per-account checkpoint creation with debug/error logging and continued processing

4. Wire worker into [`app/lib/app/application.ex`](/Users/tbrewer/projects/goodway/goodaudit/app/lib/app/application.ex) before the endpoint.

5. Add tests:
- [`app/test/app/audit/verifier_test.exs`](/Users/tbrewer/projects/goodway/goodaudit/app/test/app/audit/verifier_test.exs) for all verification scenarios
- [`app/test/app/audit/checkpoint_worker_test.exs`](/Users/tbrewer/projects/goodway/goodaudit/app/test/app/audit/checkpoint_worker_test.exs) for active/suspended/no-entry worker behavior

6. Run targeted tests (or report environment blocker), then log/review completed `td` issues.
file update
A /Users/tbrewer/projects/goodway/goodaudit/app/lib/app/audit/verifier.ex
defmodule GA.Audit.Verifier do
  @moduledoc """
  Streams and verifies account-scoped audit chains.
  """

  import Ecto.Query, warn: false

  alias GA.Accounts
  alias GA.Audit.{Chain, Checkpoint, Log}
  alias GA.Repo

  @batch_size 1000

  @doc """
  Verifies an account's audit chain and returns a report map.
  """
  @spec verify(binary()) :: map() | {:error, :invalid_id | :not_found}
  def verify(account_id) when is_binary(account_id) do
    started_at = System.monotonic_time(:millisecond)

    with {:ok, hmac_key} <- Accounts.get_hmac_key(account_id) do
      checkpoints_by_sequence = load_checkpoints(account_id)
      report = stream_and_verify(account_id, hmac_key, checkpoints_by_sequence)

      Map.put(report, :duration_ms, elapsed_ms(started_at))
    end
  end

  def verify(_), do: {:error, :invalid_id}

  defp stream_and_verify(account_id, hmac_key, checkpoints_by_sequence) do
    initial =
      new_accumulator()
      |> Map.put(:account_id, account_id)

    account_id
    |> do_verify_batches(hmac_key, checkpoints_by_sequence, initial)
    |> finalize_report()
  end

  defp do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, acc) do
    batch = load_batch(account_id, acc.last_sequence)

    case batch do
      [] ->
        acc

      _entries ->
        next_acc =
          Enum.reduce(batch, acc, fn entry, state ->
            verify_entry(entry, hmac_key, checkpoints_by_sequence, state)
          end)

        do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, next_acc)
    end
  end

  defp verify_entry(%Log{} = entry, hmac_key, checkpoints_by_sequence, acc) do
    acc
    |> verify_sequence(entry)
    |> verify_checksum(entry, hmac_key)
    |> verify_checkpoint(entry, checkpoints_by_sequence)
    |> advance_state(entry)
  end

  defp verify_sequence(acc, %Log{sequence_number: found_sequence}) do
    expected_sequence = acc.expected_sequence

    if found_sequence == expected_sequence do
      %{acc | expected_sequence: expected_sequence + 1}
    else
      missing =
        if found_sequence > expected_sequence do
          Enum.to_list(expected_sequence..(found_sequence - 1))
        else
          []
        end

      gap = %{
        expected: expected_sequence,
        found: found_sequence,
        missing: missing
      }

      acc
      |> mark_invalid(%{
        type: :sequence_gap,
        expected: expected_sequence,
        found: found_sequence,
        missing: missing
      })
      |> Map.update!(:sequence_gaps, &[gap | &1])
      |> Map.put(:expected_sequence, max(found_sequence, expected_sequence) + 1)
    end
  end

  defp verify_checksum(acc, %Log{} = entry, hmac_key) do
    if Chain.verify_checksum(hmac_key, entry, acc.previous_checksum) do
      acc
    else
      mark_invalid(acc, %{
        type: :checksum_mismatch,
        sequence_number: entry.sequence_number,
        stored_checksum: entry.checksum,
        expected_checksum: expected_checksum(hmac_key, entry, acc.previous_checksum)
      })
    end
  end

  defp verify_checkpoint(acc, %Log{} = entry, checkpoints_by_sequence) do
    case Map.get(checkpoints_by_sequence, entry.sequence_number) do
      nil ->
        acc

      %Checkpoint{} = checkpoint ->
        valid? = checkpoint.checksum == entry.checksum
        result = %{sequence_number: entry.sequence_number, valid: valid?}
        next_acc = Map.update!(acc, :checkpoint_results, &[result | &1])

        if valid? do
          next_acc
        else
          mark_invalid(next_acc, %{
            type: :checkpoint_mismatch,
            sequence_number: entry.sequence_number,
            checkpoint_checksum: checkpoint.checksum,
            entry_checksum: entry.checksum
          })
        end
    end
  end

  defp advance_state(acc, %Log{} = entry) do
    %{
      acc
      | previous_checksum: entry.checksum,
        last_sequence: entry.sequence_number,
        total_entries: acc.total_entries + 1,
        verified_entries: acc.verified_entries + 1
    }
  end

  defp expected_checksum(hmac_key, entry, previous_checksum) do
    Chain.compute_checksum(hmac_key, Map.from_struct(entry), previous_checksum)
  rescue
    _ -> nil
  end

  defp load_batch(account_id, after_sequence) do
    from(log in Log,
      where: log.account_id == ^account_id and log.sequence_number > ^after_sequence,
      order_by: [asc: log.sequence_number],
      limit: @batch_size
    )
    |> Repo.all()
  end

  defp load_checkpoints(account_id) do
    from(checkpoint in Checkpoint, where: checkpoint.account_id == ^account_id)
    |> Repo.all()
    |> Map.new(fn checkpoint -> {checkpoint.sequence_number, checkpoint} end)
  end

  defp new_accumulator do
    %{
      account_id: nil,
      valid: true,
      total_entries: 0,
      verified_entries: 0,
      expected_sequence: 1,
      previous_checksum: nil,
      last_sequence: 0,
      first_failure: nil,
      sequence_gaps: [],
      checkpoint_results: []
    }
  end

  defp finalize_report(acc) do
    %{
      valid: acc.valid,
      total_entries: acc.total_entries,
      verified_entries: acc.verified_entries,
      first_failure: acc.first_failure,
      sequence_gaps: Enum.reverse(acc.sequence_gaps),
      checkpoint_results: Enum.reverse(acc.checkpoint_results),
      duration_ms: 0
    }
  end

  defp mark_invalid(acc, failure) do
    case acc.first_failure do
      nil -> %{acc | valid: false, first_failure: failure}
      _existing -> %{acc | valid: false}
    end
  end

  defp elapsed_ms(started_at) do
    System.monotonic_time(:millisecond)
    |> Kernel.-(started_at)
    |> max(0)
  end
end
apply_patch(auto_approved=true) exited 0 in 28ms:
Success. Updated the following files:
A /Users/tbrewer/projects/goodway/goodaudit/app/lib/app/audit/verifier.ex
file update:
diff --git a/app/lib/app/audit/verifier.ex b/app/lib/app/audit/verifier.ex
new file mode 100644
index 0000000000000000000000000000000000000000..2a4bd77516a2c76ccc9b05de1778dd8781ded096
--- /dev/null
+++ b/app/lib/app/audit/verifier.ex
@@ -0,0 +1,203 @@
+defmodule GA.Audit.Verifier do
+  @moduledoc """
+  Streams and verifies account-scoped audit chains.
+  """
+
+  import Ecto.Query, warn: false
+
+  alias GA.Accounts
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  @batch_size 1000
+
+  @doc """
+  Verifies an account's audit chain and returns a report map.
+  """
+  @spec verify(binary()) :: map() | {:error, :invalid_id | :not_found}
+  def verify(account_id) when is_binary(account_id) do
+    started_at = System.monotonic_time(:millisecond)
+
+    with {:ok, hmac_key} <- Accounts.get_hmac_key(account_id) do
+      checkpoints_by_sequence = load_checkpoints(account_id)
+      report = stream_and_verify(account_id, hmac_key, checkpoints_by_sequence)
+
+      Map.put(report, :duration_ms, elapsed_ms(started_at))
+    end
+  end
+
+  def verify(_), do: {:error, :invalid_id}
+
+  defp stream_and_verify(account_id, hmac_key, checkpoints_by_sequence) do
+    initial =
+      new_accumulator()
+      |> Map.put(:account_id, account_id)
+
+    account_id
+    |> do_verify_batches(hmac_key, checkpoints_by_sequence, initial)
+    |> finalize_report()
+  end
+
+  defp do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, acc) do
+    batch = load_batch(account_id, acc.last_sequence)
+
+    case batch do
+      [] ->
+        acc
+
+      _entries ->
+        next_acc =
+          Enum.reduce(batch, acc, fn entry, state ->
+            verify_entry(entry, hmac_key, checkpoints_by_sequence, state)
+          end)
+
+        do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, next_acc)
+    end
+  end
+
+  defp verify_entry(%Log{} = entry, hmac_key, checkpoints_by_sequence, acc) do
+    acc
+    |> verify_sequence(entry)
+    |> verify_checksum(entry, hmac_key)
+    |> verify_checkpoint(entry, checkpoints_by_sequence)
+    |> advance_state(entry)
+  end
+
+  defp verify_sequence(acc, %Log{sequence_number: found_sequence}) do
+    expected_sequence = acc.expected_sequence
+
+    if found_sequence == expected_sequence do
+      %{acc | expected_sequence: expected_sequence + 1}
+    else
+      missing =
+        if found_sequence > expected_sequence do
+          Enum.to_list(expected_sequence..(found_sequence - 1))
+        else
+          []
+        end
+
+      gap = %{
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      }
+
+      acc
+      |> mark_invalid(%{
+        type: :sequence_gap,
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      })
+      |> Map.update!(:sequence_gaps, &[gap | &1])
+      |> Map.put(:expected_sequence, max(found_sequence, expected_sequence) + 1)
+    end
+  end
+
+  defp verify_checksum(acc, %Log{} = entry, hmac_key) do
+    if Chain.verify_checksum(hmac_key, entry, acc.previous_checksum) do
+      acc
+    else
+      mark_invalid(acc, %{
+        type: :checksum_mismatch,
+        sequence_number: entry.sequence_number,
+        stored_checksum: entry.checksum,
+        expected_checksum: expected_checksum(hmac_key, entry, acc.previous_checksum)
+      })
+    end
+  end
+
+  defp verify_checkpoint(acc, %Log{} = entry, checkpoints_by_sequence) do
+    case Map.get(checkpoints_by_sequence, entry.sequence_number) do
+      nil ->
+        acc
+
+      %Checkpoint{} = checkpoint ->
+        valid? = checkpoint.checksum == entry.checksum
+        result = %{sequence_number: entry.sequence_number, valid: valid?}
+        next_acc = Map.update!(acc, :checkpoint_results, &[result | &1])
+
+        if valid? do
+          next_acc
+        else
+          mark_invalid(next_acc, %{
+            type: :checkpoint_mismatch,
+            sequence_number: entry.sequence_number,
+            checkpoint_checksum: checkpoint.checksum,
+            entry_checksum: entry.checksum
+          })
+        end
+    end
+  end
+
+  defp advance_state(acc, %Log{} = entry) do
+    %{
+      acc
+      | previous_checksum: entry.checksum,
+        last_sequence: entry.sequence_number,
+        total_entries: acc.total_entries + 1,
+        verified_entries: acc.verified_entries + 1
+    }
+  end
+
+  defp expected_checksum(hmac_key, entry, previous_checksum) do
+    Chain.compute_checksum(hmac_key, Map.from_struct(entry), previous_checksum)
+  rescue
+    _ -> nil
+  end
+
+  defp load_batch(account_id, after_sequence) do
+    from(log in Log,
+      where: log.account_id == ^account_id and log.sequence_number > ^after_sequence,
+      order_by: [asc: log.sequence_number],
+      limit: @batch_size
+    )
+    |> Repo.all()
+  end
+
+  defp load_checkpoints(account_id) do
+    from(checkpoint in Checkpoint, where: checkpoint.account_id == ^account_id)
+    |> Repo.all()
+    |> Map.new(fn checkpoint -> {checkpoint.sequence_number, checkpoint} end)
+  end
+
+  defp new_accumulator do
+    %{
+      account_id: nil,
+      valid: true,
+      total_entries: 0,
+      verified_entries: 0,
+      expected_sequence: 1,
+      previous_checksum: nil,
+      last_sequence: 0,
+      first_failure: nil,
+      sequence_gaps: [],
+      checkpoint_results: []
+    }
+  end
+
+  defp finalize_report(acc) do
+    %{
+      valid: acc.valid,
+      total_entries: acc.total_entries,
+      verified_entries: acc.verified_entries,
+      first_failure: acc.first_failure,
+      sequence_gaps: Enum.reverse(acc.sequence_gaps),
+      checkpoint_results: Enum.reverse(acc.checkpoint_results),
+      duration_ms: 0
+    }
+  end
+
+  defp mark_invalid(acc, failure) do
+    case acc.first_failure do
+      nil -> %{acc | valid: false, first_failure: failure}
+      _existing -> %{acc | valid: false}
+    end
+  end
+
+  defp elapsed_ms(started_at) do
+    System.monotonic_time(:millisecond)
+    |> Kernel.-(started_at)
+    |> max(0)
+  end
+end

file update:
diff --git a/app/lib/app/audit/verifier.ex b/app/lib/app/audit/verifier.ex
new file mode 100644
index 0000000000000000000000000000000000000000..2a4bd77516a2c76ccc9b05de1778dd8781ded096
--- /dev/null
+++ b/app/lib/app/audit/verifier.ex
@@ -0,0 +1,203 @@
+defmodule GA.Audit.Verifier do
+  @moduledoc """
+  Streams and verifies account-scoped audit chains.
+  """
+
+  import Ecto.Query, warn: false
+
+  alias GA.Accounts
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  @batch_size 1000
+
+  @doc """
+  Verifies an account's audit chain and returns a report map.
+  """
+  @spec verify(binary()) :: map() | {:error, :invalid_id | :not_found}
+  def verify(account_id) when is_binary(account_id) do
+    started_at = System.monotonic_time(:millisecond)
+
+    with {:ok, hmac_key} <- Accounts.get_hmac_key(account_id) do
+      checkpoints_by_sequence = load_checkpoints(account_id)
+      report = stream_and_verify(account_id, hmac_key, checkpoints_by_sequence)
+
+      Map.put(report, :duration_ms, elapsed_ms(started_at))
+    end
+  end
+
+  def verify(_), do: {:error, :invalid_id}
+
+  defp stream_and_verify(account_id, hmac_key, checkpoints_by_sequence) do
+    initial =
+      new_accumulator()
+      |> Map.put(:account_id, account_id)
+
+    account_id
+    |> do_verify_batches(hmac_key, checkpoints_by_sequence, initial)
+    |> finalize_report()
+  end
+
+  defp do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, acc) do
+    batch = load_batch(account_id, acc.last_sequence)
+
+    case batch do
+      [] ->
+        acc
+
+      _entries ->
+        next_acc =
+          Enum.reduce(batch, acc, fn entry, state ->
+            verify_entry(entry, hmac_key, checkpoints_by_sequence, state)
+          end)
+
+        do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, next_acc)
+    end
+  end
+
+  defp verify_entry(%Log{} = entry, hmac_key, checkpoints_by_sequence, acc) do
+    acc
+    |> verify_sequence(entry)
+    |> verify_checksum(entry, hmac_key)
+    |> verify_checkpoint(entry, checkpoints_by_sequence)
+    |> advance_state(entry)
+  end
+
+  defp verify_sequence(acc, %Log{sequence_number: found_sequence}) do
+    expected_sequence = acc.expected_sequence
+
+    if found_sequence == expected_sequence do
+      %{acc | expected_sequence: expected_sequence + 1}
+    else
+      missing =
+        if found_sequence > expected_sequence do
+          Enum.to_list(expected_sequence..(found_sequence - 1))
+        else
+          []
+        end
+
+      gap = %{
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      }
+
+      acc
+      |> mark_invalid(%{
+        type: :sequence_gap,
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      })
+      |> Map.update!(:sequence_gaps, &[gap | &1])
+      |> Map.put(:expected_sequence, max(found_sequence, expected_sequence) + 1)
+    end
+  end
+
+  defp verify_checksum(acc, %Log{} = entry, hmac_key) do
+    if Chain.verify_checksum(hmac_key, entry, acc.previous_checksum) do
+      acc
+    else
+      mark_invalid(acc, %{
+        type: :checksum_mismatch,
+        sequence_number: entry.sequence_number,
+        stored_checksum: entry.checksum,
+        expected_checksum: expected_checksum(hmac_key, entry, acc.previous_checksum)
+      })
+    end
+  end
+
+  defp verify_checkpoint(acc, %Log{} = entry, checkpoints_by_sequence) do
+    case Map.get(checkpoints_by_sequence, entry.sequence_number) do
+      nil ->
+        acc
+
+      %Checkpoint{} = checkpoint ->
+        valid? = checkpoint.checksum == entry.checksum
+        result = %{sequence_number: entry.sequence_number, valid: valid?}
+        next_acc = Map.update!(acc, :checkpoint_results, &[result | &1])
+
+        if valid? do
+          next_acc
+        else
+          mark_invalid(next_acc, %{
+            type: :checkpoint_mismatch,
+            sequence_number: entry.sequence_number,
+            checkpoint_checksum: checkpoint.checksum,
+            entry_checksum: entry.checksum
+          })
+        end
+    end
+  end
+
+  defp advance_state(acc, %Log{} = entry) do
+    %{
+      acc
+      | previous_checksum: entry.checksum,
+        last_sequence: entry.sequence_number,
+        total_entries: acc.total_entries + 1,
+        verified_entries: acc.verified_entries + 1
+    }
+  end
+
+  defp expected_checksum(hmac_key, entry, previous_checksum) do
+    Chain.compute_checksum(hmac_key, Map.from_struct(entry), previous_checksum)
+  rescue
+    _ -> nil
+  end
+
+  defp load_batch(account_id, after_sequence) do
+    from(log in Log,
+      where: log.account_id == ^account_id and log.sequence_number > ^after_sequence,
+      order_by: [asc: log.sequence_number],
+      limit: @batch_size
+    )
+    |> Repo.all()
+  end
+
+  defp load_checkpoints(account_id) do
+    from(checkpoint in Checkpoint, where: checkpoint.account_id == ^account_id)
+    |> Repo.all()
+    |> Map.new(fn checkpoint -> {checkpoint.sequence_number, checkpoint} end)
+  end
+
+  defp new_accumulator do
+    %{
+      account_id: nil,
+      valid: true,
+      total_entries: 0,
+      verified_entries: 0,
+      expected_sequence: 1,
+      previous_checksum: nil,
+      last_sequence: 0,
+      first_failure: nil,
+      sequence_gaps: [],
+      checkpoint_results: []
+    }
+  end
+
+  defp finalize_report(acc) do
+    %{
+      valid: acc.valid,
+      total_entries: acc.total_entries,
+      verified_entries: acc.verified_entries,
+      first_failure: acc.first_failure,
+      sequence_gaps: Enum.reverse(acc.sequence_gaps),
+      checkpoint_results: Enum.reverse(acc.checkpoint_results),
+      duration_ms: 0
+    }
+  end
+
+  defp mark_invalid(acc, failure) do
+    case acc.first_failure do
+      nil -> %{acc | valid: false, first_failure: failure}
+      _existing -> %{acc | valid: false}
+    end
+  end
+
+  defp elapsed_ms(started_at) do
+    System.monotonic_time(:millisecond)
+    |> Kernel.-(started_at)
+    |> max(0)
+  end
+end

codex
I’ve added the verifier module; next I’m wiring `GA.Audit.verify_chain/1`, adding the checkpoint worker + supervision entry, and then creating verifier/worker test coverage.
file update
M /Users/tbrewer/projects/goodway/goodaudit/app/lib/app/audit.ex
@@ -9,3 +9,3 @@
   alias GA.Accounts
-  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Audit.{Chain, Checkpoint, Log, Verifier}
   alias GA.Repo
@@ -116,2 +116,8 @@
 
+  @doc """
+  Verifies chain integrity for a single account.
+  """
+  def verify_chain(account_id) when is_binary(account_id), do: Verifier.verify(account_id)
+  def verify_chain(_), do: {:error, :invalid_id}
+
   defp lock_account(account_id) do
apply_patch(auto_approved=true) exited 0 in 35ms:
Success. Updated the following files:
M /Users/tbrewer/projects/goodway/goodaudit/app/lib/app/audit.ex
file update:
diff --git a/app/lib/app/audit.ex b/app/lib/app/audit.ex
index a48830aacf4da8275b40f15016dcd16bbc87411f..2c7fbce41d873e721533728bcc1c965168addd3d
--- a/app/lib/app/audit.ex
+++ b/app/lib/app/audit.ex
@@ -7,7 +7,7 @@
   import Ecto.Query, warn: false
 
   alias GA.Accounts
-  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Audit.{Chain, Checkpoint, Log, Verifier}
   alias GA.Repo
 
   @default_limit 50
@@ -114,6 +114,12 @@
 
   def list_checkpoints(_), do: []
 
+  @doc """
+  Verifies chain integrity for a single account.
+  """
+  def verify_chain(account_id) when is_binary(account_id), do: Verifier.verify(account_id)
+  def verify_chain(_), do: {:error, :invalid_id}
+
   defp lock_account(account_id) do
     {key_a, key_b} = account_lock_keys(account_id)
 
diff --git a/app/lib/app/audit/verifier.ex b/app/lib/app/audit/verifier.ex
new file mode 100644
index 0000000000000000000000000000000000000000..2a4bd77516a2c76ccc9b05de1778dd8781ded096
--- /dev/null
+++ b/app/lib/app/audit/verifier.ex
@@ -0,0 +1,203 @@
+defmodule GA.Audit.Verifier do
+  @moduledoc """
+  Streams and verifies account-scoped audit chains.
+  """
+
+  import Ecto.Query, warn: false
+
+  alias GA.Accounts
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  @batch_size 1000
+
+  @doc """
+  Verifies an account's audit chain and returns a report map.
+  """
+  @spec verify(binary()) :: map() | {:error, :invalid_id | :not_found}
+  def verify(account_id) when is_binary(account_id) do
+    started_at = System.monotonic_time(:millisecond)
+
+    with {:ok, hmac_key} <- Accounts.get_hmac_key(account_id) do
+      checkpoints_by_sequence = load_checkpoints(account_id)
+      report = stream_and_verify(account_id, hmac_key, checkpoints_by_sequence)
+
+      Map.put(report, :duration_ms, elapsed_ms(started_at))
+    end
+  end
+
+  def verify(_), do: {:error, :invalid_id}
+
+  defp stream_and_verify(account_id, hmac_key, checkpoints_by_sequence) do
+    initial =
+      new_accumulator()
+      |> Map.put(:account_id, account_id)
+
+    account_id
+    |> do_verify_batches(hmac_key, checkpoints_by_sequence, initial)
+    |> finalize_report()
+  end
+
+  defp do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, acc) do
+    batch = load_batch(account_id, acc.last_sequence)
+
+    case batch do
+      [] ->
+        acc
+
+      _entries ->
+        next_acc =
+          Enum.reduce(batch, acc, fn entry, state ->
+            verify_entry(entry, hmac_key, checkpoints_by_sequence, state)
+          end)
+
+        do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, next_acc)
+    end
+  end
+
+  defp verify_entry(%Log{} = entry, hmac_key, checkpoints_by_sequence, acc) do
+    acc
+    |> verify_sequence(entry)
+    |> verify_checksum(entry, hmac_key)
+    |> verify_checkpoint(entry, checkpoints_by_sequence)
+    |> advance_state(entry)
+  end
+
+  defp verify_sequence(acc, %Log{sequence_number: found_sequence}) do
+    expected_sequence = acc.expected_sequence
+
+    if found_sequence == expected_sequence do
+      %{acc | expected_sequence: expected_sequence + 1}
+    else
+      missing =
+        if found_sequence > expected_sequence do
+          Enum.to_list(expected_sequence..(found_sequence - 1))
+        else
+          []
+        end
+
+      gap = %{
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      }
+
+      acc
+      |> mark_invalid(%{
+        type: :sequence_gap,
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      })
+      |> Map.update!(:sequence_gaps, &[gap | &1])
+      |> Map.put(:expected_sequence, max(found_sequence, expected_sequence) + 1)
+    end
+  end
+
+  defp verify_checksum(acc, %Log{} = entry, hmac_key) do
+    if Chain.verify_checksum(hmac_key, entry, acc.previous_checksum) do
+      acc
+    else
+      mark_invalid(acc, %{
+        type: :checksum_mismatch,
+        sequence_number: entry.sequence_number,
+        stored_checksum: entry.checksum,
+        expected_checksum: expected_checksum(hmac_key, entry, acc.previous_checksum)
+      })
+    end
+  end
+
+  defp verify_checkpoint(acc, %Log{} = entry, checkpoints_by_sequence) do
+    case Map.get(checkpoints_by_sequence, entry.sequence_number) do
+      nil ->
+        acc
+
+      %Checkpoint{} = checkpoint ->
+        valid? = checkpoint.checksum == entry.checksum
+        result = %{sequence_number: entry.sequence_number, valid: valid?}
+        next_acc = Map.update!(acc, :checkpoint_results, &[result | &1])
+
+        if valid? do
+          next_acc
+        else
+          mark_invalid(next_acc, %{
+            type: :checkpoint_mismatch,
+            sequence_number: entry.sequence_number,
+            checkpoint_checksum: checkpoint.checksum,
+            entry_checksum: entry.checksum
+          })
+        end
+    end
+  end
+
+  defp advance_state(acc, %Log{} = entry) do
+    %{
+      acc
+      | previous_checksum: entry.checksum,
+        last_sequence: entry.sequence_number,
+        total_entries: acc.total_entries + 1,
+        verified_entries: acc.verified_entries + 1
+    }
+  end
+
+  defp expected_checksum(hmac_key, entry, previous_checksum) do
+    Chain.compute_checksum(hmac_key, Map.from_struct(entry), previous_checksum)
+  rescue
+    _ -> nil
+  end
+
+  defp load_batch(account_id, after_sequence) do
+    from(log in Log,
+      where: log.account_id == ^account_id and log.sequence_number > ^after_sequence,
+      order_by: [asc: log.sequence_number],
+      limit: @batch_size
+    )
+    |> Repo.all()
+  end
+
+  defp load_checkpoints(account_id) do
+    from(checkpoint in Checkpoint, where: checkpoint.account_id == ^account_id)
+    |> Repo.all()
+    |> Map.new(fn checkpoint -> {checkpoint.sequence_number, checkpoint} end)
+  end
+
+  defp new_accumulator do
+    %{
+      account_id: nil,
+      valid: true,
+      total_entries: 0,
+      verified_entries: 0,
+      expected_sequence: 1,
+      previous_checksum: nil,
+      last_sequence: 0,
+      first_failure: nil,
+      sequence_gaps: [],
+      checkpoint_results: []
+    }
+  end
+
+  defp finalize_report(acc) do
+    %{
+      valid: acc.valid,
+      total_entries: acc.total_entries,
+      verified_entries: acc.verified_entries,
+      first_failure: acc.first_failure,
+      sequence_gaps: Enum.reverse(acc.sequence_gaps),
+      checkpoint_results: Enum.reverse(acc.checkpoint_results),
+      duration_ms: 0
+    }
+  end
+
+  defp mark_invalid(acc, failure) do
+    case acc.first_failure do
+      nil -> %{acc | valid: false, first_failure: failure}
+      _existing -> %{acc | valid: false}
+    end
+  end
+
+  defp elapsed_ms(started_at) do
+    System.monotonic_time(:millisecond)
+    |> Kernel.-(started_at)
+    |> max(0)
+  end
+end

file update:
diff --git a/app/lib/app/audit.ex b/app/lib/app/audit.ex
index a48830aacf4da8275b40f15016dcd16bbc87411f..2c7fbce41d873e721533728bcc1c965168addd3d
--- a/app/lib/app/audit.ex
+++ b/app/lib/app/audit.ex
@@ -7,7 +7,7 @@
   import Ecto.Query, warn: false
 
   alias GA.Accounts
-  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Audit.{Chain, Checkpoint, Log, Verifier}
   alias GA.Repo
 
   @default_limit 50
@@ -114,6 +114,12 @@
 
   def list_checkpoints(_), do: []
 
+  @doc """
+  Verifies chain integrity for a single account.
+  """
+  def verify_chain(account_id) when is_binary(account_id), do: Verifier.verify(account_id)
+  def verify_chain(_), do: {:error, :invalid_id}
+
   defp lock_account(account_id) do
     {key_a, key_b} = account_lock_keys(account_id)
 
diff --git a/app/lib/app/audit/verifier.ex b/app/lib/app/audit/verifier.ex
new file mode 100644
index 0000000000000000000000000000000000000000..2a4bd77516a2c76ccc9b05de1778dd8781ded096
--- /dev/null
+++ b/app/lib/app/audit/verifier.ex
@@ -0,0 +1,203 @@
+defmodule GA.Audit.Verifier do
+  @moduledoc """
+  Streams and verifies account-scoped audit chains.
+  """
+
+  import Ecto.Query, warn: false
+
+  alias GA.Accounts
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  @batch_size 1000
+
+  @doc """
+  Verifies an account's audit chain and returns a report map.
+  """
+  @spec verify(binary()) :: map() | {:error, :invalid_id | :not_found}
+  def verify(account_id) when is_binary(account_id) do
+    started_at = System.monotonic_time(:millisecond)
+
+    with {:ok, hmac_key} <- Accounts.get_hmac_key(account_id) do
+      checkpoints_by_sequence = load_checkpoints(account_id)
+      report = stream_and_verify(account_id, hmac_key, checkpoints_by_sequence)
+
+      Map.put(report, :duration_ms, elapsed_ms(started_at))
+    end
+  end
+
+  def verify(_), do: {:error, :invalid_id}
+
+  defp stream_and_verify(account_id, hmac_key, checkpoints_by_sequence) do
+    initial =
+      new_accumulator()
+      |> Map.put(:account_id, account_id)
+
+    account_id
+    |> do_verify_batches(hmac_key, checkpoints_by_sequence, initial)
+    |> finalize_report()
+  end
+
+  defp do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, acc) do
+    batch = load_batch(account_id, acc.last_sequence)
+
+    case batch do
+      [] ->
+        acc
+
+      _entries ->
+        next_acc =
+          Enum.reduce(batch, acc, fn entry, state ->
+            verify_entry(entry, hmac_key, checkpoints_by_sequence, state)
+          end)
+
+        do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, next_acc)
+    end
+  end
+
+  defp verify_entry(%Log{} = entry, hmac_key, checkpoints_by_sequence, acc) do
+    acc
+    |> verify_sequence(entry)
+    |> verify_checksum(entry, hmac_key)
+    |> verify_checkpoint(entry, checkpoints_by_sequence)
+    |> advance_state(entry)
+  end
+
+  defp verify_sequence(acc, %Log{sequence_number: found_sequence}) do
+    expected_sequence = acc.expected_sequence
+
+    if found_sequence == expected_sequence do
+      %{acc | expected_sequence: expected_sequence + 1}
+    else
+      missing =
+        if found_sequence > expected_sequence do
+          Enum.to_list(expected_sequence..(found_sequence - 1))
+        else
+          []
+        end
+
+      gap = %{
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      }
+
+      acc
+      |> mark_invalid(%{
+        type: :sequence_gap,
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      })
+      |> Map.update!(:sequence_gaps, &[gap | &1])
+      |> Map.put(:expected_sequence, max(found_sequence, expected_sequence) + 1)
+    end
+  end
+
+  defp verify_checksum(acc, %Log{} = entry, hmac_key) do
+    if Chain.verify_checksum(hmac_key, entry, acc.previous_checksum) do
+      acc
+    else
+      mark_invalid(acc, %{
+        type: :checksum_mismatch,
+        sequence_number: entry.sequence_number,
+        stored_checksum: entry.checksum,
+        expected_checksum: expected_checksum(hmac_key, entry, acc.previous_checksum)
+      })
+    end
+  end
+
+  defp verify_checkpoint(acc, %Log{} = entry, checkpoints_by_sequence) do
+    case Map.get(checkpoints_by_sequence, entry.sequence_number) do
+      nil ->
+        acc
+
+      %Checkpoint{} = checkpoint ->
+        valid? = checkpoint.checksum == entry.checksum
+        result = %{sequence_number: entry.sequence_number, valid: valid?}
+        next_acc = Map.update!(acc, :checkpoint_results, &[result | &1])
+
+        if valid? do
+          next_acc
+        else
+          mark_invalid(next_acc, %{
+            type: :checkpoint_mismatch,
+            sequence_number: entry.sequence_number,
+            checkpoint_checksum: checkpoint.checksum,
+            entry_checksum: entry.checksum
+          })
+        end
+    end
+  end
+
+  defp advance_state(acc, %Log{} = entry) do
+    %{
+      acc
+      | previous_checksum: entry.checksum,
+        last_sequence: entry.sequence_number,
+        total_entries: acc.total_entries + 1,
+        verified_entries: acc.verified_entries + 1
+    }
+  end
+
+  defp expected_checksum(hmac_key, entry, previous_checksum) do
+    Chain.compute_checksum(hmac_key, Map.from_struct(entry), previous_checksum)
+  rescue
+    _ -> nil
+  end
+
+  defp load_batch(account_id, after_sequence) do
+    from(log in Log,
+      where: log.account_id == ^account_id and log.sequence_number > ^after_sequence,
+      order_by: [asc: log.sequence_number],
+      limit: @batch_size
+    )
+    |> Repo.all()
+  end
+
+  defp load_checkpoints(account_id) do
+    from(checkpoint in Checkpoint, where: checkpoint.account_id == ^account_id)
+    |> Repo.all()
+    |> Map.new(fn checkpoint -> {checkpoint.sequence_number, checkpoint} end)
+  end
+
+  defp new_accumulator do
+    %{
+      account_id: nil,
+      valid: true,
+      total_entries: 0,
+      verified_entries: 0,
+      expected_sequence: 1,
+      previous_checksum: nil,
+      last_sequence: 0,
+      first_failure: nil,
+      sequence_gaps: [],
+      checkpoint_results: []
+    }
+  end
+
+  defp finalize_report(acc) do
+    %{
+      valid: acc.valid,
+      total_entries: acc.total_entries,
+      verified_entries: acc.verified_entries,
+      first_failure: acc.first_failure,
+      sequence_gaps: Enum.reverse(acc.sequence_gaps),
+      checkpoint_results: Enum.reverse(acc.checkpoint_results),
+      duration_ms: 0
+    }
+  end
+
+  defp mark_invalid(acc, failure) do
+    case acc.first_failure do
+      nil -> %{acc | valid: false, first_failure: failure}
+      _existing -> %{acc | valid: false}
+    end
+  end
+
+  defp elapsed_ms(started_at) do
+    System.monotonic_time(:millisecond)
+    |> Kernel.-(started_at)
+    |> max(0)
+  end
+end

file update
A /Users/tbrewer/projects/goodway/goodaudit/app/lib/app/audit/checkpoint_worker.ex
defmodule GA.Audit.CheckpointWorker do
  @moduledoc """
  Periodically creates audit checkpoints for all active accounts.
  """

  use GenServer

  import Ecto.Query, warn: false

  require Logger

  alias GA.Accounts.Account
  alias GA.Audit
  alias GA.Repo

  @default_interval_ms :timer.hours(1)

  def start_link(opts \\ []) do
    name = Keyword.get(opts, :name, __MODULE__)
    GenServer.start_link(__MODULE__, opts, name: name)
  end

  @impl true
  def init(opts) do
    interval_ms =
      opts
      |> Keyword.get(:interval_ms, configured_interval_ms())
      |> normalize_interval()

    schedule_tick(interval_ms)

    {:ok, %{interval_ms: interval_ms}}
  end

  @impl true
  def handle_info(:create_checkpoints, state) do
    active_account_ids()
    |> Enum.each(&create_checkpoint_for_account/1)

    schedule_tick(state.interval_ms)
    {:noreply, state}
  end

  defp active_account_ids do
    from(account in Account,
      where: account.status == :active,
      select: account.id
    )
    |> Repo.all()
  end

  defp create_checkpoint_for_account(account_id) do
    case Audit.create_checkpoint(account_id) do
      {:ok, _checkpoint} ->
        Logger.debug("checkpoint_worker created checkpoint for account=#{account_id}")

      {:error, :no_entries} ->
        Logger.debug("checkpoint_worker skipping account=#{account_id}: no audit entries")

      {:error, reason} ->
        Logger.error("checkpoint_worker failed for account=#{account_id}: #{inspect(reason)}")
    end
  rescue
    error ->
      Logger.error(
        "checkpoint_worker crashed for account=#{account_id}: #{Exception.message(error)}"
      )
  end

  defp schedule_tick(interval_ms) do
    Process.send_after(self(), :create_checkpoints, interval_ms)
  end

  defp configured_interval_ms do
    :app
    |> Application.get_env(__MODULE__, [])
    |> Keyword.get(:interval_ms, @default_interval_ms)
  end

  defp normalize_interval(interval_ms) when is_integer(interval_ms) and interval_ms > 0, do: interval_ms
  defp normalize_interval(_), do: @default_interval_ms
end
apply_patch(auto_approved=true) exited 0 in 50ms:
Success. Updated the following files:
A /Users/tbrewer/projects/goodway/goodaudit/app/lib/app/audit/checkpoint_worker.ex
file update:
diff --git a/app/lib/app/audit.ex b/app/lib/app/audit.ex
index a48830aacf4da8275b40f15016dcd16bbc87411f..2c7fbce41d873e721533728bcc1c965168addd3d
--- a/app/lib/app/audit.ex
+++ b/app/lib/app/audit.ex
@@ -7,7 +7,7 @@
   import Ecto.Query, warn: false
 
   alias GA.Accounts
-  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Audit.{Chain, Checkpoint, Log, Verifier}
   alias GA.Repo
 
   @default_limit 50
@@ -114,6 +114,12 @@
 
   def list_checkpoints(_), do: []
 
+  @doc """
+  Verifies chain integrity for a single account.
+  """
+  def verify_chain(account_id) when is_binary(account_id), do: Verifier.verify(account_id)
+  def verify_chain(_), do: {:error, :invalid_id}
+
   defp lock_account(account_id) do
     {key_a, key_b} = account_lock_keys(account_id)
 
diff --git a/app/lib/app/audit/checkpoint_worker.ex b/app/lib/app/audit/checkpoint_worker.ex
new file mode 100644
index 0000000000000000000000000000000000000000..f54908642ad7c854eae895b9605661f5beb0246f
--- /dev/null
+++ b/app/lib/app/audit/checkpoint_worker.ex
@@ -0,0 +1,82 @@
+defmodule GA.Audit.CheckpointWorker do
+  @moduledoc """
+  Periodically creates audit checkpoints for all active accounts.
+  """
+
+  use GenServer
+
+  import Ecto.Query, warn: false
+
+  require Logger
+
+  alias GA.Accounts.Account
+  alias GA.Audit
+  alias GA.Repo
+
+  @default_interval_ms :timer.hours(1)
+
+  def start_link(opts \\ []) do
+    name = Keyword.get(opts, :name, __MODULE__)
+    GenServer.start_link(__MODULE__, opts, name: name)
+  end
+
+  @impl true
+  def init(opts) do
+    interval_ms =
+      opts
+      |> Keyword.get(:interval_ms, configured_interval_ms())
+      |> normalize_interval()
+
+    schedule_tick(interval_ms)
+
+    {:ok, %{interval_ms: interval_ms}}
+  end
+
+  @impl true
+  def handle_info(:create_checkpoints, state) do
+    active_account_ids()
+    |> Enum.each(&create_checkpoint_for_account/1)
+
+    schedule_tick(state.interval_ms)
+    {:noreply, state}
+  end
+
+  defp active_account_ids do
+    from(account in Account,
+      where: account.status == :active,
+      select: account.id
+    )
+    |> Repo.all()
+  end
+
+  defp create_checkpoint_for_account(account_id) do
+    case Audit.create_checkpoint(account_id) do
+      {:ok, _checkpoint} ->
+        Logger.debug("checkpoint_worker created checkpoint for account=#{account_id}")
+
+      {:error, :no_entries} ->
+        Logger.debug("checkpoint_worker skipping account=#{account_id}: no audit entries")
+
+      {:error, reason} ->
+        Logger.error("checkpoint_worker failed for account=#{account_id}: #{inspect(reason)}")
+    end
+  rescue
+    error ->
+      Logger.error(
+        "checkpoint_worker crashed for account=#{account_id}: #{Exception.message(error)}"
+      )
+  end
+
+  defp schedule_tick(interval_ms) do
+    Process.send_after(self(), :create_checkpoints, interval_ms)
+  end
+
+  defp configured_interval_ms do
+    :app
+    |> Application.get_env(__MODULE__, [])
+    |> Keyword.get(:interval_ms, @default_interval_ms)
+  end
+
+  defp normalize_interval(interval_ms) when is_integer(interval_ms) and interval_ms > 0, do: interval_ms
+  defp normalize_interval(_), do: @default_interval_ms
+end
diff --git a/app/lib/app/audit/verifier.ex b/app/lib/app/audit/verifier.ex
new file mode 100644
index 0000000000000000000000000000000000000000..2a4bd77516a2c76ccc9b05de1778dd8781ded096
--- /dev/null
+++ b/app/lib/app/audit/verifier.ex
@@ -0,0 +1,203 @@
+defmodule GA.Audit.Verifier do
+  @moduledoc """
+  Streams and verifies account-scoped audit chains.
+  """
+
+  import Ecto.Query, warn: false
+
+  alias GA.Accounts
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  @batch_size 1000
+
+  @doc """
+  Verifies an account's audit chain and returns a report map.
+  """
+  @spec verify(binary()) :: map() | {:error, :invalid_id | :not_found}
+  def verify(account_id) when is_binary(account_id) do
+    started_at = System.monotonic_time(:millisecond)
+
+    with {:ok, hmac_key} <- Accounts.get_hmac_key(account_id) do
+      checkpoints_by_sequence = load_checkpoints(account_id)
+      report = stream_and_verify(account_id, hmac_key, checkpoints_by_sequence)
+
+      Map.put(report, :duration_ms, elapsed_ms(started_at))
+    end
+  end
+
+  def verify(_), do: {:error, :invalid_id}
+
+  defp stream_and_verify(account_id, hmac_key, checkpoints_by_sequence) do
+    initial =
+      new_accumulator()
+      |> Map.put(:account_id, account_id)
+
+    account_id
+    |> do_verify_batches(hmac_key, checkpoints_by_sequence, initial)
+    |> finalize_report()
+  end
+
+  defp do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, acc) do
+    batch = load_batch(account_id, acc.last_sequence)
+
+    case batch do
+      [] ->
+        acc
+
+      _entries ->
+        next_acc =
+          Enum.reduce(batch, acc, fn entry, state ->
+            verify_entry(entry, hmac_key, checkpoints_by_sequence, state)
+          end)
+
+        do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, next_acc)
+    end
+  end
+
+  defp verify_entry(%Log{} = entry, hmac_key, checkpoints_by_sequence, acc) do
+    acc
+    |> verify_sequence(entry)
+    |> verify_checksum(entry, hmac_key)
+    |> verify_checkpoint(entry, checkpoints_by_sequence)
+    |> advance_state(entry)
+  end
+
+  defp verify_sequence(acc, %Log{sequence_number: found_sequence}) do
+    expected_sequence = acc.expected_sequence
+
+    if found_sequence == expected_sequence do
+      %{acc | expected_sequence: expected_sequence + 1}
+    else
+      missing =
+        if found_sequence > expected_sequence do
+          Enum.to_list(expected_sequence..(found_sequence - 1))
+        else
+          []
+        end
+
+      gap = %{
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      }
+
+      acc
+      |> mark_invalid(%{
+        type: :sequence_gap,
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      })
+      |> Map.update!(:sequence_gaps, &[gap | &1])
+      |> Map.put(:expected_sequence, max(found_sequence, expected_sequence) + 1)
+    end
+  end
+
+  defp verify_checksum(acc, %Log{} = entry, hmac_key) do
+    if Chain.verify_checksum(hmac_key, entry, acc.previous_checksum) do
+      acc
+    else
+      mark_invalid(acc, %{
+        type: :checksum_mismatch,
+        sequence_number: entry.sequence_number,
+        stored_checksum: entry.checksum,
+        expected_checksum: expected_checksum(hmac_key, entry, acc.previous_checksum)
+      })
+    end
+  end
+
+  defp verify_checkpoint(acc, %Log{} = entry, checkpoints_by_sequence) do
+    case Map.get(checkpoints_by_sequence, entry.sequence_number) do
+      nil ->
+        acc
+
+      %Checkpoint{} = checkpoint ->
+        valid? = checkpoint.checksum == entry.checksum
+        result = %{sequence_number: entry.sequence_number, valid: valid?}
+        next_acc = Map.update!(acc, :checkpoint_results, &[result | &1])
+
+        if valid? do
+          next_acc
+        else
+          mark_invalid(next_acc, %{
+            type: :checkpoint_mismatch,
+            sequence_number: entry.sequence_number,
+            checkpoint_checksum: checkpoint.checksum,
+            entry_checksum: entry.checksum
+          })
+        end
+    end
+  end
+
+  defp advance_state(acc, %Log{} = entry) do
+    %{
+      acc
+      | previous_checksum: entry.checksum,
+        last_sequence: entry.sequence_number,
+        total_entries: acc.total_entries + 1,
+        verified_entries: acc.verified_entries + 1
+    }
+  end
+
+  defp expected_checksum(hmac_key, entry, previous_checksum) do
+    Chain.compute_checksum(hmac_key, Map.from_struct(entry), previous_checksum)
+  rescue
+    _ -> nil
+  end
+
+  defp load_batch(account_id, after_sequence) do
+    from(log in Log,
+      where: log.account_id == ^account_id and log.sequence_number > ^after_sequence,
+      order_by: [asc: log.sequence_number],
+      limit: @batch_size
+    )
+    |> Repo.all()
+  end
+
+  defp load_checkpoints(account_id) do
+    from(checkpoint in Checkpoint, where: checkpoint.account_id == ^account_id)
+    |> Repo.all()
+    |> Map.new(fn checkpoint -> {checkpoint.sequence_number, checkpoint} end)
+  end
+
+  defp new_accumulator do
+    %{
+      account_id: nil,
+      valid: true,
+      total_entries: 0,
+      verified_entries: 0,
+      expected_sequence: 1,
+      previous_checksum: nil,
+      last_sequence: 0,
+      first_failure: nil,
+      sequence_gaps: [],
+      checkpoint_results: []
+    }
+  end
+
+  defp finalize_report(acc) do
+    %{
+      valid: acc.valid,
+      total_entries: acc.total_entries,
+      verified_entries: acc.verified_entries,
+      first_failure: acc.first_failure,
+      sequence_gaps: Enum.reverse(acc.sequence_gaps),
+      checkpoint_results: Enum.reverse(acc.checkpoint_results),
+      duration_ms: 0
+    }
+  end
+
+  defp mark_invalid(acc, failure) do
+    case acc.first_failure do
+      nil -> %{acc | valid: false, first_failure: failure}
+      _existing -> %{acc | valid: false}
+    end
+  end
+
+  defp elapsed_ms(started_at) do
+    System.monotonic_time(:millisecond)
+    |> Kernel.-(started_at)
+    |> max(0)
+  end
+end

file update:
diff --git a/app/lib/app/audit.ex b/app/lib/app/audit.ex
index a48830aacf4da8275b40f15016dcd16bbc87411f..2c7fbce41d873e721533728bcc1c965168addd3d
--- a/app/lib/app/audit.ex
+++ b/app/lib/app/audit.ex
@@ -7,7 +7,7 @@
   import Ecto.Query, warn: false
 
   alias GA.Accounts
-  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Audit.{Chain, Checkpoint, Log, Verifier}
   alias GA.Repo
 
   @default_limit 50
@@ -114,6 +114,12 @@
 
   def list_checkpoints(_), do: []
 
+  @doc """
+  Verifies chain integrity for a single account.
+  """
+  def verify_chain(account_id) when is_binary(account_id), do: Verifier.verify(account_id)
+  def verify_chain(_), do: {:error, :invalid_id}
+
   defp lock_account(account_id) do
     {key_a, key_b} = account_lock_keys(account_id)
 
diff --git a/app/lib/app/audit/checkpoint_worker.ex b/app/lib/app/audit/checkpoint_worker.ex
new file mode 100644
index 0000000000000000000000000000000000000000..f54908642ad7c854eae895b9605661f5beb0246f
--- /dev/null
+++ b/app/lib/app/audit/checkpoint_worker.ex
@@ -0,0 +1,82 @@
+defmodule GA.Audit.CheckpointWorker do
+  @moduledoc """
+  Periodically creates audit checkpoints for all active accounts.
+  """
+
+  use GenServer
+
+  import Ecto.Query, warn: false
+
+  require Logger
+
+  alias GA.Accounts.Account
+  alias GA.Audit
+  alias GA.Repo
+
+  @default_interval_ms :timer.hours(1)
+
+  def start_link(opts \\ []) do
+    name = Keyword.get(opts, :name, __MODULE__)
+    GenServer.start_link(__MODULE__, opts, name: name)
+  end
+
+  @impl true
+  def init(opts) do
+    interval_ms =
+      opts
+      |> Keyword.get(:interval_ms, configured_interval_ms())
+      |> normalize_interval()
+
+    schedule_tick(interval_ms)
+
+    {:ok, %{interval_ms: interval_ms}}
+  end
+
+  @impl true
+  def handle_info(:create_checkpoints, state) do
+    active_account_ids()
+    |> Enum.each(&create_checkpoint_for_account/1)
+
+    schedule_tick(state.interval_ms)
+    {:noreply, state}
+  end
+
+  defp active_account_ids do
+    from(account in Account,
+      where: account.status == :active,
+      select: account.id
+    )
+    |> Repo.all()
+  end
+
+  defp create_checkpoint_for_account(account_id) do
+    case Audit.create_checkpoint(account_id) do
+      {:ok, _checkpoint} ->
+        Logger.debug("checkpoint_worker created checkpoint for account=#{account_id}")
+
+      {:error, :no_entries} ->
+        Logger.debug("checkpoint_worker skipping account=#{account_id}: no audit entries")
+
+      {:error, reason} ->
+        Logger.error("checkpoint_worker failed for account=#{account_id}: #{inspect(reason)}")
+    end
+  rescue
+    error ->
+      Logger.error(
+        "checkpoint_worker crashed for account=#{account_id}: #{Exception.message(error)}"
+      )
+  end
+
+  defp schedule_tick(interval_ms) do
+    Process.send_after(self(), :create_checkpoints, interval_ms)
+  end
+
+  defp configured_interval_ms do
+    :app
+    |> Application.get_env(__MODULE__, [])
+    |> Keyword.get(:interval_ms, @default_interval_ms)
+  end
+
+  defp normalize_interval(interval_ms) when is_integer(interval_ms) and interval_ms > 0, do: interval_ms
+  defp normalize_interval(_), do: @default_interval_ms
+end
diff --git a/app/lib/app/audit/verifier.ex b/app/lib/app/audit/verifier.ex
new file mode 100644
index 0000000000000000000000000000000000000000..2a4bd77516a2c76ccc9b05de1778dd8781ded096
--- /dev/null
+++ b/app/lib/app/audit/verifier.ex
@@ -0,0 +1,203 @@
+defmodule GA.Audit.Verifier do
+  @moduledoc """
+  Streams and verifies account-scoped audit chains.
+  """
+
+  import Ecto.Query, warn: false
+
+  alias GA.Accounts
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  @batch_size 1000
+
+  @doc """
+  Verifies an account's audit chain and returns a report map.
+  """
+  @spec verify(binary()) :: map() | {:error, :invalid_id | :not_found}
+  def verify(account_id) when is_binary(account_id) do
+    started_at = System.monotonic_time(:millisecond)
+
+    with {:ok, hmac_key} <- Accounts.get_hmac_key(account_id) do
+      checkpoints_by_sequence = load_checkpoints(account_id)
+      report = stream_and_verify(account_id, hmac_key, checkpoints_by_sequence)
+
+      Map.put(report, :duration_ms, elapsed_ms(started_at))
+    end
+  end
+
+  def verify(_), do: {:error, :invalid_id}
+
+  defp stream_and_verify(account_id, hmac_key, checkpoints_by_sequence) do
+    initial =
+      new_accumulator()
+      |> Map.put(:account_id, account_id)
+
+    account_id
+    |> do_verify_batches(hmac_key, checkpoints_by_sequence, initial)
+    |> finalize_report()
+  end
+
+  defp do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, acc) do
+    batch = load_batch(account_id, acc.last_sequence)
+
+    case batch do
+      [] ->
+        acc
+
+      _entries ->
+        next_acc =
+          Enum.reduce(batch, acc, fn entry, state ->
+            verify_entry(entry, hmac_key, checkpoints_by_sequence, state)
+          end)
+
+        do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, next_acc)
+    end
+  end
+
+  defp verify_entry(%Log{} = entry, hmac_key, checkpoints_by_sequence, acc) do
+    acc
+    |> verify_sequence(entry)
+    |> verify_checksum(entry, hmac_key)
+    |> verify_checkpoint(entry, checkpoints_by_sequence)
+    |> advance_state(entry)
+  end
+
+  defp verify_sequence(acc, %Log{sequence_number: found_sequence}) do
+    expected_sequence = acc.expected_sequence
+
+    if found_sequence == expected_sequence do
+      %{acc | expected_sequence: expected_sequence + 1}
+    else
+      missing =
+        if found_sequence > expected_sequence do
+          Enum.to_list(expected_sequence..(found_sequence - 1))
+        else
+          []
+        end
+
+      gap = %{
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      }
+
+      acc
+      |> mark_invalid(%{
+        type: :sequence_gap,
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      })
+      |> Map.update!(:sequence_gaps, &[gap | &1])
+      |> Map.put(:expected_sequence, max(found_sequence, expected_sequence) + 1)
+    end
+  end
+
+  defp verify_checksum(acc, %Log{} = entry, hmac_key) do
+    if Chain.verify_checksum(hmac_key, entry, acc.previous_checksum) do
+      acc
+    else
+      mark_invalid(acc, %{
+        type: :checksum_mismatch,
+        sequence_number: entry.sequence_number,
+        stored_checksum: entry.checksum,
+        expected_checksum: expected_checksum(hmac_key, entry, acc.previous_checksum)
+      })
+    end
+  end
+
+  defp verify_checkpoint(acc, %Log{} = entry, checkpoints_by_sequence) do
+    case Map.get(checkpoints_by_sequence, entry.sequence_number) do
+      nil ->
+        acc
+
+      %Checkpoint{} = checkpoint ->
+        valid? = checkpoint.checksum == entry.checksum
+        result = %{sequence_number: entry.sequence_number, valid: valid?}
+        next_acc = Map.update!(acc, :checkpoint_results, &[result | &1])
+
+        if valid? do
+          next_acc
+        else
+          mark_invalid(next_acc, %{
+            type: :checkpoint_mismatch,
+            sequence_number: entry.sequence_number,
+            checkpoint_checksum: checkpoint.checksum,
+            entry_checksum: entry.checksum
+          })
+        end
+    end
+  end
+
+  defp advance_state(acc, %Log{} = entry) do
+    %{
+      acc
+      | previous_checksum: entry.checksum,
+        last_sequence: entry.sequence_number,
+        total_entries: acc.total_entries + 1,
+        verified_entries: acc.verified_entries + 1
+    }
+  end
+
+  defp expected_checksum(hmac_key, entry, previous_checksum) do
+    Chain.compute_checksum(hmac_key, Map.from_struct(entry), previous_checksum)
+  rescue
+    _ -> nil
+  end
+
+  defp load_batch(account_id, after_sequence) do
+    from(log in Log,
+      where: log.account_id == ^account_id and log.sequence_number > ^after_sequence,
+      order_by: [asc: log.sequence_number],
+      limit: @batch_size
+    )
+    |> Repo.all()
+  end
+
+  defp load_checkpoints(account_id) do
+    from(checkpoint in Checkpoint, where: checkpoint.account_id == ^account_id)
+    |> Repo.all()
+    |> Map.new(fn checkpoint -> {checkpoint.sequence_number, checkpoint} end)
+  end
+
+  defp new_accumulator do
+    %{
+      account_id: nil,
+      valid: true,
+      total_entries: 0,
+      verified_entries: 0,
+      expected_sequence: 1,
+      previous_checksum: nil,
+      last_sequence: 0,
+      first_failure: nil,
+      sequence_gaps: [],
+      checkpoint_results: []
+    }
+  end
+
+  defp finalize_report(acc) do
+    %{
+      valid: acc.valid,
+      total_entries: acc.total_entries,
+      verified_entries: acc.verified_entries,
+      first_failure: acc.first_failure,
+      sequence_gaps: Enum.reverse(acc.sequence_gaps),
+      checkpoint_results: Enum.reverse(acc.checkpoint_results),
+      duration_ms: 0
+    }
+  end
+
+  defp mark_invalid(acc, failure) do
+    case acc.first_failure do
+      nil -> %{acc | valid: false, first_failure: failure}
+      _existing -> %{acc | valid: false}
+    end
+  end
+
+  defp elapsed_ms(started_at) do
+    System.monotonic_time(:millisecond)
+    |> Kernel.-(started_at)
+    |> max(0)
+  end
+end

file update
M /Users/tbrewer/projects/goodway/goodaudit/app/lib/app/application.ex
@@ -16,2 +16,3 @@
       # {GA.Worker, arg},
+      {GA.Audit.CheckpointWorker, checkpoint_worker_opts()},
       # Start to serve requests, typically the last entry
@@ -33,2 +34,6 @@
   end
+
+  defp checkpoint_worker_opts do
+    Application.get_env(:app, GA.Audit.CheckpointWorker, [])
+  end
 end
apply_patch(auto_approved=true) exited 0 in 60ms:
Success. Updated the following files:
M /Users/tbrewer/projects/goodway/goodaudit/app/lib/app/application.ex
file update:
diff --git a/app/lib/app/application.ex b/app/lib/app/application.ex
index d865bfe6a48ed688366f7d85220490621cd809a7..08bab073bf607f5bb04aae846094b0b08ec40fe4
--- a/app/lib/app/application.ex
+++ b/app/lib/app/application.ex
@@ -14,6 +14,7 @@
       {Phoenix.PubSub, name: GA.PubSub},
       # Start a worker by calling: GA.Worker.start_link(arg)
       # {GA.Worker, arg},
+      {GA.Audit.CheckpointWorker, checkpoint_worker_opts()},
       # Start to serve requests, typically the last entry
       GAWeb.Endpoint
     ]
@@ -31,4 +32,8 @@
     GAWeb.Endpoint.config_change(changed, removed)
     :ok
   end
+
+  defp checkpoint_worker_opts do
+    Application.get_env(:app, GA.Audit.CheckpointWorker, [])
+  end
 end
diff --git a/app/lib/app/audit.ex b/app/lib/app/audit.ex
index a48830aacf4da8275b40f15016dcd16bbc87411f..2c7fbce41d873e721533728bcc1c965168addd3d
--- a/app/lib/app/audit.ex
+++ b/app/lib/app/audit.ex
@@ -7,7 +7,7 @@
   import Ecto.Query, warn: false
 
   alias GA.Accounts
-  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Audit.{Chain, Checkpoint, Log, Verifier}
   alias GA.Repo
 
   @default_limit 50
@@ -114,6 +114,12 @@
 
   def list_checkpoints(_), do: []
 
+  @doc """
+  Verifies chain integrity for a single account.
+  """
+  def verify_chain(account_id) when is_binary(account_id), do: Verifier.verify(account_id)
+  def verify_chain(_), do: {:error, :invalid_id}
+
   defp lock_account(account_id) do
     {key_a, key_b} = account_lock_keys(account_id)
 
diff --git a/app/lib/app/audit/checkpoint_worker.ex b/app/lib/app/audit/checkpoint_worker.ex
new file mode 100644
index 0000000000000000000000000000000000000000..f54908642ad7c854eae895b9605661f5beb0246f
--- /dev/null
+++ b/app/lib/app/audit/checkpoint_worker.ex
@@ -0,0 +1,82 @@
+defmodule GA.Audit.CheckpointWorker do
+  @moduledoc """
+  Periodically creates audit checkpoints for all active accounts.
+  """
+
+  use GenServer
+
+  import Ecto.Query, warn: false
+
+  require Logger
+
+  alias GA.Accounts.Account
+  alias GA.Audit
+  alias GA.Repo
+
+  @default_interval_ms :timer.hours(1)
+
+  def start_link(opts \\ []) do
+    name = Keyword.get(opts, :name, __MODULE__)
+    GenServer.start_link(__MODULE__, opts, name: name)
+  end
+
+  @impl true
+  def init(opts) do
+    interval_ms =
+      opts
+      |> Keyword.get(:interval_ms, configured_interval_ms())
+      |> normalize_interval()
+
+    schedule_tick(interval_ms)
+
+    {:ok, %{interval_ms: interval_ms}}
+  end
+
+  @impl true
+  def handle_info(:create_checkpoints, state) do
+    active_account_ids()
+    |> Enum.each(&create_checkpoint_for_account/1)
+
+    schedule_tick(state.interval_ms)
+    {:noreply, state}
+  end
+
+  defp active_account_ids do
+    from(account in Account,
+      where: account.status == :active,
+      select: account.id
+    )
+    |> Repo.all()
+  end
+
+  defp create_checkpoint_for_account(account_id) do
+    case Audit.create_checkpoint(account_id) do
+      {:ok, _checkpoint} ->
+        Logger.debug("checkpoint_worker created checkpoint for account=#{account_id}")
+
+      {:error, :no_entries} ->
+        Logger.debug("checkpoint_worker skipping account=#{account_id}: no audit entries")
+
+      {:error, reason} ->
+        Logger.error("checkpoint_worker failed for account=#{account_id}: #{inspect(reason)}")
+    end
+  rescue
+    error ->
+      Logger.error(
+        "checkpoint_worker crashed for account=#{account_id}: #{Exception.message(error)}"
+      )
+  end
+
+  defp schedule_tick(interval_ms) do
+    Process.send_after(self(), :create_checkpoints, interval_ms)
+  end
+
+  defp configured_interval_ms do
+    :app
+    |> Application.get_env(__MODULE__, [])
+    |> Keyword.get(:interval_ms, @default_interval_ms)
+  end
+
+  defp normalize_interval(interval_ms) when is_integer(interval_ms) and interval_ms > 0, do: interval_ms
+  defp normalize_interval(_), do: @default_interval_ms
+end
diff --git a/app/lib/app/audit/verifier.ex b/app/lib/app/audit/verifier.ex
new file mode 100644
index 0000000000000000000000000000000000000000..2a4bd77516a2c76ccc9b05de1778dd8781ded096
--- /dev/null
+++ b/app/lib/app/audit/verifier.ex
@@ -0,0 +1,203 @@
+defmodule GA.Audit.Verifier do
+  @moduledoc """
+  Streams and verifies account-scoped audit chains.
+  """
+
+  import Ecto.Query, warn: false
+
+  alias GA.Accounts
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  @batch_size 1000
+
+  @doc """
+  Verifies an account's audit chain and returns a report map.
+  """
+  @spec verify(binary()) :: map() | {:error, :invalid_id | :not_found}
+  def verify(account_id) when is_binary(account_id) do
+    started_at = System.monotonic_time(:millisecond)
+
+    with {:ok, hmac_key} <- Accounts.get_hmac_key(account_id) do
+      checkpoints_by_sequence = load_checkpoints(account_id)
+      report = stream_and_verify(account_id, hmac_key, checkpoints_by_sequence)
+
+      Map.put(report, :duration_ms, elapsed_ms(started_at))
+    end
+  end
+
+  def verify(_), do: {:error, :invalid_id}
+
+  defp stream_and_verify(account_id, hmac_key, checkpoints_by_sequence) do
+    initial =
+      new_accumulator()
+      |> Map.put(:account_id, account_id)
+
+    account_id
+    |> do_verify_batches(hmac_key, checkpoints_by_sequence, initial)
+    |> finalize_report()
+  end
+
+  defp do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, acc) do
+    batch = load_batch(account_id, acc.last_sequence)
+
+    case batch do
+      [] ->
+        acc
+
+      _entries ->
+        next_acc =
+          Enum.reduce(batch, acc, fn entry, state ->
+            verify_entry(entry, hmac_key, checkpoints_by_sequence, state)
+          end)
+
+        do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, next_acc)
+    end
+  end
+
+  defp verify_entry(%Log{} = entry, hmac_key, checkpoints_by_sequence, acc) do
+    acc
+    |> verify_sequence(entry)
+    |> verify_checksum(entry, hmac_key)
+    |> verify_checkpoint(entry, checkpoints_by_sequence)
+    |> advance_state(entry)
+  end
+
+  defp verify_sequence(acc, %Log{sequence_number: found_sequence}) do
+    expected_sequence = acc.expected_sequence
+
+    if found_sequence == expected_sequence do
+      %{acc | expected_sequence: expected_sequence + 1}
+    else
+      missing =
+        if found_sequence > expected_sequence do
+          Enum.to_list(expected_sequence..(found_sequence - 1))
+        else
+          []
+        end
+
+      gap = %{
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      }
+
+      acc
+      |> mark_invalid(%{
+        type: :sequence_gap,
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      })
+      |> Map.update!(:sequence_gaps, &[gap | &1])
+      |> Map.put(:expected_sequence, max(found_sequence, expected_sequence) + 1)
+    end
+  end
+
+  defp verify_checksum(acc, %Log{} = entry, hmac_key) do
+    if Chain.verify_checksum(hmac_key, entry, acc.previous_checksum) do
+      acc
+    else
+      mark_invalid(acc, %{
+        type: :checksum_mismatch,
+        sequence_number: entry.sequence_number,
+        stored_checksum: entry.checksum,
+        expected_checksum: expected_checksum(hmac_key, entry, acc.previous_checksum)
+      })
+    end
+  end
+
+  defp verify_checkpoint(acc, %Log{} = entry, checkpoints_by_sequence) do
+    case Map.get(checkpoints_by_sequence, entry.sequence_number) do
+      nil ->
+        acc
+
+      %Checkpoint{} = checkpoint ->
+        valid? = checkpoint.checksum == entry.checksum
+        result = %{sequence_number: entry.sequence_number, valid: valid?}
+        next_acc = Map.update!(acc, :checkpoint_results, &[result | &1])
+
+        if valid? do
+          next_acc
+        else
+          mark_invalid(next_acc, %{
+            type: :checkpoint_mismatch,
+            sequence_number: entry.sequence_number,
+            checkpoint_checksum: checkpoint.checksum,
+            entry_checksum: entry.checksum
+          })
+        end
+    end
+  end
+
+  defp advance_state(acc, %Log{} = entry) do
+    %{
+      acc
+      | previous_checksum: entry.checksum,
+        last_sequence: entry.sequence_number,
+        total_entries: acc.total_entries + 1,
+        verified_entries: acc.verified_entries + 1
+    }
+  end
+
+  defp expected_checksum(hmac_key, entry, previous_checksum) do
+    Chain.compute_checksum(hmac_key, Map.from_struct(entry), previous_checksum)
+  rescue
+    _ -> nil
+  end
+
+  defp load_batch(account_id, after_sequence) do
+    from(log in Log,
+      where: log.account_id == ^account_id and log.sequence_number > ^after_sequence,
+      order_by: [asc: log.sequence_number],
+      limit: @batch_size
+    )
+    |> Repo.all()
+  end
+
+  defp load_checkpoints(account_id) do
+    from(checkpoint in Checkpoint, where: checkpoint.account_id == ^account_id)
+    |> Repo.all()
+    |> Map.new(fn checkpoint -> {checkpoint.sequence_number, checkpoint} end)
+  end
+
+  defp new_accumulator do
+    %{
+      account_id: nil,
+      valid: true,
+      total_entries: 0,
+      verified_entries: 0,
+      expected_sequence: 1,
+      previous_checksum: nil,
+      last_sequence: 0,
+      first_failure: nil,
+      sequence_gaps: [],
+      checkpoint_results: []
+    }
+  end
+
+  defp finalize_report(acc) do
+    %{
+      valid: acc.valid,
+      total_entries: acc.total_entries,
+      verified_entries: acc.verified_entries,
+      first_failure: acc.first_failure,
+      sequence_gaps: Enum.reverse(acc.sequence_gaps),
+      checkpoint_results: Enum.reverse(acc.checkpoint_results),
+      duration_ms: 0
+    }
+  end
+
+  defp mark_invalid(acc, failure) do
+    case acc.first_failure do
+      nil -> %{acc | valid: false, first_failure: failure}
+      _existing -> %{acc | valid: false}
+    end
+  end
+
+  defp elapsed_ms(started_at) do
+    System.monotonic_time(:millisecond)
+    |> Kernel.-(started_at)
+    |> max(0)
+  end
+end

file update:
diff --git a/app/lib/app/application.ex b/app/lib/app/application.ex
index d865bfe6a48ed688366f7d85220490621cd809a7..08bab073bf607f5bb04aae846094b0b08ec40fe4
--- a/app/lib/app/application.ex
+++ b/app/lib/app/application.ex
@@ -14,6 +14,7 @@
       {Phoenix.PubSub, name: GA.PubSub},
       # Start a worker by calling: GA.Worker.start_link(arg)
       # {GA.Worker, arg},
+      {GA.Audit.CheckpointWorker, checkpoint_worker_opts()},
       # Start to serve requests, typically the last entry
       GAWeb.Endpoint
     ]
@@ -31,4 +32,8 @@
     GAWeb.Endpoint.config_change(changed, removed)
     :ok
   end
+
+  defp checkpoint_worker_opts do
+    Application.get_env(:app, GA.Audit.CheckpointWorker, [])
+  end
 end
diff --git a/app/lib/app/audit.ex b/app/lib/app/audit.ex
index a48830aacf4da8275b40f15016dcd16bbc87411f..2c7fbce41d873e721533728bcc1c965168addd3d
--- a/app/lib/app/audit.ex
+++ b/app/lib/app/audit.ex
@@ -7,7 +7,7 @@
   import Ecto.Query, warn: false
 
   alias GA.Accounts
-  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Audit.{Chain, Checkpoint, Log, Verifier}
   alias GA.Repo
 
   @default_limit 50
@@ -114,6 +114,12 @@
 
   def list_checkpoints(_), do: []
 
+  @doc """
+  Verifies chain integrity for a single account.
+  """
+  def verify_chain(account_id) when is_binary(account_id), do: Verifier.verify(account_id)
+  def verify_chain(_), do: {:error, :invalid_id}
+
   defp lock_account(account_id) do
     {key_a, key_b} = account_lock_keys(account_id)
 
diff --git a/app/lib/app/audit/checkpoint_worker.ex b/app/lib/app/audit/checkpoint_worker.ex
new file mode 100644
index 0000000000000000000000000000000000000000..f54908642ad7c854eae895b9605661f5beb0246f
--- /dev/null
+++ b/app/lib/app/audit/checkpoint_worker.ex
@@ -0,0 +1,82 @@
+defmodule GA.Audit.CheckpointWorker do
+  @moduledoc """
+  Periodically creates audit checkpoints for all active accounts.
+  """
+
+  use GenServer
+
+  import Ecto.Query, warn: false
+
+  require Logger
+
+  alias GA.Accounts.Account
+  alias GA.Audit
+  alias GA.Repo
+
+  @default_interval_ms :timer.hours(1)
+
+  def start_link(opts \\ []) do
+    name = Keyword.get(opts, :name, __MODULE__)
+    GenServer.start_link(__MODULE__, opts, name: name)
+  end
+
+  @impl true
+  def init(opts) do
+    interval_ms =
+      opts
+      |> Keyword.get(:interval_ms, configured_interval_ms())
+      |> normalize_interval()
+
+    schedule_tick(interval_ms)
+
+    {:ok, %{interval_ms: interval_ms}}
+  end
+
+  @impl true
+  def handle_info(:create_checkpoints, state) do
+    active_account_ids()
+    |> Enum.each(&create_checkpoint_for_account/1)
+
+    schedule_tick(state.interval_ms)
+    {:noreply, state}
+  end
+
+  defp active_account_ids do
+    from(account in Account,
+      where: account.status == :active,
+      select: account.id
+    )
+    |> Repo.all()
+  end
+
+  defp create_checkpoint_for_account(account_id) do
+    case Audit.create_checkpoint(account_id) do
+      {:ok, _checkpoint} ->
+        Logger.debug("checkpoint_worker created checkpoint for account=#{account_id}")
+
+      {:error, :no_entries} ->
+        Logger.debug("checkpoint_worker skipping account=#{account_id}: no audit entries")
+
+      {:error, reason} ->
+        Logger.error("checkpoint_worker failed for account=#{account_id}: #{inspect(reason)}")
+    end
+  rescue
+    error ->
+      Logger.error(
+        "checkpoint_worker crashed for account=#{account_id}: #{Exception.message(error)}"
+      )
+  end
+
+  defp schedule_tick(interval_ms) do
+    Process.send_after(self(), :create_checkpoints, interval_ms)
+  end
+
+  defp configured_interval_ms do
+    :app
+    |> Application.get_env(__MODULE__, [])
+    |> Keyword.get(:interval_ms, @default_interval_ms)
+  end
+
+  defp normalize_interval(interval_ms) when is_integer(interval_ms) and interval_ms > 0, do: interval_ms
+  defp normalize_interval(_), do: @default_interval_ms
+end
diff --git a/app/lib/app/audit/verifier.ex b/app/lib/app/audit/verifier.ex
new file mode 100644
index 0000000000000000000000000000000000000000..2a4bd77516a2c76ccc9b05de1778dd8781ded096
--- /dev/null
+++ b/app/lib/app/audit/verifier.ex
@@ -0,0 +1,203 @@
+defmodule GA.Audit.Verifier do
+  @moduledoc """
+  Streams and verifies account-scoped audit chains.
+  """
+
+  import Ecto.Query, warn: false
+
+  alias GA.Accounts
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  @batch_size 1000
+
+  @doc """
+  Verifies an account's audit chain and returns a report map.
+  """
+  @spec verify(binary()) :: map() | {:error, :invalid_id | :not_found}
+  def verify(account_id) when is_binary(account_id) do
+    started_at = System.monotonic_time(:millisecond)
+
+    with {:ok, hmac_key} <- Accounts.get_hmac_key(account_id) do
+      checkpoints_by_sequence = load_checkpoints(account_id)
+      report = stream_and_verify(account_id, hmac_key, checkpoints_by_sequence)
+
+      Map.put(report, :duration_ms, elapsed_ms(started_at))
+    end
+  end
+
+  def verify(_), do: {:error, :invalid_id}
+
+  defp stream_and_verify(account_id, hmac_key, checkpoints_by_sequence) do
+    initial =
+      new_accumulator()
+      |> Map.put(:account_id, account_id)
+
+    account_id
+    |> do_verify_batches(hmac_key, checkpoints_by_sequence, initial)
+    |> finalize_report()
+  end
+
+  defp do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, acc) do
+    batch = load_batch(account_id, acc.last_sequence)
+
+    case batch do
+      [] ->
+        acc
+
+      _entries ->
+        next_acc =
+          Enum.reduce(batch, acc, fn entry, state ->
+            verify_entry(entry, hmac_key, checkpoints_by_sequence, state)
+          end)
+
+        do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, next_acc)
+    end
+  end
+
+  defp verify_entry(%Log{} = entry, hmac_key, checkpoints_by_sequence, acc) do
+    acc
+    |> verify_sequence(entry)
+    |> verify_checksum(entry, hmac_key)
+    |> verify_checkpoint(entry, checkpoints_by_sequence)
+    |> advance_state(entry)
+  end
+
+  defp verify_sequence(acc, %Log{sequence_number: found_sequence}) do
+    expected_sequence = acc.expected_sequence
+
+    if found_sequence == expected_sequence do
+      %{acc | expected_sequence: expected_sequence + 1}
+    else
+      missing =
+        if found_sequence > expected_sequence do
+          Enum.to_list(expected_sequence..(found_sequence - 1))
+        else
+          []
+        end
+
+      gap = %{
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      }
+
+      acc
+      |> mark_invalid(%{
+        type: :sequence_gap,
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      })
+      |> Map.update!(:sequence_gaps, &[gap | &1])
+      |> Map.put(:expected_sequence, max(found_sequence, expected_sequence) + 1)
+    end
+  end
+
+  defp verify_checksum(acc, %Log{} = entry, hmac_key) do
+    if Chain.verify_checksum(hmac_key, entry, acc.previous_checksum) do
+      acc
+    else
+      mark_invalid(acc, %{
+        type: :checksum_mismatch,
+        sequence_number: entry.sequence_number,
+        stored_checksum: entry.checksum,
+        expected_checksum: expected_checksum(hmac_key, entry, acc.previous_checksum)
+      })
+    end
+  end
+
+  defp verify_checkpoint(acc, %Log{} = entry, checkpoints_by_sequence) do
+    case Map.get(checkpoints_by_sequence, entry.sequence_number) do
+      nil ->
+        acc
+
+      %Checkpoint{} = checkpoint ->
+        valid? = checkpoint.checksum == entry.checksum
+        result = %{sequence_number: entry.sequence_number, valid: valid?}
+        next_acc = Map.update!(acc, :checkpoint_results, &[result | &1])
+
+        if valid? do
+          next_acc
+        else
+          mark_invalid(next_acc, %{
+            type: :checkpoint_mismatch,
+            sequence_number: entry.sequence_number,
+            checkpoint_checksum: checkpoint.checksum,
+            entry_checksum: entry.checksum
+          })
+        end
+    end
+  end
+
+  defp advance_state(acc, %Log{} = entry) do
+    %{
+      acc
+      | previous_checksum: entry.checksum,
+        last_sequence: entry.sequence_number,
+        total_entries: acc.total_entries + 1,
+        verified_entries: acc.verified_entries + 1
+    }
+  end
+
+  defp expected_checksum(hmac_key, entry, previous_checksum) do
+    Chain.compute_checksum(hmac_key, Map.from_struct(entry), previous_checksum)
+  rescue
+    _ -> nil
+  end
+
+  defp load_batch(account_id, after_sequence) do
+    from(log in Log,
+      where: log.account_id == ^account_id and log.sequence_number > ^after_sequence,
+      order_by: [asc: log.sequence_number],
+      limit: @batch_size
+    )
+    |> Repo.all()
+  end
+
+  defp load_checkpoints(account_id) do
+    from(checkpoint in Checkpoint, where: checkpoint.account_id == ^account_id)
+    |> Repo.all()
+    |> Map.new(fn checkpoint -> {checkpoint.sequence_number, checkpoint} end)
+  end
+
+  defp new_accumulator do
+    %{
+      account_id: nil,
+      valid: true,
+      total_entries: 0,
+      verified_entries: 0,
+      expected_sequence: 1,
+      previous_checksum: nil,
+      last_sequence: 0,
+      first_failure: nil,
+      sequence_gaps: [],
+      checkpoint_results: []
+    }
+  end
+
+  defp finalize_report(acc) do
+    %{
+      valid: acc.valid,
+      total_entries: acc.total_entries,
+      verified_entries: acc.verified_entries,
+      first_failure: acc.first_failure,
+      sequence_gaps: Enum.reverse(acc.sequence_gaps),
+      checkpoint_results: Enum.reverse(acc.checkpoint_results),
+      duration_ms: 0
+    }
+  end
+
+  defp mark_invalid(acc, failure) do
+    case acc.first_failure do
+      nil -> %{acc | valid: false, first_failure: failure}
+      _existing -> %{acc | valid: false}
+    end
+  end
+
+  defp elapsed_ms(started_at) do
+    System.monotonic_time(:millisecond)
+    |> Kernel.-(started_at)
+    |> max(0)
+  end
+end

file update
A /Users/tbrewer/projects/goodway/goodaudit/app/test/app/audit/verifier_test.exs
defmodule GA.Audit.VerifierTest do
  use GA.DataCase, async: false

  alias GA.Accounts
  alias GA.Audit
  alias GA.Audit.{Chain, Checkpoint, Log}
  alias GA.Repo

  describe "verify_chain/1" do
    test "returns a valid report for an intact account chain" do
      account = account_fixture()

      assert {:ok, _first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "r-1"}))
      assert {:ok, _second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "r-2"}))
      assert {:ok, checkpoint} = Audit.create_checkpoint(account.id)

      report = Audit.verify_chain(account.id)

      assert report.valid == true
      assert report.total_entries == 2
      assert report.verified_entries == 2
      assert report.first_failure == nil
      assert report.sequence_gaps == []
      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == checkpoint.sequence_number and &1.valid))
      assert is_integer(report.duration_ms)
      assert report.duration_ms >= 0
    end

    test "detects tampered checksum entries" do
      account = account_fixture()

      assert {:ok, first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "seed"}))

      insert_raw_log(account.id, %{
        sequence_number: 2,
        previous_checksum: first.checksum,
        checksum: String.duplicate("0", 64),
        resource_id: "tampered"
      })

      report = Audit.verify_chain(account.id)

      assert report.valid == false
      assert report.first_failure.type == :checksum_mismatch
      assert report.first_failure.sequence_number == 2
      assert report.first_failure.stored_checksum == String.duplicate("0", 64)
      refute is_nil(report.first_failure.expected_checksum)
    end

    test "detects sequence gaps within an account" do
      account = account_fixture()
      assert {:ok, _first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "gap-1"}))
      assert {:ok, second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "gap-2"}))
      assert {:ok, hmac_key} = Accounts.get_hmac_key(account.id)

      checksum =
        checksum_for_sequence(
          account.id,
          hmac_key,
          4,
          second.checksum,
          %{resource_id: "gap-4"}
        )

      insert_raw_log(account.id, %{
        sequence_number: 4,
        previous_checksum: second.checksum,
        checksum: checksum,
        resource_id: "gap-4"
      })

      report = Audit.verify_chain(account.id)

      assert report.valid == false
      assert report.first_failure.type == :sequence_gap
      assert [%{expected: 3, found: 4, missing: [3]}] = report.sequence_gaps
    end

    test "validates checkpoint anchors and reports invalid anchors" do
      account = account_fixture()
      assert {:ok, first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "cp-1"}))
      assert {:ok, second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "cp-2"}))

      insert_checkpoint(account.id, first.sequence_number, first.checksum)
      insert_checkpoint(account.id, second.sequence_number, String.duplicate("f", 64))

      report = Audit.verify_chain(account.id)

      assert report.valid == false
      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == first.sequence_number and &1.valid))
      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == second.sequence_number and not &1.valid))
    end

    test "includes non-negative duration tracking" do
      account = account_fixture()
      assert {:ok, _} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "duration"}))

      report = Audit.verify_chain(account.id)

      assert is_integer(report.duration_ms)
      assert report.duration_ms >= 0
    end

    test "verification remains account-scoped" do
      account_a = account_fixture()
      account_b = account_fixture()

      assert {:ok, _} = Audit.create_log_entry(account_a.id, valid_attrs(%{resource_id: "a-1"}))
      assert {:ok, first_b} = Audit.create_log_entry(account_b.id, valid_attrs(%{resource_id: "b-1"}))

      insert_raw_log(account_b.id, %{
        sequence_number: 2,
        previous_checksum: first_b.checksum,
        checksum: String.duplicate("a", 64),
        resource_id: "b-2-tampered"
      })

      report = Audit.verify_chain(account_a.id)

      assert report.valid == true
      assert report.total_entries == 1
      assert report.first_failure == nil
      assert report.sequence_gaps == []
    end
  end

  defp account_fixture(attrs \\ %{}) do
    defaults = %{name: "Verifier Account #{System.unique_integer([:positive])}"}
    {:ok, account} = Accounts.create_account(Map.merge(defaults, attrs))
    account
  end

  defp valid_attrs(overrides \\ %{}) do
    defaults = %{
      user_id: Ecto.UUID.generate(),
      user_role: "admin",
      session_id: "session-#{System.unique_integer([:positive])}",
      action: "read",
      resource_type: "patient",
      resource_id: Ecto.UUID.generate(),
      timestamp: ~U[2026-03-03 16:30:00Z],
      source_ip: "127.0.0.1",
      user_agent: "ExUnit",
      outcome: "success",
      failure_reason: nil,
      phi_accessed: false,
      metadata: %{"source" => "verifier_test"}
    }

    Map.merge(defaults, overrides)
  end

  defp checksum_for_sequence(account_id, hmac_key, sequence_number, previous_checksum, overrides \\ %{}) do
    attrs =
      valid_attrs(overrides)
      |> Map.put(:account_id, account_id)
      |> Map.put(:sequence_number, sequence_number)

    Chain.compute_checksum(hmac_key, attrs, previous_checksum)
  end

  defp insert_raw_log(account_id, attrs) do
    now = DateTime.utc_now() |> DateTime.truncate(:microsecond)
    id = Ecto.UUID.generate()

    row =
      valid_attrs()
      |> Map.merge(attrs)
      |> Map.merge(%{
        id: id,
        account_id: account_id,
        inserted_at: now,
        updated_at: now
      })

    {1, _} = Repo.insert_all(Log, [row])
    Repo.get!(Log, id)
  end

  defp insert_checkpoint(account_id, sequence_number, checksum) do
    %Checkpoint{}
    |> Checkpoint.changeset(%{
      account_id: account_id,
      sequence_number: sequence_number,
      checksum: checksum
    })
    |> Repo.insert!()
  end
end
apply_patch(auto_approved=true) exited 0 in 75ms:
Success. Updated the following files:
A /Users/tbrewer/projects/goodway/goodaudit/app/test/app/audit/verifier_test.exs
file update:
diff --git a/app/lib/app/application.ex b/app/lib/app/application.ex
index d865bfe6a48ed688366f7d85220490621cd809a7..08bab073bf607f5bb04aae846094b0b08ec40fe4
--- a/app/lib/app/application.ex
+++ b/app/lib/app/application.ex
@@ -14,6 +14,7 @@
       {Phoenix.PubSub, name: GA.PubSub},
       # Start a worker by calling: GA.Worker.start_link(arg)
       # {GA.Worker, arg},
+      {GA.Audit.CheckpointWorker, checkpoint_worker_opts()},
       # Start to serve requests, typically the last entry
       GAWeb.Endpoint
     ]
@@ -31,4 +32,8 @@
     GAWeb.Endpoint.config_change(changed, removed)
     :ok
   end
+
+  defp checkpoint_worker_opts do
+    Application.get_env(:app, GA.Audit.CheckpointWorker, [])
+  end
 end
diff --git a/app/lib/app/audit.ex b/app/lib/app/audit.ex
index a48830aacf4da8275b40f15016dcd16bbc87411f..2c7fbce41d873e721533728bcc1c965168addd3d
--- a/app/lib/app/audit.ex
+++ b/app/lib/app/audit.ex
@@ -7,7 +7,7 @@
   import Ecto.Query, warn: false
 
   alias GA.Accounts
-  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Audit.{Chain, Checkpoint, Log, Verifier}
   alias GA.Repo
 
   @default_limit 50
@@ -114,6 +114,12 @@
 
   def list_checkpoints(_), do: []
 
+  @doc """
+  Verifies chain integrity for a single account.
+  """
+  def verify_chain(account_id) when is_binary(account_id), do: Verifier.verify(account_id)
+  def verify_chain(_), do: {:error, :invalid_id}
+
   defp lock_account(account_id) do
     {key_a, key_b} = account_lock_keys(account_id)
 
diff --git a/app/lib/app/audit/checkpoint_worker.ex b/app/lib/app/audit/checkpoint_worker.ex
new file mode 100644
index 0000000000000000000000000000000000000000..f54908642ad7c854eae895b9605661f5beb0246f
--- /dev/null
+++ b/app/lib/app/audit/checkpoint_worker.ex
@@ -0,0 +1,82 @@
+defmodule GA.Audit.CheckpointWorker do
+  @moduledoc """
+  Periodically creates audit checkpoints for all active accounts.
+  """
+
+  use GenServer
+
+  import Ecto.Query, warn: false
+
+  require Logger
+
+  alias GA.Accounts.Account
+  alias GA.Audit
+  alias GA.Repo
+
+  @default_interval_ms :timer.hours(1)
+
+  def start_link(opts \\ []) do
+    name = Keyword.get(opts, :name, __MODULE__)
+    GenServer.start_link(__MODULE__, opts, name: name)
+  end
+
+  @impl true
+  def init(opts) do
+    interval_ms =
+      opts
+      |> Keyword.get(:interval_ms, configured_interval_ms())
+      |> normalize_interval()
+
+    schedule_tick(interval_ms)
+
+    {:ok, %{interval_ms: interval_ms}}
+  end
+
+  @impl true
+  def handle_info(:create_checkpoints, state) do
+    active_account_ids()
+    |> Enum.each(&create_checkpoint_for_account/1)
+
+    schedule_tick(state.interval_ms)
+    {:noreply, state}
+  end
+
+  defp active_account_ids do
+    from(account in Account,
+      where: account.status == :active,
+      select: account.id
+    )
+    |> Repo.all()
+  end
+
+  defp create_checkpoint_for_account(account_id) do
+    case Audit.create_checkpoint(account_id) do
+      {:ok, _checkpoint} ->
+        Logger.debug("checkpoint_worker created checkpoint for account=#{account_id}")
+
+      {:error, :no_entries} ->
+        Logger.debug("checkpoint_worker skipping account=#{account_id}: no audit entries")
+
+      {:error, reason} ->
+        Logger.error("checkpoint_worker failed for account=#{account_id}: #{inspect(reason)}")
+    end
+  rescue
+    error ->
+      Logger.error(
+        "checkpoint_worker crashed for account=#{account_id}: #{Exception.message(error)}"
+      )
+  end
+
+  defp schedule_tick(interval_ms) do
+    Process.send_after(self(), :create_checkpoints, interval_ms)
+  end
+
+  defp configured_interval_ms do
+    :app
+    |> Application.get_env(__MODULE__, [])
+    |> Keyword.get(:interval_ms, @default_interval_ms)
+  end
+
+  defp normalize_interval(interval_ms) when is_integer(interval_ms) and interval_ms > 0, do: interval_ms
+  defp normalize_interval(_), do: @default_interval_ms
+end
diff --git a/app/lib/app/audit/verifier.ex b/app/lib/app/audit/verifier.ex
new file mode 100644
index 0000000000000000000000000000000000000000..2a4bd77516a2c76ccc9b05de1778dd8781ded096
--- /dev/null
+++ b/app/lib/app/audit/verifier.ex
@@ -0,0 +1,203 @@
+defmodule GA.Audit.Verifier do
+  @moduledoc """
+  Streams and verifies account-scoped audit chains.
+  """
+
+  import Ecto.Query, warn: false
+
+  alias GA.Accounts
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  @batch_size 1000
+
+  @doc """
+  Verifies an account's audit chain and returns a report map.
+  """
+  @spec verify(binary()) :: map() | {:error, :invalid_id | :not_found}
+  def verify(account_id) when is_binary(account_id) do
+    started_at = System.monotonic_time(:millisecond)
+
+    with {:ok, hmac_key} <- Accounts.get_hmac_key(account_id) do
+      checkpoints_by_sequence = load_checkpoints(account_id)
+      report = stream_and_verify(account_id, hmac_key, checkpoints_by_sequence)
+
+      Map.put(report, :duration_ms, elapsed_ms(started_at))
+    end
+  end
+
+  def verify(_), do: {:error, :invalid_id}
+
+  defp stream_and_verify(account_id, hmac_key, checkpoints_by_sequence) do
+    initial =
+      new_accumulator()
+      |> Map.put(:account_id, account_id)
+
+    account_id
+    |> do_verify_batches(hmac_key, checkpoints_by_sequence, initial)
+    |> finalize_report()
+  end
+
+  defp do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, acc) do
+    batch = load_batch(account_id, acc.last_sequence)
+
+    case batch do
+      [] ->
+        acc
+
+      _entries ->
+        next_acc =
+          Enum.reduce(batch, acc, fn entry, state ->
+            verify_entry(entry, hmac_key, checkpoints_by_sequence, state)
+          end)
+
+        do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, next_acc)
+    end
+  end
+
+  defp verify_entry(%Log{} = entry, hmac_key, checkpoints_by_sequence, acc) do
+    acc
+    |> verify_sequence(entry)
+    |> verify_checksum(entry, hmac_key)
+    |> verify_checkpoint(entry, checkpoints_by_sequence)
+    |> advance_state(entry)
+  end
+
+  defp verify_sequence(acc, %Log{sequence_number: found_sequence}) do
+    expected_sequence = acc.expected_sequence
+
+    if found_sequence == expected_sequence do
+      %{acc | expected_sequence: expected_sequence + 1}
+    else
+      missing =
+        if found_sequence > expected_sequence do
+          Enum.to_list(expected_sequence..(found_sequence - 1))
+        else
+          []
+        end
+
+      gap = %{
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      }
+
+      acc
+      |> mark_invalid(%{
+        type: :sequence_gap,
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      })
+      |> Map.update!(:sequence_gaps, &[gap | &1])
+      |> Map.put(:expected_sequence, max(found_sequence, expected_sequence) + 1)
+    end
+  end
+
+  defp verify_checksum(acc, %Log{} = entry, hmac_key) do
+    if Chain.verify_checksum(hmac_key, entry, acc.previous_checksum) do
+      acc
+    else
+      mark_invalid(acc, %{
+        type: :checksum_mismatch,
+        sequence_number: entry.sequence_number,
+        stored_checksum: entry.checksum,
+        expected_checksum: expected_checksum(hmac_key, entry, acc.previous_checksum)
+      })
+    end
+  end
+
+  defp verify_checkpoint(acc, %Log{} = entry, checkpoints_by_sequence) do
+    case Map.get(checkpoints_by_sequence, entry.sequence_number) do
+      nil ->
+        acc
+
+      %Checkpoint{} = checkpoint ->
+        valid? = checkpoint.checksum == entry.checksum
+        result = %{sequence_number: entry.sequence_number, valid: valid?}
+        next_acc = Map.update!(acc, :checkpoint_results, &[result | &1])
+
+        if valid? do
+          next_acc
+        else
+          mark_invalid(next_acc, %{
+            type: :checkpoint_mismatch,
+            sequence_number: entry.sequence_number,
+            checkpoint_checksum: checkpoint.checksum,
+            entry_checksum: entry.checksum
+          })
+        end
+    end
+  end
+
+  defp advance_state(acc, %Log{} = entry) do
+    %{
+      acc
+      | previous_checksum: entry.checksum,
+        last_sequence: entry.sequence_number,
+        total_entries: acc.total_entries + 1,
+        verified_entries: acc.verified_entries + 1
+    }
+  end
+
+  defp expected_checksum(hmac_key, entry, previous_checksum) do
+    Chain.compute_checksum(hmac_key, Map.from_struct(entry), previous_checksum)
+  rescue
+    _ -> nil
+  end
+
+  defp load_batch(account_id, after_sequence) do
+    from(log in Log,
+      where: log.account_id == ^account_id and log.sequence_number > ^after_sequence,
+      order_by: [asc: log.sequence_number],
+      limit: @batch_size
+    )
+    |> Repo.all()
+  end
+
+  defp load_checkpoints(account_id) do
+    from(checkpoint in Checkpoint, where: checkpoint.account_id == ^account_id)
+    |> Repo.all()
+    |> Map.new(fn checkpoint -> {checkpoint.sequence_number, checkpoint} end)
+  end
+
+  defp new_accumulator do
+    %{
+      account_id: nil,
+      valid: true,
+      total_entries: 0,
+      verified_entries: 0,
+      expected_sequence: 1,
+      previous_checksum: nil,
+      last_sequence: 0,
+      first_failure: nil,
+      sequence_gaps: [],
+      checkpoint_results: []
+    }
+  end
+
+  defp finalize_report(acc) do
+    %{
+      valid: acc.valid,
+      total_entries: acc.total_entries,
+      verified_entries: acc.verified_entries,
+      first_failure: acc.first_failure,
+      sequence_gaps: Enum.reverse(acc.sequence_gaps),
+      checkpoint_results: Enum.reverse(acc.checkpoint_results),
+      duration_ms: 0
+    }
+  end
+
+  defp mark_invalid(acc, failure) do
+    case acc.first_failure do
+      nil -> %{acc | valid: false, first_failure: failure}
+      _existing -> %{acc | valid: false}
+    end
+  end
+
+  defp elapsed_ms(started_at) do
+    System.monotonic_time(:millisecond)
+    |> Kernel.-(started_at)
+    |> max(0)
+  end
+end
diff --git a/app/test/app/audit/verifier_test.exs b/app/test/app/audit/verifier_test.exs
new file mode 100644
index 0000000000000000000000000000000000000000..430e2450186c77bb9e816cfa26910a774995b717
--- /dev/null
+++ b/app/test/app/audit/verifier_test.exs
@@ -0,0 +1,189 @@
+defmodule GA.Audit.VerifierTest do
+  use GA.DataCase, async: false
+
+  alias GA.Accounts
+  alias GA.Audit
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  describe "verify_chain/1" do
+    test "returns a valid report for an intact account chain" do
+      account = account_fixture()
+
+      assert {:ok, _first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "r-1"}))
+      assert {:ok, _second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "r-2"}))
+      assert {:ok, checkpoint} = Audit.create_checkpoint(account.id)
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == true
+      assert report.total_entries == 2
+      assert report.verified_entries == 2
+      assert report.first_failure == nil
+      assert report.sequence_gaps == []
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == checkpoint.sequence_number and &1.valid))
+      assert is_integer(report.duration_ms)
+      assert report.duration_ms >= 0
+    end
+
+    test "detects tampered checksum entries" do
+      account = account_fixture()
+
+      assert {:ok, first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "seed"}))
+
+      insert_raw_log(account.id, %{
+        sequence_number: 2,
+        previous_checksum: first.checksum,
+        checksum: String.duplicate("0", 64),
+        resource_id: "tampered"
+      })
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert report.first_failure.type == :checksum_mismatch
+      assert report.first_failure.sequence_number == 2
+      assert report.first_failure.stored_checksum == String.duplicate("0", 64)
+      refute is_nil(report.first_failure.expected_checksum)
+    end
+
+    test "detects sequence gaps within an account" do
+      account = account_fixture()
+      assert {:ok, _first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "gap-1"}))
+      assert {:ok, second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "gap-2"}))
+      assert {:ok, hmac_key} = Accounts.get_hmac_key(account.id)
+
+      checksum =
+        checksum_for_sequence(
+          account.id,
+          hmac_key,
+          4,
+          second.checksum,
+          %{resource_id: "gap-4"}
+        )
+
+      insert_raw_log(account.id, %{
+        sequence_number: 4,
+        previous_checksum: second.checksum,
+        checksum: checksum,
+        resource_id: "gap-4"
+      })
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert report.first_failure.type == :sequence_gap
+      assert [%{expected: 3, found: 4, missing: [3]}] = report.sequence_gaps
+    end
+
+    test "validates checkpoint anchors and reports invalid anchors" do
+      account = account_fixture()
+      assert {:ok, first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "cp-1"}))
+      assert {:ok, second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "cp-2"}))
+
+      insert_checkpoint(account.id, first.sequence_number, first.checksum)
+      insert_checkpoint(account.id, second.sequence_number, String.duplicate("f", 64))
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == first.sequence_number and &1.valid))
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == second.sequence_number and not &1.valid))
+    end
+
+    test "includes non-negative duration tracking" do
+      account = account_fixture()
+      assert {:ok, _} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "duration"}))
+
+      report = Audit.verify_chain(account.id)
+
+      assert is_integer(report.duration_ms)
+      assert report.duration_ms >= 0
+    end
+
+    test "verification remains account-scoped" do
+      account_a = account_fixture()
+      account_b = account_fixture()
+
+      assert {:ok, _} = Audit.create_log_entry(account_a.id, valid_attrs(%{resource_id: "a-1"}))
+      assert {:ok, first_b} = Audit.create_log_entry(account_b.id, valid_attrs(%{resource_id: "b-1"}))
+
+      insert_raw_log(account_b.id, %{
+        sequence_number: 2,
+        previous_checksum: first_b.checksum,
+        checksum: String.duplicate("a", 64),
+        resource_id: "b-2-tampered"
+      })
+
+      report = Audit.verify_chain(account_a.id)
+
+      assert report.valid == true
+      assert report.total_entries == 1
+      assert report.first_failure == nil
+      assert report.sequence_gaps == []
+    end
+  end
+
+  defp account_fixture(attrs \\ %{}) do
+    defaults = %{name: "Verifier Account #{System.unique_integer([:positive])}"}
+    {:ok, account} = Accounts.create_account(Map.merge(defaults, attrs))
+    account
+  end
+
+  defp valid_attrs(overrides \\ %{}) do
+    defaults = %{
+      user_id: Ecto.UUID.generate(),
+      user_role: "admin",
+      session_id: "session-#{System.unique_integer([:positive])}",
+      action: "read",
+      resource_type: "patient",
+      resource_id: Ecto.UUID.generate(),
+      timestamp: ~U[2026-03-03 16:30:00Z],
+      source_ip: "127.0.0.1",
+      user_agent: "ExUnit",
+      outcome: "success",
+      failure_reason: nil,
+      phi_accessed: false,
+      metadata: %{"source" => "verifier_test"}
+    }
+
+    Map.merge(defaults, overrides)
+  end
+
+  defp checksum_for_sequence(account_id, hmac_key, sequence_number, previous_checksum, overrides \\ %{}) do
+    attrs =
+      valid_attrs(overrides)
+      |> Map.put(:account_id, account_id)
+      |> Map.put(:sequence_number, sequence_number)
+
+    Chain.compute_checksum(hmac_key, attrs, previous_checksum)
+  end
+
+  defp insert_raw_log(account_id, attrs) do
+    now = DateTime.utc_now() |> DateTime.truncate(:microsecond)
+    id = Ecto.UUID.generate()
+
+    row =
+      valid_attrs()
+      |> Map.merge(attrs)
+      |> Map.merge(%{
+        id: id,
+        account_id: account_id,
+        inserted_at: now,
+        updated_at: now
+      })
+
+    {1, _} = Repo.insert_all(Log, [row])
+    Repo.get!(Log, id)
+  end
+
+  defp insert_checkpoint(account_id, sequence_number, checksum) do
+    %Checkpoint{}
+    |> Checkpoint.changeset(%{
+      account_id: account_id,
+      sequence_number: sequence_number,
+      checksum: checksum
+    })
+    |> Repo.insert!()
+  end
+end

file update:
diff --git a/app/lib/app/application.ex b/app/lib/app/application.ex
index d865bfe6a48ed688366f7d85220490621cd809a7..08bab073bf607f5bb04aae846094b0b08ec40fe4
--- a/app/lib/app/application.ex
+++ b/app/lib/app/application.ex
@@ -14,6 +14,7 @@
       {Phoenix.PubSub, name: GA.PubSub},
       # Start a worker by calling: GA.Worker.start_link(arg)
       # {GA.Worker, arg},
+      {GA.Audit.CheckpointWorker, checkpoint_worker_opts()},
       # Start to serve requests, typically the last entry
       GAWeb.Endpoint
     ]
@@ -31,4 +32,8 @@
     GAWeb.Endpoint.config_change(changed, removed)
     :ok
   end
+
+  defp checkpoint_worker_opts do
+    Application.get_env(:app, GA.Audit.CheckpointWorker, [])
+  end
 end
diff --git a/app/lib/app/audit.ex b/app/lib/app/audit.ex
index a48830aacf4da8275b40f15016dcd16bbc87411f..2c7fbce41d873e721533728bcc1c965168addd3d
--- a/app/lib/app/audit.ex
+++ b/app/lib/app/audit.ex
@@ -7,7 +7,7 @@
   import Ecto.Query, warn: false
 
   alias GA.Accounts
-  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Audit.{Chain, Checkpoint, Log, Verifier}
   alias GA.Repo
 
   @default_limit 50
@@ -114,6 +114,12 @@
 
   def list_checkpoints(_), do: []
 
+  @doc """
+  Verifies chain integrity for a single account.
+  """
+  def verify_chain(account_id) when is_binary(account_id), do: Verifier.verify(account_id)
+  def verify_chain(_), do: {:error, :invalid_id}
+
   defp lock_account(account_id) do
     {key_a, key_b} = account_lock_keys(account_id)
 
diff --git a/app/lib/app/audit/checkpoint_worker.ex b/app/lib/app/audit/checkpoint_worker.ex
new file mode 100644
index 0000000000000000000000000000000000000000..f54908642ad7c854eae895b9605661f5beb0246f
--- /dev/null
+++ b/app/lib/app/audit/checkpoint_worker.ex
@@ -0,0 +1,82 @@
+defmodule GA.Audit.CheckpointWorker do
+  @moduledoc """
+  Periodically creates audit checkpoints for all active accounts.
+  """
+
+  use GenServer
+
+  import Ecto.Query, warn: false
+
+  require Logger
+
+  alias GA.Accounts.Account
+  alias GA.Audit
+  alias GA.Repo
+
+  @default_interval_ms :timer.hours(1)
+
+  def start_link(opts \\ []) do
+    name = Keyword.get(opts, :name, __MODULE__)
+    GenServer.start_link(__MODULE__, opts, name: name)
+  end
+
+  @impl true
+  def init(opts) do
+    interval_ms =
+      opts
+      |> Keyword.get(:interval_ms, configured_interval_ms())
+      |> normalize_interval()
+
+    schedule_tick(interval_ms)
+
+    {:ok, %{interval_ms: interval_ms}}
+  end
+
+  @impl true
+  def handle_info(:create_checkpoints, state) do
+    active_account_ids()
+    |> Enum.each(&create_checkpoint_for_account/1)
+
+    schedule_tick(state.interval_ms)
+    {:noreply, state}
+  end
+
+  defp active_account_ids do
+    from(account in Account,
+      where: account.status == :active,
+      select: account.id
+    )
+    |> Repo.all()
+  end
+
+  defp create_checkpoint_for_account(account_id) do
+    case Audit.create_checkpoint(account_id) do
+      {:ok, _checkpoint} ->
+        Logger.debug("checkpoint_worker created checkpoint for account=#{account_id}")
+
+      {:error, :no_entries} ->
+        Logger.debug("checkpoint_worker skipping account=#{account_id}: no audit entries")
+
+      {:error, reason} ->
+        Logger.error("checkpoint_worker failed for account=#{account_id}: #{inspect(reason)}")
+    end
+  rescue
+    error ->
+      Logger.error(
+        "checkpoint_worker crashed for account=#{account_id}: #{Exception.message(error)}"
+      )
+  end
+
+  defp schedule_tick(interval_ms) do
+    Process.send_after(self(), :create_checkpoints, interval_ms)
+  end
+
+  defp configured_interval_ms do
+    :app
+    |> Application.get_env(__MODULE__, [])
+    |> Keyword.get(:interval_ms, @default_interval_ms)
+  end
+
+  defp normalize_interval(interval_ms) when is_integer(interval_ms) and interval_ms > 0, do: interval_ms
+  defp normalize_interval(_), do: @default_interval_ms
+end
diff --git a/app/lib/app/audit/verifier.ex b/app/lib/app/audit/verifier.ex
new file mode 100644
index 0000000000000000000000000000000000000000..2a4bd77516a2c76ccc9b05de1778dd8781ded096
--- /dev/null
+++ b/app/lib/app/audit/verifier.ex
@@ -0,0 +1,203 @@
+defmodule GA.Audit.Verifier do
+  @moduledoc """
+  Streams and verifies account-scoped audit chains.
+  """
+
+  import Ecto.Query, warn: false
+
+  alias GA.Accounts
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  @batch_size 1000
+
+  @doc """
+  Verifies an account's audit chain and returns a report map.
+  """
+  @spec verify(binary()) :: map() | {:error, :invalid_id | :not_found}
+  def verify(account_id) when is_binary(account_id) do
+    started_at = System.monotonic_time(:millisecond)
+
+    with {:ok, hmac_key} <- Accounts.get_hmac_key(account_id) do
+      checkpoints_by_sequence = load_checkpoints(account_id)
+      report = stream_and_verify(account_id, hmac_key, checkpoints_by_sequence)
+
+      Map.put(report, :duration_ms, elapsed_ms(started_at))
+    end
+  end
+
+  def verify(_), do: {:error, :invalid_id}
+
+  defp stream_and_verify(account_id, hmac_key, checkpoints_by_sequence) do
+    initial =
+      new_accumulator()
+      |> Map.put(:account_id, account_id)
+
+    account_id
+    |> do_verify_batches(hmac_key, checkpoints_by_sequence, initial)
+    |> finalize_report()
+  end
+
+  defp do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, acc) do
+    batch = load_batch(account_id, acc.last_sequence)
+
+    case batch do
+      [] ->
+        acc
+
+      _entries ->
+        next_acc =
+          Enum.reduce(batch, acc, fn entry, state ->
+            verify_entry(entry, hmac_key, checkpoints_by_sequence, state)
+          end)
+
+        do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, next_acc)
+    end
+  end
+
+  defp verify_entry(%Log{} = entry, hmac_key, checkpoints_by_sequence, acc) do
+    acc
+    |> verify_sequence(entry)
+    |> verify_checksum(entry, hmac_key)
+    |> verify_checkpoint(entry, checkpoints_by_sequence)
+    |> advance_state(entry)
+  end
+
+  defp verify_sequence(acc, %Log{sequence_number: found_sequence}) do
+    expected_sequence = acc.expected_sequence
+
+    if found_sequence == expected_sequence do
+      %{acc | expected_sequence: expected_sequence + 1}
+    else
+      missing =
+        if found_sequence > expected_sequence do
+          Enum.to_list(expected_sequence..(found_sequence - 1))
+        else
+          []
+        end
+
+      gap = %{
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      }
+
+      acc
+      |> mark_invalid(%{
+        type: :sequence_gap,
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      })
+      |> Map.update!(:sequence_gaps, &[gap | &1])
+      |> Map.put(:expected_sequence, max(found_sequence, expected_sequence) + 1)
+    end
+  end
+
+  defp verify_checksum(acc, %Log{} = entry, hmac_key) do
+    if Chain.verify_checksum(hmac_key, entry, acc.previous_checksum) do
+      acc
+    else
+      mark_invalid(acc, %{
+        type: :checksum_mismatch,
+        sequence_number: entry.sequence_number,
+        stored_checksum: entry.checksum,
+        expected_checksum: expected_checksum(hmac_key, entry, acc.previous_checksum)
+      })
+    end
+  end
+
+  defp verify_checkpoint(acc, %Log{} = entry, checkpoints_by_sequence) do
+    case Map.get(checkpoints_by_sequence, entry.sequence_number) do
+      nil ->
+        acc
+
+      %Checkpoint{} = checkpoint ->
+        valid? = checkpoint.checksum == entry.checksum
+        result = %{sequence_number: entry.sequence_number, valid: valid?}
+        next_acc = Map.update!(acc, :checkpoint_results, &[result | &1])
+
+        if valid? do
+          next_acc
+        else
+          mark_invalid(next_acc, %{
+            type: :checkpoint_mismatch,
+            sequence_number: entry.sequence_number,
+            checkpoint_checksum: checkpoint.checksum,
+            entry_checksum: entry.checksum
+          })
+        end
+    end
+  end
+
+  defp advance_state(acc, %Log{} = entry) do
+    %{
+      acc
+      | previous_checksum: entry.checksum,
+        last_sequence: entry.sequence_number,
+        total_entries: acc.total_entries + 1,
+        verified_entries: acc.verified_entries + 1
+    }
+  end
+
+  defp expected_checksum(hmac_key, entry, previous_checksum) do
+    Chain.compute_checksum(hmac_key, Map.from_struct(entry), previous_checksum)
+  rescue
+    _ -> nil
+  end
+
+  defp load_batch(account_id, after_sequence) do
+    from(log in Log,
+      where: log.account_id == ^account_id and log.sequence_number > ^after_sequence,
+      order_by: [asc: log.sequence_number],
+      limit: @batch_size
+    )
+    |> Repo.all()
+  end
+
+  defp load_checkpoints(account_id) do
+    from(checkpoint in Checkpoint, where: checkpoint.account_id == ^account_id)
+    |> Repo.all()
+    |> Map.new(fn checkpoint -> {checkpoint.sequence_number, checkpoint} end)
+  end
+
+  defp new_accumulator do
+    %{
+      account_id: nil,
+      valid: true,
+      total_entries: 0,
+      verified_entries: 0,
+      expected_sequence: 1,
+      previous_checksum: nil,
+      last_sequence: 0,
+      first_failure: nil,
+      sequence_gaps: [],
+      checkpoint_results: []
+    }
+  end
+
+  defp finalize_report(acc) do
+    %{
+      valid: acc.valid,
+      total_entries: acc.total_entries,
+      verified_entries: acc.verified_entries,
+      first_failure: acc.first_failure,
+      sequence_gaps: Enum.reverse(acc.sequence_gaps),
+      checkpoint_results: Enum.reverse(acc.checkpoint_results),
+      duration_ms: 0
+    }
+  end
+
+  defp mark_invalid(acc, failure) do
+    case acc.first_failure do
+      nil -> %{acc | valid: false, first_failure: failure}
+      _existing -> %{acc | valid: false}
+    end
+  end
+
+  defp elapsed_ms(started_at) do
+    System.monotonic_time(:millisecond)
+    |> Kernel.-(started_at)
+    |> max(0)
+  end
+end
diff --git a/app/test/app/audit/verifier_test.exs b/app/test/app/audit/verifier_test.exs
new file mode 100644
index 0000000000000000000000000000000000000000..430e2450186c77bb9e816cfa26910a774995b717
--- /dev/null
+++ b/app/test/app/audit/verifier_test.exs
@@ -0,0 +1,189 @@
+defmodule GA.Audit.VerifierTest do
+  use GA.DataCase, async: false
+
+  alias GA.Accounts
+  alias GA.Audit
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  describe "verify_chain/1" do
+    test "returns a valid report for an intact account chain" do
+      account = account_fixture()
+
+      assert {:ok, _first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "r-1"}))
+      assert {:ok, _second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "r-2"}))
+      assert {:ok, checkpoint} = Audit.create_checkpoint(account.id)
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == true
+      assert report.total_entries == 2
+      assert report.verified_entries == 2
+      assert report.first_failure == nil
+      assert report.sequence_gaps == []
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == checkpoint.sequence_number and &1.valid))
+      assert is_integer(report.duration_ms)
+      assert report.duration_ms >= 0
+    end
+
+    test "detects tampered checksum entries" do
+      account = account_fixture()
+
+      assert {:ok, first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "seed"}))
+
+      insert_raw_log(account.id, %{
+        sequence_number: 2,
+        previous_checksum: first.checksum,
+        checksum: String.duplicate("0", 64),
+        resource_id: "tampered"
+      })
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert report.first_failure.type == :checksum_mismatch
+      assert report.first_failure.sequence_number == 2
+      assert report.first_failure.stored_checksum == String.duplicate("0", 64)
+      refute is_nil(report.first_failure.expected_checksum)
+    end
+
+    test "detects sequence gaps within an account" do
+      account = account_fixture()
+      assert {:ok, _first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "gap-1"}))
+      assert {:ok, second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "gap-2"}))
+      assert {:ok, hmac_key} = Accounts.get_hmac_key(account.id)
+
+      checksum =
+        checksum_for_sequence(
+          account.id,
+          hmac_key,
+          4,
+          second.checksum,
+          %{resource_id: "gap-4"}
+        )
+
+      insert_raw_log(account.id, %{
+        sequence_number: 4,
+        previous_checksum: second.checksum,
+        checksum: checksum,
+        resource_id: "gap-4"
+      })
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert report.first_failure.type == :sequence_gap
+      assert [%{expected: 3, found: 4, missing: [3]}] = report.sequence_gaps
+    end
+
+    test "validates checkpoint anchors and reports invalid anchors" do
+      account = account_fixture()
+      assert {:ok, first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "cp-1"}))
+      assert {:ok, second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "cp-2"}))
+
+      insert_checkpoint(account.id, first.sequence_number, first.checksum)
+      insert_checkpoint(account.id, second.sequence_number, String.duplicate("f", 64))
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == first.sequence_number and &1.valid))
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == second.sequence_number and not &1.valid))
+    end
+
+    test "includes non-negative duration tracking" do
+      account = account_fixture()
+      assert {:ok, _} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "duration"}))
+
+      report = Audit.verify_chain(account.id)
+
+      assert is_integer(report.duration_ms)
+      assert report.duration_ms >= 0
+    end
+
+    test "verification remains account-scoped" do
+      account_a = account_fixture()
+      account_b = account_fixture()
+
+      assert {:ok, _} = Audit.create_log_entry(account_a.id, valid_attrs(%{resource_id: "a-1"}))
+      assert {:ok, first_b} = Audit.create_log_entry(account_b.id, valid_attrs(%{resource_id: "b-1"}))
+
+      insert_raw_log(account_b.id, %{
+        sequence_number: 2,
+        previous_checksum: first_b.checksum,
+        checksum: String.duplicate("a", 64),
+        resource_id: "b-2-tampered"
+      })
+
+      report = Audit.verify_chain(account_a.id)
+
+      assert report.valid == true
+      assert report.total_entries == 1
+      assert report.first_failure == nil
+      assert report.sequence_gaps == []
+    end
+  end
+
+  defp account_fixture(attrs \\ %{}) do
+    defaults = %{name: "Verifier Account #{System.unique_integer([:positive])}"}
+    {:ok, account} = Accounts.create_account(Map.merge(defaults, attrs))
+    account
+  end
+
+  defp valid_attrs(overrides \\ %{}) do
+    defaults = %{
+      user_id: Ecto.UUID.generate(),
+      user_role: "admin",
+      session_id: "session-#{System.unique_integer([:positive])}",
+      action: "read",
+      resource_type: "patient",
+      resource_id: Ecto.UUID.generate(),
+      timestamp: ~U[2026-03-03 16:30:00Z],
+      source_ip: "127.0.0.1",
+      user_agent: "ExUnit",
+      outcome: "success",
+      failure_reason: nil,
+      phi_accessed: false,
+      metadata: %{"source" => "verifier_test"}
+    }
+
+    Map.merge(defaults, overrides)
+  end
+
+  defp checksum_for_sequence(account_id, hmac_key, sequence_number, previous_checksum, overrides \\ %{}) do
+    attrs =
+      valid_attrs(overrides)
+      |> Map.put(:account_id, account_id)
+      |> Map.put(:sequence_number, sequence_number)
+
+    Chain.compute_checksum(hmac_key, attrs, previous_checksum)
+  end
+
+  defp insert_raw_log(account_id, attrs) do
+    now = DateTime.utc_now() |> DateTime.truncate(:microsecond)
+    id = Ecto.UUID.generate()
+
+    row =
+      valid_attrs()
+      |> Map.merge(attrs)
+      |> Map.merge(%{
+        id: id,
+        account_id: account_id,
+        inserted_at: now,
+        updated_at: now
+      })
+
+    {1, _} = Repo.insert_all(Log, [row])
+    Repo.get!(Log, id)
+  end
+
+  defp insert_checkpoint(account_id, sequence_number, checksum) do
+    %Checkpoint{}
+    |> Checkpoint.changeset(%{
+      account_id: account_id,
+      sequence_number: sequence_number,
+      checksum: checksum
+    })
+    |> Repo.insert!()
+  end
+end

file update
A /Users/tbrewer/projects/goodway/goodaudit/app/test/app/audit/checkpoint_worker_test.exs
defmodule GA.Audit.CheckpointWorkerTest do
  use GA.DataCase, async: false

  import ExUnit.CaptureLog

  alias GA.Accounts
  alias GA.Audit
  alias GA.Audit.CheckpointWorker

  describe "checkpoint worker ticks" do
    test "creates checkpoints for all active accounts" do
      account_a = account_fixture()
      account_b = account_fixture()
      add_log(account_a.id, "active-a")
      add_log(account_b.id, "active-b")

      worker = start_worker()
      send(worker, :create_checkpoints)

      assert_eventually(fn ->
        length(Audit.list_checkpoints(account_a.id)) == 1 and
          length(Audit.list_checkpoints(account_b.id)) == 1
      end)
    end

    test "skips suspended accounts" do
      active_account = account_fixture()
      suspended_account = account_fixture(%{status: :suspended})
      add_log(active_account.id, "active")
      add_log(suspended_account.id, "suspended")

      worker = start_worker()
      send(worker, :create_checkpoints)

      assert_eventually(fn ->
        length(Audit.list_checkpoints(active_account.id)) == 1
      end)

      assert Audit.list_checkpoints(suspended_account.id) == []
    end

    test "logs no-entry accounts and continues processing" do
      empty_account = account_fixture()
      populated_account = account_fixture()
      add_log(populated_account.id, "populated")

      worker = start_worker()

      log =
        capture_log([level: :debug], fn ->
          send(worker, :create_checkpoints)

          assert_eventually(fn ->
            length(Audit.list_checkpoints(populated_account.id)) == 1
          end)
        end)

      assert log =~ "checkpoint_worker skipping"
      assert log =~ empty_account.id
      assert Audit.list_checkpoints(empty_account.id) == []
    end
  end

  defp start_worker do
    start_supervised!(
      {CheckpointWorker,
       [
         name: {:global, {:checkpoint_worker_test, System.unique_integer([:positive])}},
         interval_ms: 60_000
       ]}
    )
  end

  defp account_fixture(attrs \\ %{}) do
    defaults = %{name: "Worker Account #{System.unique_integer([:positive])}"}
    {:ok, account} = Accounts.create_account(Map.merge(defaults, attrs))
    account
  end

  defp add_log(account_id, resource_id) do
    attrs = %{
      user_id: Ecto.UUID.generate(),
      user_role: "admin",
      session_id: "session-#{System.unique_integer([:positive])}",
      action: "read",
      resource_type: "patient",
      resource_id: resource_id,
      timestamp: ~U[2026-03-03 16:30:00Z],
      source_ip: "127.0.0.1",
      user_agent: "ExUnit",
      outcome: "success",
      failure_reason: nil,
      phi_accessed: false,
      metadata: %{"source" => "checkpoint_worker_test"}
    }

    assert {:ok, _} = Audit.create_log_entry(account_id, attrs)
  end

  defp assert_eventually(fun, attempts \\ 50)
  defp assert_eventually(fun, 0), do: assert(fun.())

  defp assert_eventually(fun, attempts) do
    if fun.() do
      :ok
    else
      Process.sleep(20)
      assert_eventually(fun, attempts - 1)
    end
  end
end
apply_patch(auto_approved=true) exited 0 in 81ms:
Success. Updated the following files:
A /Users/tbrewer/projects/goodway/goodaudit/app/test/app/audit/checkpoint_worker_test.exs
file update:
diff --git a/app/lib/app/application.ex b/app/lib/app/application.ex
index d865bfe6a48ed688366f7d85220490621cd809a7..08bab073bf607f5bb04aae846094b0b08ec40fe4
--- a/app/lib/app/application.ex
+++ b/app/lib/app/application.ex
@@ -14,6 +14,7 @@
       {Phoenix.PubSub, name: GA.PubSub},
       # Start a worker by calling: GA.Worker.start_link(arg)
       # {GA.Worker, arg},
+      {GA.Audit.CheckpointWorker, checkpoint_worker_opts()},
       # Start to serve requests, typically the last entry
       GAWeb.Endpoint
     ]
@@ -31,4 +32,8 @@
     GAWeb.Endpoint.config_change(changed, removed)
     :ok
   end
+
+  defp checkpoint_worker_opts do
+    Application.get_env(:app, GA.Audit.CheckpointWorker, [])
+  end
 end
diff --git a/app/lib/app/audit.ex b/app/lib/app/audit.ex
index a48830aacf4da8275b40f15016dcd16bbc87411f..2c7fbce41d873e721533728bcc1c965168addd3d
--- a/app/lib/app/audit.ex
+++ b/app/lib/app/audit.ex
@@ -7,7 +7,7 @@
   import Ecto.Query, warn: false
 
   alias GA.Accounts
-  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Audit.{Chain, Checkpoint, Log, Verifier}
   alias GA.Repo
 
   @default_limit 50
@@ -114,6 +114,12 @@
 
   def list_checkpoints(_), do: []
 
+  @doc """
+  Verifies chain integrity for a single account.
+  """
+  def verify_chain(account_id) when is_binary(account_id), do: Verifier.verify(account_id)
+  def verify_chain(_), do: {:error, :invalid_id}
+
   defp lock_account(account_id) do
     {key_a, key_b} = account_lock_keys(account_id)
 
diff --git a/app/lib/app/audit/checkpoint_worker.ex b/app/lib/app/audit/checkpoint_worker.ex
new file mode 100644
index 0000000000000000000000000000000000000000..f54908642ad7c854eae895b9605661f5beb0246f
--- /dev/null
+++ b/app/lib/app/audit/checkpoint_worker.ex
@@ -0,0 +1,82 @@
+defmodule GA.Audit.CheckpointWorker do
+  @moduledoc """
+  Periodically creates audit checkpoints for all active accounts.
+  """
+
+  use GenServer
+
+  import Ecto.Query, warn: false
+
+  require Logger
+
+  alias GA.Accounts.Account
+  alias GA.Audit
+  alias GA.Repo
+
+  @default_interval_ms :timer.hours(1)
+
+  def start_link(opts \\ []) do
+    name = Keyword.get(opts, :name, __MODULE__)
+    GenServer.start_link(__MODULE__, opts, name: name)
+  end
+
+  @impl true
+  def init(opts) do
+    interval_ms =
+      opts
+      |> Keyword.get(:interval_ms, configured_interval_ms())
+      |> normalize_interval()
+
+    schedule_tick(interval_ms)
+
+    {:ok, %{interval_ms: interval_ms}}
+  end
+
+  @impl true
+  def handle_info(:create_checkpoints, state) do
+    active_account_ids()
+    |> Enum.each(&create_checkpoint_for_account/1)
+
+    schedule_tick(state.interval_ms)
+    {:noreply, state}
+  end
+
+  defp active_account_ids do
+    from(account in Account,
+      where: account.status == :active,
+      select: account.id
+    )
+    |> Repo.all()
+  end
+
+  defp create_checkpoint_for_account(account_id) do
+    case Audit.create_checkpoint(account_id) do
+      {:ok, _checkpoint} ->
+        Logger.debug("checkpoint_worker created checkpoint for account=#{account_id}")
+
+      {:error, :no_entries} ->
+        Logger.debug("checkpoint_worker skipping account=#{account_id}: no audit entries")
+
+      {:error, reason} ->
+        Logger.error("checkpoint_worker failed for account=#{account_id}: #{inspect(reason)}")
+    end
+  rescue
+    error ->
+      Logger.error(
+        "checkpoint_worker crashed for account=#{account_id}: #{Exception.message(error)}"
+      )
+  end
+
+  defp schedule_tick(interval_ms) do
+    Process.send_after(self(), :create_checkpoints, interval_ms)
+  end
+
+  defp configured_interval_ms do
+    :app
+    |> Application.get_env(__MODULE__, [])
+    |> Keyword.get(:interval_ms, @default_interval_ms)
+  end
+
+  defp normalize_interval(interval_ms) when is_integer(interval_ms) and interval_ms > 0, do: interval_ms
+  defp normalize_interval(_), do: @default_interval_ms
+end
diff --git a/app/lib/app/audit/verifier.ex b/app/lib/app/audit/verifier.ex
new file mode 100644
index 0000000000000000000000000000000000000000..2a4bd77516a2c76ccc9b05de1778dd8781ded096
--- /dev/null
+++ b/app/lib/app/audit/verifier.ex
@@ -0,0 +1,203 @@
+defmodule GA.Audit.Verifier do
+  @moduledoc """
+  Streams and verifies account-scoped audit chains.
+  """
+
+  import Ecto.Query, warn: false
+
+  alias GA.Accounts
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  @batch_size 1000
+
+  @doc """
+  Verifies an account's audit chain and returns a report map.
+  """
+  @spec verify(binary()) :: map() | {:error, :invalid_id | :not_found}
+  def verify(account_id) when is_binary(account_id) do
+    started_at = System.monotonic_time(:millisecond)
+
+    with {:ok, hmac_key} <- Accounts.get_hmac_key(account_id) do
+      checkpoints_by_sequence = load_checkpoints(account_id)
+      report = stream_and_verify(account_id, hmac_key, checkpoints_by_sequence)
+
+      Map.put(report, :duration_ms, elapsed_ms(started_at))
+    end
+  end
+
+  def verify(_), do: {:error, :invalid_id}
+
+  defp stream_and_verify(account_id, hmac_key, checkpoints_by_sequence) do
+    initial =
+      new_accumulator()
+      |> Map.put(:account_id, account_id)
+
+    account_id
+    |> do_verify_batches(hmac_key, checkpoints_by_sequence, initial)
+    |> finalize_report()
+  end
+
+  defp do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, acc) do
+    batch = load_batch(account_id, acc.last_sequence)
+
+    case batch do
+      [] ->
+        acc
+
+      _entries ->
+        next_acc =
+          Enum.reduce(batch, acc, fn entry, state ->
+            verify_entry(entry, hmac_key, checkpoints_by_sequence, state)
+          end)
+
+        do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, next_acc)
+    end
+  end
+
+  defp verify_entry(%Log{} = entry, hmac_key, checkpoints_by_sequence, acc) do
+    acc
+    |> verify_sequence(entry)
+    |> verify_checksum(entry, hmac_key)
+    |> verify_checkpoint(entry, checkpoints_by_sequence)
+    |> advance_state(entry)
+  end
+
+  defp verify_sequence(acc, %Log{sequence_number: found_sequence}) do
+    expected_sequence = acc.expected_sequence
+
+    if found_sequence == expected_sequence do
+      %{acc | expected_sequence: expected_sequence + 1}
+    else
+      missing =
+        if found_sequence > expected_sequence do
+          Enum.to_list(expected_sequence..(found_sequence - 1))
+        else
+          []
+        end
+
+      gap = %{
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      }
+
+      acc
+      |> mark_invalid(%{
+        type: :sequence_gap,
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      })
+      |> Map.update!(:sequence_gaps, &[gap | &1])
+      |> Map.put(:expected_sequence, max(found_sequence, expected_sequence) + 1)
+    end
+  end
+
+  defp verify_checksum(acc, %Log{} = entry, hmac_key) do
+    if Chain.verify_checksum(hmac_key, entry, acc.previous_checksum) do
+      acc
+    else
+      mark_invalid(acc, %{
+        type: :checksum_mismatch,
+        sequence_number: entry.sequence_number,
+        stored_checksum: entry.checksum,
+        expected_checksum: expected_checksum(hmac_key, entry, acc.previous_checksum)
+      })
+    end
+  end
+
+  defp verify_checkpoint(acc, %Log{} = entry, checkpoints_by_sequence) do
+    case Map.get(checkpoints_by_sequence, entry.sequence_number) do
+      nil ->
+        acc
+
+      %Checkpoint{} = checkpoint ->
+        valid? = checkpoint.checksum == entry.checksum
+        result = %{sequence_number: entry.sequence_number, valid: valid?}
+        next_acc = Map.update!(acc, :checkpoint_results, &[result | &1])
+
+        if valid? do
+          next_acc
+        else
+          mark_invalid(next_acc, %{
+            type: :checkpoint_mismatch,
+            sequence_number: entry.sequence_number,
+            checkpoint_checksum: checkpoint.checksum,
+            entry_checksum: entry.checksum
+          })
+        end
+    end
+  end
+
+  defp advance_state(acc, %Log{} = entry) do
+    %{
+      acc
+      | previous_checksum: entry.checksum,
+        last_sequence: entry.sequence_number,
+        total_entries: acc.total_entries + 1,
+        verified_entries: acc.verified_entries + 1
+    }
+  end
+
+  defp expected_checksum(hmac_key, entry, previous_checksum) do
+    Chain.compute_checksum(hmac_key, Map.from_struct(entry), previous_checksum)
+  rescue
+    _ -> nil
+  end
+
+  defp load_batch(account_id, after_sequence) do
+    from(log in Log,
+      where: log.account_id == ^account_id and log.sequence_number > ^after_sequence,
+      order_by: [asc: log.sequence_number],
+      limit: @batch_size
+    )
+    |> Repo.all()
+  end
+
+  defp load_checkpoints(account_id) do
+    from(checkpoint in Checkpoint, where: checkpoint.account_id == ^account_id)
+    |> Repo.all()
+    |> Map.new(fn checkpoint -> {checkpoint.sequence_number, checkpoint} end)
+  end
+
+  defp new_accumulator do
+    %{
+      account_id: nil,
+      valid: true,
+      total_entries: 0,
+      verified_entries: 0,
+      expected_sequence: 1,
+      previous_checksum: nil,
+      last_sequence: 0,
+      first_failure: nil,
+      sequence_gaps: [],
+      checkpoint_results: []
+    }
+  end
+
+  defp finalize_report(acc) do
+    %{
+      valid: acc.valid,
+      total_entries: acc.total_entries,
+      verified_entries: acc.verified_entries,
+      first_failure: acc.first_failure,
+      sequence_gaps: Enum.reverse(acc.sequence_gaps),
+      checkpoint_results: Enum.reverse(acc.checkpoint_results),
+      duration_ms: 0
+    }
+  end
+
+  defp mark_invalid(acc, failure) do
+    case acc.first_failure do
+      nil -> %{acc | valid: false, first_failure: failure}
+      _existing -> %{acc | valid: false}
+    end
+  end
+
+  defp elapsed_ms(started_at) do
+    System.monotonic_time(:millisecond)
+    |> Kernel.-(started_at)
+    |> max(0)
+  end
+end
diff --git a/app/test/app/audit/checkpoint_worker_test.exs b/app/test/app/audit/checkpoint_worker_test.exs
new file mode 100644
index 0000000000000000000000000000000000000000..ec9ea5e45a2b2d84cd0e7872a7bf7288d8e3ea11
--- /dev/null
+++ b/app/test/app/audit/checkpoint_worker_test.exs
@@ -0,0 +1,111 @@
+defmodule GA.Audit.CheckpointWorkerTest do
+  use GA.DataCase, async: false
+
+  import ExUnit.CaptureLog
+
+  alias GA.Accounts
+  alias GA.Audit
+  alias GA.Audit.CheckpointWorker
+
+  describe "checkpoint worker ticks" do
+    test "creates checkpoints for all active accounts" do
+      account_a = account_fixture()
+      account_b = account_fixture()
+      add_log(account_a.id, "active-a")
+      add_log(account_b.id, "active-b")
+
+      worker = start_worker()
+      send(worker, :create_checkpoints)
+
+      assert_eventually(fn ->
+        length(Audit.list_checkpoints(account_a.id)) == 1 and
+          length(Audit.list_checkpoints(account_b.id)) == 1
+      end)
+    end
+
+    test "skips suspended accounts" do
+      active_account = account_fixture()
+      suspended_account = account_fixture(%{status: :suspended})
+      add_log(active_account.id, "active")
+      add_log(suspended_account.id, "suspended")
+
+      worker = start_worker()
+      send(worker, :create_checkpoints)
+
+      assert_eventually(fn ->
+        length(Audit.list_checkpoints(active_account.id)) == 1
+      end)
+
+      assert Audit.list_checkpoints(suspended_account.id) == []
+    end
+
+    test "logs no-entry accounts and continues processing" do
+      empty_account = account_fixture()
+      populated_account = account_fixture()
+      add_log(populated_account.id, "populated")
+
+      worker = start_worker()
+
+      log =
+        capture_log([level: :debug], fn ->
+          send(worker, :create_checkpoints)
+
+          assert_eventually(fn ->
+            length(Audit.list_checkpoints(populated_account.id)) == 1
+          end)
+        end)
+
+      assert log =~ "checkpoint_worker skipping"
+      assert log =~ empty_account.id
+      assert Audit.list_checkpoints(empty_account.id) == []
+    end
+  end
+
+  defp start_worker do
+    start_supervised!(
+      {CheckpointWorker,
+       [
+         name: {:global, {:checkpoint_worker_test, System.unique_integer([:positive])}},
+         interval_ms: 60_000
+       ]}
+    )
+  end
+
+  defp account_fixture(attrs \\ %{}) do
+    defaults = %{name: "Worker Account #{System.unique_integer([:positive])}"}
+    {:ok, account} = Accounts.create_account(Map.merge(defaults, attrs))
+    account
+  end
+
+  defp add_log(account_id, resource_id) do
+    attrs = %{
+      user_id: Ecto.UUID.generate(),
+      user_role: "admin",
+      session_id: "session-#{System.unique_integer([:positive])}",
+      action: "read",
+      resource_type: "patient",
+      resource_id: resource_id,
+      timestamp: ~U[2026-03-03 16:30:00Z],
+      source_ip: "127.0.0.1",
+      user_agent: "ExUnit",
+      outcome: "success",
+      failure_reason: nil,
+      phi_accessed: false,
+      metadata: %{"source" => "checkpoint_worker_test"}
+    }
+
+    assert {:ok, _} = Audit.create_log_entry(account_id, attrs)
+  end
+
+  defp assert_eventually(fun, attempts \\ 50)
+  defp assert_eventually(fun, 0), do: assert(fun.())
+
+  defp assert_eventually(fun, attempts) do
+    if fun.() do
+      :ok
+    else
+      Process.sleep(20)
+      assert_eventually(fun, attempts - 1)
+    end
+  end
+end
diff --git a/app/test/app/audit/verifier_test.exs b/app/test/app/audit/verifier_test.exs
new file mode 100644
index 0000000000000000000000000000000000000000..430e2450186c77bb9e816cfa26910a774995b717
--- /dev/null
+++ b/app/test/app/audit/verifier_test.exs
@@ -0,0 +1,189 @@
+defmodule GA.Audit.VerifierTest do
+  use GA.DataCase, async: false
+
+  alias GA.Accounts
+  alias GA.Audit
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  describe "verify_chain/1" do
+    test "returns a valid report for an intact account chain" do
+      account = account_fixture()
+
+      assert {:ok, _first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "r-1"}))
+      assert {:ok, _second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "r-2"}))
+      assert {:ok, checkpoint} = Audit.create_checkpoint(account.id)
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == true
+      assert report.total_entries == 2
+      assert report.verified_entries == 2
+      assert report.first_failure == nil
+      assert report.sequence_gaps == []
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == checkpoint.sequence_number and &1.valid))
+      assert is_integer(report.duration_ms)
+      assert report.duration_ms >= 0
+    end
+
+    test "detects tampered checksum entries" do
+      account = account_fixture()
+
+      assert {:ok, first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "seed"}))
+
+      insert_raw_log(account.id, %{
+        sequence_number: 2,
+        previous_checksum: first.checksum,
+        checksum: String.duplicate("0", 64),
+        resource_id: "tampered"
+      })
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert report.first_failure.type == :checksum_mismatch
+      assert report.first_failure.sequence_number == 2
+      assert report.first_failure.stored_checksum == String.duplicate("0", 64)
+      refute is_nil(report.first_failure.expected_checksum)
+    end
+
+    test "detects sequence gaps within an account" do
+      account = account_fixture()
+      assert {:ok, _first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "gap-1"}))
+      assert {:ok, second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "gap-2"}))
+      assert {:ok, hmac_key} = Accounts.get_hmac_key(account.id)
+
+      checksum =
+        checksum_for_sequence(
+          account.id,
+          hmac_key,
+          4,
+          second.checksum,
+          %{resource_id: "gap-4"}
+        )
+
+      insert_raw_log(account.id, %{
+        sequence_number: 4,
+        previous_checksum: second.checksum,
+        checksum: checksum,
+        resource_id: "gap-4"
+      })
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert report.first_failure.type == :sequence_gap
+      assert [%{expected: 3, found: 4, missing: [3]}] = report.sequence_gaps
+    end
+
+    test "validates checkpoint anchors and reports invalid anchors" do
+      account = account_fixture()
+      assert {:ok, first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "cp-1"}))
+      assert {:ok, second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "cp-2"}))
+
+      insert_checkpoint(account.id, first.sequence_number, first.checksum)
+      insert_checkpoint(account.id, second.sequence_number, String.duplicate("f", 64))
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == first.sequence_number and &1.valid))
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == second.sequence_number and not &1.valid))
+    end
+
+    test "includes non-negative duration tracking" do
+      account = account_fixture()
+      assert {:ok, _} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "duration"}))
+
+      report = Audit.verify_chain(account.id)
+
+      assert is_integer(report.duration_ms)
+      assert report.duration_ms >= 0
+    end
+
+    test "verification remains account-scoped" do
+      account_a = account_fixture()
+      account_b = account_fixture()
+
+      assert {:ok, _} = Audit.create_log_entry(account_a.id, valid_attrs(%{resource_id: "a-1"}))
+      assert {:ok, first_b} = Audit.create_log_entry(account_b.id, valid_attrs(%{resource_id: "b-1"}))
+
+      insert_raw_log(account_b.id, %{
+        sequence_number: 2,
+        previous_checksum: first_b.checksum,
+        checksum: String.duplicate("a", 64),
+        resource_id: "b-2-tampered"
+      })
+
+      report = Audit.verify_chain(account_a.id)
+
+      assert report.valid == true
+      assert report.total_entries == 1
+      assert report.first_failure == nil
+      assert report.sequence_gaps == []
+    end
+  end
+
+  defp account_fixture(attrs \\ %{}) do
+    defaults = %{name: "Verifier Account #{System.unique_integer([:positive])}"}
+    {:ok, account} = Accounts.create_account(Map.merge(defaults, attrs))
+    account
+  end
+
+  defp valid_attrs(overrides \\ %{}) do
+    defaults = %{
+      user_id: Ecto.UUID.generate(),
+      user_role: "admin",
+      session_id: "session-#{System.unique_integer([:positive])}",
+      action: "read",
+      resource_type: "patient",
+      resource_id: Ecto.UUID.generate(),
+      timestamp: ~U[2026-03-03 16:30:00Z],
+      source_ip: "127.0.0.1",
+      user_agent: "ExUnit",
+      outcome: "success",
+      failure_reason: nil,
+      phi_accessed: false,
+      metadata: %{"source" => "verifier_test"}
+    }
+
+    Map.merge(defaults, overrides)
+  end
+
+  defp checksum_for_sequence(account_id, hmac_key, sequence_number, previous_checksum, overrides \\ %{}) do
+    attrs =
+      valid_attrs(overrides)
+      |> Map.put(:account_id, account_id)
+      |> Map.put(:sequence_number, sequence_number)
+
+    Chain.compute_checksum(hmac_key, attrs, previous_checksum)
+  end
+
+  defp insert_raw_log(account_id, attrs) do
+    now = DateTime.utc_now() |> DateTime.truncate(:microsecond)
+    id = Ecto.UUID.generate()
+
+    row =
+      valid_attrs()
+      |> Map.merge(attrs)
+      |> Map.merge(%{
+        id: id,
+        account_id: account_id,
+        inserted_at: now,
+        updated_at: now
+      })
+
+    {1, _} = Repo.insert_all(Log, [row])
+    Repo.get!(Log, id)
+  end
+
+  defp insert_checkpoint(account_id, sequence_number, checksum) do
+    %Checkpoint{}
+    |> Checkpoint.changeset(%{
+      account_id: account_id,
+      sequence_number: sequence_number,
+      checksum: checksum
+    })
+    |> Repo.insert!()
+  end
+end

file update:
diff --git a/app/lib/app/application.ex b/app/lib/app/application.ex
index d865bfe6a48ed688366f7d85220490621cd809a7..08bab073bf607f5bb04aae846094b0b08ec40fe4
--- a/app/lib/app/application.ex
+++ b/app/lib/app/application.ex
@@ -14,6 +14,7 @@
       {Phoenix.PubSub, name: GA.PubSub},
       # Start a worker by calling: GA.Worker.start_link(arg)
       # {GA.Worker, arg},
+      {GA.Audit.CheckpointWorker, checkpoint_worker_opts()},
       # Start to serve requests, typically the last entry
       GAWeb.Endpoint
     ]
@@ -31,4 +32,8 @@
     GAWeb.Endpoint.config_change(changed, removed)
     :ok
   end
+
+  defp checkpoint_worker_opts do
+    Application.get_env(:app, GA.Audit.CheckpointWorker, [])
+  end
 end
diff --git a/app/lib/app/audit.ex b/app/lib/app/audit.ex
index a48830aacf4da8275b40f15016dcd16bbc87411f..2c7fbce41d873e721533728bcc1c965168addd3d
--- a/app/lib/app/audit.ex
+++ b/app/lib/app/audit.ex
@@ -7,7 +7,7 @@
   import Ecto.Query, warn: false
 
   alias GA.Accounts
-  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Audit.{Chain, Checkpoint, Log, Verifier}
   alias GA.Repo
 
   @default_limit 50
@@ -114,6 +114,12 @@
 
   def list_checkpoints(_), do: []
 
+  @doc """
+  Verifies chain integrity for a single account.
+  """
+  def verify_chain(account_id) when is_binary(account_id), do: Verifier.verify(account_id)
+  def verify_chain(_), do: {:error, :invalid_id}
+
   defp lock_account(account_id) do
     {key_a, key_b} = account_lock_keys(account_id)
 
diff --git a/app/lib/app/audit/checkpoint_worker.ex b/app/lib/app/audit/checkpoint_worker.ex
new file mode 100644
index 0000000000000000000000000000000000000000..f54908642ad7c854eae895b9605661f5beb0246f
--- /dev/null
+++ b/app/lib/app/audit/checkpoint_worker.ex
@@ -0,0 +1,82 @@
+defmodule GA.Audit.CheckpointWorker do
+  @moduledoc """
+  Periodically creates audit checkpoints for all active accounts.
+  """
+
+  use GenServer
+
+  import Ecto.Query, warn: false
+
+  require Logger
+
+  alias GA.Accounts.Account
+  alias GA.Audit
+  alias GA.Repo
+
+  @default_interval_ms :timer.hours(1)
+
+  def start_link(opts \\ []) do
+    name = Keyword.get(opts, :name, __MODULE__)
+    GenServer.start_link(__MODULE__, opts, name: name)
+  end
+
+  @impl true
+  def init(opts) do
+    interval_ms =
+      opts
+      |> Keyword.get(:interval_ms, configured_interval_ms())
+      |> normalize_interval()
+
+    schedule_tick(interval_ms)
+
+    {:ok, %{interval_ms: interval_ms}}
+  end
+
+  @impl true
+  def handle_info(:create_checkpoints, state) do
+    active_account_ids()
+    |> Enum.each(&create_checkpoint_for_account/1)
+
+    schedule_tick(state.interval_ms)
+    {:noreply, state}
+  end
+
+  defp active_account_ids do
+    from(account in Account,
+      where: account.status == :active,
+      select: account.id
+    )
+    |> Repo.all()
+  end
+
+  defp create_checkpoint_for_account(account_id) do
+    case Audit.create_checkpoint(account_id) do
+      {:ok, _checkpoint} ->
+        Logger.debug("checkpoint_worker created checkpoint for account=#{account_id}")
+
+      {:error, :no_entries} ->
+        Logger.debug("checkpoint_worker skipping account=#{account_id}: no audit entries")
+
+      {:error, reason} ->
+        Logger.error("checkpoint_worker failed for account=#{account_id}: #{inspect(reason)}")
+    end
+  rescue
+    error ->
+      Logger.error(
+        "checkpoint_worker crashed for account=#{account_id}: #{Exception.message(error)}"
+      )
+  end
+
+  defp schedule_tick(interval_ms) do
+    Process.send_after(self(), :create_checkpoints, interval_ms)
+  end
+
+  defp configured_interval_ms do
+    :app
+    |> Application.get_env(__MODULE__, [])
+    |> Keyword.get(:interval_ms, @default_interval_ms)
+  end
+
+  defp normalize_interval(interval_ms) when is_integer(interval_ms) and interval_ms > 0, do: interval_ms
+  defp normalize_interval(_), do: @default_interval_ms
+end
diff --git a/app/lib/app/audit/verifier.ex b/app/lib/app/audit/verifier.ex
new file mode 100644
index 0000000000000000000000000000000000000000..2a4bd77516a2c76ccc9b05de1778dd8781ded096
--- /dev/null
+++ b/app/lib/app/audit/verifier.ex
@@ -0,0 +1,203 @@
+defmodule GA.Audit.Verifier do
+  @moduledoc """
+  Streams and verifies account-scoped audit chains.
+  """
+
+  import Ecto.Query, warn: false
+
+  alias GA.Accounts
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  @batch_size 1000
+
+  @doc """
+  Verifies an account's audit chain and returns a report map.
+  """
+  @spec verify(binary()) :: map() | {:error, :invalid_id | :not_found}
+  def verify(account_id) when is_binary(account_id) do
+    started_at = System.monotonic_time(:millisecond)
+
+    with {:ok, hmac_key} <- Accounts.get_hmac_key(account_id) do
+      checkpoints_by_sequence = load_checkpoints(account_id)
+      report = stream_and_verify(account_id, hmac_key, checkpoints_by_sequence)
+
+      Map.put(report, :duration_ms, elapsed_ms(started_at))
+    end
+  end
+
+  def verify(_), do: {:error, :invalid_id}
+
+  defp stream_and_verify(account_id, hmac_key, checkpoints_by_sequence) do
+    initial =
+      new_accumulator()
+      |> Map.put(:account_id, account_id)
+
+    account_id
+    |> do_verify_batches(hmac_key, checkpoints_by_sequence, initial)
+    |> finalize_report()
+  end
+
+  defp do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, acc) do
+    batch = load_batch(account_id, acc.last_sequence)
+
+    case batch do
+      [] ->
+        acc
+
+      _entries ->
+        next_acc =
+          Enum.reduce(batch, acc, fn entry, state ->
+            verify_entry(entry, hmac_key, checkpoints_by_sequence, state)
+          end)
+
+        do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, next_acc)
+    end
+  end
+
+  defp verify_entry(%Log{} = entry, hmac_key, checkpoints_by_sequence, acc) do
+    acc
+    |> verify_sequence(entry)
+    |> verify_checksum(entry, hmac_key)
+    |> verify_checkpoint(entry, checkpoints_by_sequence)
+    |> advance_state(entry)
+  end
+
+  defp verify_sequence(acc, %Log{sequence_number: found_sequence}) do
+    expected_sequence = acc.expected_sequence
+
+    if found_sequence == expected_sequence do
+      %{acc | expected_sequence: expected_sequence + 1}
+    else
+      missing =
+        if found_sequence > expected_sequence do
+          Enum.to_list(expected_sequence..(found_sequence - 1))
+        else
+          []
+        end
+
+      gap = %{
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      }
+
+      acc
+      |> mark_invalid(%{
+        type: :sequence_gap,
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      })
+      |> Map.update!(:sequence_gaps, &[gap | &1])
+      |> Map.put(:expected_sequence, max(found_sequence, expected_sequence) + 1)
+    end
+  end
+
+  defp verify_checksum(acc, %Log{} = entry, hmac_key) do
+    if Chain.verify_checksum(hmac_key, entry, acc.previous_checksum) do
+      acc
+    else
+      mark_invalid(acc, %{
+        type: :checksum_mismatch,
+        sequence_number: entry.sequence_number,
+        stored_checksum: entry.checksum,
+        expected_checksum: expected_checksum(hmac_key, entry, acc.previous_checksum)
+      })
+    end
+  end
+
+  defp verify_checkpoint(acc, %Log{} = entry, checkpoints_by_sequence) do
+    case Map.get(checkpoints_by_sequence, entry.sequence_number) do
+      nil ->
+        acc
+
+      %Checkpoint{} = checkpoint ->
+        valid? = checkpoint.checksum == entry.checksum
+        result = %{sequence_number: entry.sequence_number, valid: valid?}
+        next_acc = Map.update!(acc, :checkpoint_results, &[result | &1])
+
+        if valid? do
+          next_acc
+        else
+          mark_invalid(next_acc, %{
+            type: :checkpoint_mismatch,
+            sequence_number: entry.sequence_number,
+            checkpoint_checksum: checkpoint.checksum,
+            entry_checksum: entry.checksum
+          })
+        end
+    end
+  end
+
+  defp advance_state(acc, %Log{} = entry) do
+    %{
+      acc
+      | previous_checksum: entry.checksum,
+        last_sequence: entry.sequence_number,
+        total_entries: acc.total_entries + 1,
+        verified_entries: acc.verified_entries + 1
+    }
+  end
+
+  defp expected_checksum(hmac_key, entry, previous_checksum) do
+    Chain.compute_checksum(hmac_key, Map.from_struct(entry), previous_checksum)
+  rescue
+    _ -> nil
+  end
+
+  defp load_batch(account_id, after_sequence) do
+    from(log in Log,
+      where: log.account_id == ^account_id and log.sequence_number > ^after_sequence,
+      order_by: [asc: log.sequence_number],
+      limit: @batch_size
+    )
+    |> Repo.all()
+  end
+
+  defp load_checkpoints(account_id) do
+    from(checkpoint in Checkpoint, where: checkpoint.account_id == ^account_id)
+    |> Repo.all()
+    |> Map.new(fn checkpoint -> {checkpoint.sequence_number, checkpoint} end)
+  end
+
+  defp new_accumulator do
+    %{
+      account_id: nil,
+      valid: true,
+      total_entries: 0,
+      verified_entries: 0,
+      expected_sequence: 1,
+      previous_checksum: nil,
+      last_sequence: 0,
+      first_failure: nil,
+      sequence_gaps: [],
+      checkpoint_results: []
+    }
+  end
+
+  defp finalize_report(acc) do
+    %{
+      valid: acc.valid,
+      total_entries: acc.total_entries,
+      verified_entries: acc.verified_entries,
+      first_failure: acc.first_failure,
+      sequence_gaps: Enum.reverse(acc.sequence_gaps),
+      checkpoint_results: Enum.reverse(acc.checkpoint_results),
+      duration_ms: 0
+    }
+  end
+
+  defp mark_invalid(acc, failure) do
+    case acc.first_failure do
+      nil -> %{acc | valid: false, first_failure: failure}
+      _existing -> %{acc | valid: false}
+    end
+  end
+
+  defp elapsed_ms(started_at) do
+    System.monotonic_time(:millisecond)
+    |> Kernel.-(started_at)
+    |> max(0)
+  end
+end
diff --git a/app/test/app/audit/checkpoint_worker_test.exs b/app/test/app/audit/checkpoint_worker_test.exs
new file mode 100644
index 0000000000000000000000000000000000000000..ec9ea5e45a2b2d84cd0e7872a7bf7288d8e3ea11
--- /dev/null
+++ b/app/test/app/audit/checkpoint_worker_test.exs
@@ -0,0 +1,111 @@
+defmodule GA.Audit.CheckpointWorkerTest do
+  use GA.DataCase, async: false
+
+  import ExUnit.CaptureLog
+
+  alias GA.Accounts
+  alias GA.Audit
+  alias GA.Audit.CheckpointWorker
+
+  describe "checkpoint worker ticks" do
+    test "creates checkpoints for all active accounts" do
+      account_a = account_fixture()
+      account_b = account_fixture()
+      add_log(account_a.id, "active-a")
+      add_log(account_b.id, "active-b")
+
+      worker = start_worker()
+      send(worker, :create_checkpoints)
+
+      assert_eventually(fn ->
+        length(Audit.list_checkpoints(account_a.id)) == 1 and
+          length(Audit.list_checkpoints(account_b.id)) == 1
+      end)
+    end
+
+    test "skips suspended accounts" do
+      active_account = account_fixture()
+      suspended_account = account_fixture(%{status: :suspended})
+      add_log(active_account.id, "active")
+      add_log(suspended_account.id, "suspended")
+
+      worker = start_worker()
+      send(worker, :create_checkpoints)
+
+      assert_eventually(fn ->
+        length(Audit.list_checkpoints(active_account.id)) == 1
+      end)
+
+      assert Audit.list_checkpoints(suspended_account.id) == []
+    end
+
+    test "logs no-entry accounts and continues processing" do
+      empty_account = account_fixture()
+      populated_account = account_fixture()
+      add_log(populated_account.id, "populated")
+
+      worker = start_worker()
+
+      log =
+        capture_log([level: :debug], fn ->
+          send(worker, :create_checkpoints)
+
+          assert_eventually(fn ->
+            length(Audit.list_checkpoints(populated_account.id)) == 1
+          end)
+        end)
+
+      assert log =~ "checkpoint_worker skipping"
+      assert log =~ empty_account.id
+      assert Audit.list_checkpoints(empty_account.id) == []
+    end
+  end
+
+  defp start_worker do
+    start_supervised!(
+      {CheckpointWorker,
+       [
+         name: {:global, {:checkpoint_worker_test, System.unique_integer([:positive])}},
+         interval_ms: 60_000
+       ]}
+    )
+  end
+
+  defp account_fixture(attrs \\ %{}) do
+    defaults = %{name: "Worker Account #{System.unique_integer([:positive])}"}
+    {:ok, account} = Accounts.create_account(Map.merge(defaults, attrs))
+    account
+  end
+
+  defp add_log(account_id, resource_id) do
+    attrs = %{
+      user_id: Ecto.UUID.generate(),
+      user_role: "admin",
+      session_id: "session-#{System.unique_integer([:positive])}",
+      action: "read",
+      resource_type: "patient",
+      resource_id: resource_id,
+      timestamp: ~U[2026-03-03 16:30:00Z],
+      source_ip: "127.0.0.1",
+      user_agent: "ExUnit",
+      outcome: "success",
+      failure_reason: nil,
+      phi_accessed: false,
+      metadata: %{"source" => "checkpoint_worker_test"}
+    }
+
+    assert {:ok, _} = Audit.create_log_entry(account_id, attrs)
+  end
+
+  defp assert_eventually(fun, attempts \\ 50)
+  defp assert_eventually(fun, 0), do: assert(fun.())
+
+  defp assert_eventually(fun, attempts) do
+    if fun.() do
+      :ok
+    else
+      Process.sleep(20)
+      assert_eventually(fun, attempts - 1)
+    end
+  end
+end
diff --git a/app/test/app/audit/verifier_test.exs b/app/test/app/audit/verifier_test.exs
new file mode 100644
index 0000000000000000000000000000000000000000..430e2450186c77bb9e816cfa26910a774995b717
--- /dev/null
+++ b/app/test/app/audit/verifier_test.exs
@@ -0,0 +1,189 @@
+defmodule GA.Audit.VerifierTest do
+  use GA.DataCase, async: false
+
+  alias GA.Accounts
+  alias GA.Audit
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  describe "verify_chain/1" do
+    test "returns a valid report for an intact account chain" do
+      account = account_fixture()
+
+      assert {:ok, _first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "r-1"}))
+      assert {:ok, _second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "r-2"}))
+      assert {:ok, checkpoint} = Audit.create_checkpoint(account.id)
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == true
+      assert report.total_entries == 2
+      assert report.verified_entries == 2
+      assert report.first_failure == nil
+      assert report.sequence_gaps == []
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == checkpoint.sequence_number and &1.valid))
+      assert is_integer(report.duration_ms)
+      assert report.duration_ms >= 0
+    end
+
+    test "detects tampered checksum entries" do
+      account = account_fixture()
+
+      assert {:ok, first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "seed"}))
+
+      insert_raw_log(account.id, %{
+        sequence_number: 2,
+        previous_checksum: first.checksum,
+        checksum: String.duplicate("0", 64),
+        resource_id: "tampered"
+      })
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert report.first_failure.type == :checksum_mismatch
+      assert report.first_failure.sequence_number == 2
+      assert report.first_failure.stored_checksum == String.duplicate("0", 64)
+      refute is_nil(report.first_failure.expected_checksum)
+    end
+
+    test "detects sequence gaps within an account" do
+      account = account_fixture()
+      assert {:ok, _first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "gap-1"}))
+      assert {:ok, second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "gap-2"}))
+      assert {:ok, hmac_key} = Accounts.get_hmac_key(account.id)
+
+      checksum =
+        checksum_for_sequence(
+          account.id,
+          hmac_key,
+          4,
+          second.checksum,
+          %{resource_id: "gap-4"}
+        )
+
+      insert_raw_log(account.id, %{
+        sequence_number: 4,
+        previous_checksum: second.checksum,
+        checksum: checksum,
+        resource_id: "gap-4"
+      })
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert report.first_failure.type == :sequence_gap
+      assert [%{expected: 3, found: 4, missing: [3]}] = report.sequence_gaps
+    end
+
+    test "validates checkpoint anchors and reports invalid anchors" do
+      account = account_fixture()
+      assert {:ok, first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "cp-1"}))
+      assert {:ok, second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "cp-2"}))
+
+      insert_checkpoint(account.id, first.sequence_number, first.checksum)
+      insert_checkpoint(account.id, second.sequence_number, String.duplicate("f", 64))
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == first.sequence_number and &1.valid))
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == second.sequence_number and not &1.valid))
+    end
+
+    test "includes non-negative duration tracking" do
+      account = account_fixture()
+      assert {:ok, _} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "duration"}))
+
+      report = Audit.verify_chain(account.id)
+
+      assert is_integer(report.duration_ms)
+      assert report.duration_ms >= 0
+    end
+
+    test "verification remains account-scoped" do
+      account_a = account_fixture()
+      account_b = account_fixture()
+
+      assert {:ok, _} = Audit.create_log_entry(account_a.id, valid_attrs(%{resource_id: "a-1"}))
+      assert {:ok, first_b} = Audit.create_log_entry(account_b.id, valid_attrs(%{resource_id: "b-1"}))
+
+      insert_raw_log(account_b.id, %{
+        sequence_number: 2,
+        previous_checksum: first_b.checksum,
+        checksum: String.duplicate("a", 64),
+        resource_id: "b-2-tampered"
+      })
+
+      report = Audit.verify_chain(account_a.id)
+
+      assert report.valid == true
+      assert report.total_entries == 1
+      assert report.first_failure == nil
+      assert report.sequence_gaps == []
+    end
+  end
+
+  defp account_fixture(attrs \\ %{}) do
+    defaults = %{name: "Verifier Account #{System.unique_integer([:positive])}"}
+    {:ok, account} = Accounts.create_account(Map.merge(defaults, attrs))
+    account
+  end
+
+  defp valid_attrs(overrides \\ %{}) do
+    defaults = %{
+      user_id: Ecto.UUID.generate(),
+      user_role: "admin",
+      session_id: "session-#{System.unique_integer([:positive])}",
+      action: "read",
+      resource_type: "patient",
+      resource_id: Ecto.UUID.generate(),
+      timestamp: ~U[2026-03-03 16:30:00Z],
+      source_ip: "127.0.0.1",
+      user_agent: "ExUnit",
+      outcome: "success",
+      failure_reason: nil,
+      phi_accessed: false,
+      metadata: %{"source" => "verifier_test"}
+    }
+
+    Map.merge(defaults, overrides)
+  end
+
+  defp checksum_for_sequence(account_id, hmac_key, sequence_number, previous_checksum, overrides \\ %{}) do
+    attrs =
+      valid_attrs(overrides)
+      |> Map.put(:account_id, account_id)
+      |> Map.put(:sequence_number, sequence_number)
+
+    Chain.compute_checksum(hmac_key, attrs, previous_checksum)
+  end
+
+  defp insert_raw_log(account_id, attrs) do
+    now = DateTime.utc_now() |> DateTime.truncate(:microsecond)
+    id = Ecto.UUID.generate()
+
+    row =
+      valid_attrs()
+      |> Map.merge(attrs)
+      |> Map.merge(%{
+        id: id,
+        account_id: account_id,
+        inserted_at: now,
+        updated_at: now
+      })
+
+    {1, _} = Repo.insert_all(Log, [row])
+    Repo.get!(Log, id)
+  end
+
+  defp insert_checkpoint(account_id, sequence_number, checksum) do
+    %Checkpoint{}
+    |> Checkpoint.changeset(%{
+      account_id: account_id,
+      sequence_number: sequence_number,
+      checksum: checksum
+    })
+    |> Repo.insert!()
+  end
+end

codex
Core code and tests are in place; I’m doing a quick pass for syntax/import issues and then running targeted test commands (or capturing the known Elixir/OTP blocker if it still applies).
exec
/bin/zsh -lc "sed -n '1,320p' app/lib/app/audit/verifier.ex" in /Users/tbrewer/projects/goodway/goodauditexec
/bin/zsh -lc "sed -n '1,320p' app/lib/app/audit/checkpoint_worker.ex" in /Users/tbrewer/projects/goodway/goodauditexec
/bin/zsh -lc "sed -n '1,260p' app/lib/app/audit.ex" in /Users/tbrewer/projects/goodway/goodauditexec
/bin/zsh -lc "sed -n '1,240p' app/test/app/audit/verifier_test.exs" in /Users/tbrewer/projects/goodway/goodauditexec
/bin/zsh -lc "sed -n '1,220p' app/lib/app/application.ex" in /Users/tbrewer/projects/goodway/goodauditexec
/bin/zsh -lc "sed -n '1,240p' app/test/app/audit/checkpoint_worker_test.exs" in /Users/tbrewer/projects/goodway/goodaudit succeeded in 50ms:
defmodule GA.Audit.Verifier do
  @moduledoc """
  Streams and verifies account-scoped audit chains.
  """

  import Ecto.Query, warn: false

  alias GA.Accounts
  alias GA.Audit.{Chain, Checkpoint, Log}
  alias GA.Repo

  @batch_size 1000

  @doc """
  Verifies an account's audit chain and returns a report map.
  """
  @spec verify(binary()) :: map() | {:error, :invalid_id | :not_found}
  def verify(account_id) when is_binary(account_id) do
    started_at = System.monotonic_time(:millisecond)

 succeeded in 50ms:
defmodule GA.Audit.CheckpointWorker do
  @moduledoc """
  Periodically creates audit checkpoints for all active accounts.
  """

  use GenServer

  import Ecto.Query, warn: false

  require Logger

  alias GA.Accounts.Account
  alias GA.Audit
  alias GA.Repo

  @default_interval_ms :timer.hours(1)

  def start_link(opts \\ []) do
    name = Keyword.get(opts, :name, __MODULE__)
    GenServer.start_link(__MODULE__, opts, name: name)
 succeeded in 50ms:
defmodule GA.Audit do
  @moduledoc """
  Business logic for account-scoped append-only audit logs and checkpoints.
  """

  import Ecto.Changeset, only: [put_change: 3]
  import Ecto.Query, warn: false

  alias GA.Accounts
  alias GA.Audit.{Chain, Checkpoint, Log, Verifier}
  alias GA.Repo

  @default_limit 50
  @max_limit 1000

  @doc """
  Creates a new audit log entry with per-account chain fields in a single transaction.
  """
  def create_log_entry(account_id, attrs)
      when is_binary(account_id) and (is_map(attrs) or is_list(attrs)) do
 succeeded in 52ms:
defmodule GA.Audit.VerifierTest do
  use GA.DataCase, async: false

  alias GA.Accounts
  alias GA.Audit
  alias GA.Audit.{Chain, Checkpoint, Log}
  alias GA.Repo

  describe "verify_chain/1" do
    test "returns a valid report for an intact account chain" do
      account = account_fixture()

      assert {:ok, _first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "r-1"}))
      assert {:ok, _second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "r-2"}))
      assert {:ok, checkpoint} = Audit.create_checkpoint(account.id)

      report = Audit.verify_chain(account.id)

      assert report.valid == true
      assert report.total_entries == 2
 succeeded in 52ms:
defmodule GA.Application do
  # See https://hexdocs.pm/elixir/Application.html
  # for more information on OTP Applications
  @moduledoc false

  use Application

  @impl true
  def start(_type, _args) do
    children = [
      GAWeb.Telemetry,
      GA.Repo,
      {DNSCluster, query: Application.get_env(:app, :dns_cluster_query) || :ignore},
      {Phoenix.PubSub, name: GA.PubSub},
      # Start a worker by calling: GA.Worker.start_link(arg)
      # {GA.Worker, arg},
      {GA.Audit.CheckpointWorker, checkpoint_worker_opts()},
      # Start to serve requests, typically the last entry
      GAWeb.Endpoint
    ]
 succeeded in 52ms:
defmodule GA.Audit.CheckpointWorkerTest do
  use GA.DataCase, async: false

  import ExUnit.CaptureLog

  alias GA.Accounts
  alias GA.Audit
  alias GA.Audit.CheckpointWorker

  describe "checkpoint worker ticks" do
    test "creates checkpoints for all active accounts" do
      account_a = account_fixture()
      account_b = account_fixture()
      add_log(account_a.id, "active-a")
      add_log(account_b.id, "active-b")

      worker = start_worker()
      send(worker, :create_checkpoints)

      assert_eventually(fn ->
file update:
diff --git a/app/lib/app/application.ex b/app/lib/app/application.ex
index d865bfe6a48ed688366f7d85220490621cd809a7..08bab073bf607f5bb04aae846094b0b08ec40fe4
--- a/app/lib/app/application.ex
+++ b/app/lib/app/application.ex
@@ -14,6 +14,7 @@
       {Phoenix.PubSub, name: GA.PubSub},
       # Start a worker by calling: GA.Worker.start_link(arg)
       # {GA.Worker, arg},
+      {GA.Audit.CheckpointWorker, checkpoint_worker_opts()},
       # Start to serve requests, typically the last entry
       GAWeb.Endpoint
     ]
@@ -31,4 +32,8 @@
     GAWeb.Endpoint.config_change(changed, removed)
     :ok
   end
+
+  defp checkpoint_worker_opts do
+    Application.get_env(:app, GA.Audit.CheckpointWorker, [])
+  end
 end
diff --git a/app/lib/app/audit.ex b/app/lib/app/audit.ex
index a48830aacf4da8275b40f15016dcd16bbc87411f..2c7fbce41d873e721533728bcc1c965168addd3d
--- a/app/lib/app/audit.ex
+++ b/app/lib/app/audit.ex
@@ -7,7 +7,7 @@
   import Ecto.Query, warn: false
 
   alias GA.Accounts
-  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Audit.{Chain, Checkpoint, Log, Verifier}
   alias GA.Repo
 
   @default_limit 50
@@ -114,6 +114,12 @@
 
   def list_checkpoints(_), do: []
 
+  @doc """
+  Verifies chain integrity for a single account.
+  """
+  def verify_chain(account_id) when is_binary(account_id), do: Verifier.verify(account_id)
+  def verify_chain(_), do: {:error, :invalid_id}
+
   defp lock_account(account_id) do
     {key_a, key_b} = account_lock_keys(account_id)
 
diff --git a/app/lib/app/audit/checkpoint_worker.ex b/app/lib/app/audit/checkpoint_worker.ex
new file mode 100644
index 0000000000000000000000000000000000000000..f54908642ad7c854eae895b9605661f5beb0246f
--- /dev/null
+++ b/app/lib/app/audit/checkpoint_worker.ex
@@ -0,0 +1,82 @@
+defmodule GA.Audit.CheckpointWorker do
+  @moduledoc """
+  Periodically creates audit checkpoints for all active accounts.
+  """
+
+  use GenServer
+
+  import Ecto.Query, warn: false
+
+  require Logger
+
+  alias GA.Accounts.Account
+  alias GA.Audit
+  alias GA.Repo
+
+  @default_interval_ms :timer.hours(1)
+
+  def start_link(opts \\ []) do
+    name = Keyword.get(opts, :name, __MODULE__)
+    GenServer.start_link(__MODULE__, opts, name: name)
+  end
+
+  @impl true
+  def init(opts) do
+    interval_ms =
+      opts
+      |> Keyword.get(:interval_ms, configured_interval_ms())
+      |> normalize_interval()
+
+    schedule_tick(interval_ms)
+
+    {:ok, %{interval_ms: interval_ms}}
+  end
+
+  @impl true
+  def handle_info(:create_checkpoints, state) do
+    active_account_ids()
+    |> Enum.each(&create_checkpoint_for_account/1)
+
+    schedule_tick(state.interval_ms)
+    {:noreply, state}
+  end
+
+  defp active_account_ids do
+    from(account in Account,
+      where: account.status == :active,
+      select: account.id
+    )
+    |> Repo.all()
+  end
+
+  defp create_checkpoint_for_account(account_id) do
+    case Audit.create_checkpoint(account_id) do
+      {:ok, _checkpoint} ->
+        Logger.debug("checkpoint_worker created checkpoint for account=#{account_id}")
+
+      {:error, :no_entries} ->
+        Logger.debug("checkpoint_worker skipping account=#{account_id}: no audit entries")
+
+      {:error, reason} ->
+        Logger.error("checkpoint_worker failed for account=#{account_id}: #{inspect(reason)}")
+    end
+  rescue
+    error ->
+      Logger.error(
+        "checkpoint_worker crashed for account=#{account_id}: #{Exception.message(error)}"
+      )
+  end
+
+  defp schedule_tick(interval_ms) do
+    Process.send_after(self(), :create_checkpoints, interval_ms)
+  end
+
+  defp configured_interval_ms do
+    :app
+    |> Application.get_env(__MODULE__, [])
+    |> Keyword.get(:interval_ms, @default_interval_ms)
+  end
+
+  defp normalize_interval(interval_ms) when is_integer(interval_ms) and interval_ms > 0, do: interval_ms
+  defp normalize_interval(_), do: @default_interval_ms
+end
diff --git a/app/lib/app/audit/verifier.ex b/app/lib/app/audit/verifier.ex
new file mode 100644
index 0000000000000000000000000000000000000000..2a4bd77516a2c76ccc9b05de1778dd8781ded096
--- /dev/null
+++ b/app/lib/app/audit/verifier.ex
@@ -0,0 +1,203 @@
+defmodule GA.Audit.Verifier do
+  @moduledoc """
+  Streams and verifies account-scoped audit chains.
+  """
+
+  import Ecto.Query, warn: false
+
+  alias GA.Accounts
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  @batch_size 1000
+
+  @doc """
+  Verifies an account's audit chain and returns a report map.
+  """
+  @spec verify(binary()) :: map() | {:error, :invalid_id | :not_found}
+  def verify(account_id) when is_binary(account_id) do
+    started_at = System.monotonic_time(:millisecond)
+
+    with {:ok, hmac_key} <- Accounts.get_hmac_key(account_id) do
+      checkpoints_by_sequence = load_checkpoints(account_id)
+      report = stream_and_verify(account_id, hmac_key, checkpoints_by_sequence)
+
+      Map.put(report, :duration_ms, elapsed_ms(started_at))
+    end
+  end
+
+  def verify(_), do: {:error, :invalid_id}
+
+  defp stream_and_verify(account_id, hmac_key, checkpoints_by_sequence) do
+    initial =
+      new_accumulator()
+      |> Map.put(:account_id, account_id)
+
+    account_id
+    |> do_verify_batches(hmac_key, checkpoints_by_sequence, initial)
+    |> finalize_report()
+  end
+
+  defp do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, acc) do
+    batch = load_batch(account_id, acc.last_sequence)
+
+    case batch do
+      [] ->
+        acc
+
+      _entries ->
+        next_acc =
+          Enum.reduce(batch, acc, fn entry, state ->
+            verify_entry(entry, hmac_key, checkpoints_by_sequence, state)
+          end)
+
+        do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, next_acc)
+    end
+  end
+
+  defp verify_entry(%Log{} = entry, hmac_key, checkpoints_by_sequence, acc) do
+    acc
+    |> verify_sequence(entry)
+    |> verify_checksum(entry, hmac_key)
+    |> verify_checkpoint(entry, checkpoints_by_sequence)
+    |> advance_state(entry)
+  end
+
+  defp verify_sequence(acc, %Log{sequence_number: found_sequence}) do
+    expected_sequence = acc.expected_sequence
+
+    if found_sequence == expected_sequence do
+      %{acc | expected_sequence: expected_sequence + 1}
+    else
+      missing =
+        if found_sequence > expected_sequence do
+          Enum.to_list(expected_sequence..(found_sequence - 1))
+        else
+          []
+        end
+
+      gap = %{
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      }
+
+      acc
+      |> mark_invalid(%{
+        type: :sequence_gap,
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      })
+      |> Map.update!(:sequence_gaps, &[gap | &1])
+      |> Map.put(:expected_sequence, max(found_sequence, expected_sequence) + 1)
+    end
+  end
+
+  defp verify_checksum(acc, %Log{} = entry, hmac_key) do
+    if Chain.verify_checksum(hmac_key, entry, acc.previous_checksum) do
+      acc
+    else
+      mark_invalid(acc, %{
+        type: :checksum_mismatch,
+        sequence_number: entry.sequence_number,
+        stored_checksum: entry.checksum,
+        expected_checksum: expected_checksum(hmac_key, entry, acc.previous_checksum)
+      })
+    end
+  end
+
+  defp verify_checkpoint(acc, %Log{} = entry, checkpoints_by_sequence) do
+    case Map.get(checkpoints_by_sequence, entry.sequence_number) do
+      nil ->
+        acc
+
+      %Checkpoint{} = checkpoint ->
+        valid? = checkpoint.checksum == entry.checksum
+        result = %{sequence_number: entry.sequence_number, valid: valid?}
+        next_acc = Map.update!(acc, :checkpoint_results, &[result | &1])
+
+        if valid? do
+          next_acc
+        else
+          mark_invalid(next_acc, %{
+            type: :checkpoint_mismatch,
+            sequence_number: entry.sequence_number,
+            checkpoint_checksum: checkpoint.checksum,
+            entry_checksum: entry.checksum
+          })
+        end
+    end
+  end
+
+  defp advance_state(acc, %Log{} = entry) do
+    %{
+      acc
+      | previous_checksum: entry.checksum,
+        last_sequence: entry.sequence_number,
+        total_entries: acc.total_entries + 1,
+        verified_entries: acc.verified_entries + 1
+    }
+  end
+
+  defp expected_checksum(hmac_key, entry, previous_checksum) do
+    Chain.compute_checksum(hmac_key, Map.from_struct(entry), previous_checksum)
+  rescue
+    _ -> nil
+  end
+
+  defp load_batch(account_id, after_sequence) do
+    from(log in Log,
+      where: log.account_id == ^account_id and log.sequence_number > ^after_sequence,
+      order_by: [asc: log.sequence_number],
+      limit: @batch_size
+    )
+    |> Repo.all()
+  end
+
+  defp load_checkpoints(account_id) do
+    from(checkpoint in Checkpoint, where: checkpoint.account_id == ^account_id)
+    |> Repo.all()
+    |> Map.new(fn checkpoint -> {checkpoint.sequence_number, checkpoint} end)
+  end
+
+  defp new_accumulator do
+    %{
+      account_id: nil,
+      valid: true,
+      total_entries: 0,
+      verified_entries: 0,
+      expected_sequence: 1,
+      previous_checksum: nil,
+      last_sequence: 0,
+      first_failure: nil,
+      sequence_gaps: [],
+      checkpoint_results: []
+    }
+  end
+
+  defp finalize_report(acc) do
+    %{
+      valid: acc.valid,
+      total_entries: acc.total_entries,
+      verified_entries: acc.verified_entries,
+      first_failure: acc.first_failure,
+      sequence_gaps: Enum.reverse(acc.sequence_gaps),
+      checkpoint_results: Enum.reverse(acc.checkpoint_results),
+      duration_ms: 0
+    }
+  end
+
+  defp mark_invalid(acc, failure) do
+    case acc.first_failure do
+      nil -> %{acc | valid: false, first_failure: failure}
+      _existing -> %{acc | valid: false}
+    end
+  end
+
+  defp elapsed_ms(started_at) do
+    System.monotonic_time(:millisecond)
+    |> Kernel.-(started_at)
+    |> max(0)
+  end
+end
diff --git a/app/test/app/audit/checkpoint_worker_test.exs b/app/test/app/audit/checkpoint_worker_test.exs
new file mode 100644
index 0000000000000000000000000000000000000000..ec9ea5e45a2b2d84cd0e7872a7bf7288d8e3ea11
--- /dev/null
+++ b/app/test/app/audit/checkpoint_worker_test.exs
@@ -0,0 +1,111 @@
+defmodule GA.Audit.CheckpointWorkerTest do
+  use GA.DataCase, async: false
+
+  import ExUnit.CaptureLog
+
+  alias GA.Accounts
+  alias GA.Audit
+  alias GA.Audit.CheckpointWorker
+
+  describe "checkpoint worker ticks" do
+    test "creates checkpoints for all active accounts" do
+      account_a = account_fixture()
+      account_b = account_fixture()
+      add_log(account_a.id, "active-a")
+      add_log(account_b.id, "active-b")
+
+      worker = start_worker()
+      send(worker, :create_checkpoints)
+
+      assert_eventually(fn ->
+        length(Audit.list_checkpoints(account_a.id)) == 1 and
+          length(Audit.list_checkpoints(account_b.id)) == 1
+      end)
+    end
+
+    test "skips suspended accounts" do
+      active_account = account_fixture()
+      suspended_account = account_fixture(%{status: :suspended})
+      add_log(active_account.id, "active")
+      add_log(suspended_account.id, "suspended")
+
+      worker = start_worker()
+      send(worker, :create_checkpoints)
+
+      assert_eventually(fn ->
+        length(Audit.list_checkpoints(active_account.id)) == 1
+      end)
+
+      assert Audit.list_checkpoints(suspended_account.id) == []
+    end
+
+    test "logs no-entry accounts and continues processing" do
+      empty_account = account_fixture()
+      populated_account = account_fixture()
+      add_log(populated_account.id, "populated")
+
+      worker = start_worker()
+
+      log =
+        capture_log([level: :debug], fn ->
+          send(worker, :create_checkpoints)
+
+          assert_eventually(fn ->
+            length(Audit.list_checkpoints(populated_account.id)) == 1
+          end)
+        end)
+
+      assert log =~ "checkpoint_worker skipping"
+      assert log =~ empty_account.id
+      assert Audit.list_checkpoints(empty_account.id) == []
+    end
+  end
+
+  defp start_worker do
+    start_supervised!(
+      {CheckpointWorker,
+       [
+         name: {:global, {:checkpoint_worker_test, System.unique_integer([:positive])}},
+         interval_ms: 60_000
+       ]}
+    )
+  end
+
+  defp account_fixture(attrs \\ %{}) do
+    defaults = %{name: "Worker Account #{System.unique_integer([:positive])}"}
+    {:ok, account} = Accounts.create_account(Map.merge(defaults, attrs))
+    account
+  end
+
+  defp add_log(account_id, resource_id) do
+    attrs = %{
+      user_id: Ecto.UUID.generate(),
+      user_role: "admin",
+      session_id: "session-#{System.unique_integer([:positive])}",
+      action: "read",
+      resource_type: "patient",
+      resource_id: resource_id,
+      timestamp: ~U[2026-03-03 16:30:00Z],
+      source_ip: "127.0.0.1",
+      user_agent: "ExUnit",
+      outcome: "success",
+      failure_reason: nil,
+      phi_accessed: false,
+      metadata: %{"source" => "checkpoint_worker_test"}
+    }
+
+    assert {:ok, _} = Audit.create_log_entry(account_id, attrs)
+  end
+
+  defp assert_eventually(fun, attempts \\ 50)
+  defp assert_eventually(fun, 0), do: assert(fun.())
+
+  defp assert_eventually(fun, attempts) do
+    if fun.() do
+      :ok
+    else
+      Process.sleep(20)
+      assert_eventually(fun, attempts - 1)
+    end
+  end
+end
diff --git a/app/test/app/audit/verifier_test.exs b/app/test/app/audit/verifier_test.exs
new file mode 100644
index 0000000000000000000000000000000000000000..430e2450186c77bb9e816cfa26910a774995b717
--- /dev/null
+++ b/app/test/app/audit/verifier_test.exs
@@ -0,0 +1,189 @@
+defmodule GA.Audit.VerifierTest do
+  use GA.DataCase, async: false
+
+  alias GA.Accounts
+  alias GA.Audit
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  describe "verify_chain/1" do
+    test "returns a valid report for an intact account chain" do
+      account = account_fixture()
+
+      assert {:ok, _first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "r-1"}))
+      assert {:ok, _second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "r-2"}))
+      assert {:ok, checkpoint} = Audit.create_checkpoint(account.id)
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == true
+      assert report.total_entries == 2
+      assert report.verified_entries == 2
+      assert report.first_failure == nil
+      assert report.sequence_gaps == []
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == checkpoint.sequence_number and &1.valid))
+      assert is_integer(report.duration_ms)
+      assert report.duration_ms >= 0
+    end
+
+    test "detects tampered checksum entries" do
+      account = account_fixture()
+
+      assert {:ok, first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "seed"}))
+
+      insert_raw_log(account.id, %{
+        sequence_number: 2,
+        previous_checksum: first.checksum,
+        checksum: String.duplicate("0", 64),
+        resource_id: "tampered"
+      })
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert report.first_failure.type == :checksum_mismatch
+      assert report.first_failure.sequence_number == 2
+      assert report.first_failure.stored_checksum == String.duplicate("0", 64)
+      refute is_nil(report.first_failure.expected_checksum)
+    end
+
+    test "detects sequence gaps within an account" do
+      account = account_fixture()
+      assert {:ok, _first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "gap-1"}))
+      assert {:ok, second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "gap-2"}))
+      assert {:ok, hmac_key} = Accounts.get_hmac_key(account.id)
+
+      checksum =
+        checksum_for_sequence(
+          account.id,
+          hmac_key,
+          4,
+          second.checksum,
+          %{resource_id: "gap-4"}
+        )
+
+      insert_raw_log(account.id, %{
+        sequence_number: 4,
+        previous_checksum: second.checksum,
+        checksum: checksum,
+        resource_id: "gap-4"
+      })
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert report.first_failure.type == :sequence_gap
+      assert [%{expected: 3, found: 4, missing: [3]}] = report.sequence_gaps
+    end
+
+    test "validates checkpoint anchors and reports invalid anchors" do
+      account = account_fixture()
+      assert {:ok, first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "cp-1"}))
+      assert {:ok, second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "cp-2"}))
+
+      insert_checkpoint(account.id, first.sequence_number, first.checksum)
+      insert_checkpoint(account.id, second.sequence_number, String.duplicate("f", 64))
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == first.sequence_number and &1.valid))
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == second.sequence_number and not &1.valid))
+    end
+
+    test "includes non-negative duration tracking" do
+      account = account_fixture()
+      assert {:ok, _} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "duration"}))
+
+      report = Audit.verify_chain(account.id)
+
+      assert is_integer(report.duration_ms)
+      assert report.duration_ms >= 0
+    end
+
+    test "verification remains account-scoped" do
+      account_a = account_fixture()
+      account_b = account_fixture()
+
+      assert {:ok, _} = Audit.create_log_entry(account_a.id, valid_attrs(%{resource_id: "a-1"}))
+      assert {:ok, first_b} = Audit.create_log_entry(account_b.id, valid_attrs(%{resource_id: "b-1"}))
+
+      insert_raw_log(account_b.id, %{
+        sequence_number: 2,
+        previous_checksum: first_b.checksum,
+        checksum: String.duplicate("a", 64),
+        resource_id: "b-2-tampered"
+      })
+
+      report = Audit.verify_chain(account_a.id)
+
+      assert report.valid == true
+      assert report.total_entries == 1
+      assert report.first_failure == nil
+      assert report.sequence_gaps == []
+    end
+  end
+
+  defp account_fixture(attrs \\ %{}) do
+    defaults = %{name: "Verifier Account #{System.unique_integer([:positive])}"}
+    {:ok, account} = Accounts.create_account(Map.merge(defaults, attrs))
+    account
+  end
+
+  defp valid_attrs(overrides \\ %{}) do
+    defaults = %{
+      user_id: Ecto.UUID.generate(),
+      user_role: "admin",
+      session_id: "session-#{System.unique_integer([:positive])}",
+      action: "read",
+      resource_type: "patient",
+      resource_id: Ecto.UUID.generate(),
+      timestamp: ~U[2026-03-03 16:30:00Z],
+      source_ip: "127.0.0.1",
+      user_agent: "ExUnit",
+      outcome: "success",
+      failure_reason: nil,
+      phi_accessed: false,
+      metadata: %{"source" => "verifier_test"}
+    }
+
+    Map.merge(defaults, overrides)
+  end
+
+  defp checksum_for_sequence(account_id, hmac_key, sequence_number, previous_checksum, overrides \\ %{}) do
+    attrs =
+      valid_attrs(overrides)
+      |> Map.put(:account_id, account_id)
+      |> Map.put(:sequence_number, sequence_number)
+
+    Chain.compute_checksum(hmac_key, attrs, previous_checksum)
+  end
+
+  defp insert_raw_log(account_id, attrs) do
+    now = DateTime.utc_now() |> DateTime.truncate(:microsecond)
+    id = Ecto.UUID.generate()
+
+    row =
+      valid_attrs()
+      |> Map.merge(attrs)
+      |> Map.merge(%{
+        id: id,
+        account_id: account_id,
+        inserted_at: now,
+        updated_at: now
+      })
+
+    {1, _} = Repo.insert_all(Log, [row])
+    Repo.get!(Log, id)
+  end
+
+  defp insert_checkpoint(account_id, sequence_number, checksum) do
+    %Checkpoint{}
+    |> Checkpoint.changeset(%{
+      account_id: account_id,
+      sequence_number: sequence_number,
+      checksum: checksum
+    })
+    |> Repo.insert!()
+  end
+end

codex
I’m running formatter/tests now from `app/`; if the known Erlang/Elixir mismatch blocks execution again, I’ll report it and still close out the `td` tracking with what was implemented.
exec
/bin/zsh -lc 'mix format' in /Users/tbrewer/projects/goodway/goodaudit/app exited 1 in 302ms:
Error! Failed to load module 'elixir' because it requires a more recent Erlang/OTP version or its .beam file was corrupted.
(You are running Erlang/OTP 27.)

Runtime terminating during boot ({undef,[{init,start_it,1,[]},{init,start_em,1,[]},{init,do_boot,3,[]}]})

Crash dump is being written to: erl_crash.dump...done
file update:
diff --git a/app/lib/app/application.ex b/app/lib/app/application.ex
index d865bfe6a48ed688366f7d85220490621cd809a7..08bab073bf607f5bb04aae846094b0b08ec40fe4
--- a/app/lib/app/application.ex
+++ b/app/lib/app/application.ex
@@ -14,6 +14,7 @@
       {Phoenix.PubSub, name: GA.PubSub},
       # Start a worker by calling: GA.Worker.start_link(arg)
       # {GA.Worker, arg},
+      {GA.Audit.CheckpointWorker, checkpoint_worker_opts()},
       # Start to serve requests, typically the last entry
       GAWeb.Endpoint
     ]
@@ -31,4 +32,8 @@
     GAWeb.Endpoint.config_change(changed, removed)
     :ok
   end
+
+  defp checkpoint_worker_opts do
+    Application.get_env(:app, GA.Audit.CheckpointWorker, [])
+  end
 end
diff --git a/app/lib/app/audit.ex b/app/lib/app/audit.ex
index a48830aacf4da8275b40f15016dcd16bbc87411f..2c7fbce41d873e721533728bcc1c965168addd3d
--- a/app/lib/app/audit.ex
+++ b/app/lib/app/audit.ex
@@ -7,7 +7,7 @@
   import Ecto.Query, warn: false
 
   alias GA.Accounts
-  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Audit.{Chain, Checkpoint, Log, Verifier}
   alias GA.Repo
 
   @default_limit 50
@@ -114,6 +114,12 @@
 
   def list_checkpoints(_), do: []
 
+  @doc """
+  Verifies chain integrity for a single account.
+  """
+  def verify_chain(account_id) when is_binary(account_id), do: Verifier.verify(account_id)
+  def verify_chain(_), do: {:error, :invalid_id}
+
   defp lock_account(account_id) do
     {key_a, key_b} = account_lock_keys(account_id)
 
diff --git a/app/lib/app/audit/checkpoint_worker.ex b/app/lib/app/audit/checkpoint_worker.ex
new file mode 100644
index 0000000000000000000000000000000000000000..f54908642ad7c854eae895b9605661f5beb0246f
--- /dev/null
+++ b/app/lib/app/audit/checkpoint_worker.ex
@@ -0,0 +1,82 @@
+defmodule GA.Audit.CheckpointWorker do
+  @moduledoc """
+  Periodically creates audit checkpoints for all active accounts.
+  """
+
+  use GenServer
+
+  import Ecto.Query, warn: false
+
+  require Logger
+
+  alias GA.Accounts.Account
+  alias GA.Audit
+  alias GA.Repo
+
+  @default_interval_ms :timer.hours(1)
+
+  def start_link(opts \\ []) do
+    name = Keyword.get(opts, :name, __MODULE__)
+    GenServer.start_link(__MODULE__, opts, name: name)
+  end
+
+  @impl true
+  def init(opts) do
+    interval_ms =
+      opts
+      |> Keyword.get(:interval_ms, configured_interval_ms())
+      |> normalize_interval()
+
+    schedule_tick(interval_ms)
+
+    {:ok, %{interval_ms: interval_ms}}
+  end
+
+  @impl true
+  def handle_info(:create_checkpoints, state) do
+    active_account_ids()
+    |> Enum.each(&create_checkpoint_for_account/1)
+
+    schedule_tick(state.interval_ms)
+    {:noreply, state}
+  end
+
+  defp active_account_ids do
+    from(account in Account,
+      where: account.status == :active,
+      select: account.id
+    )
+    |> Repo.all()
+  end
+
+  defp create_checkpoint_for_account(account_id) do
+    case Audit.create_checkpoint(account_id) do
+      {:ok, _checkpoint} ->
+        Logger.debug("checkpoint_worker created checkpoint for account=#{account_id}")
+
+      {:error, :no_entries} ->
+        Logger.debug("checkpoint_worker skipping account=#{account_id}: no audit entries")
+
+      {:error, reason} ->
+        Logger.error("checkpoint_worker failed for account=#{account_id}: #{inspect(reason)}")
+    end
+  rescue
+    error ->
+      Logger.error(
+        "checkpoint_worker crashed for account=#{account_id}: #{Exception.message(error)}"
+      )
+  end
+
+  defp schedule_tick(interval_ms) do
+    Process.send_after(self(), :create_checkpoints, interval_ms)
+  end
+
+  defp configured_interval_ms do
+    :app
+    |> Application.get_env(__MODULE__, [])
+    |> Keyword.get(:interval_ms, @default_interval_ms)
+  end
+
+  defp normalize_interval(interval_ms) when is_integer(interval_ms) and interval_ms > 0, do: interval_ms
+  defp normalize_interval(_), do: @default_interval_ms
+end
diff --git a/app/lib/app/audit/verifier.ex b/app/lib/app/audit/verifier.ex
new file mode 100644
index 0000000000000000000000000000000000000000..2a4bd77516a2c76ccc9b05de1778dd8781ded096
--- /dev/null
+++ b/app/lib/app/audit/verifier.ex
@@ -0,0 +1,203 @@
+defmodule GA.Audit.Verifier do
+  @moduledoc """
+  Streams and verifies account-scoped audit chains.
+  """
+
+  import Ecto.Query, warn: false
+
+  alias GA.Accounts
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  @batch_size 1000
+
+  @doc """
+  Verifies an account's audit chain and returns a report map.
+  """
+  @spec verify(binary()) :: map() | {:error, :invalid_id | :not_found}
+  def verify(account_id) when is_binary(account_id) do
+    started_at = System.monotonic_time(:millisecond)
+
+    with {:ok, hmac_key} <- Accounts.get_hmac_key(account_id) do
+      checkpoints_by_sequence = load_checkpoints(account_id)
+      report = stream_and_verify(account_id, hmac_key, checkpoints_by_sequence)
+
+      Map.put(report, :duration_ms, elapsed_ms(started_at))
+    end
+  end
+
+  def verify(_), do: {:error, :invalid_id}
+
+  defp stream_and_verify(account_id, hmac_key, checkpoints_by_sequence) do
+    initial =
+      new_accumulator()
+      |> Map.put(:account_id, account_id)
+
+    account_id
+    |> do_verify_batches(hmac_key, checkpoints_by_sequence, initial)
+    |> finalize_report()
+  end
+
+  defp do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, acc) do
+    batch = load_batch(account_id, acc.last_sequence)
+
+    case batch do
+      [] ->
+        acc
+
+      _entries ->
+        next_acc =
+          Enum.reduce(batch, acc, fn entry, state ->
+            verify_entry(entry, hmac_key, checkpoints_by_sequence, state)
+          end)
+
+        do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, next_acc)
+    end
+  end
+
+  defp verify_entry(%Log{} = entry, hmac_key, checkpoints_by_sequence, acc) do
+    acc
+    |> verify_sequence(entry)
+    |> verify_checksum(entry, hmac_key)
+    |> verify_checkpoint(entry, checkpoints_by_sequence)
+    |> advance_state(entry)
+  end
+
+  defp verify_sequence(acc, %Log{sequence_number: found_sequence}) do
+    expected_sequence = acc.expected_sequence
+
+    if found_sequence == expected_sequence do
+      %{acc | expected_sequence: expected_sequence + 1}
+    else
+      missing =
+        if found_sequence > expected_sequence do
+          Enum.to_list(expected_sequence..(found_sequence - 1))
+        else
+          []
+        end
+
+      gap = %{
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      }
+
+      acc
+      |> mark_invalid(%{
+        type: :sequence_gap,
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      })
+      |> Map.update!(:sequence_gaps, &[gap | &1])
+      |> Map.put(:expected_sequence, max(found_sequence, expected_sequence) + 1)
+    end
+  end
+
+  defp verify_checksum(acc, %Log{} = entry, hmac_key) do
+    if Chain.verify_checksum(hmac_key, entry, acc.previous_checksum) do
+      acc
+    else
+      mark_invalid(acc, %{
+        type: :checksum_mismatch,
+        sequence_number: entry.sequence_number,
+        stored_checksum: entry.checksum,
+        expected_checksum: expected_checksum(hmac_key, entry, acc.previous_checksum)
+      })
+    end
+  end
+
+  defp verify_checkpoint(acc, %Log{} = entry, checkpoints_by_sequence) do
+    case Map.get(checkpoints_by_sequence, entry.sequence_number) do
+      nil ->
+        acc
+
+      %Checkpoint{} = checkpoint ->
+        valid? = checkpoint.checksum == entry.checksum
+        result = %{sequence_number: entry.sequence_number, valid: valid?}
+        next_acc = Map.update!(acc, :checkpoint_results, &[result | &1])
+
+        if valid? do
+          next_acc
+        else
+          mark_invalid(next_acc, %{
+            type: :checkpoint_mismatch,
+            sequence_number: entry.sequence_number,
+            checkpoint_checksum: checkpoint.checksum,
+            entry_checksum: entry.checksum
+          })
+        end
+    end
+  end
+
+  defp advance_state(acc, %Log{} = entry) do
+    %{
+      acc
+      | previous_checksum: entry.checksum,
+        last_sequence: entry.sequence_number,
+        total_entries: acc.total_entries + 1,
+        verified_entries: acc.verified_entries + 1
+    }
+  end
+
+  defp expected_checksum(hmac_key, entry, previous_checksum) do
+    Chain.compute_checksum(hmac_key, Map.from_struct(entry), previous_checksum)
+  rescue
+    _ -> nil
+  end
+
+  defp load_batch(account_id, after_sequence) do
+    from(log in Log,
+      where: log.account_id == ^account_id and log.sequence_number > ^after_sequence,
+      order_by: [asc: log.sequence_number],
+      limit: @batch_size
+    )
+    |> Repo.all()
+  end
+
+  defp load_checkpoints(account_id) do
+    from(checkpoint in Checkpoint, where: checkpoint.account_id == ^account_id)
+    |> Repo.all()
+    |> Map.new(fn checkpoint -> {checkpoint.sequence_number, checkpoint} end)
+  end
+
+  defp new_accumulator do
+    %{
+      account_id: nil,
+      valid: true,
+      total_entries: 0,
+      verified_entries: 0,
+      expected_sequence: 1,
+      previous_checksum: nil,
+      last_sequence: 0,
+      first_failure: nil,
+      sequence_gaps: [],
+      checkpoint_results: []
+    }
+  end
+
+  defp finalize_report(acc) do
+    %{
+      valid: acc.valid,
+      total_entries: acc.total_entries,
+      verified_entries: acc.verified_entries,
+      first_failure: acc.first_failure,
+      sequence_gaps: Enum.reverse(acc.sequence_gaps),
+      checkpoint_results: Enum.reverse(acc.checkpoint_results),
+      duration_ms: 0
+    }
+  end
+
+  defp mark_invalid(acc, failure) do
+    case acc.first_failure do
+      nil -> %{acc | valid: false, first_failure: failure}
+      _existing -> %{acc | valid: false}
+    end
+  end
+
+  defp elapsed_ms(started_at) do
+    System.monotonic_time(:millisecond)
+    |> Kernel.-(started_at)
+    |> max(0)
+  end
+end
diff --git a/app/test/app/audit/checkpoint_worker_test.exs b/app/test/app/audit/checkpoint_worker_test.exs
new file mode 100644
index 0000000000000000000000000000000000000000..ec9ea5e45a2b2d84cd0e7872a7bf7288d8e3ea11
--- /dev/null
+++ b/app/test/app/audit/checkpoint_worker_test.exs
@@ -0,0 +1,111 @@
+defmodule GA.Audit.CheckpointWorkerTest do
+  use GA.DataCase, async: false
+
+  import ExUnit.CaptureLog
+
+  alias GA.Accounts
+  alias GA.Audit
+  alias GA.Audit.CheckpointWorker
+
+  describe "checkpoint worker ticks" do
+    test "creates checkpoints for all active accounts" do
+      account_a = account_fixture()
+      account_b = account_fixture()
+      add_log(account_a.id, "active-a")
+      add_log(account_b.id, "active-b")
+
+      worker = start_worker()
+      send(worker, :create_checkpoints)
+
+      assert_eventually(fn ->
+        length(Audit.list_checkpoints(account_a.id)) == 1 and
+          length(Audit.list_checkpoints(account_b.id)) == 1
+      end)
+    end
+
+    test "skips suspended accounts" do
+      active_account = account_fixture()
+      suspended_account = account_fixture(%{status: :suspended})
+      add_log(active_account.id, "active")
+      add_log(suspended_account.id, "suspended")
+
+      worker = start_worker()
+      send(worker, :create_checkpoints)
+
+      assert_eventually(fn ->
+        length(Audit.list_checkpoints(active_account.id)) == 1
+      end)
+
+      assert Audit.list_checkpoints(suspended_account.id) == []
+    end
+
+    test "logs no-entry accounts and continues processing" do
+      empty_account = account_fixture()
+      populated_account = account_fixture()
+      add_log(populated_account.id, "populated")
+
+      worker = start_worker()
+
+      log =
+        capture_log([level: :debug], fn ->
+          send(worker, :create_checkpoints)
+
+          assert_eventually(fn ->
+            length(Audit.list_checkpoints(populated_account.id)) == 1
+          end)
+        end)
+
+      assert log =~ "checkpoint_worker skipping"
+      assert log =~ empty_account.id
+      assert Audit.list_checkpoints(empty_account.id) == []
+    end
+  end
+
+  defp start_worker do
+    start_supervised!(
+      {CheckpointWorker,
+       [
+         name: {:global, {:checkpoint_worker_test, System.unique_integer([:positive])}},
+         interval_ms: 60_000
+       ]}
+    )
+  end
+
+  defp account_fixture(attrs \\ %{}) do
+    defaults = %{name: "Worker Account #{System.unique_integer([:positive])}"}
+    {:ok, account} = Accounts.create_account(Map.merge(defaults, attrs))
+    account
+  end
+
+  defp add_log(account_id, resource_id) do
+    attrs = %{
+      user_id: Ecto.UUID.generate(),
+      user_role: "admin",
+      session_id: "session-#{System.unique_integer([:positive])}",
+      action: "read",
+      resource_type: "patient",
+      resource_id: resource_id,
+      timestamp: ~U[2026-03-03 16:30:00Z],
+      source_ip: "127.0.0.1",
+      user_agent: "ExUnit",
+      outcome: "success",
+      failure_reason: nil,
+      phi_accessed: false,
+      metadata: %{"source" => "checkpoint_worker_test"}
+    }
+
+    assert {:ok, _} = Audit.create_log_entry(account_id, attrs)
+  end
+
+  defp assert_eventually(fun, attempts \\ 50)
+  defp assert_eventually(fun, 0), do: assert(fun.())
+
+  defp assert_eventually(fun, attempts) do
+    if fun.() do
+      :ok
+    else
+      Process.sleep(20)
+      assert_eventually(fun, attempts - 1)
+    end
+  end
+end
diff --git a/app/test/app/audit/verifier_test.exs b/app/test/app/audit/verifier_test.exs
new file mode 100644
index 0000000000000000000000000000000000000000..430e2450186c77bb9e816cfa26910a774995b717
--- /dev/null
+++ b/app/test/app/audit/verifier_test.exs
@@ -0,0 +1,189 @@
+defmodule GA.Audit.VerifierTest do
+  use GA.DataCase, async: false
+
+  alias GA.Accounts
+  alias GA.Audit
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  describe "verify_chain/1" do
+    test "returns a valid report for an intact account chain" do
+      account = account_fixture()
+
+      assert {:ok, _first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "r-1"}))
+      assert {:ok, _second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "r-2"}))
+      assert {:ok, checkpoint} = Audit.create_checkpoint(account.id)
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == true
+      assert report.total_entries == 2
+      assert report.verified_entries == 2
+      assert report.first_failure == nil
+      assert report.sequence_gaps == []
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == checkpoint.sequence_number and &1.valid))
+      assert is_integer(report.duration_ms)
+      assert report.duration_ms >= 0
+    end
+
+    test "detects tampered checksum entries" do
+      account = account_fixture()
+
+      assert {:ok, first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "seed"}))
+
+      insert_raw_log(account.id, %{
+        sequence_number: 2,
+        previous_checksum: first.checksum,
+        checksum: String.duplicate("0", 64),
+        resource_id: "tampered"
+      })
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert report.first_failure.type == :checksum_mismatch
+      assert report.first_failure.sequence_number == 2
+      assert report.first_failure.stored_checksum == String.duplicate("0", 64)
+      refute is_nil(report.first_failure.expected_checksum)
+    end
+
+    test "detects sequence gaps within an account" do
+      account = account_fixture()
+      assert {:ok, _first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "gap-1"}))
+      assert {:ok, second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "gap-2"}))
+      assert {:ok, hmac_key} = Accounts.get_hmac_key(account.id)
+
+      checksum =
+        checksum_for_sequence(
+          account.id,
+          hmac_key,
+          4,
+          second.checksum,
+          %{resource_id: "gap-4"}
+        )
+
+      insert_raw_log(account.id, %{
+        sequence_number: 4,
+        previous_checksum: second.checksum,
+        checksum: checksum,
+        resource_id: "gap-4"
+      })
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert report.first_failure.type == :sequence_gap
+      assert [%{expected: 3, found: 4, missing: [3]}] = report.sequence_gaps
+    end
+
+    test "validates checkpoint anchors and reports invalid anchors" do
+      account = account_fixture()
+      assert {:ok, first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "cp-1"}))
+      assert {:ok, second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "cp-2"}))
+
+      insert_checkpoint(account.id, first.sequence_number, first.checksum)
+      insert_checkpoint(account.id, second.sequence_number, String.duplicate("f", 64))
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == first.sequence_number and &1.valid))
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == second.sequence_number and not &1.valid))
+    end
+
+    test "includes non-negative duration tracking" do
+      account = account_fixture()
+      assert {:ok, _} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "duration"}))
+
+      report = Audit.verify_chain(account.id)
+
+      assert is_integer(report.duration_ms)
+      assert report.duration_ms >= 0
+    end
+
+    test "verification remains account-scoped" do
+      account_a = account_fixture()
+      account_b = account_fixture()
+
+      assert {:ok, _} = Audit.create_log_entry(account_a.id, valid_attrs(%{resource_id: "a-1"}))
+      assert {:ok, first_b} = Audit.create_log_entry(account_b.id, valid_attrs(%{resource_id: "b-1"}))
+
+      insert_raw_log(account_b.id, %{
+        sequence_number: 2,
+        previous_checksum: first_b.checksum,
+        checksum: String.duplicate("a", 64),
+        resource_id: "b-2-tampered"
+      })
+
+      report = Audit.verify_chain(account_a.id)
+
+      assert report.valid == true
+      assert report.total_entries == 1
+      assert report.first_failure == nil
+      assert report.sequence_gaps == []
+    end
+  end
+
+  defp account_fixture(attrs \\ %{}) do
+    defaults = %{name: "Verifier Account #{System.unique_integer([:positive])}"}
+    {:ok, account} = Accounts.create_account(Map.merge(defaults, attrs))
+    account
+  end
+
+  defp valid_attrs(overrides \\ %{}) do
+    defaults = %{
+      user_id: Ecto.UUID.generate(),
+      user_role: "admin",
+      session_id: "session-#{System.unique_integer([:positive])}",
+      action: "read",
+      resource_type: "patient",
+      resource_id: Ecto.UUID.generate(),
+      timestamp: ~U[2026-03-03 16:30:00Z],
+      source_ip: "127.0.0.1",
+      user_agent: "ExUnit",
+      outcome: "success",
+      failure_reason: nil,
+      phi_accessed: false,
+      metadata: %{"source" => "verifier_test"}
+    }
+
+    Map.merge(defaults, overrides)
+  end
+
+  defp checksum_for_sequence(account_id, hmac_key, sequence_number, previous_checksum, overrides \\ %{}) do
+    attrs =
+      valid_attrs(overrides)
+      |> Map.put(:account_id, account_id)
+      |> Map.put(:sequence_number, sequence_number)
+
+    Chain.compute_checksum(hmac_key, attrs, previous_checksum)
+  end
+
+  defp insert_raw_log(account_id, attrs) do
+    now = DateTime.utc_now() |> DateTime.truncate(:microsecond)
+    id = Ecto.UUID.generate()
+
+    row =
+      valid_attrs()
+      |> Map.merge(attrs)
+      |> Map.merge(%{
+        id: id,
+        account_id: account_id,
+        inserted_at: now,
+        updated_at: now
+      })
+
+    {1, _} = Repo.insert_all(Log, [row])
+    Repo.get!(Log, id)
+  end
+
+  defp insert_checkpoint(account_id, sequence_number, checksum) do
+    %Checkpoint{}
+    |> Checkpoint.changeset(%{
+      account_id: account_id,
+      sequence_number: sequence_number,
+      checksum: checksum
+    })
+    |> Repo.insert!()
+  end
+end

exec
/bin/zsh -lc 'git status --short' in /Users/tbrewer/projects/goodway/goodaudit succeeded in 51ms:
 M .autopilot-logs/audit-context/phase-5-commit.log
 M .autopilot-logs/pipeline.log
 M app/lib/app/application.ex
 M app/lib/app/audit.ex
?? .autopilot-logs/chain-verification/
?? app/lib/app/audit/checkpoint_worker.ex
?? app/lib/app/audit/verifier.ex
?? app/test/app/audit/checkpoint_worker_test.exs
?? app/test/app/audit/verifier_test.exs
file update:
diff --git a/app/lib/app/application.ex b/app/lib/app/application.ex
index d865bfe6a48ed688366f7d85220490621cd809a7..08bab073bf607f5bb04aae846094b0b08ec40fe4
--- a/app/lib/app/application.ex
+++ b/app/lib/app/application.ex
@@ -14,6 +14,7 @@
       {Phoenix.PubSub, name: GA.PubSub},
       # Start a worker by calling: GA.Worker.start_link(arg)
       # {GA.Worker, arg},
+      {GA.Audit.CheckpointWorker, checkpoint_worker_opts()},
       # Start to serve requests, typically the last entry
       GAWeb.Endpoint
     ]
@@ -31,4 +32,8 @@
     GAWeb.Endpoint.config_change(changed, removed)
     :ok
   end
+
+  defp checkpoint_worker_opts do
+    Application.get_env(:app, GA.Audit.CheckpointWorker, [])
+  end
 end
diff --git a/app/lib/app/audit.ex b/app/lib/app/audit.ex
index a48830aacf4da8275b40f15016dcd16bbc87411f..2c7fbce41d873e721533728bcc1c965168addd3d
--- a/app/lib/app/audit.ex
+++ b/app/lib/app/audit.ex
@@ -7,7 +7,7 @@
   import Ecto.Query, warn: false
 
   alias GA.Accounts
-  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Audit.{Chain, Checkpoint, Log, Verifier}
   alias GA.Repo
 
   @default_limit 50
@@ -114,6 +114,12 @@
 
   def list_checkpoints(_), do: []
 
+  @doc """
+  Verifies chain integrity for a single account.
+  """
+  def verify_chain(account_id) when is_binary(account_id), do: Verifier.verify(account_id)
+  def verify_chain(_), do: {:error, :invalid_id}
+
   defp lock_account(account_id) do
     {key_a, key_b} = account_lock_keys(account_id)
 
diff --git a/app/lib/app/audit/checkpoint_worker.ex b/app/lib/app/audit/checkpoint_worker.ex
new file mode 100644
index 0000000000000000000000000000000000000000..f54908642ad7c854eae895b9605661f5beb0246f
--- /dev/null
+++ b/app/lib/app/audit/checkpoint_worker.ex
@@ -0,0 +1,82 @@
+defmodule GA.Audit.CheckpointWorker do
+  @moduledoc """
+  Periodically creates audit checkpoints for all active accounts.
+  """
+
+  use GenServer
+
+  import Ecto.Query, warn: false
+
+  require Logger
+
+  alias GA.Accounts.Account
+  alias GA.Audit
+  alias GA.Repo
+
+  @default_interval_ms :timer.hours(1)
+
+  def start_link(opts \\ []) do
+    name = Keyword.get(opts, :name, __MODULE__)
+    GenServer.start_link(__MODULE__, opts, name: name)
+  end
+
+  @impl true
+  def init(opts) do
+    interval_ms =
+      opts
+      |> Keyword.get(:interval_ms, configured_interval_ms())
+      |> normalize_interval()
+
+    schedule_tick(interval_ms)
+
+    {:ok, %{interval_ms: interval_ms}}
+  end
+
+  @impl true
+  def handle_info(:create_checkpoints, state) do
+    active_account_ids()
+    |> Enum.each(&create_checkpoint_for_account/1)
+
+    schedule_tick(state.interval_ms)
+    {:noreply, state}
+  end
+
+  defp active_account_ids do
+    from(account in Account,
+      where: account.status == :active,
+      select: account.id
+    )
+    |> Repo.all()
+  end
+
+  defp create_checkpoint_for_account(account_id) do
+    case Audit.create_checkpoint(account_id) do
+      {:ok, _checkpoint} ->
+        Logger.debug("checkpoint_worker created checkpoint for account=#{account_id}")
+
+      {:error, :no_entries} ->
+        Logger.debug("checkpoint_worker skipping account=#{account_id}: no audit entries")
+
+      {:error, reason} ->
+        Logger.error("checkpoint_worker failed for account=#{account_id}: #{inspect(reason)}")
+    end
+  rescue
+    error ->
+      Logger.error(
+        "checkpoint_worker crashed for account=#{account_id}: #{Exception.message(error)}"
+      )
+  end
+
+  defp schedule_tick(interval_ms) do
+    Process.send_after(self(), :create_checkpoints, interval_ms)
+  end
+
+  defp configured_interval_ms do
+    :app
+    |> Application.get_env(__MODULE__, [])
+    |> Keyword.get(:interval_ms, @default_interval_ms)
+  end
+
+  defp normalize_interval(interval_ms) when is_integer(interval_ms) and interval_ms > 0, do: interval_ms
+  defp normalize_interval(_), do: @default_interval_ms
+end
diff --git a/app/lib/app/audit/verifier.ex b/app/lib/app/audit/verifier.ex
new file mode 100644
index 0000000000000000000000000000000000000000..2a4bd77516a2c76ccc9b05de1778dd8781ded096
--- /dev/null
+++ b/app/lib/app/audit/verifier.ex
@@ -0,0 +1,203 @@
+defmodule GA.Audit.Verifier do
+  @moduledoc """
+  Streams and verifies account-scoped audit chains.
+  """
+
+  import Ecto.Query, warn: false
+
+  alias GA.Accounts
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  @batch_size 1000
+
+  @doc """
+  Verifies an account's audit chain and returns a report map.
+  """
+  @spec verify(binary()) :: map() | {:error, :invalid_id | :not_found}
+  def verify(account_id) when is_binary(account_id) do
+    started_at = System.monotonic_time(:millisecond)
+
+    with {:ok, hmac_key} <- Accounts.get_hmac_key(account_id) do
+      checkpoints_by_sequence = load_checkpoints(account_id)
+      report = stream_and_verify(account_id, hmac_key, checkpoints_by_sequence)
+
+      Map.put(report, :duration_ms, elapsed_ms(started_at))
+    end
+  end
+
+  def verify(_), do: {:error, :invalid_id}
+
+  defp stream_and_verify(account_id, hmac_key, checkpoints_by_sequence) do
+    initial =
+      new_accumulator()
+      |> Map.put(:account_id, account_id)
+
+    account_id
+    |> do_verify_batches(hmac_key, checkpoints_by_sequence, initial)
+    |> finalize_report()
+  end
+
+  defp do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, acc) do
+    batch = load_batch(account_id, acc.last_sequence)
+
+    case batch do
+      [] ->
+        acc
+
+      _entries ->
+        next_acc =
+          Enum.reduce(batch, acc, fn entry, state ->
+            verify_entry(entry, hmac_key, checkpoints_by_sequence, state)
+          end)
+
+        do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, next_acc)
+    end
+  end
+
+  defp verify_entry(%Log{} = entry, hmac_key, checkpoints_by_sequence, acc) do
+    acc
+    |> verify_sequence(entry)
+    |> verify_checksum(entry, hmac_key)
+    |> verify_checkpoint(entry, checkpoints_by_sequence)
+    |> advance_state(entry)
+  end
+
+  defp verify_sequence(acc, %Log{sequence_number: found_sequence}) do
+    expected_sequence = acc.expected_sequence
+
+    if found_sequence == expected_sequence do
+      %{acc | expected_sequence: expected_sequence + 1}
+    else
+      missing =
+        if found_sequence > expected_sequence do
+          Enum.to_list(expected_sequence..(found_sequence - 1))
+        else
+          []
+        end
+
+      gap = %{
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      }
+
+      acc
+      |> mark_invalid(%{
+        type: :sequence_gap,
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      })
+      |> Map.update!(:sequence_gaps, &[gap | &1])
+      |> Map.put(:expected_sequence, max(found_sequence, expected_sequence) + 1)
+    end
+  end
+
+  defp verify_checksum(acc, %Log{} = entry, hmac_key) do
+    if Chain.verify_checksum(hmac_key, entry, acc.previous_checksum) do
+      acc
+    else
+      mark_invalid(acc, %{
+        type: :checksum_mismatch,
+        sequence_number: entry.sequence_number,
+        stored_checksum: entry.checksum,
+        expected_checksum: expected_checksum(hmac_key, entry, acc.previous_checksum)
+      })
+    end
+  end
+
+  defp verify_checkpoint(acc, %Log{} = entry, checkpoints_by_sequence) do
+    case Map.get(checkpoints_by_sequence, entry.sequence_number) do
+      nil ->
+        acc
+
+      %Checkpoint{} = checkpoint ->
+        valid? = checkpoint.checksum == entry.checksum
+        result = %{sequence_number: entry.sequence_number, valid: valid?}
+        next_acc = Map.update!(acc, :checkpoint_results, &[result | &1])
+
+        if valid? do
+          next_acc
+        else
+          mark_invalid(next_acc, %{
+            type: :checkpoint_mismatch,
+            sequence_number: entry.sequence_number,
+            checkpoint_checksum: checkpoint.checksum,
+            entry_checksum: entry.checksum
+          })
+        end
+    end
+  end
+
+  defp advance_state(acc, %Log{} = entry) do
+    %{
+      acc
+      | previous_checksum: entry.checksum,
+        last_sequence: entry.sequence_number,
+        total_entries: acc.total_entries + 1,
+        verified_entries: acc.verified_entries + 1
+    }
+  end
+
+  defp expected_checksum(hmac_key, entry, previous_checksum) do
+    Chain.compute_checksum(hmac_key, Map.from_struct(entry), previous_checksum)
+  rescue
+    _ -> nil
+  end
+
+  defp load_batch(account_id, after_sequence) do
+    from(log in Log,
+      where: log.account_id == ^account_id and log.sequence_number > ^after_sequence,
+      order_by: [asc: log.sequence_number],
+      limit: @batch_size
+    )
+    |> Repo.all()
+  end
+
+  defp load_checkpoints(account_id) do
+    from(checkpoint in Checkpoint, where: checkpoint.account_id == ^account_id)
+    |> Repo.all()
+    |> Map.new(fn checkpoint -> {checkpoint.sequence_number, checkpoint} end)
+  end
+
+  defp new_accumulator do
+    %{
+      account_id: nil,
+      valid: true,
+      total_entries: 0,
+      verified_entries: 0,
+      expected_sequence: 1,
+      previous_checksum: nil,
+      last_sequence: 0,
+      first_failure: nil,
+      sequence_gaps: [],
+      checkpoint_results: []
+    }
+  end
+
+  defp finalize_report(acc) do
+    %{
+      valid: acc.valid,
+      total_entries: acc.total_entries,
+      verified_entries: acc.verified_entries,
+      first_failure: acc.first_failure,
+      sequence_gaps: Enum.reverse(acc.sequence_gaps),
+      checkpoint_results: Enum.reverse(acc.checkpoint_results),
+      duration_ms: 0
+    }
+  end
+
+  defp mark_invalid(acc, failure) do
+    case acc.first_failure do
+      nil -> %{acc | valid: false, first_failure: failure}
+      _existing -> %{acc | valid: false}
+    end
+  end
+
+  defp elapsed_ms(started_at) do
+    System.monotonic_time(:millisecond)
+    |> Kernel.-(started_at)
+    |> max(0)
+  end
+end
diff --git a/app/test/app/audit/checkpoint_worker_test.exs b/app/test/app/audit/checkpoint_worker_test.exs
new file mode 100644
index 0000000000000000000000000000000000000000..ec9ea5e45a2b2d84cd0e7872a7bf7288d8e3ea11
--- /dev/null
+++ b/app/test/app/audit/checkpoint_worker_test.exs
@@ -0,0 +1,111 @@
+defmodule GA.Audit.CheckpointWorkerTest do
+  use GA.DataCase, async: false
+
+  import ExUnit.CaptureLog
+
+  alias GA.Accounts
+  alias GA.Audit
+  alias GA.Audit.CheckpointWorker
+
+  describe "checkpoint worker ticks" do
+    test "creates checkpoints for all active accounts" do
+      account_a = account_fixture()
+      account_b = account_fixture()
+      add_log(account_a.id, "active-a")
+      add_log(account_b.id, "active-b")
+
+      worker = start_worker()
+      send(worker, :create_checkpoints)
+
+      assert_eventually(fn ->
+        length(Audit.list_checkpoints(account_a.id)) == 1 and
+          length(Audit.list_checkpoints(account_b.id)) == 1
+      end)
+    end
+
+    test "skips suspended accounts" do
+      active_account = account_fixture()
+      suspended_account = account_fixture(%{status: :suspended})
+      add_log(active_account.id, "active")
+      add_log(suspended_account.id, "suspended")
+
+      worker = start_worker()
+      send(worker, :create_checkpoints)
+
+      assert_eventually(fn ->
+        length(Audit.list_checkpoints(active_account.id)) == 1
+      end)
+
+      assert Audit.list_checkpoints(suspended_account.id) == []
+    end
+
+    test "logs no-entry accounts and continues processing" do
+      empty_account = account_fixture()
+      populated_account = account_fixture()
+      add_log(populated_account.id, "populated")
+
+      worker = start_worker()
+
+      log =
+        capture_log([level: :debug], fn ->
+          send(worker, :create_checkpoints)
+
+          assert_eventually(fn ->
+            length(Audit.list_checkpoints(populated_account.id)) == 1
+          end)
+        end)
+
+      assert log =~ "checkpoint_worker skipping"
+      assert log =~ empty_account.id
+      assert Audit.list_checkpoints(empty_account.id) == []
+    end
+  end
+
+  defp start_worker do
+    start_supervised!(
+      {CheckpointWorker,
+       [
+         name: {:global, {:checkpoint_worker_test, System.unique_integer([:positive])}},
+         interval_ms: 60_000
+       ]}
+    )
+  end
+
+  defp account_fixture(attrs \\ %{}) do
+    defaults = %{name: "Worker Account #{System.unique_integer([:positive])}"}
+    {:ok, account} = Accounts.create_account(Map.merge(defaults, attrs))
+    account
+  end
+
+  defp add_log(account_id, resource_id) do
+    attrs = %{
+      user_id: Ecto.UUID.generate(),
+      user_role: "admin",
+      session_id: "session-#{System.unique_integer([:positive])}",
+      action: "read",
+      resource_type: "patient",
+      resource_id: resource_id,
+      timestamp: ~U[2026-03-03 16:30:00Z],
+      source_ip: "127.0.0.1",
+      user_agent: "ExUnit",
+      outcome: "success",
+      failure_reason: nil,
+      phi_accessed: false,
+      metadata: %{"source" => "checkpoint_worker_test"}
+    }
+
+    assert {:ok, _} = Audit.create_log_entry(account_id, attrs)
+  end
+
+  defp assert_eventually(fun, attempts \\ 50)
+  defp assert_eventually(fun, 0), do: assert(fun.())
+
+  defp assert_eventually(fun, attempts) do
+    if fun.() do
+      :ok
+    else
+      Process.sleep(20)
+      assert_eventually(fun, attempts - 1)
+    end
+  end
+end
diff --git a/app/test/app/audit/verifier_test.exs b/app/test/app/audit/verifier_test.exs
new file mode 100644
index 0000000000000000000000000000000000000000..430e2450186c77bb9e816cfa26910a774995b717
--- /dev/null
+++ b/app/test/app/audit/verifier_test.exs
@@ -0,0 +1,189 @@
+defmodule GA.Audit.VerifierTest do
+  use GA.DataCase, async: false
+
+  alias GA.Accounts
+  alias GA.Audit
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  describe "verify_chain/1" do
+    test "returns a valid report for an intact account chain" do
+      account = account_fixture()
+
+      assert {:ok, _first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "r-1"}))
+      assert {:ok, _second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "r-2"}))
+      assert {:ok, checkpoint} = Audit.create_checkpoint(account.id)
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == true
+      assert report.total_entries == 2
+      assert report.verified_entries == 2
+      assert report.first_failure == nil
+      assert report.sequence_gaps == []
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == checkpoint.sequence_number and &1.valid))
+      assert is_integer(report.duration_ms)
+      assert report.duration_ms >= 0
+    end
+
+    test "detects tampered checksum entries" do
+      account = account_fixture()
+
+      assert {:ok, first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "seed"}))
+
+      insert_raw_log(account.id, %{
+        sequence_number: 2,
+        previous_checksum: first.checksum,
+        checksum: String.duplicate("0", 64),
+        resource_id: "tampered"
+      })
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert report.first_failure.type == :checksum_mismatch
+      assert report.first_failure.sequence_number == 2
+      assert report.first_failure.stored_checksum == String.duplicate("0", 64)
+      refute is_nil(report.first_failure.expected_checksum)
+    end
+
+    test "detects sequence gaps within an account" do
+      account = account_fixture()
+      assert {:ok, _first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "gap-1"}))
+      assert {:ok, second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "gap-2"}))
+      assert {:ok, hmac_key} = Accounts.get_hmac_key(account.id)
+
+      checksum =
+        checksum_for_sequence(
+          account.id,
+          hmac_key,
+          4,
+          second.checksum,
+          %{resource_id: "gap-4"}
+        )
+
+      insert_raw_log(account.id, %{
+        sequence_number: 4,
+        previous_checksum: second.checksum,
+        checksum: checksum,
+        resource_id: "gap-4"
+      })
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert report.first_failure.type == :sequence_gap
+      assert [%{expected: 3, found: 4, missing: [3]}] = report.sequence_gaps
+    end
+
+    test "validates checkpoint anchors and reports invalid anchors" do
+      account = account_fixture()
+      assert {:ok, first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "cp-1"}))
+      assert {:ok, second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "cp-2"}))
+
+      insert_checkpoint(account.id, first.sequence_number, first.checksum)
+      insert_checkpoint(account.id, second.sequence_number, String.duplicate("f", 64))
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == first.sequence_number and &1.valid))
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == second.sequence_number and not &1.valid))
+    end
+
+    test "includes non-negative duration tracking" do
+      account = account_fixture()
+      assert {:ok, _} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "duration"}))
+
+      report = Audit.verify_chain(account.id)
+
+      assert is_integer(report.duration_ms)
+      assert report.duration_ms >= 0
+    end
+
+    test "verification remains account-scoped" do
+      account_a = account_fixture()
+      account_b = account_fixture()
+
+      assert {:ok, _} = Audit.create_log_entry(account_a.id, valid_attrs(%{resource_id: "a-1"}))
+      assert {:ok, first_b} = Audit.create_log_entry(account_b.id, valid_attrs(%{resource_id: "b-1"}))
+
+      insert_raw_log(account_b.id, %{
+        sequence_number: 2,
+        previous_checksum: first_b.checksum,
+        checksum: String.duplicate("a", 64),
+        resource_id: "b-2-tampered"
+      })
+
+      report = Audit.verify_chain(account_a.id)
+
+      assert report.valid == true
+      assert report.total_entries == 1
+      assert report.first_failure == nil
+      assert report.sequence_gaps == []
+    end
+  end
+
+  defp account_fixture(attrs \\ %{}) do
+    defaults = %{name: "Verifier Account #{System.unique_integer([:positive])}"}
+    {:ok, account} = Accounts.create_account(Map.merge(defaults, attrs))
+    account
+  end
+
+  defp valid_attrs(overrides \\ %{}) do
+    defaults = %{
+      user_id: Ecto.UUID.generate(),
+      user_role: "admin",
+      session_id: "session-#{System.unique_integer([:positive])}",
+      action: "read",
+      resource_type: "patient",
+      resource_id: Ecto.UUID.generate(),
+      timestamp: ~U[2026-03-03 16:30:00Z],
+      source_ip: "127.0.0.1",
+      user_agent: "ExUnit",
+      outcome: "success",
+      failure_reason: nil,
+      phi_accessed: false,
+      metadata: %{"source" => "verifier_test"}
+    }
+
+    Map.merge(defaults, overrides)
+  end
+
+  defp checksum_for_sequence(account_id, hmac_key, sequence_number, previous_checksum, overrides \\ %{}) do
+    attrs =
+      valid_attrs(overrides)
+      |> Map.put(:account_id, account_id)
+      |> Map.put(:sequence_number, sequence_number)
+
+    Chain.compute_checksum(hmac_key, attrs, previous_checksum)
+  end
+
+  defp insert_raw_log(account_id, attrs) do
+    now = DateTime.utc_now() |> DateTime.truncate(:microsecond)
+    id = Ecto.UUID.generate()
+
+    row =
+      valid_attrs()
+      |> Map.merge(attrs)
+      |> Map.merge(%{
+        id: id,
+        account_id: account_id,
+        inserted_at: now,
+        updated_at: now
+      })
+
+    {1, _} = Repo.insert_all(Log, [row])
+    Repo.get!(Log, id)
+  end
+
+  defp insert_checkpoint(account_id, sequence_number, checksum) do
+    %Checkpoint{}
+    |> Checkpoint.changeset(%{
+      account_id: account_id,
+      sequence_number: sequence_number,
+      checksum: checksum
+    })
+    |> Repo.insert!()
+  end
+end

exec
/bin/zsh -lc 'td ws log "Implemented chain-verification core: added GA.Audit.Verifier with batch streaming verification, sequence-gap/checksum/checkpoint validation, duration timing, and GA.Audit.verify_chain/1 delegation; added GA.Audit.CheckpointWorker with scheduled active-account checkpointing and per-account error isolation; added verifier and checkpoint worker test suites covering all OpenSpec scenarios."' in /Users/tbrewer/projects/goodway/goodaudit succeeded in 77ms:
LOGGED ws-e303 → td-1d7eca, td-46c84b, td-8a9954, td-486ab1, td-5806a7, td-7ae373, td-e596e4, td-39f158, td-cb684b, td-bb90d7, td-c5e429, td-166913, td-dbf670, td-c4e66e, td-f96191, td-54c8dc, td-e39b5f, td-ad7419
file update:
diff --git a/app/lib/app/application.ex b/app/lib/app/application.ex
index d865bfe6a48ed688366f7d85220490621cd809a7..08bab073bf607f5bb04aae846094b0b08ec40fe4
--- a/app/lib/app/application.ex
+++ b/app/lib/app/application.ex
@@ -14,6 +14,7 @@
       {Phoenix.PubSub, name: GA.PubSub},
       # Start a worker by calling: GA.Worker.start_link(arg)
       # {GA.Worker, arg},
+      {GA.Audit.CheckpointWorker, checkpoint_worker_opts()},
       # Start to serve requests, typically the last entry
       GAWeb.Endpoint
     ]
@@ -31,4 +32,8 @@
     GAWeb.Endpoint.config_change(changed, removed)
     :ok
   end
+
+  defp checkpoint_worker_opts do
+    Application.get_env(:app, GA.Audit.CheckpointWorker, [])
+  end
 end
diff --git a/app/lib/app/audit.ex b/app/lib/app/audit.ex
index a48830aacf4da8275b40f15016dcd16bbc87411f..2c7fbce41d873e721533728bcc1c965168addd3d
--- a/app/lib/app/audit.ex
+++ b/app/lib/app/audit.ex
@@ -7,7 +7,7 @@
   import Ecto.Query, warn: false
 
   alias GA.Accounts
-  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Audit.{Chain, Checkpoint, Log, Verifier}
   alias GA.Repo
 
   @default_limit 50
@@ -114,6 +114,12 @@
 
   def list_checkpoints(_), do: []
 
+  @doc """
+  Verifies chain integrity for a single account.
+  """
+  def verify_chain(account_id) when is_binary(account_id), do: Verifier.verify(account_id)
+  def verify_chain(_), do: {:error, :invalid_id}
+
   defp lock_account(account_id) do
     {key_a, key_b} = account_lock_keys(account_id)
 
diff --git a/app/lib/app/audit/checkpoint_worker.ex b/app/lib/app/audit/checkpoint_worker.ex
new file mode 100644
index 0000000000000000000000000000000000000000..f54908642ad7c854eae895b9605661f5beb0246f
--- /dev/null
+++ b/app/lib/app/audit/checkpoint_worker.ex
@@ -0,0 +1,82 @@
+defmodule GA.Audit.CheckpointWorker do
+  @moduledoc """
+  Periodically creates audit checkpoints for all active accounts.
+  """
+
+  use GenServer
+
+  import Ecto.Query, warn: false
+
+  require Logger
+
+  alias GA.Accounts.Account
+  alias GA.Audit
+  alias GA.Repo
+
+  @default_interval_ms :timer.hours(1)
+
+  def start_link(opts \\ []) do
+    name = Keyword.get(opts, :name, __MODULE__)
+    GenServer.start_link(__MODULE__, opts, name: name)
+  end
+
+  @impl true
+  def init(opts) do
+    interval_ms =
+      opts
+      |> Keyword.get(:interval_ms, configured_interval_ms())
+      |> normalize_interval()
+
+    schedule_tick(interval_ms)
+
+    {:ok, %{interval_ms: interval_ms}}
+  end
+
+  @impl true
+  def handle_info(:create_checkpoints, state) do
+    active_account_ids()
+    |> Enum.each(&create_checkpoint_for_account/1)
+
+    schedule_tick(state.interval_ms)
+    {:noreply, state}
+  end
+
+  defp active_account_ids do
+    from(account in Account,
+      where: account.status == :active,
+      select: account.id
+    )
+    |> Repo.all()
+  end
+
+  defp create_checkpoint_for_account(account_id) do
+    case Audit.create_checkpoint(account_id) do
+      {:ok, _checkpoint} ->
+        Logger.debug("checkpoint_worker created checkpoint for account=#{account_id}")
+
+      {:error, :no_entries} ->
+        Logger.debug("checkpoint_worker skipping account=#{account_id}: no audit entries")
+
+      {:error, reason} ->
+        Logger.error("checkpoint_worker failed for account=#{account_id}: #{inspect(reason)}")
+    end
+  rescue
+    error ->
+      Logger.error(
+        "checkpoint_worker crashed for account=#{account_id}: #{Exception.message(error)}"
+      )
+  end
+
+  defp schedule_tick(interval_ms) do
+    Process.send_after(self(), :create_checkpoints, interval_ms)
+  end
+
+  defp configured_interval_ms do
+    :app
+    |> Application.get_env(__MODULE__, [])
+    |> Keyword.get(:interval_ms, @default_interval_ms)
+  end
+
+  defp normalize_interval(interval_ms) when is_integer(interval_ms) and interval_ms > 0, do: interval_ms
+  defp normalize_interval(_), do: @default_interval_ms
+end
diff --git a/app/lib/app/audit/verifier.ex b/app/lib/app/audit/verifier.ex
new file mode 100644
index 0000000000000000000000000000000000000000..2a4bd77516a2c76ccc9b05de1778dd8781ded096
--- /dev/null
+++ b/app/lib/app/audit/verifier.ex
@@ -0,0 +1,203 @@
+defmodule GA.Audit.Verifier do
+  @moduledoc """
+  Streams and verifies account-scoped audit chains.
+  """
+
+  import Ecto.Query, warn: false
+
+  alias GA.Accounts
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  @batch_size 1000
+
+  @doc """
+  Verifies an account's audit chain and returns a report map.
+  """
+  @spec verify(binary()) :: map() | {:error, :invalid_id | :not_found}
+  def verify(account_id) when is_binary(account_id) do
+    started_at = System.monotonic_time(:millisecond)
+
+    with {:ok, hmac_key} <- Accounts.get_hmac_key(account_id) do
+      checkpoints_by_sequence = load_checkpoints(account_id)
+      report = stream_and_verify(account_id, hmac_key, checkpoints_by_sequence)
+
+      Map.put(report, :duration_ms, elapsed_ms(started_at))
+    end
+  end
+
+  def verify(_), do: {:error, :invalid_id}
+
+  defp stream_and_verify(account_id, hmac_key, checkpoints_by_sequence) do
+    initial =
+      new_accumulator()
+      |> Map.put(:account_id, account_id)
+
+    account_id
+    |> do_verify_batches(hmac_key, checkpoints_by_sequence, initial)
+    |> finalize_report()
+  end
+
+  defp do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, acc) do
+    batch = load_batch(account_id, acc.last_sequence)
+
+    case batch do
+      [] ->
+        acc
+
+      _entries ->
+        next_acc =
+          Enum.reduce(batch, acc, fn entry, state ->
+            verify_entry(entry, hmac_key, checkpoints_by_sequence, state)
+          end)
+
+        do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, next_acc)
+    end
+  end
+
+  defp verify_entry(%Log{} = entry, hmac_key, checkpoints_by_sequence, acc) do
+    acc
+    |> verify_sequence(entry)
+    |> verify_checksum(entry, hmac_key)
+    |> verify_checkpoint(entry, checkpoints_by_sequence)
+    |> advance_state(entry)
+  end
+
+  defp verify_sequence(acc, %Log{sequence_number: found_sequence}) do
+    expected_sequence = acc.expected_sequence
+
+    if found_sequence == expected_sequence do
+      %{acc | expected_sequence: expected_sequence + 1}
+    else
+      missing =
+        if found_sequence > expected_sequence do
+          Enum.to_list(expected_sequence..(found_sequence - 1))
+        else
+          []
+        end
+
+      gap = %{
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      }
+
+      acc
+      |> mark_invalid(%{
+        type: :sequence_gap,
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      })
+      |> Map.update!(:sequence_gaps, &[gap | &1])
+      |> Map.put(:expected_sequence, max(found_sequence, expected_sequence) + 1)
+    end
+  end
+
+  defp verify_checksum(acc, %Log{} = entry, hmac_key) do
+    if Chain.verify_checksum(hmac_key, entry, acc.previous_checksum) do
+      acc
+    else
+      mark_invalid(acc, %{
+        type: :checksum_mismatch,
+        sequence_number: entry.sequence_number,
+        stored_checksum: entry.checksum,
+        expected_checksum: expected_checksum(hmac_key, entry, acc.previous_checksum)
+      })
+    end
+  end
+
+  defp verify_checkpoint(acc, %Log{} = entry, checkpoints_by_sequence) do
+    case Map.get(checkpoints_by_sequence, entry.sequence_number) do
+      nil ->
+        acc
+
+      %Checkpoint{} = checkpoint ->
+        valid? = checkpoint.checksum == entry.checksum
+        result = %{sequence_number: entry.sequence_number, valid: valid?}
+        next_acc = Map.update!(acc, :checkpoint_results, &[result | &1])
+
+        if valid? do
+          next_acc
+        else
+          mark_invalid(next_acc, %{
+            type: :checkpoint_mismatch,
+            sequence_number: entry.sequence_number,
+            checkpoint_checksum: checkpoint.checksum,
+            entry_checksum: entry.checksum
+          })
+        end
+    end
+  end
+
+  defp advance_state(acc, %Log{} = entry) do
+    %{
+      acc
+      | previous_checksum: entry.checksum,
+        last_sequence: entry.sequence_number,
+        total_entries: acc.total_entries + 1,
+        verified_entries: acc.verified_entries + 1
+    }
+  end
+
+  defp expected_checksum(hmac_key, entry, previous_checksum) do
+    Chain.compute_checksum(hmac_key, Map.from_struct(entry), previous_checksum)
+  rescue
+    _ -> nil
+  end
+
+  defp load_batch(account_id, after_sequence) do
+    from(log in Log,
+      where: log.account_id == ^account_id and log.sequence_number > ^after_sequence,
+      order_by: [asc: log.sequence_number],
+      limit: @batch_size
+    )
+    |> Repo.all()
+  end
+
+  defp load_checkpoints(account_id) do
+    from(checkpoint in Checkpoint, where: checkpoint.account_id == ^account_id)
+    |> Repo.all()
+    |> Map.new(fn checkpoint -> {checkpoint.sequence_number, checkpoint} end)
+  end
+
+  defp new_accumulator do
+    %{
+      account_id: nil,
+      valid: true,
+      total_entries: 0,
+      verified_entries: 0,
+      expected_sequence: 1,
+      previous_checksum: nil,
+      last_sequence: 0,
+      first_failure: nil,
+      sequence_gaps: [],
+      checkpoint_results: []
+    }
+  end
+
+  defp finalize_report(acc) do
+    %{
+      valid: acc.valid,
+      total_entries: acc.total_entries,
+      verified_entries: acc.verified_entries,
+      first_failure: acc.first_failure,
+      sequence_gaps: Enum.reverse(acc.sequence_gaps),
+      checkpoint_results: Enum.reverse(acc.checkpoint_results),
+      duration_ms: 0
+    }
+  end
+
+  defp mark_invalid(acc, failure) do
+    case acc.first_failure do
+      nil -> %{acc | valid: false, first_failure: failure}
+      _existing -> %{acc | valid: false}
+    end
+  end
+
+  defp elapsed_ms(started_at) do
+    System.monotonic_time(:millisecond)
+    |> Kernel.-(started_at)
+    |> max(0)
+  end
+end
diff --git a/app/test/app/audit/checkpoint_worker_test.exs b/app/test/app/audit/checkpoint_worker_test.exs
new file mode 100644
index 0000000000000000000000000000000000000000..ec9ea5e45a2b2d84cd0e7872a7bf7288d8e3ea11
--- /dev/null
+++ b/app/test/app/audit/checkpoint_worker_test.exs
@@ -0,0 +1,111 @@
+defmodule GA.Audit.CheckpointWorkerTest do
+  use GA.DataCase, async: false
+
+  import ExUnit.CaptureLog
+
+  alias GA.Accounts
+  alias GA.Audit
+  alias GA.Audit.CheckpointWorker
+
+  describe "checkpoint worker ticks" do
+    test "creates checkpoints for all active accounts" do
+      account_a = account_fixture()
+      account_b = account_fixture()
+      add_log(account_a.id, "active-a")
+      add_log(account_b.id, "active-b")
+
+      worker = start_worker()
+      send(worker, :create_checkpoints)
+
+      assert_eventually(fn ->
+        length(Audit.list_checkpoints(account_a.id)) == 1 and
+          length(Audit.list_checkpoints(account_b.id)) == 1
+      end)
+    end
+
+    test "skips suspended accounts" do
+      active_account = account_fixture()
+      suspended_account = account_fixture(%{status: :suspended})
+      add_log(active_account.id, "active")
+      add_log(suspended_account.id, "suspended")
+
+      worker = start_worker()
+      send(worker, :create_checkpoints)
+
+      assert_eventually(fn ->
+        length(Audit.list_checkpoints(active_account.id)) == 1
+      end)
+
+      assert Audit.list_checkpoints(suspended_account.id) == []
+    end
+
+    test "logs no-entry accounts and continues processing" do
+      empty_account = account_fixture()
+      populated_account = account_fixture()
+      add_log(populated_account.id, "populated")
+
+      worker = start_worker()
+
+      log =
+        capture_log([level: :debug], fn ->
+          send(worker, :create_checkpoints)
+
+          assert_eventually(fn ->
+            length(Audit.list_checkpoints(populated_account.id)) == 1
+          end)
+        end)
+
+      assert log =~ "checkpoint_worker skipping"
+      assert log =~ empty_account.id
+      assert Audit.list_checkpoints(empty_account.id) == []
+    end
+  end
+
+  defp start_worker do
+    start_supervised!(
+      {CheckpointWorker,
+       [
+         name: {:global, {:checkpoint_worker_test, System.unique_integer([:positive])}},
+         interval_ms: 60_000
+       ]}
+    )
+  end
+
+  defp account_fixture(attrs \\ %{}) do
+    defaults = %{name: "Worker Account #{System.unique_integer([:positive])}"}
+    {:ok, account} = Accounts.create_account(Map.merge(defaults, attrs))
+    account
+  end
+
+  defp add_log(account_id, resource_id) do
+    attrs = %{
+      user_id: Ecto.UUID.generate(),
+      user_role: "admin",
+      session_id: "session-#{System.unique_integer([:positive])}",
+      action: "read",
+      resource_type: "patient",
+      resource_id: resource_id,
+      timestamp: ~U[2026-03-03 16:30:00Z],
+      source_ip: "127.0.0.1",
+      user_agent: "ExUnit",
+      outcome: "success",
+      failure_reason: nil,
+      phi_accessed: false,
+      metadata: %{"source" => "checkpoint_worker_test"}
+    }
+
+    assert {:ok, _} = Audit.create_log_entry(account_id, attrs)
+  end
+
+  defp assert_eventually(fun, attempts \\ 50)
+  defp assert_eventually(fun, 0), do: assert(fun.())
+
+  defp assert_eventually(fun, attempts) do
+    if fun.() do
+      :ok
+    else
+      Process.sleep(20)
+      assert_eventually(fun, attempts - 1)
+    end
+  end
+end
diff --git a/app/test/app/audit/verifier_test.exs b/app/test/app/audit/verifier_test.exs
new file mode 100644
index 0000000000000000000000000000000000000000..430e2450186c77bb9e816cfa26910a774995b717
--- /dev/null
+++ b/app/test/app/audit/verifier_test.exs
@@ -0,0 +1,189 @@
+defmodule GA.Audit.VerifierTest do
+  use GA.DataCase, async: false
+
+  alias GA.Accounts
+  alias GA.Audit
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  describe "verify_chain/1" do
+    test "returns a valid report for an intact account chain" do
+      account = account_fixture()
+
+      assert {:ok, _first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "r-1"}))
+      assert {:ok, _second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "r-2"}))
+      assert {:ok, checkpoint} = Audit.create_checkpoint(account.id)
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == true
+      assert report.total_entries == 2
+      assert report.verified_entries == 2
+      assert report.first_failure == nil
+      assert report.sequence_gaps == []
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == checkpoint.sequence_number and &1.valid))
+      assert is_integer(report.duration_ms)
+      assert report.duration_ms >= 0
+    end
+
+    test "detects tampered checksum entries" do
+      account = account_fixture()
+
+      assert {:ok, first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "seed"}))
+
+      insert_raw_log(account.id, %{
+        sequence_number: 2,
+        previous_checksum: first.checksum,
+        checksum: String.duplicate("0", 64),
+        resource_id: "tampered"
+      })
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert report.first_failure.type == :checksum_mismatch
+      assert report.first_failure.sequence_number == 2
+      assert report.first_failure.stored_checksum == String.duplicate("0", 64)
+      refute is_nil(report.first_failure.expected_checksum)
+    end
+
+    test "detects sequence gaps within an account" do
+      account = account_fixture()
+      assert {:ok, _first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "gap-1"}))
+      assert {:ok, second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "gap-2"}))
+      assert {:ok, hmac_key} = Accounts.get_hmac_key(account.id)
+
+      checksum =
+        checksum_for_sequence(
+          account.id,
+          hmac_key,
+          4,
+          second.checksum,
+          %{resource_id: "gap-4"}
+        )
+
+      insert_raw_log(account.id, %{
+        sequence_number: 4,
+        previous_checksum: second.checksum,
+        checksum: checksum,
+        resource_id: "gap-4"
+      })
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert report.first_failure.type == :sequence_gap
+      assert [%{expected: 3, found: 4, missing: [3]}] = report.sequence_gaps
+    end
+
+    test "validates checkpoint anchors and reports invalid anchors" do
+      account = account_fixture()
+      assert {:ok, first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "cp-1"}))
+      assert {:ok, second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "cp-2"}))
+
+      insert_checkpoint(account.id, first.sequence_number, first.checksum)
+      insert_checkpoint(account.id, second.sequence_number, String.duplicate("f", 64))
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == first.sequence_number and &1.valid))
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == second.sequence_number and not &1.valid))
+    end
+
+    test "includes non-negative duration tracking" do
+      account = account_fixture()
+      assert {:ok, _} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "duration"}))
+
+      report = Audit.verify_chain(account.id)
+
+      assert is_integer(report.duration_ms)
+      assert report.duration_ms >= 0
+    end
+
+    test "verification remains account-scoped" do
+      account_a = account_fixture()
+      account_b = account_fixture()
+
+      assert {:ok, _} = Audit.create_log_entry(account_a.id, valid_attrs(%{resource_id: "a-1"}))
+      assert {:ok, first_b} = Audit.create_log_entry(account_b.id, valid_attrs(%{resource_id: "b-1"}))
+
+      insert_raw_log(account_b.id, %{
+        sequence_number: 2,
+        previous_checksum: first_b.checksum,
+        checksum: String.duplicate("a", 64),
+        resource_id: "b-2-tampered"
+      })
+
+      report = Audit.verify_chain(account_a.id)
+
+      assert report.valid == true
+      assert report.total_entries == 1
+      assert report.first_failure == nil
+      assert report.sequence_gaps == []
+    end
+  end
+
+  defp account_fixture(attrs \\ %{}) do
+    defaults = %{name: "Verifier Account #{System.unique_integer([:positive])}"}
+    {:ok, account} = Accounts.create_account(Map.merge(defaults, attrs))
+    account
+  end
+
+  defp valid_attrs(overrides \\ %{}) do
+    defaults = %{
+      user_id: Ecto.UUID.generate(),
+      user_role: "admin",
+      session_id: "session-#{System.unique_integer([:positive])}",
+      action: "read",
+      resource_type: "patient",
+      resource_id: Ecto.UUID.generate(),
+      timestamp: ~U[2026-03-03 16:30:00Z],
+      source_ip: "127.0.0.1",
+      user_agent: "ExUnit",
+      outcome: "success",
+      failure_reason: nil,
+      phi_accessed: false,
+      metadata: %{"source" => "verifier_test"}
+    }
+
+    Map.merge(defaults, overrides)
+  end
+
+  defp checksum_for_sequence(account_id, hmac_key, sequence_number, previous_checksum, overrides \\ %{}) do
+    attrs =
+      valid_attrs(overrides)
+      |> Map.put(:account_id, account_id)
+      |> Map.put(:sequence_number, sequence_number)
+
+    Chain.compute_checksum(hmac_key, attrs, previous_checksum)
+  end
+
+  defp insert_raw_log(account_id, attrs) do
+    now = DateTime.utc_now() |> DateTime.truncate(:microsecond)
+    id = Ecto.UUID.generate()
+
+    row =
+      valid_attrs()
+      |> Map.merge(attrs)
+      |> Map.merge(%{
+        id: id,
+        account_id: account_id,
+        inserted_at: now,
+        updated_at: now
+      })
+
+    {1, _} = Repo.insert_all(Log, [row])
+    Repo.get!(Log, id)
+  end
+
+  defp insert_checkpoint(account_id, sequence_number, checksum) do
+    %Checkpoint{}
+    |> Checkpoint.changeset(%{
+      account_id: account_id,
+      sequence_number: sequence_number,
+      checksum: checksum
+    })
+    |> Repo.insert!()
+  end
+end

exec
/bin/zsh -lc 'td ws log --help' in /Users/tbrewer/projects/goodway/goodaudit succeeded in 51ms:
Log entry is attached to the session AND all tagged issues.

Usage:
  td ws log "message" [flags]

Flags:
      --blocker       Mark as blocker
      --decision      Mark as decision
  -h, --help          help for log
      --hypothesis    Mark as hypothesis
      --only string   Log to specific issue only
      --result        Mark as result
      --tried         Mark as attempted approach

Global Flags:
  -w, --work-dir string   project directory (resolves .td-root and git worktrees from this path)
file update:
diff --git a/app/lib/app/application.ex b/app/lib/app/application.ex
index d865bfe6a48ed688366f7d85220490621cd809a7..08bab073bf607f5bb04aae846094b0b08ec40fe4
--- a/app/lib/app/application.ex
+++ b/app/lib/app/application.ex
@@ -14,6 +14,7 @@
       {Phoenix.PubSub, name: GA.PubSub},
       # Start a worker by calling: GA.Worker.start_link(arg)
       # {GA.Worker, arg},
+      {GA.Audit.CheckpointWorker, checkpoint_worker_opts()},
       # Start to serve requests, typically the last entry
       GAWeb.Endpoint
     ]
@@ -31,4 +32,8 @@
     GAWeb.Endpoint.config_change(changed, removed)
     :ok
   end
+
+  defp checkpoint_worker_opts do
+    Application.get_env(:app, GA.Audit.CheckpointWorker, [])
+  end
 end
diff --git a/app/lib/app/audit.ex b/app/lib/app/audit.ex
index a48830aacf4da8275b40f15016dcd16bbc87411f..2c7fbce41d873e721533728bcc1c965168addd3d
--- a/app/lib/app/audit.ex
+++ b/app/lib/app/audit.ex
@@ -7,7 +7,7 @@
   import Ecto.Query, warn: false
 
   alias GA.Accounts
-  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Audit.{Chain, Checkpoint, Log, Verifier}
   alias GA.Repo
 
   @default_limit 50
@@ -114,6 +114,12 @@
 
   def list_checkpoints(_), do: []
 
+  @doc """
+  Verifies chain integrity for a single account.
+  """
+  def verify_chain(account_id) when is_binary(account_id), do: Verifier.verify(account_id)
+  def verify_chain(_), do: {:error, :invalid_id}
+
   defp lock_account(account_id) do
     {key_a, key_b} = account_lock_keys(account_id)
 
diff --git a/app/lib/app/audit/checkpoint_worker.ex b/app/lib/app/audit/checkpoint_worker.ex
new file mode 100644
index 0000000000000000000000000000000000000000..f54908642ad7c854eae895b9605661f5beb0246f
--- /dev/null
+++ b/app/lib/app/audit/checkpoint_worker.ex
@@ -0,0 +1,82 @@
+defmodule GA.Audit.CheckpointWorker do
+  @moduledoc """
+  Periodically creates audit checkpoints for all active accounts.
+  """
+
+  use GenServer
+
+  import Ecto.Query, warn: false
+
+  require Logger
+
+  alias GA.Accounts.Account
+  alias GA.Audit
+  alias GA.Repo
+
+  @default_interval_ms :timer.hours(1)
+
+  def start_link(opts \\ []) do
+    name = Keyword.get(opts, :name, __MODULE__)
+    GenServer.start_link(__MODULE__, opts, name: name)
+  end
+
+  @impl true
+  def init(opts) do
+    interval_ms =
+      opts
+      |> Keyword.get(:interval_ms, configured_interval_ms())
+      |> normalize_interval()
+
+    schedule_tick(interval_ms)
+
+    {:ok, %{interval_ms: interval_ms}}
+  end
+
+  @impl true
+  def handle_info(:create_checkpoints, state) do
+    active_account_ids()
+    |> Enum.each(&create_checkpoint_for_account/1)
+
+    schedule_tick(state.interval_ms)
+    {:noreply, state}
+  end
+
+  defp active_account_ids do
+    from(account in Account,
+      where: account.status == :active,
+      select: account.id
+    )
+    |> Repo.all()
+  end
+
+  defp create_checkpoint_for_account(account_id) do
+    case Audit.create_checkpoint(account_id) do
+      {:ok, _checkpoint} ->
+        Logger.debug("checkpoint_worker created checkpoint for account=#{account_id}")
+
+      {:error, :no_entries} ->
+        Logger.debug("checkpoint_worker skipping account=#{account_id}: no audit entries")
+
+      {:error, reason} ->
+        Logger.error("checkpoint_worker failed for account=#{account_id}: #{inspect(reason)}")
+    end
+  rescue
+    error ->
+      Logger.error(
+        "checkpoint_worker crashed for account=#{account_id}: #{Exception.message(error)}"
+      )
+  end
+
+  defp schedule_tick(interval_ms) do
+    Process.send_after(self(), :create_checkpoints, interval_ms)
+  end
+
+  defp configured_interval_ms do
+    :app
+    |> Application.get_env(__MODULE__, [])
+    |> Keyword.get(:interval_ms, @default_interval_ms)
+  end
+
+  defp normalize_interval(interval_ms) when is_integer(interval_ms) and interval_ms > 0, do: interval_ms
+  defp normalize_interval(_), do: @default_interval_ms
+end
diff --git a/app/lib/app/audit/verifier.ex b/app/lib/app/audit/verifier.ex
new file mode 100644
index 0000000000000000000000000000000000000000..2a4bd77516a2c76ccc9b05de1778dd8781ded096
--- /dev/null
+++ b/app/lib/app/audit/verifier.ex
@@ -0,0 +1,203 @@
+defmodule GA.Audit.Verifier do
+  @moduledoc """
+  Streams and verifies account-scoped audit chains.
+  """
+
+  import Ecto.Query, warn: false
+
+  alias GA.Accounts
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  @batch_size 1000
+
+  @doc """
+  Verifies an account's audit chain and returns a report map.
+  """
+  @spec verify(binary()) :: map() | {:error, :invalid_id | :not_found}
+  def verify(account_id) when is_binary(account_id) do
+    started_at = System.monotonic_time(:millisecond)
+
+    with {:ok, hmac_key} <- Accounts.get_hmac_key(account_id) do
+      checkpoints_by_sequence = load_checkpoints(account_id)
+      report = stream_and_verify(account_id, hmac_key, checkpoints_by_sequence)
+
+      Map.put(report, :duration_ms, elapsed_ms(started_at))
+    end
+  end
+
+  def verify(_), do: {:error, :invalid_id}
+
+  defp stream_and_verify(account_id, hmac_key, checkpoints_by_sequence) do
+    initial =
+      new_accumulator()
+      |> Map.put(:account_id, account_id)
+
+    account_id
+    |> do_verify_batches(hmac_key, checkpoints_by_sequence, initial)
+    |> finalize_report()
+  end
+
+  defp do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, acc) do
+    batch = load_batch(account_id, acc.last_sequence)
+
+    case batch do
+      [] ->
+        acc
+
+      _entries ->
+        next_acc =
+          Enum.reduce(batch, acc, fn entry, state ->
+            verify_entry(entry, hmac_key, checkpoints_by_sequence, state)
+          end)
+
+        do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, next_acc)
+    end
+  end
+
+  defp verify_entry(%Log{} = entry, hmac_key, checkpoints_by_sequence, acc) do
+    acc
+    |> verify_sequence(entry)
+    |> verify_checksum(entry, hmac_key)
+    |> verify_checkpoint(entry, checkpoints_by_sequence)
+    |> advance_state(entry)
+  end
+
+  defp verify_sequence(acc, %Log{sequence_number: found_sequence}) do
+    expected_sequence = acc.expected_sequence
+
+    if found_sequence == expected_sequence do
+      %{acc | expected_sequence: expected_sequence + 1}
+    else
+      missing =
+        if found_sequence > expected_sequence do
+          Enum.to_list(expected_sequence..(found_sequence - 1))
+        else
+          []
+        end
+
+      gap = %{
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      }
+
+      acc
+      |> mark_invalid(%{
+        type: :sequence_gap,
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      })
+      |> Map.update!(:sequence_gaps, &[gap | &1])
+      |> Map.put(:expected_sequence, max(found_sequence, expected_sequence) + 1)
+    end
+  end
+
+  defp verify_checksum(acc, %Log{} = entry, hmac_key) do
+    if Chain.verify_checksum(hmac_key, entry, acc.previous_checksum) do
+      acc
+    else
+      mark_invalid(acc, %{
+        type: :checksum_mismatch,
+        sequence_number: entry.sequence_number,
+        stored_checksum: entry.checksum,
+        expected_checksum: expected_checksum(hmac_key, entry, acc.previous_checksum)
+      })
+    end
+  end
+
+  defp verify_checkpoint(acc, %Log{} = entry, checkpoints_by_sequence) do
+    case Map.get(checkpoints_by_sequence, entry.sequence_number) do
+      nil ->
+        acc
+
+      %Checkpoint{} = checkpoint ->
+        valid? = checkpoint.checksum == entry.checksum
+        result = %{sequence_number: entry.sequence_number, valid: valid?}
+        next_acc = Map.update!(acc, :checkpoint_results, &[result | &1])
+
+        if valid? do
+          next_acc
+        else
+          mark_invalid(next_acc, %{
+            type: :checkpoint_mismatch,
+            sequence_number: entry.sequence_number,
+            checkpoint_checksum: checkpoint.checksum,
+            entry_checksum: entry.checksum
+          })
+        end
+    end
+  end
+
+  defp advance_state(acc, %Log{} = entry) do
+    %{
+      acc
+      | previous_checksum: entry.checksum,
+        last_sequence: entry.sequence_number,
+        total_entries: acc.total_entries + 1,
+        verified_entries: acc.verified_entries + 1
+    }
+  end
+
+  defp expected_checksum(hmac_key, entry, previous_checksum) do
+    Chain.compute_checksum(hmac_key, Map.from_struct(entry), previous_checksum)
+  rescue
+    _ -> nil
+  end
+
+  defp load_batch(account_id, after_sequence) do
+    from(log in Log,
+      where: log.account_id == ^account_id and log.sequence_number > ^after_sequence,
+      order_by: [asc: log.sequence_number],
+      limit: @batch_size
+    )
+    |> Repo.all()
+  end
+
+  defp load_checkpoints(account_id) do
+    from(checkpoint in Checkpoint, where: checkpoint.account_id == ^account_id)
+    |> Repo.all()
+    |> Map.new(fn checkpoint -> {checkpoint.sequence_number, checkpoint} end)
+  end
+
+  defp new_accumulator do
+    %{
+      account_id: nil,
+      valid: true,
+      total_entries: 0,
+      verified_entries: 0,
+      expected_sequence: 1,
+      previous_checksum: nil,
+      last_sequence: 0,
+      first_failure: nil,
+      sequence_gaps: [],
+      checkpoint_results: []
+    }
+  end
+
+  defp finalize_report(acc) do
+    %{
+      valid: acc.valid,
+      total_entries: acc.total_entries,
+      verified_entries: acc.verified_entries,
+      first_failure: acc.first_failure,
+      sequence_gaps: Enum.reverse(acc.sequence_gaps),
+      checkpoint_results: Enum.reverse(acc.checkpoint_results),
+      duration_ms: 0
+    }
+  end
+
+  defp mark_invalid(acc, failure) do
+    case acc.first_failure do
+      nil -> %{acc | valid: false, first_failure: failure}
+      _existing -> %{acc | valid: false}
+    end
+  end
+
+  defp elapsed_ms(started_at) do
+    System.monotonic_time(:millisecond)
+    |> Kernel.-(started_at)
+    |> max(0)
+  end
+end
diff --git a/app/test/app/audit/checkpoint_worker_test.exs b/app/test/app/audit/checkpoint_worker_test.exs
new file mode 100644
index 0000000000000000000000000000000000000000..ec9ea5e45a2b2d84cd0e7872a7bf7288d8e3ea11
--- /dev/null
+++ b/app/test/app/audit/checkpoint_worker_test.exs
@@ -0,0 +1,111 @@
+defmodule GA.Audit.CheckpointWorkerTest do
+  use GA.DataCase, async: false
+
+  import ExUnit.CaptureLog
+
+  alias GA.Accounts
+  alias GA.Audit
+  alias GA.Audit.CheckpointWorker
+
+  describe "checkpoint worker ticks" do
+    test "creates checkpoints for all active accounts" do
+      account_a = account_fixture()
+      account_b = account_fixture()
+      add_log(account_a.id, "active-a")
+      add_log(account_b.id, "active-b")
+
+      worker = start_worker()
+      send(worker, :create_checkpoints)
+
+      assert_eventually(fn ->
+        length(Audit.list_checkpoints(account_a.id)) == 1 and
+          length(Audit.list_checkpoints(account_b.id)) == 1
+      end)
+    end
+
+    test "skips suspended accounts" do
+      active_account = account_fixture()
+      suspended_account = account_fixture(%{status: :suspended})
+      add_log(active_account.id, "active")
+      add_log(suspended_account.id, "suspended")
+
+      worker = start_worker()
+      send(worker, :create_checkpoints)
+
+      assert_eventually(fn ->
+        length(Audit.list_checkpoints(active_account.id)) == 1
+      end)
+
+      assert Audit.list_checkpoints(suspended_account.id) == []
+    end
+
+    test "logs no-entry accounts and continues processing" do
+      empty_account = account_fixture()
+      populated_account = account_fixture()
+      add_log(populated_account.id, "populated")
+
+      worker = start_worker()
+
+      log =
+        capture_log([level: :debug], fn ->
+          send(worker, :create_checkpoints)
+
+          assert_eventually(fn ->
+            length(Audit.list_checkpoints(populated_account.id)) == 1
+          end)
+        end)
+
+      assert log =~ "checkpoint_worker skipping"
+      assert log =~ empty_account.id
+      assert Audit.list_checkpoints(empty_account.id) == []
+    end
+  end
+
+  defp start_worker do
+    start_supervised!(
+      {CheckpointWorker,
+       [
+         name: {:global, {:checkpoint_worker_test, System.unique_integer([:positive])}},
+         interval_ms: 60_000
+       ]}
+    )
+  end
+
+  defp account_fixture(attrs \\ %{}) do
+    defaults = %{name: "Worker Account #{System.unique_integer([:positive])}"}
+    {:ok, account} = Accounts.create_account(Map.merge(defaults, attrs))
+    account
+  end
+
+  defp add_log(account_id, resource_id) do
+    attrs = %{
+      user_id: Ecto.UUID.generate(),
+      user_role: "admin",
+      session_id: "session-#{System.unique_integer([:positive])}",
+      action: "read",
+      resource_type: "patient",
+      resource_id: resource_id,
+      timestamp: ~U[2026-03-03 16:30:00Z],
+      source_ip: "127.0.0.1",
+      user_agent: "ExUnit",
+      outcome: "success",
+      failure_reason: nil,
+      phi_accessed: false,
+      metadata: %{"source" => "checkpoint_worker_test"}
+    }
+
+    assert {:ok, _} = Audit.create_log_entry(account_id, attrs)
+  end
+
+  defp assert_eventually(fun, attempts \\ 50)
+  defp assert_eventually(fun, 0), do: assert(fun.())
+
+  defp assert_eventually(fun, attempts) do
+    if fun.() do
+      :ok
+    else
+      Process.sleep(20)
+      assert_eventually(fun, attempts - 1)
+    end
+  end
+end
diff --git a/app/test/app/audit/verifier_test.exs b/app/test/app/audit/verifier_test.exs
new file mode 100644
index 0000000000000000000000000000000000000000..430e2450186c77bb9e816cfa26910a774995b717
--- /dev/null
+++ b/app/test/app/audit/verifier_test.exs
@@ -0,0 +1,189 @@
+defmodule GA.Audit.VerifierTest do
+  use GA.DataCase, async: false
+
+  alias GA.Accounts
+  alias GA.Audit
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  describe "verify_chain/1" do
+    test "returns a valid report for an intact account chain" do
+      account = account_fixture()
+
+      assert {:ok, _first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "r-1"}))
+      assert {:ok, _second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "r-2"}))
+      assert {:ok, checkpoint} = Audit.create_checkpoint(account.id)
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == true
+      assert report.total_entries == 2
+      assert report.verified_entries == 2
+      assert report.first_failure == nil
+      assert report.sequence_gaps == []
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == checkpoint.sequence_number and &1.valid))
+      assert is_integer(report.duration_ms)
+      assert report.duration_ms >= 0
+    end
+
+    test "detects tampered checksum entries" do
+      account = account_fixture()
+
+      assert {:ok, first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "seed"}))
+
+      insert_raw_log(account.id, %{
+        sequence_number: 2,
+        previous_checksum: first.checksum,
+        checksum: String.duplicate("0", 64),
+        resource_id: "tampered"
+      })
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert report.first_failure.type == :checksum_mismatch
+      assert report.first_failure.sequence_number == 2
+      assert report.first_failure.stored_checksum == String.duplicate("0", 64)
+      refute is_nil(report.first_failure.expected_checksum)
+    end
+
+    test "detects sequence gaps within an account" do
+      account = account_fixture()
+      assert {:ok, _first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "gap-1"}))
+      assert {:ok, second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "gap-2"}))
+      assert {:ok, hmac_key} = Accounts.get_hmac_key(account.id)
+
+      checksum =
+        checksum_for_sequence(
+          account.id,
+          hmac_key,
+          4,
+          second.checksum,
+          %{resource_id: "gap-4"}
+        )
+
+      insert_raw_log(account.id, %{
+        sequence_number: 4,
+        previous_checksum: second.checksum,
+        checksum: checksum,
+        resource_id: "gap-4"
+      })
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert report.first_failure.type == :sequence_gap
+      assert [%{expected: 3, found: 4, missing: [3]}] = report.sequence_gaps
+    end
+
+    test "validates checkpoint anchors and reports invalid anchors" do
+      account = account_fixture()
+      assert {:ok, first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "cp-1"}))
+      assert {:ok, second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "cp-2"}))
+
+      insert_checkpoint(account.id, first.sequence_number, first.checksum)
+      insert_checkpoint(account.id, second.sequence_number, String.duplicate("f", 64))
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == first.sequence_number and &1.valid))
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == second.sequence_number and not &1.valid))
+    end
+
+    test "includes non-negative duration tracking" do
+      account = account_fixture()
+      assert {:ok, _} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "duration"}))
+
+      report = Audit.verify_chain(account.id)
+
+      assert is_integer(report.duration_ms)
+      assert report.duration_ms >= 0
+    end
+
+    test "verification remains account-scoped" do
+      account_a = account_fixture()
+      account_b = account_fixture()
+
+      assert {:ok, _} = Audit.create_log_entry(account_a.id, valid_attrs(%{resource_id: "a-1"}))
+      assert {:ok, first_b} = Audit.create_log_entry(account_b.id, valid_attrs(%{resource_id: "b-1"}))
+
+      insert_raw_log(account_b.id, %{
+        sequence_number: 2,
+        previous_checksum: first_b.checksum,
+        checksum: String.duplicate("a", 64),
+        resource_id: "b-2-tampered"
+      })
+
+      report = Audit.verify_chain(account_a.id)
+
+      assert report.valid == true
+      assert report.total_entries == 1
+      assert report.first_failure == nil
+      assert report.sequence_gaps == []
+    end
+  end
+
+  defp account_fixture(attrs \\ %{}) do
+    defaults = %{name: "Verifier Account #{System.unique_integer([:positive])}"}
+    {:ok, account} = Accounts.create_account(Map.merge(defaults, attrs))
+    account
+  end
+
+  defp valid_attrs(overrides \\ %{}) do
+    defaults = %{
+      user_id: Ecto.UUID.generate(),
+      user_role: "admin",
+      session_id: "session-#{System.unique_integer([:positive])}",
+      action: "read",
+      resource_type: "patient",
+      resource_id: Ecto.UUID.generate(),
+      timestamp: ~U[2026-03-03 16:30:00Z],
+      source_ip: "127.0.0.1",
+      user_agent: "ExUnit",
+      outcome: "success",
+      failure_reason: nil,
+      phi_accessed: false,
+      metadata: %{"source" => "verifier_test"}
+    }
+
+    Map.merge(defaults, overrides)
+  end
+
+  defp checksum_for_sequence(account_id, hmac_key, sequence_number, previous_checksum, overrides \\ %{}) do
+    attrs =
+      valid_attrs(overrides)
+      |> Map.put(:account_id, account_id)
+      |> Map.put(:sequence_number, sequence_number)
+
+    Chain.compute_checksum(hmac_key, attrs, previous_checksum)
+  end
+
+  defp insert_raw_log(account_id, attrs) do
+    now = DateTime.utc_now() |> DateTime.truncate(:microsecond)
+    id = Ecto.UUID.generate()
+
+    row =
+      valid_attrs()
+      |> Map.merge(attrs)
+      |> Map.merge(%{
+        id: id,
+        account_id: account_id,
+        inserted_at: now,
+        updated_at: now
+      })
+
+    {1, _} = Repo.insert_all(Log, [row])
+    Repo.get!(Log, id)
+  end
+
+  defp insert_checkpoint(account_id, sequence_number, checksum) do
+    %Checkpoint{}
+    |> Checkpoint.changeset(%{
+      account_id: account_id,
+      sequence_number: sequence_number,
+      checksum: checksum
+    })
+    |> Repo.insert!()
+  end
+end

exec
/bin/zsh -lc 'td ws log --decision "Used a fully streamed verifier with explicit accumulator state and account-scoped cursor queries (LIMIT 1000) so integrity checks stay tenant-isolated and memory-bounded while still reporting first failure, sequence gaps, and checkpoint outcomes."' in /Users/tbrewer/projects/goodway/goodaudit succeeded in 52ms:
LOGGED ws-e303 [decision] → td-1d7eca, td-46c84b, td-8a9954, td-486ab1, td-5806a7, td-7ae373, td-e596e4, td-39f158, td-cb684b, td-bb90d7, td-c5e429, td-166913, td-dbf670, td-c4e66e, td-f96191, td-54c8dc, td-e39b5f, td-ad7419
file update:
diff --git a/app/lib/app/application.ex b/app/lib/app/application.ex
index d865bfe6a48ed688366f7d85220490621cd809a7..08bab073bf607f5bb04aae846094b0b08ec40fe4
--- a/app/lib/app/application.ex
+++ b/app/lib/app/application.ex
@@ -14,6 +14,7 @@
       {Phoenix.PubSub, name: GA.PubSub},
       # Start a worker by calling: GA.Worker.start_link(arg)
       # {GA.Worker, arg},
+      {GA.Audit.CheckpointWorker, checkpoint_worker_opts()},
       # Start to serve requests, typically the last entry
       GAWeb.Endpoint
     ]
@@ -31,4 +32,8 @@
     GAWeb.Endpoint.config_change(changed, removed)
     :ok
   end
+
+  defp checkpoint_worker_opts do
+    Application.get_env(:app, GA.Audit.CheckpointWorker, [])
+  end
 end
diff --git a/app/lib/app/audit.ex b/app/lib/app/audit.ex
index a48830aacf4da8275b40f15016dcd16bbc87411f..2c7fbce41d873e721533728bcc1c965168addd3d
--- a/app/lib/app/audit.ex
+++ b/app/lib/app/audit.ex
@@ -7,7 +7,7 @@
   import Ecto.Query, warn: false
 
   alias GA.Accounts
-  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Audit.{Chain, Checkpoint, Log, Verifier}
   alias GA.Repo
 
   @default_limit 50
@@ -114,6 +114,12 @@
 
   def list_checkpoints(_), do: []
 
+  @doc """
+  Verifies chain integrity for a single account.
+  """
+  def verify_chain(account_id) when is_binary(account_id), do: Verifier.verify(account_id)
+  def verify_chain(_), do: {:error, :invalid_id}
+
   defp lock_account(account_id) do
     {key_a, key_b} = account_lock_keys(account_id)
 
diff --git a/app/lib/app/audit/checkpoint_worker.ex b/app/lib/app/audit/checkpoint_worker.ex
new file mode 100644
index 0000000000000000000000000000000000000000..f54908642ad7c854eae895b9605661f5beb0246f
--- /dev/null
+++ b/app/lib/app/audit/checkpoint_worker.ex
@@ -0,0 +1,82 @@
+defmodule GA.Audit.CheckpointWorker do
+  @moduledoc """
+  Periodically creates audit checkpoints for all active accounts.
+  """
+
+  use GenServer
+
+  import Ecto.Query, warn: false
+
+  require Logger
+
+  alias GA.Accounts.Account
+  alias GA.Audit
+  alias GA.Repo
+
+  @default_interval_ms :timer.hours(1)
+
+  def start_link(opts \\ []) do
+    name = Keyword.get(opts, :name, __MODULE__)
+    GenServer.start_link(__MODULE__, opts, name: name)
+  end
+
+  @impl true
+  def init(opts) do
+    interval_ms =
+      opts
+      |> Keyword.get(:interval_ms, configured_interval_ms())
+      |> normalize_interval()
+
+    schedule_tick(interval_ms)
+
+    {:ok, %{interval_ms: interval_ms}}
+  end
+
+  @impl true
+  def handle_info(:create_checkpoints, state) do
+    active_account_ids()
+    |> Enum.each(&create_checkpoint_for_account/1)
+
+    schedule_tick(state.interval_ms)
+    {:noreply, state}
+  end
+
+  defp active_account_ids do
+    from(account in Account,
+      where: account.status == :active,
+      select: account.id
+    )
+    |> Repo.all()
+  end
+
+  defp create_checkpoint_for_account(account_id) do
+    case Audit.create_checkpoint(account_id) do
+      {:ok, _checkpoint} ->
+        Logger.debug("checkpoint_worker created checkpoint for account=#{account_id}")
+
+      {:error, :no_entries} ->
+        Logger.debug("checkpoint_worker skipping account=#{account_id}: no audit entries")
+
+      {:error, reason} ->
+        Logger.error("checkpoint_worker failed for account=#{account_id}: #{inspect(reason)}")
+    end
+  rescue
+    error ->
+      Logger.error(
+        "checkpoint_worker crashed for account=#{account_id}: #{Exception.message(error)}"
+      )
+  end
+
+  defp schedule_tick(interval_ms) do
+    Process.send_after(self(), :create_checkpoints, interval_ms)
+  end
+
+  defp configured_interval_ms do
+    :app
+    |> Application.get_env(__MODULE__, [])
+    |> Keyword.get(:interval_ms, @default_interval_ms)
+  end
+
+  defp normalize_interval(interval_ms) when is_integer(interval_ms) and interval_ms > 0, do: interval_ms
+  defp normalize_interval(_), do: @default_interval_ms
+end
diff --git a/app/lib/app/audit/verifier.ex b/app/lib/app/audit/verifier.ex
new file mode 100644
index 0000000000000000000000000000000000000000..2a4bd77516a2c76ccc9b05de1778dd8781ded096
--- /dev/null
+++ b/app/lib/app/audit/verifier.ex
@@ -0,0 +1,203 @@
+defmodule GA.Audit.Verifier do
+  @moduledoc """
+  Streams and verifies account-scoped audit chains.
+  """
+
+  import Ecto.Query, warn: false
+
+  alias GA.Accounts
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  @batch_size 1000
+
+  @doc """
+  Verifies an account's audit chain and returns a report map.
+  """
+  @spec verify(binary()) :: map() | {:error, :invalid_id | :not_found}
+  def verify(account_id) when is_binary(account_id) do
+    started_at = System.monotonic_time(:millisecond)
+
+    with {:ok, hmac_key} <- Accounts.get_hmac_key(account_id) do
+      checkpoints_by_sequence = load_checkpoints(account_id)
+      report = stream_and_verify(account_id, hmac_key, checkpoints_by_sequence)
+
+      Map.put(report, :duration_ms, elapsed_ms(started_at))
+    end
+  end
+
+  def verify(_), do: {:error, :invalid_id}
+
+  defp stream_and_verify(account_id, hmac_key, checkpoints_by_sequence) do
+    initial =
+      new_accumulator()
+      |> Map.put(:account_id, account_id)
+
+    account_id
+    |> do_verify_batches(hmac_key, checkpoints_by_sequence, initial)
+    |> finalize_report()
+  end
+
+  defp do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, acc) do
+    batch = load_batch(account_id, acc.last_sequence)
+
+    case batch do
+      [] ->
+        acc
+
+      _entries ->
+        next_acc =
+          Enum.reduce(batch, acc, fn entry, state ->
+            verify_entry(entry, hmac_key, checkpoints_by_sequence, state)
+          end)
+
+        do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, next_acc)
+    end
+  end
+
+  defp verify_entry(%Log{} = entry, hmac_key, checkpoints_by_sequence, acc) do
+    acc
+    |> verify_sequence(entry)
+    |> verify_checksum(entry, hmac_key)
+    |> verify_checkpoint(entry, checkpoints_by_sequence)
+    |> advance_state(entry)
+  end
+
+  defp verify_sequence(acc, %Log{sequence_number: found_sequence}) do
+    expected_sequence = acc.expected_sequence
+
+    if found_sequence == expected_sequence do
+      %{acc | expected_sequence: expected_sequence + 1}
+    else
+      missing =
+        if found_sequence > expected_sequence do
+          Enum.to_list(expected_sequence..(found_sequence - 1))
+        else
+          []
+        end
+
+      gap = %{
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      }
+
+      acc
+      |> mark_invalid(%{
+        type: :sequence_gap,
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      })
+      |> Map.update!(:sequence_gaps, &[gap | &1])
+      |> Map.put(:expected_sequence, max(found_sequence, expected_sequence) + 1)
+    end
+  end
+
+  defp verify_checksum(acc, %Log{} = entry, hmac_key) do
+    if Chain.verify_checksum(hmac_key, entry, acc.previous_checksum) do
+      acc
+    else
+      mark_invalid(acc, %{
+        type: :checksum_mismatch,
+        sequence_number: entry.sequence_number,
+        stored_checksum: entry.checksum,
+        expected_checksum: expected_checksum(hmac_key, entry, acc.previous_checksum)
+      })
+    end
+  end
+
+  defp verify_checkpoint(acc, %Log{} = entry, checkpoints_by_sequence) do
+    case Map.get(checkpoints_by_sequence, entry.sequence_number) do
+      nil ->
+        acc
+
+      %Checkpoint{} = checkpoint ->
+        valid? = checkpoint.checksum == entry.checksum
+        result = %{sequence_number: entry.sequence_number, valid: valid?}
+        next_acc = Map.update!(acc, :checkpoint_results, &[result | &1])
+
+        if valid? do
+          next_acc
+        else
+          mark_invalid(next_acc, %{
+            type: :checkpoint_mismatch,
+            sequence_number: entry.sequence_number,
+            checkpoint_checksum: checkpoint.checksum,
+            entry_checksum: entry.checksum
+          })
+        end
+    end
+  end
+
+  defp advance_state(acc, %Log{} = entry) do
+    %{
+      acc
+      | previous_checksum: entry.checksum,
+        last_sequence: entry.sequence_number,
+        total_entries: acc.total_entries + 1,
+        verified_entries: acc.verified_entries + 1
+    }
+  end
+
+  defp expected_checksum(hmac_key, entry, previous_checksum) do
+    Chain.compute_checksum(hmac_key, Map.from_struct(entry), previous_checksum)
+  rescue
+    _ -> nil
+  end
+
+  defp load_batch(account_id, after_sequence) do
+    from(log in Log,
+      where: log.account_id == ^account_id and log.sequence_number > ^after_sequence,
+      order_by: [asc: log.sequence_number],
+      limit: @batch_size
+    )
+    |> Repo.all()
+  end
+
+  defp load_checkpoints(account_id) do
+    from(checkpoint in Checkpoint, where: checkpoint.account_id == ^account_id)
+    |> Repo.all()
+    |> Map.new(fn checkpoint -> {checkpoint.sequence_number, checkpoint} end)
+  end
+
+  defp new_accumulator do
+    %{
+      account_id: nil,
+      valid: true,
+      total_entries: 0,
+      verified_entries: 0,
+      expected_sequence: 1,
+      previous_checksum: nil,
+      last_sequence: 0,
+      first_failure: nil,
+      sequence_gaps: [],
+      checkpoint_results: []
+    }
+  end
+
+  defp finalize_report(acc) do
+    %{
+      valid: acc.valid,
+      total_entries: acc.total_entries,
+      verified_entries: acc.verified_entries,
+      first_failure: acc.first_failure,
+      sequence_gaps: Enum.reverse(acc.sequence_gaps),
+      checkpoint_results: Enum.reverse(acc.checkpoint_results),
+      duration_ms: 0
+    }
+  end
+
+  defp mark_invalid(acc, failure) do
+    case acc.first_failure do
+      nil -> %{acc | valid: false, first_failure: failure}
+      _existing -> %{acc | valid: false}
+    end
+  end
+
+  defp elapsed_ms(started_at) do
+    System.monotonic_time(:millisecond)
+    |> Kernel.-(started_at)
+    |> max(0)
+  end
+end
diff --git a/app/test/app/audit/checkpoint_worker_test.exs b/app/test/app/audit/checkpoint_worker_test.exs
new file mode 100644
index 0000000000000000000000000000000000000000..ec9ea5e45a2b2d84cd0e7872a7bf7288d8e3ea11
--- /dev/null
+++ b/app/test/app/audit/checkpoint_worker_test.exs
@@ -0,0 +1,111 @@
+defmodule GA.Audit.CheckpointWorkerTest do
+  use GA.DataCase, async: false
+
+  import ExUnit.CaptureLog
+
+  alias GA.Accounts
+  alias GA.Audit
+  alias GA.Audit.CheckpointWorker
+
+  describe "checkpoint worker ticks" do
+    test "creates checkpoints for all active accounts" do
+      account_a = account_fixture()
+      account_b = account_fixture()
+      add_log(account_a.id, "active-a")
+      add_log(account_b.id, "active-b")
+
+      worker = start_worker()
+      send(worker, :create_checkpoints)
+
+      assert_eventually(fn ->
+        length(Audit.list_checkpoints(account_a.id)) == 1 and
+          length(Audit.list_checkpoints(account_b.id)) == 1
+      end)
+    end
+
+    test "skips suspended accounts" do
+      active_account = account_fixture()
+      suspended_account = account_fixture(%{status: :suspended})
+      add_log(active_account.id, "active")
+      add_log(suspended_account.id, "suspended")
+
+      worker = start_worker()
+      send(worker, :create_checkpoints)
+
+      assert_eventually(fn ->
+        length(Audit.list_checkpoints(active_account.id)) == 1
+      end)
+
+      assert Audit.list_checkpoints(suspended_account.id) == []
+    end
+
+    test "logs no-entry accounts and continues processing" do
+      empty_account = account_fixture()
+      populated_account = account_fixture()
+      add_log(populated_account.id, "populated")
+
+      worker = start_worker()
+
+      log =
+        capture_log([level: :debug], fn ->
+          send(worker, :create_checkpoints)
+
+          assert_eventually(fn ->
+            length(Audit.list_checkpoints(populated_account.id)) == 1
+          end)
+        end)
+
+      assert log =~ "checkpoint_worker skipping"
+      assert log =~ empty_account.id
+      assert Audit.list_checkpoints(empty_account.id) == []
+    end
+  end
+
+  defp start_worker do
+    start_supervised!(
+      {CheckpointWorker,
+       [
+         name: {:global, {:checkpoint_worker_test, System.unique_integer([:positive])}},
+         interval_ms: 60_000
+       ]}
+    )
+  end
+
+  defp account_fixture(attrs \\ %{}) do
+    defaults = %{name: "Worker Account #{System.unique_integer([:positive])}"}
+    {:ok, account} = Accounts.create_account(Map.merge(defaults, attrs))
+    account
+  end
+
+  defp add_log(account_id, resource_id) do
+    attrs = %{
+      user_id: Ecto.UUID.generate(),
+      user_role: "admin",
+      session_id: "session-#{System.unique_integer([:positive])}",
+      action: "read",
+      resource_type: "patient",
+      resource_id: resource_id,
+      timestamp: ~U[2026-03-03 16:30:00Z],
+      source_ip: "127.0.0.1",
+      user_agent: "ExUnit",
+      outcome: "success",
+      failure_reason: nil,
+      phi_accessed: false,
+      metadata: %{"source" => "checkpoint_worker_test"}
+    }
+
+    assert {:ok, _} = Audit.create_log_entry(account_id, attrs)
+  end
+
+  defp assert_eventually(fun, attempts \\ 50)
+  defp assert_eventually(fun, 0), do: assert(fun.())
+
+  defp assert_eventually(fun, attempts) do
+    if fun.() do
+      :ok
+    else
+      Process.sleep(20)
+      assert_eventually(fun, attempts - 1)
+    end
+  end
+end
diff --git a/app/test/app/audit/verifier_test.exs b/app/test/app/audit/verifier_test.exs
new file mode 100644
index 0000000000000000000000000000000000000000..430e2450186c77bb9e816cfa26910a774995b717
--- /dev/null
+++ b/app/test/app/audit/verifier_test.exs
@@ -0,0 +1,189 @@
+defmodule GA.Audit.VerifierTest do
+  use GA.DataCase, async: false
+
+  alias GA.Accounts
+  alias GA.Audit
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  describe "verify_chain/1" do
+    test "returns a valid report for an intact account chain" do
+      account = account_fixture()
+
+      assert {:ok, _first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "r-1"}))
+      assert {:ok, _second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "r-2"}))
+      assert {:ok, checkpoint} = Audit.create_checkpoint(account.id)
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == true
+      assert report.total_entries == 2
+      assert report.verified_entries == 2
+      assert report.first_failure == nil
+      assert report.sequence_gaps == []
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == checkpoint.sequence_number and &1.valid))
+      assert is_integer(report.duration_ms)
+      assert report.duration_ms >= 0
+    end
+
+    test "detects tampered checksum entries" do
+      account = account_fixture()
+
+      assert {:ok, first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "seed"}))
+
+      insert_raw_log(account.id, %{
+        sequence_number: 2,
+        previous_checksum: first.checksum,
+        checksum: String.duplicate("0", 64),
+        resource_id: "tampered"
+      })
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert report.first_failure.type == :checksum_mismatch
+      assert report.first_failure.sequence_number == 2
+      assert report.first_failure.stored_checksum == String.duplicate("0", 64)
+      refute is_nil(report.first_failure.expected_checksum)
+    end
+
+    test "detects sequence gaps within an account" do
+      account = account_fixture()
+      assert {:ok, _first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "gap-1"}))
+      assert {:ok, second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "gap-2"}))
+      assert {:ok, hmac_key} = Accounts.get_hmac_key(account.id)
+
+      checksum =
+        checksum_for_sequence(
+          account.id,
+          hmac_key,
+          4,
+          second.checksum,
+          %{resource_id: "gap-4"}
+        )
+
+      insert_raw_log(account.id, %{
+        sequence_number: 4,
+        previous_checksum: second.checksum,
+        checksum: checksum,
+        resource_id: "gap-4"
+      })
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert report.first_failure.type == :sequence_gap
+      assert [%{expected: 3, found: 4, missing: [3]}] = report.sequence_gaps
+    end
+
+    test "validates checkpoint anchors and reports invalid anchors" do
+      account = account_fixture()
+      assert {:ok, first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "cp-1"}))
+      assert {:ok, second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "cp-2"}))
+
+      insert_checkpoint(account.id, first.sequence_number, first.checksum)
+      insert_checkpoint(account.id, second.sequence_number, String.duplicate("f", 64))
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == first.sequence_number and &1.valid))
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == second.sequence_number and not &1.valid))
+    end
+
+    test "includes non-negative duration tracking" do
+      account = account_fixture()
+      assert {:ok, _} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "duration"}))
+
+      report = Audit.verify_chain(account.id)
+
+      assert is_integer(report.duration_ms)
+      assert report.duration_ms >= 0
+    end
+
+    test "verification remains account-scoped" do
+      account_a = account_fixture()
+      account_b = account_fixture()
+
+      assert {:ok, _} = Audit.create_log_entry(account_a.id, valid_attrs(%{resource_id: "a-1"}))
+      assert {:ok, first_b} = Audit.create_log_entry(account_b.id, valid_attrs(%{resource_id: "b-1"}))
+
+      insert_raw_log(account_b.id, %{
+        sequence_number: 2,
+        previous_checksum: first_b.checksum,
+        checksum: String.duplicate("a", 64),
+        resource_id: "b-2-tampered"
+      })
+
+      report = Audit.verify_chain(account_a.id)
+
+      assert report.valid == true
+      assert report.total_entries == 1
+      assert report.first_failure == nil
+      assert report.sequence_gaps == []
+    end
+  end
+
+  defp account_fixture(attrs \\ %{}) do
+    defaults = %{name: "Verifier Account #{System.unique_integer([:positive])}"}
+    {:ok, account} = Accounts.create_account(Map.merge(defaults, attrs))
+    account
+  end
+
+  defp valid_attrs(overrides \\ %{}) do
+    defaults = %{
+      user_id: Ecto.UUID.generate(),
+      user_role: "admin",
+      session_id: "session-#{System.unique_integer([:positive])}",
+      action: "read",
+      resource_type: "patient",
+      resource_id: Ecto.UUID.generate(),
+      timestamp: ~U[2026-03-03 16:30:00Z],
+      source_ip: "127.0.0.1",
+      user_agent: "ExUnit",
+      outcome: "success",
+      failure_reason: nil,
+      phi_accessed: false,
+      metadata: %{"source" => "verifier_test"}
+    }
+
+    Map.merge(defaults, overrides)
+  end
+
+  defp checksum_for_sequence(account_id, hmac_key, sequence_number, previous_checksum, overrides \\ %{}) do
+    attrs =
+      valid_attrs(overrides)
+      |> Map.put(:account_id, account_id)
+      |> Map.put(:sequence_number, sequence_number)
+
+    Chain.compute_checksum(hmac_key, attrs, previous_checksum)
+  end
+
+  defp insert_raw_log(account_id, attrs) do
+    now = DateTime.utc_now() |> DateTime.truncate(:microsecond)
+    id = Ecto.UUID.generate()
+
+    row =
+      valid_attrs()
+      |> Map.merge(attrs)
+      |> Map.merge(%{
+        id: id,
+        account_id: account_id,
+        inserted_at: now,
+        updated_at: now
+      })
+
+    {1, _} = Repo.insert_all(Log, [row])
+    Repo.get!(Log, id)
+  end
+
+  defp insert_checkpoint(account_id, sequence_number, checksum) do
+    %Checkpoint{}
+    |> Checkpoint.changeset(%{
+      account_id: account_id,
+      sequence_number: sequence_number,
+      checksum: checksum
+    })
+    |> Repo.insert!()
+  end
+end

exec
/bin/zsh -lc 'td ws log --result "Validation is blocked in this environment: mix format/mix test cannot run because installed Elixir requires newer Erlang than available (running OTP 27)."' in /Users/tbrewer/projects/goodway/goodaudit succeeded in 51ms:
LOGGED ws-e303 [result] → td-1d7eca, td-46c84b, td-8a9954, td-486ab1, td-5806a7, td-7ae373, td-e596e4, td-39f158, td-cb684b, td-bb90d7, td-c5e429, td-166913, td-dbf670, td-c4e66e, td-f96191, td-54c8dc, td-e39b5f, td-ad7419
file update:
diff --git a/app/lib/app/application.ex b/app/lib/app/application.ex
index d865bfe6a48ed688366f7d85220490621cd809a7..08bab073bf607f5bb04aae846094b0b08ec40fe4
--- a/app/lib/app/application.ex
+++ b/app/lib/app/application.ex
@@ -14,6 +14,7 @@
       {Phoenix.PubSub, name: GA.PubSub},
       # Start a worker by calling: GA.Worker.start_link(arg)
       # {GA.Worker, arg},
+      {GA.Audit.CheckpointWorker, checkpoint_worker_opts()},
       # Start to serve requests, typically the last entry
       GAWeb.Endpoint
     ]
@@ -31,4 +32,8 @@
     GAWeb.Endpoint.config_change(changed, removed)
     :ok
   end
+
+  defp checkpoint_worker_opts do
+    Application.get_env(:app, GA.Audit.CheckpointWorker, [])
+  end
 end
diff --git a/app/lib/app/audit.ex b/app/lib/app/audit.ex
index a48830aacf4da8275b40f15016dcd16bbc87411f..2c7fbce41d873e721533728bcc1c965168addd3d
--- a/app/lib/app/audit.ex
+++ b/app/lib/app/audit.ex
@@ -7,7 +7,7 @@
   import Ecto.Query, warn: false
 
   alias GA.Accounts
-  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Audit.{Chain, Checkpoint, Log, Verifier}
   alias GA.Repo
 
   @default_limit 50
@@ -114,6 +114,12 @@
 
   def list_checkpoints(_), do: []
 
+  @doc """
+  Verifies chain integrity for a single account.
+  """
+  def verify_chain(account_id) when is_binary(account_id), do: Verifier.verify(account_id)
+  def verify_chain(_), do: {:error, :invalid_id}
+
   defp lock_account(account_id) do
     {key_a, key_b} = account_lock_keys(account_id)
 
diff --git a/app/lib/app/audit/checkpoint_worker.ex b/app/lib/app/audit/checkpoint_worker.ex
new file mode 100644
index 0000000000000000000000000000000000000000..f54908642ad7c854eae895b9605661f5beb0246f
--- /dev/null
+++ b/app/lib/app/audit/checkpoint_worker.ex
@@ -0,0 +1,82 @@
+defmodule GA.Audit.CheckpointWorker do
+  @moduledoc """
+  Periodically creates audit checkpoints for all active accounts.
+  """
+
+  use GenServer
+
+  import Ecto.Query, warn: false
+
+  require Logger
+
+  alias GA.Accounts.Account
+  alias GA.Audit
+  alias GA.Repo
+
+  @default_interval_ms :timer.hours(1)
+
+  def start_link(opts \\ []) do
+    name = Keyword.get(opts, :name, __MODULE__)
+    GenServer.start_link(__MODULE__, opts, name: name)
+  end
+
+  @impl true
+  def init(opts) do
+    interval_ms =
+      opts
+      |> Keyword.get(:interval_ms, configured_interval_ms())
+      |> normalize_interval()
+
+    schedule_tick(interval_ms)
+
+    {:ok, %{interval_ms: interval_ms}}
+  end
+
+  @impl true
+  def handle_info(:create_checkpoints, state) do
+    active_account_ids()
+    |> Enum.each(&create_checkpoint_for_account/1)
+
+    schedule_tick(state.interval_ms)
+    {:noreply, state}
+  end
+
+  defp active_account_ids do
+    from(account in Account,
+      where: account.status == :active,
+      select: account.id
+    )
+    |> Repo.all()
+  end
+
+  defp create_checkpoint_for_account(account_id) do
+    case Audit.create_checkpoint(account_id) do
+      {:ok, _checkpoint} ->
+        Logger.debug("checkpoint_worker created checkpoint for account=#{account_id}")
+
+      {:error, :no_entries} ->
+        Logger.debug("checkpoint_worker skipping account=#{account_id}: no audit entries")
+
+      {:error, reason} ->
+        Logger.error("checkpoint_worker failed for account=#{account_id}: #{inspect(reason)}")
+    end
+  rescue
+    error ->
+      Logger.error(
+        "checkpoint_worker crashed for account=#{account_id}: #{Exception.message(error)}"
+      )
+  end
+
+  defp schedule_tick(interval_ms) do
+    Process.send_after(self(), :create_checkpoints, interval_ms)
+  end
+
+  defp configured_interval_ms do
+    :app
+    |> Application.get_env(__MODULE__, [])
+    |> Keyword.get(:interval_ms, @default_interval_ms)
+  end
+
+  defp normalize_interval(interval_ms) when is_integer(interval_ms) and interval_ms > 0, do: interval_ms
+  defp normalize_interval(_), do: @default_interval_ms
+end
diff --git a/app/lib/app/audit/verifier.ex b/app/lib/app/audit/verifier.ex
new file mode 100644
index 0000000000000000000000000000000000000000..2a4bd77516a2c76ccc9b05de1778dd8781ded096
--- /dev/null
+++ b/app/lib/app/audit/verifier.ex
@@ -0,0 +1,203 @@
+defmodule GA.Audit.Verifier do
+  @moduledoc """
+  Streams and verifies account-scoped audit chains.
+  """
+
+  import Ecto.Query, warn: false
+
+  alias GA.Accounts
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  @batch_size 1000
+
+  @doc """
+  Verifies an account's audit chain and returns a report map.
+  """
+  @spec verify(binary()) :: map() | {:error, :invalid_id | :not_found}
+  def verify(account_id) when is_binary(account_id) do
+    started_at = System.monotonic_time(:millisecond)
+
+    with {:ok, hmac_key} <- Accounts.get_hmac_key(account_id) do
+      checkpoints_by_sequence = load_checkpoints(account_id)
+      report = stream_and_verify(account_id, hmac_key, checkpoints_by_sequence)
+
+      Map.put(report, :duration_ms, elapsed_ms(started_at))
+    end
+  end
+
+  def verify(_), do: {:error, :invalid_id}
+
+  defp stream_and_verify(account_id, hmac_key, checkpoints_by_sequence) do
+    initial =
+      new_accumulator()
+      |> Map.put(:account_id, account_id)
+
+    account_id
+    |> do_verify_batches(hmac_key, checkpoints_by_sequence, initial)
+    |> finalize_report()
+  end
+
+  defp do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, acc) do
+    batch = load_batch(account_id, acc.last_sequence)
+
+    case batch do
+      [] ->
+        acc
+
+      _entries ->
+        next_acc =
+          Enum.reduce(batch, acc, fn entry, state ->
+            verify_entry(entry, hmac_key, checkpoints_by_sequence, state)
+          end)
+
+        do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, next_acc)
+    end
+  end
+
+  defp verify_entry(%Log{} = entry, hmac_key, checkpoints_by_sequence, acc) do
+    acc
+    |> verify_sequence(entry)
+    |> verify_checksum(entry, hmac_key)
+    |> verify_checkpoint(entry, checkpoints_by_sequence)
+    |> advance_state(entry)
+  end
+
+  defp verify_sequence(acc, %Log{sequence_number: found_sequence}) do
+    expected_sequence = acc.expected_sequence
+
+    if found_sequence == expected_sequence do
+      %{acc | expected_sequence: expected_sequence + 1}
+    else
+      missing =
+        if found_sequence > expected_sequence do
+          Enum.to_list(expected_sequence..(found_sequence - 1))
+        else
+          []
+        end
+
+      gap = %{
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      }
+
+      acc
+      |> mark_invalid(%{
+        type: :sequence_gap,
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      })
+      |> Map.update!(:sequence_gaps, &[gap | &1])
+      |> Map.put(:expected_sequence, max(found_sequence, expected_sequence) + 1)
+    end
+  end
+
+  defp verify_checksum(acc, %Log{} = entry, hmac_key) do
+    if Chain.verify_checksum(hmac_key, entry, acc.previous_checksum) do
+      acc
+    else
+      mark_invalid(acc, %{
+        type: :checksum_mismatch,
+        sequence_number: entry.sequence_number,
+        stored_checksum: entry.checksum,
+        expected_checksum: expected_checksum(hmac_key, entry, acc.previous_checksum)
+      })
+    end
+  end
+
+  defp verify_checkpoint(acc, %Log{} = entry, checkpoints_by_sequence) do
+    case Map.get(checkpoints_by_sequence, entry.sequence_number) do
+      nil ->
+        acc
+
+      %Checkpoint{} = checkpoint ->
+        valid? = checkpoint.checksum == entry.checksum
+        result = %{sequence_number: entry.sequence_number, valid: valid?}
+        next_acc = Map.update!(acc, :checkpoint_results, &[result | &1])
+
+        if valid? do
+          next_acc
+        else
+          mark_invalid(next_acc, %{
+            type: :checkpoint_mismatch,
+            sequence_number: entry.sequence_number,
+            checkpoint_checksum: checkpoint.checksum,
+            entry_checksum: entry.checksum
+          })
+        end
+    end
+  end
+
+  defp advance_state(acc, %Log{} = entry) do
+    %{
+      acc
+      | previous_checksum: entry.checksum,
+        last_sequence: entry.sequence_number,
+        total_entries: acc.total_entries + 1,
+        verified_entries: acc.verified_entries + 1
+    }
+  end
+
+  defp expected_checksum(hmac_key, entry, previous_checksum) do
+    Chain.compute_checksum(hmac_key, Map.from_struct(entry), previous_checksum)
+  rescue
+    _ -> nil
+  end
+
+  defp load_batch(account_id, after_sequence) do
+    from(log in Log,
+      where: log.account_id == ^account_id and log.sequence_number > ^after_sequence,
+      order_by: [asc: log.sequence_number],
+      limit: @batch_size
+    )
+    |> Repo.all()
+  end
+
+  defp load_checkpoints(account_id) do
+    from(checkpoint in Checkpoint, where: checkpoint.account_id == ^account_id)
+    |> Repo.all()
+    |> Map.new(fn checkpoint -> {checkpoint.sequence_number, checkpoint} end)
+  end
+
+  defp new_accumulator do
+    %{
+      account_id: nil,
+      valid: true,
+      total_entries: 0,
+      verified_entries: 0,
+      expected_sequence: 1,
+      previous_checksum: nil,
+      last_sequence: 0,
+      first_failure: nil,
+      sequence_gaps: [],
+      checkpoint_results: []
+    }
+  end
+
+  defp finalize_report(acc) do
+    %{
+      valid: acc.valid,
+      total_entries: acc.total_entries,
+      verified_entries: acc.verified_entries,
+      first_failure: acc.first_failure,
+      sequence_gaps: Enum.reverse(acc.sequence_gaps),
+      checkpoint_results: Enum.reverse(acc.checkpoint_results),
+      duration_ms: 0
+    }
+  end
+
+  defp mark_invalid(acc, failure) do
+    case acc.first_failure do
+      nil -> %{acc | valid: false, first_failure: failure}
+      _existing -> %{acc | valid: false}
+    end
+  end
+
+  defp elapsed_ms(started_at) do
+    System.monotonic_time(:millisecond)
+    |> Kernel.-(started_at)
+    |> max(0)
+  end
+end
diff --git a/app/test/app/audit/checkpoint_worker_test.exs b/app/test/app/audit/checkpoint_worker_test.exs
new file mode 100644
index 0000000000000000000000000000000000000000..ec9ea5e45a2b2d84cd0e7872a7bf7288d8e3ea11
--- /dev/null
+++ b/app/test/app/audit/checkpoint_worker_test.exs
@@ -0,0 +1,111 @@
+defmodule GA.Audit.CheckpointWorkerTest do
+  use GA.DataCase, async: false
+
+  import ExUnit.CaptureLog
+
+  alias GA.Accounts
+  alias GA.Audit
+  alias GA.Audit.CheckpointWorker
+
+  describe "checkpoint worker ticks" do
+    test "creates checkpoints for all active accounts" do
+      account_a = account_fixture()
+      account_b = account_fixture()
+      add_log(account_a.id, "active-a")
+      add_log(account_b.id, "active-b")
+
+      worker = start_worker()
+      send(worker, :create_checkpoints)
+
+      assert_eventually(fn ->
+        length(Audit.list_checkpoints(account_a.id)) == 1 and
+          length(Audit.list_checkpoints(account_b.id)) == 1
+      end)
+    end
+
+    test "skips suspended accounts" do
+      active_account = account_fixture()
+      suspended_account = account_fixture(%{status: :suspended})
+      add_log(active_account.id, "active")
+      add_log(suspended_account.id, "suspended")
+
+      worker = start_worker()
+      send(worker, :create_checkpoints)
+
+      assert_eventually(fn ->
+        length(Audit.list_checkpoints(active_account.id)) == 1
+      end)
+
+      assert Audit.list_checkpoints(suspended_account.id) == []
+    end
+
+    test "logs no-entry accounts and continues processing" do
+      empty_account = account_fixture()
+      populated_account = account_fixture()
+      add_log(populated_account.id, "populated")
+
+      worker = start_worker()
+
+      log =
+        capture_log([level: :debug], fn ->
+          send(worker, :create_checkpoints)
+
+          assert_eventually(fn ->
+            length(Audit.list_checkpoints(populated_account.id)) == 1
+          end)
+        end)
+
+      assert log =~ "checkpoint_worker skipping"
+      assert log =~ empty_account.id
+      assert Audit.list_checkpoints(empty_account.id) == []
+    end
+  end
+
+  defp start_worker do
+    start_supervised!(
+      {CheckpointWorker,
+       [
+         name: {:global, {:checkpoint_worker_test, System.unique_integer([:positive])}},
+         interval_ms: 60_000
+       ]}
+    )
+  end
+
+  defp account_fixture(attrs \\ %{}) do
+    defaults = %{name: "Worker Account #{System.unique_integer([:positive])}"}
+    {:ok, account} = Accounts.create_account(Map.merge(defaults, attrs))
+    account
+  end
+
+  defp add_log(account_id, resource_id) do
+    attrs = %{
+      user_id: Ecto.UUID.generate(),
+      user_role: "admin",
+      session_id: "session-#{System.unique_integer([:positive])}",
+      action: "read",
+      resource_type: "patient",
+      resource_id: resource_id,
+      timestamp: ~U[2026-03-03 16:30:00Z],
+      source_ip: "127.0.0.1",
+      user_agent: "ExUnit",
+      outcome: "success",
+      failure_reason: nil,
+      phi_accessed: false,
+      metadata: %{"source" => "checkpoint_worker_test"}
+    }
+
+    assert {:ok, _} = Audit.create_log_entry(account_id, attrs)
+  end
+
+  defp assert_eventually(fun, attempts \\ 50)
+  defp assert_eventually(fun, 0), do: assert(fun.())
+
+  defp assert_eventually(fun, attempts) do
+    if fun.() do
+      :ok
+    else
+      Process.sleep(20)
+      assert_eventually(fun, attempts - 1)
+    end
+  end
+end
diff --git a/app/test/app/audit/verifier_test.exs b/app/test/app/audit/verifier_test.exs
new file mode 100644
index 0000000000000000000000000000000000000000..430e2450186c77bb9e816cfa26910a774995b717
--- /dev/null
+++ b/app/test/app/audit/verifier_test.exs
@@ -0,0 +1,189 @@
+defmodule GA.Audit.VerifierTest do
+  use GA.DataCase, async: false
+
+  alias GA.Accounts
+  alias GA.Audit
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  describe "verify_chain/1" do
+    test "returns a valid report for an intact account chain" do
+      account = account_fixture()
+
+      assert {:ok, _first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "r-1"}))
+      assert {:ok, _second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "r-2"}))
+      assert {:ok, checkpoint} = Audit.create_checkpoint(account.id)
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == true
+      assert report.total_entries == 2
+      assert report.verified_entries == 2
+      assert report.first_failure == nil
+      assert report.sequence_gaps == []
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == checkpoint.sequence_number and &1.valid))
+      assert is_integer(report.duration_ms)
+      assert report.duration_ms >= 0
+    end
+
+    test "detects tampered checksum entries" do
+      account = account_fixture()
+
+      assert {:ok, first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "seed"}))
+
+      insert_raw_log(account.id, %{
+        sequence_number: 2,
+        previous_checksum: first.checksum,
+        checksum: String.duplicate("0", 64),
+        resource_id: "tampered"
+      })
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert report.first_failure.type == :checksum_mismatch
+      assert report.first_failure.sequence_number == 2
+      assert report.first_failure.stored_checksum == String.duplicate("0", 64)
+      refute is_nil(report.first_failure.expected_checksum)
+    end
+
+    test "detects sequence gaps within an account" do
+      account = account_fixture()
+      assert {:ok, _first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "gap-1"}))
+      assert {:ok, second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "gap-2"}))
+      assert {:ok, hmac_key} = Accounts.get_hmac_key(account.id)
+
+      checksum =
+        checksum_for_sequence(
+          account.id,
+          hmac_key,
+          4,
+          second.checksum,
+          %{resource_id: "gap-4"}
+        )
+
+      insert_raw_log(account.id, %{
+        sequence_number: 4,
+        previous_checksum: second.checksum,
+        checksum: checksum,
+        resource_id: "gap-4"
+      })
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert report.first_failure.type == :sequence_gap
+      assert [%{expected: 3, found: 4, missing: [3]}] = report.sequence_gaps
+    end
+
+    test "validates checkpoint anchors and reports invalid anchors" do
+      account = account_fixture()
+      assert {:ok, first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "cp-1"}))
+      assert {:ok, second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "cp-2"}))
+
+      insert_checkpoint(account.id, first.sequence_number, first.checksum)
+      insert_checkpoint(account.id, second.sequence_number, String.duplicate("f", 64))
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == first.sequence_number and &1.valid))
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == second.sequence_number and not &1.valid))
+    end
+
+    test "includes non-negative duration tracking" do
+      account = account_fixture()
+      assert {:ok, _} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "duration"}))
+
+      report = Audit.verify_chain(account.id)
+
+      assert is_integer(report.duration_ms)
+      assert report.duration_ms >= 0
+    end
+
+    test "verification remains account-scoped" do
+      account_a = account_fixture()
+      account_b = account_fixture()
+
+      assert {:ok, _} = Audit.create_log_entry(account_a.id, valid_attrs(%{resource_id: "a-1"}))
+      assert {:ok, first_b} = Audit.create_log_entry(account_b.id, valid_attrs(%{resource_id: "b-1"}))
+
+      insert_raw_log(account_b.id, %{
+        sequence_number: 2,
+        previous_checksum: first_b.checksum,
+        checksum: String.duplicate("a", 64),
+        resource_id: "b-2-tampered"
+      })
+
+      report = Audit.verify_chain(account_a.id)
+
+      assert report.valid == true
+      assert report.total_entries == 1
+      assert report.first_failure == nil
+      assert report.sequence_gaps == []
+    end
+  end
+
+  defp account_fixture(attrs \\ %{}) do
+    defaults = %{name: "Verifier Account #{System.unique_integer([:positive])}"}
+    {:ok, account} = Accounts.create_account(Map.merge(defaults, attrs))
+    account
+  end
+
+  defp valid_attrs(overrides \\ %{}) do
+    defaults = %{
+      user_id: Ecto.UUID.generate(),
+      user_role: "admin",
+      session_id: "session-#{System.unique_integer([:positive])}",
+      action: "read",
+      resource_type: "patient",
+      resource_id: Ecto.UUID.generate(),
+      timestamp: ~U[2026-03-03 16:30:00Z],
+      source_ip: "127.0.0.1",
+      user_agent: "ExUnit",
+      outcome: "success",
+      failure_reason: nil,
+      phi_accessed: false,
+      metadata: %{"source" => "verifier_test"}
+    }
+
+    Map.merge(defaults, overrides)
+  end
+
+  defp checksum_for_sequence(account_id, hmac_key, sequence_number, previous_checksum, overrides \\ %{}) do
+    attrs =
+      valid_attrs(overrides)
+      |> Map.put(:account_id, account_id)
+      |> Map.put(:sequence_number, sequence_number)
+
+    Chain.compute_checksum(hmac_key, attrs, previous_checksum)
+  end
+
+  defp insert_raw_log(account_id, attrs) do
+    now = DateTime.utc_now() |> DateTime.truncate(:microsecond)
+    id = Ecto.UUID.generate()
+
+    row =
+      valid_attrs()
+      |> Map.merge(attrs)
+      |> Map.merge(%{
+        id: id,
+        account_id: account_id,
+        inserted_at: now,
+        updated_at: now
+      })
+
+    {1, _} = Repo.insert_all(Log, [row])
+    Repo.get!(Log, id)
+  end
+
+  defp insert_checkpoint(account_id, sequence_number, checksum) do
+    %Checkpoint{}
+    |> Checkpoint.changeset(%{
+      account_id: account_id,
+      sequence_number: sequence_number,
+      checksum: checksum
+    })
+    |> Repo.insert!()
+  end
+end

codex
I’ve logged implementation/decision/results into the active work session and I’m now moving the completed child tasks to review status (keeping the review-gate issue untouched).
exec
/bin/zsh -lc 'td review --help' in /Users/tbrewer/projects/goodway/goodaudit succeeded in 51ms:
Submits the issue(s) for review. If no handoff exists, a minimal one is
auto-created (consider using 'td handoff' for better documentation).

For epics/parent issues, automatically cascades to all open/in_progress
descendants. Cascaded children don't require individual handoffs.

Supports bulk operations:
  td review td-abc1 td-abc2 td-abc3    # Submit multiple issues for review

Usage:
  td review [issue-id...] [flags]

Aliases:
  review, submit, finish

Flags:
      --comment string   Reason for submitting (alias for --reason)
  -h, --help             help for review
      --json             JSON output
      --message string   Reason for submitting (alias for --reason)
file update:
diff --git a/app/lib/app/application.ex b/app/lib/app/application.ex
index d865bfe6a48ed688366f7d85220490621cd809a7..08bab073bf607f5bb04aae846094b0b08ec40fe4
--- a/app/lib/app/application.ex
+++ b/app/lib/app/application.ex
@@ -14,6 +14,7 @@
       {Phoenix.PubSub, name: GA.PubSub},
       # Start a worker by calling: GA.Worker.start_link(arg)
       # {GA.Worker, arg},
+      {GA.Audit.CheckpointWorker, checkpoint_worker_opts()},
       # Start to serve requests, typically the last entry
       GAWeb.Endpoint
     ]
@@ -31,4 +32,8 @@
     GAWeb.Endpoint.config_change(changed, removed)
     :ok
   end
+
+  defp checkpoint_worker_opts do
+    Application.get_env(:app, GA.Audit.CheckpointWorker, [])
+  end
 end
diff --git a/app/lib/app/audit.ex b/app/lib/app/audit.ex
index a48830aacf4da8275b40f15016dcd16bbc87411f..2c7fbce41d873e721533728bcc1c965168addd3d
--- a/app/lib/app/audit.ex
+++ b/app/lib/app/audit.ex
@@ -7,7 +7,7 @@
   import Ecto.Query, warn: false
 
   alias GA.Accounts
-  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Audit.{Chain, Checkpoint, Log, Verifier}
   alias GA.Repo
 
   @default_limit 50
@@ -114,6 +114,12 @@
 
   def list_checkpoints(_), do: []
 
+  @doc """
+  Verifies chain integrity for a single account.
+  """
+  def verify_chain(account_id) when is_binary(account_id), do: Verifier.verify(account_id)
+  def verify_chain(_), do: {:error, :invalid_id}
+
   defp lock_account(account_id) do
     {key_a, key_b} = account_lock_keys(account_id)
 
diff --git a/app/lib/app/audit/checkpoint_worker.ex b/app/lib/app/audit/checkpoint_worker.ex
new file mode 100644
index 0000000000000000000000000000000000000000..f54908642ad7c854eae895b9605661f5beb0246f
--- /dev/null
+++ b/app/lib/app/audit/checkpoint_worker.ex
@@ -0,0 +1,82 @@
+defmodule GA.Audit.CheckpointWorker do
+  @moduledoc """
+  Periodically creates audit checkpoints for all active accounts.
+  """
+
+  use GenServer
+
+  import Ecto.Query, warn: false
+
+  require Logger
+
+  alias GA.Accounts.Account
+  alias GA.Audit
+  alias GA.Repo
+
+  @default_interval_ms :timer.hours(1)
+
+  def start_link(opts \\ []) do
+    name = Keyword.get(opts, :name, __MODULE__)
+    GenServer.start_link(__MODULE__, opts, name: name)
+  end
+
+  @impl true
+  def init(opts) do
+    interval_ms =
+      opts
+      |> Keyword.get(:interval_ms, configured_interval_ms())
+      |> normalize_interval()
+
+    schedule_tick(interval_ms)
+
+    {:ok, %{interval_ms: interval_ms}}
+  end
+
+  @impl true
+  def handle_info(:create_checkpoints, state) do
+    active_account_ids()
+    |> Enum.each(&create_checkpoint_for_account/1)
+
+    schedule_tick(state.interval_ms)
+    {:noreply, state}
+  end
+
+  defp active_account_ids do
+    from(account in Account,
+      where: account.status == :active,
+      select: account.id
+    )
+    |> Repo.all()
+  end
+
+  defp create_checkpoint_for_account(account_id) do
+    case Audit.create_checkpoint(account_id) do
+      {:ok, _checkpoint} ->
+        Logger.debug("checkpoint_worker created checkpoint for account=#{account_id}")
+
+      {:error, :no_entries} ->
+        Logger.debug("checkpoint_worker skipping account=#{account_id}: no audit entries")
+
+      {:error, reason} ->
+        Logger.error("checkpoint_worker failed for account=#{account_id}: #{inspect(reason)}")
+    end
+  rescue
+    error ->
+      Logger.error(
+        "checkpoint_worker crashed for account=#{account_id}: #{Exception.message(error)}"
+      )
+  end
+
+  defp schedule_tick(interval_ms) do
+    Process.send_after(self(), :create_checkpoints, interval_ms)
+  end
+
+  defp configured_interval_ms do
+    :app
+    |> Application.get_env(__MODULE__, [])
+    |> Keyword.get(:interval_ms, @default_interval_ms)
+  end
+
+  defp normalize_interval(interval_ms) when is_integer(interval_ms) and interval_ms > 0, do: interval_ms
+  defp normalize_interval(_), do: @default_interval_ms
+end
diff --git a/app/lib/app/audit/verifier.ex b/app/lib/app/audit/verifier.ex
new file mode 100644
index 0000000000000000000000000000000000000000..2a4bd77516a2c76ccc9b05de1778dd8781ded096
--- /dev/null
+++ b/app/lib/app/audit/verifier.ex
@@ -0,0 +1,203 @@
+defmodule GA.Audit.Verifier do
+  @moduledoc """
+  Streams and verifies account-scoped audit chains.
+  """
+
+  import Ecto.Query, warn: false
+
+  alias GA.Accounts
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  @batch_size 1000
+
+  @doc """
+  Verifies an account's audit chain and returns a report map.
+  """
+  @spec verify(binary()) :: map() | {:error, :invalid_id | :not_found}
+  def verify(account_id) when is_binary(account_id) do
+    started_at = System.monotonic_time(:millisecond)
+
+    with {:ok, hmac_key} <- Accounts.get_hmac_key(account_id) do
+      checkpoints_by_sequence = load_checkpoints(account_id)
+      report = stream_and_verify(account_id, hmac_key, checkpoints_by_sequence)
+
+      Map.put(report, :duration_ms, elapsed_ms(started_at))
+    end
+  end
+
+  def verify(_), do: {:error, :invalid_id}
+
+  defp stream_and_verify(account_id, hmac_key, checkpoints_by_sequence) do
+    initial =
+      new_accumulator()
+      |> Map.put(:account_id, account_id)
+
+    account_id
+    |> do_verify_batches(hmac_key, checkpoints_by_sequence, initial)
+    |> finalize_report()
+  end
+
+  defp do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, acc) do
+    batch = load_batch(account_id, acc.last_sequence)
+
+    case batch do
+      [] ->
+        acc
+
+      _entries ->
+        next_acc =
+          Enum.reduce(batch, acc, fn entry, state ->
+            verify_entry(entry, hmac_key, checkpoints_by_sequence, state)
+          end)
+
+        do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, next_acc)
+    end
+  end
+
+  defp verify_entry(%Log{} = entry, hmac_key, checkpoints_by_sequence, acc) do
+    acc
+    |> verify_sequence(entry)
+    |> verify_checksum(entry, hmac_key)
+    |> verify_checkpoint(entry, checkpoints_by_sequence)
+    |> advance_state(entry)
+  end
+
+  defp verify_sequence(acc, %Log{sequence_number: found_sequence}) do
+    expected_sequence = acc.expected_sequence
+
+    if found_sequence == expected_sequence do
+      %{acc | expected_sequence: expected_sequence + 1}
+    else
+      missing =
+        if found_sequence > expected_sequence do
+          Enum.to_list(expected_sequence..(found_sequence - 1))
+        else
+          []
+        end
+
+      gap = %{
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      }
+
+      acc
+      |> mark_invalid(%{
+        type: :sequence_gap,
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      })
+      |> Map.update!(:sequence_gaps, &[gap | &1])
+      |> Map.put(:expected_sequence, max(found_sequence, expected_sequence) + 1)
+    end
+  end
+
+  defp verify_checksum(acc, %Log{} = entry, hmac_key) do
+    if Chain.verify_checksum(hmac_key, entry, acc.previous_checksum) do
+      acc
+    else
+      mark_invalid(acc, %{
+        type: :checksum_mismatch,
+        sequence_number: entry.sequence_number,
+        stored_checksum: entry.checksum,
+        expected_checksum: expected_checksum(hmac_key, entry, acc.previous_checksum)
+      })
+    end
+  end
+
+  defp verify_checkpoint(acc, %Log{} = entry, checkpoints_by_sequence) do
+    case Map.get(checkpoints_by_sequence, entry.sequence_number) do
+      nil ->
+        acc
+
+      %Checkpoint{} = checkpoint ->
+        valid? = checkpoint.checksum == entry.checksum
+        result = %{sequence_number: entry.sequence_number, valid: valid?}
+        next_acc = Map.update!(acc, :checkpoint_results, &[result | &1])
+
+        if valid? do
+          next_acc
+        else
+          mark_invalid(next_acc, %{
+            type: :checkpoint_mismatch,
+            sequence_number: entry.sequence_number,
+            checkpoint_checksum: checkpoint.checksum,
+            entry_checksum: entry.checksum
+          })
+        end
+    end
+  end
+
+  defp advance_state(acc, %Log{} = entry) do
+    %{
+      acc
+      | previous_checksum: entry.checksum,
+        last_sequence: entry.sequence_number,
+        total_entries: acc.total_entries + 1,
+        verified_entries: acc.verified_entries + 1
+    }
+  end
+
+  defp expected_checksum(hmac_key, entry, previous_checksum) do
+    Chain.compute_checksum(hmac_key, Map.from_struct(entry), previous_checksum)
+  rescue
+    _ -> nil
+  end
+
+  defp load_batch(account_id, after_sequence) do
+    from(log in Log,
+      where: log.account_id == ^account_id and log.sequence_number > ^after_sequence,
+      order_by: [asc: log.sequence_number],
+      limit: @batch_size
+    )
+    |> Repo.all()
+  end
+
+  defp load_checkpoints(account_id) do
+    from(checkpoint in Checkpoint, where: checkpoint.account_id == ^account_id)
+    |> Repo.all()
+    |> Map.new(fn checkpoint -> {checkpoint.sequence_number, checkpoint} end)
+  end
+
+  defp new_accumulator do
+    %{
+      account_id: nil,
+      valid: true,
+      total_entries: 0,
+      verified_entries: 0,
+      expected_sequence: 1,
+      previous_checksum: nil,
+      last_sequence: 0,
+      first_failure: nil,
+      sequence_gaps: [],
+      checkpoint_results: []
+    }
+  end
+
+  defp finalize_report(acc) do
+    %{
+      valid: acc.valid,
+      total_entries: acc.total_entries,
+      verified_entries: acc.verified_entries,
+      first_failure: acc.first_failure,
+      sequence_gaps: Enum.reverse(acc.sequence_gaps),
+      checkpoint_results: Enum.reverse(acc.checkpoint_results),
+      duration_ms: 0
+    }
+  end
+
+  defp mark_invalid(acc, failure) do
+    case acc.first_failure do
+      nil -> %{acc | valid: false, first_failure: failure}
+      _existing -> %{acc | valid: false}
+    end
+  end
+
+  defp elapsed_ms(started_at) do
+    System.monotonic_time(:millisecond)
+    |> Kernel.-(started_at)
+    |> max(0)
+  end
+end
diff --git a/app/test/app/audit/checkpoint_worker_test.exs b/app/test/app/audit/checkpoint_worker_test.exs
new file mode 100644
index 0000000000000000000000000000000000000000..ec9ea5e45a2b2d84cd0e7872a7bf7288d8e3ea11
--- /dev/null
+++ b/app/test/app/audit/checkpoint_worker_test.exs
@@ -0,0 +1,111 @@
+defmodule GA.Audit.CheckpointWorkerTest do
+  use GA.DataCase, async: false
+
+  import ExUnit.CaptureLog
+
+  alias GA.Accounts
+  alias GA.Audit
+  alias GA.Audit.CheckpointWorker
+
+  describe "checkpoint worker ticks" do
+    test "creates checkpoints for all active accounts" do
+      account_a = account_fixture()
+      account_b = account_fixture()
+      add_log(account_a.id, "active-a")
+      add_log(account_b.id, "active-b")
+
+      worker = start_worker()
+      send(worker, :create_checkpoints)
+
+      assert_eventually(fn ->
+        length(Audit.list_checkpoints(account_a.id)) == 1 and
+          length(Audit.list_checkpoints(account_b.id)) == 1
+      end)
+    end
+
+    test "skips suspended accounts" do
+      active_account = account_fixture()
+      suspended_account = account_fixture(%{status: :suspended})
+      add_log(active_account.id, "active")
+      add_log(suspended_account.id, "suspended")
+
+      worker = start_worker()
+      send(worker, :create_checkpoints)
+
+      assert_eventually(fn ->
+        length(Audit.list_checkpoints(active_account.id)) == 1
+      end)
+
+      assert Audit.list_checkpoints(suspended_account.id) == []
+    end
+
+    test "logs no-entry accounts and continues processing" do
+      empty_account = account_fixture()
+      populated_account = account_fixture()
+      add_log(populated_account.id, "populated")
+
+      worker = start_worker()
+
+      log =
+        capture_log([level: :debug], fn ->
+          send(worker, :create_checkpoints)
+
+          assert_eventually(fn ->
+            length(Audit.list_checkpoints(populated_account.id)) == 1
+          end)
+        end)
+
+      assert log =~ "checkpoint_worker skipping"
+      assert log =~ empty_account.id
+      assert Audit.list_checkpoints(empty_account.id) == []
+    end
+  end
+
+  defp start_worker do
+    start_supervised!(
+      {CheckpointWorker,
+       [
+         name: {:global, {:checkpoint_worker_test, System.unique_integer([:positive])}},
+         interval_ms: 60_000
+       ]}
+    )
+  end
+
+  defp account_fixture(attrs \\ %{}) do
+    defaults = %{name: "Worker Account #{System.unique_integer([:positive])}"}
+    {:ok, account} = Accounts.create_account(Map.merge(defaults, attrs))
+    account
+  end
+
+  defp add_log(account_id, resource_id) do
+    attrs = %{
+      user_id: Ecto.UUID.generate(),
+      user_role: "admin",
+      session_id: "session-#{System.unique_integer([:positive])}",
+      action: "read",
+      resource_type: "patient",
+      resource_id: resource_id,
+      timestamp: ~U[2026-03-03 16:30:00Z],
+      source_ip: "127.0.0.1",
+      user_agent: "ExUnit",
+      outcome: "success",
+      failure_reason: nil,
+      phi_accessed: false,
+      metadata: %{"source" => "checkpoint_worker_test"}
+    }
+
+    assert {:ok, _} = Audit.create_log_entry(account_id, attrs)
+  end
+
+  defp assert_eventually(fun, attempts \\ 50)
+  defp assert_eventually(fun, 0), do: assert(fun.())
+
+  defp assert_eventually(fun, attempts) do
+    if fun.() do
+      :ok
+    else
+      Process.sleep(20)
+      assert_eventually(fun, attempts - 1)
+    end
+  end
+end
diff --git a/app/test/app/audit/verifier_test.exs b/app/test/app/audit/verifier_test.exs
new file mode 100644
index 0000000000000000000000000000000000000000..430e2450186c77bb9e816cfa26910a774995b717
--- /dev/null
+++ b/app/test/app/audit/verifier_test.exs
@@ -0,0 +1,189 @@
+defmodule GA.Audit.VerifierTest do
+  use GA.DataCase, async: false
+
+  alias GA.Accounts
+  alias GA.Audit
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  describe "verify_chain/1" do
+    test "returns a valid report for an intact account chain" do
+      account = account_fixture()
+
+      assert {:ok, _first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "r-1"}))
+      assert {:ok, _second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "r-2"}))
+      assert {:ok, checkpoint} = Audit.create_checkpoint(account.id)
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == true
+      assert report.total_entries == 2
+      assert report.verified_entries == 2
+      assert report.first_failure == nil
+      assert report.sequence_gaps == []
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == checkpoint.sequence_number and &1.valid))
+      assert is_integer(report.duration_ms)
+      assert report.duration_ms >= 0
+    end
+
+    test "detects tampered checksum entries" do
+      account = account_fixture()
+
+      assert {:ok, first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "seed"}))
+
+      insert_raw_log(account.id, %{
+        sequence_number: 2,
+        previous_checksum: first.checksum,
+        checksum: String.duplicate("0", 64),
+        resource_id: "tampered"
+      })
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert report.first_failure.type == :checksum_mismatch
+      assert report.first_failure.sequence_number == 2
+      assert report.first_failure.stored_checksum == String.duplicate("0", 64)
+      refute is_nil(report.first_failure.expected_checksum)
+    end
+
+    test "detects sequence gaps within an account" do
+      account = account_fixture()
+      assert {:ok, _first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "gap-1"}))
+      assert {:ok, second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "gap-2"}))
+      assert {:ok, hmac_key} = Accounts.get_hmac_key(account.id)
+
+      checksum =
+        checksum_for_sequence(
+          account.id,
+          hmac_key,
+          4,
+          second.checksum,
+          %{resource_id: "gap-4"}
+        )
+
+      insert_raw_log(account.id, %{
+        sequence_number: 4,
+        previous_checksum: second.checksum,
+        checksum: checksum,
+        resource_id: "gap-4"
+      })
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert report.first_failure.type == :sequence_gap
+      assert [%{expected: 3, found: 4, missing: [3]}] = report.sequence_gaps
+    end
+
+    test "validates checkpoint anchors and reports invalid anchors" do
+      account = account_fixture()
+      assert {:ok, first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "cp-1"}))
+      assert {:ok, second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "cp-2"}))
+
+      insert_checkpoint(account.id, first.sequence_number, first.checksum)
+      insert_checkpoint(account.id, second.sequence_number, String.duplicate("f", 64))
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == first.sequence_number and &1.valid))
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == second.sequence_number and not &1.valid))
+    end
+
+    test "includes non-negative duration tracking" do
+      account = account_fixture()
+      assert {:ok, _} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "duration"}))
+
+      report = Audit.verify_chain(account.id)
+
+      assert is_integer(report.duration_ms)
+      assert report.duration_ms >= 0
+    end
+
+    test "verification remains account-scoped" do
+      account_a = account_fixture()
+      account_b = account_fixture()
+
+      assert {:ok, _} = Audit.create_log_entry(account_a.id, valid_attrs(%{resource_id: "a-1"}))
+      assert {:ok, first_b} = Audit.create_log_entry(account_b.id, valid_attrs(%{resource_id: "b-1"}))
+
+      insert_raw_log(account_b.id, %{
+        sequence_number: 2,
+        previous_checksum: first_b.checksum,
+        checksum: String.duplicate("a", 64),
+        resource_id: "b-2-tampered"
+      })
+
+      report = Audit.verify_chain(account_a.id)
+
+      assert report.valid == true
+      assert report.total_entries == 1
+      assert report.first_failure == nil
+      assert report.sequence_gaps == []
+    end
+  end
+
+  defp account_fixture(attrs \\ %{}) do
+    defaults = %{name: "Verifier Account #{System.unique_integer([:positive])}"}
+    {:ok, account} = Accounts.create_account(Map.merge(defaults, attrs))
+    account
+  end
+
+  defp valid_attrs(overrides \\ %{}) do
+    defaults = %{
+      user_id: Ecto.UUID.generate(),
+      user_role: "admin",
+      session_id: "session-#{System.unique_integer([:positive])}",
+      action: "read",
+      resource_type: "patient",
+      resource_id: Ecto.UUID.generate(),
+      timestamp: ~U[2026-03-03 16:30:00Z],
+      source_ip: "127.0.0.1",
+      user_agent: "ExUnit",
+      outcome: "success",
+      failure_reason: nil,
+      phi_accessed: false,
+      metadata: %{"source" => "verifier_test"}
+    }
+
+    Map.merge(defaults, overrides)
+  end
+
+  defp checksum_for_sequence(account_id, hmac_key, sequence_number, previous_checksum, overrides \\ %{}) do
+    attrs =
+      valid_attrs(overrides)
+      |> Map.put(:account_id, account_id)
+      |> Map.put(:sequence_number, sequence_number)
+
+    Chain.compute_checksum(hmac_key, attrs, previous_checksum)
+  end
+
+  defp insert_raw_log(account_id, attrs) do
+    now = DateTime.utc_now() |> DateTime.truncate(:microsecond)
+    id = Ecto.UUID.generate()
+
+    row =
+      valid_attrs()
+      |> Map.merge(attrs)
+      |> Map.merge(%{
+        id: id,
+        account_id: account_id,
+        inserted_at: now,
+        updated_at: now
+      })
+
+    {1, _} = Repo.insert_all(Log, [row])
+    Repo.get!(Log, id)
+  end
+
+  defp insert_checkpoint(account_id, sequence_number, checksum) do
+    %Checkpoint{}
+    |> Checkpoint.changeset(%{
+      account_id: account_id,
+      sequence_number: sequence_number,
+      checksum: checksum
+    })
+    |> Repo.insert!()
+  end
+end

exec
/bin/zsh -lc 'td review td-1d7eca td-46c84b td-8a9954 td-486ab1 td-5806a7 td-7ae373 td-e596e4 td-39f158 td-cb684b td-bb90d7 td-c5e429 td-166913 td-dbf670 td-c4e66e td-f96191 td-54c8dc td-e39b5f td-ad7419 -m "Implemented for chain-verification takeoff pass; awaiting test execution once Elixir/OTP toolchain is aligned."' in /Users/tbrewer/projects/goodway/goodaudit succeeded in 384ms:
REVIEW REQUESTED td-486ab1 (session: ses_cded60)
Warning: auto-created minimal handoff for td-5806a7 - consider using 'td handoff' for better documentation
REVIEW REQUESTED td-5806a7 (session: ses_cded60)
Warning: auto-created minimal handoff for td-7ae373 - consider using 'td handoff' for better documentation
REVIEW REQUESTED td-7ae373 (session: ses_cded60)
Warning: auto-created minimal handoff for td-e596e4 - consider using 'td handoff' for better documentation
REVIEW REQUESTED td-e596e4 (session: ses_cded60)
Warning: auto-created minimal handoff for td-39f158 - consider using 'td handoff' for better documentation
REVIEW REQUESTED td-39f158 (session: ses_cded60)
Warning: auto-created minimal handoff for td-cb684b - consider using 'td handoff' for better documentation
REVIEW REQUESTED td-cb684b (session: ses_cded60)
Warning: auto-created minimal handoff for td-bb90d7 - consider using 'td handoff' for better documentation
REVIEW REQUESTED td-bb90d7 (session: ses_cded60)
Warning: auto-created minimal handoff for td-c5e429 - consider using 'td handoff' for better documentation
REVIEW REQUESTED td-c5e429 (session: ses_cded60)
Warning: auto-created minimal handoff for td-166913 - consider using 'td handoff' for better documentation
REVIEW REQUESTED td-166913 (session: ses_cded60)
Warning: auto-created minimal handoff for td-dbf670 - consider using 'td handoff' for better documentation
REVIEW REQUESTED td-dbf670 (session: ses_cded60)
Warning: auto-created minimal handoff for td-c4e66e - consider using 'td handoff' for better documentation
file update:
diff --git a/app/lib/app/application.ex b/app/lib/app/application.ex
index d865bfe6a48ed688366f7d85220490621cd809a7..08bab073bf607f5bb04aae846094b0b08ec40fe4
--- a/app/lib/app/application.ex
+++ b/app/lib/app/application.ex
@@ -14,6 +14,7 @@
       {Phoenix.PubSub, name: GA.PubSub},
       # Start a worker by calling: GA.Worker.start_link(arg)
       # {GA.Worker, arg},
+      {GA.Audit.CheckpointWorker, checkpoint_worker_opts()},
       # Start to serve requests, typically the last entry
       GAWeb.Endpoint
     ]
@@ -31,4 +32,8 @@
     GAWeb.Endpoint.config_change(changed, removed)
     :ok
   end
+
+  defp checkpoint_worker_opts do
+    Application.get_env(:app, GA.Audit.CheckpointWorker, [])
+  end
 end
diff --git a/app/lib/app/audit.ex b/app/lib/app/audit.ex
index a48830aacf4da8275b40f15016dcd16bbc87411f..2c7fbce41d873e721533728bcc1c965168addd3d
--- a/app/lib/app/audit.ex
+++ b/app/lib/app/audit.ex
@@ -7,7 +7,7 @@
   import Ecto.Query, warn: false
 
   alias GA.Accounts
-  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Audit.{Chain, Checkpoint, Log, Verifier}
   alias GA.Repo
 
   @default_limit 50
@@ -114,6 +114,12 @@
 
   def list_checkpoints(_), do: []
 
+  @doc """
+  Verifies chain integrity for a single account.
+  """
+  def verify_chain(account_id) when is_binary(account_id), do: Verifier.verify(account_id)
+  def verify_chain(_), do: {:error, :invalid_id}
+
   defp lock_account(account_id) do
     {key_a, key_b} = account_lock_keys(account_id)
 
diff --git a/app/lib/app/audit/checkpoint_worker.ex b/app/lib/app/audit/checkpoint_worker.ex
new file mode 100644
index 0000000000000000000000000000000000000000..f54908642ad7c854eae895b9605661f5beb0246f
--- /dev/null
+++ b/app/lib/app/audit/checkpoint_worker.ex
@@ -0,0 +1,82 @@
+defmodule GA.Audit.CheckpointWorker do
+  @moduledoc """
+  Periodically creates audit checkpoints for all active accounts.
+  """
+
+  use GenServer
+
+  import Ecto.Query, warn: false
+
+  require Logger
+
+  alias GA.Accounts.Account
+  alias GA.Audit
+  alias GA.Repo
+
+  @default_interval_ms :timer.hours(1)
+
+  def start_link(opts \\ []) do
+    name = Keyword.get(opts, :name, __MODULE__)
+    GenServer.start_link(__MODULE__, opts, name: name)
+  end
+
+  @impl true
+  def init(opts) do
+    interval_ms =
+      opts
+      |> Keyword.get(:interval_ms, configured_interval_ms())
+      |> normalize_interval()
+
+    schedule_tick(interval_ms)
+
+    {:ok, %{interval_ms: interval_ms}}
+  end
+
+  @impl true
+  def handle_info(:create_checkpoints, state) do
+    active_account_ids()
+    |> Enum.each(&create_checkpoint_for_account/1)
+
+    schedule_tick(state.interval_ms)
+    {:noreply, state}
+  end
+
+  defp active_account_ids do
+    from(account in Account,
+      where: account.status == :active,
+      select: account.id
+    )
+    |> Repo.all()
+  end
+
+  defp create_checkpoint_for_account(account_id) do
+    case Audit.create_checkpoint(account_id) do
+      {:ok, _checkpoint} ->
+        Logger.debug("checkpoint_worker created checkpoint for account=#{account_id}")
+
+      {:error, :no_entries} ->
+        Logger.debug("checkpoint_worker skipping account=#{account_id}: no audit entries")
+
+      {:error, reason} ->
+        Logger.error("checkpoint_worker failed for account=#{account_id}: #{inspect(reason)}")
+    end
+  rescue
+    error ->
+      Logger.error(
+        "checkpoint_worker crashed for account=#{account_id}: #{Exception.message(error)}"
+      )
+  end
+
+  defp schedule_tick(interval_ms) do
+    Process.send_after(self(), :create_checkpoints, interval_ms)
+  end
+
+  defp configured_interval_ms do
+    :app
+    |> Application.get_env(__MODULE__, [])
+    |> Keyword.get(:interval_ms, @default_interval_ms)
+  end
+
+  defp normalize_interval(interval_ms) when is_integer(interval_ms) and interval_ms > 0, do: interval_ms
+  defp normalize_interval(_), do: @default_interval_ms
+end
diff --git a/app/lib/app/audit/verifier.ex b/app/lib/app/audit/verifier.ex
new file mode 100644
index 0000000000000000000000000000000000000000..2a4bd77516a2c76ccc9b05de1778dd8781ded096
--- /dev/null
+++ b/app/lib/app/audit/verifier.ex
@@ -0,0 +1,203 @@
+defmodule GA.Audit.Verifier do
+  @moduledoc """
+  Streams and verifies account-scoped audit chains.
+  """
+
+  import Ecto.Query, warn: false
+
+  alias GA.Accounts
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  @batch_size 1000
+
+  @doc """
+  Verifies an account's audit chain and returns a report map.
+  """
+  @spec verify(binary()) :: map() | {:error, :invalid_id | :not_found}
+  def verify(account_id) when is_binary(account_id) do
+    started_at = System.monotonic_time(:millisecond)
+
+    with {:ok, hmac_key} <- Accounts.get_hmac_key(account_id) do
+      checkpoints_by_sequence = load_checkpoints(account_id)
+      report = stream_and_verify(account_id, hmac_key, checkpoints_by_sequence)
+
+      Map.put(report, :duration_ms, elapsed_ms(started_at))
+    end
+  end
+
+  def verify(_), do: {:error, :invalid_id}
+
+  defp stream_and_verify(account_id, hmac_key, checkpoints_by_sequence) do
+    initial =
+      new_accumulator()
+      |> Map.put(:account_id, account_id)
+
+    account_id
+    |> do_verify_batches(hmac_key, checkpoints_by_sequence, initial)
+    |> finalize_report()
+  end
+
+  defp do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, acc) do
+    batch = load_batch(account_id, acc.last_sequence)
+
+    case batch do
+      [] ->
+        acc
+
+      _entries ->
+        next_acc =
+          Enum.reduce(batch, acc, fn entry, state ->
+            verify_entry(entry, hmac_key, checkpoints_by_sequence, state)
+          end)
+
+        do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, next_acc)
+    end
+  end
+
+  defp verify_entry(%Log{} = entry, hmac_key, checkpoints_by_sequence, acc) do
+    acc
+    |> verify_sequence(entry)
+    |> verify_checksum(entry, hmac_key)
+    |> verify_checkpoint(entry, checkpoints_by_sequence)
+    |> advance_state(entry)
+  end
+
+  defp verify_sequence(acc, %Log{sequence_number: found_sequence}) do
+    expected_sequence = acc.expected_sequence
+
+    if found_sequence == expected_sequence do
+      %{acc | expected_sequence: expected_sequence + 1}
+    else
+      missing =
+        if found_sequence > expected_sequence do
+          Enum.to_list(expected_sequence..(found_sequence - 1))
+        else
+          []
+        end
+
+      gap = %{
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      }
+
+      acc
+      |> mark_invalid(%{
+        type: :sequence_gap,
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      })
+      |> Map.update!(:sequence_gaps, &[gap | &1])
+      |> Map.put(:expected_sequence, max(found_sequence, expected_sequence) + 1)
+    end
+  end
+
+  defp verify_checksum(acc, %Log{} = entry, hmac_key) do
+    if Chain.verify_checksum(hmac_key, entry, acc.previous_checksum) do
+      acc
+    else
+      mark_invalid(acc, %{
+        type: :checksum_mismatch,
+        sequence_number: entry.sequence_number,
+        stored_checksum: entry.checksum,
+        expected_checksum: expected_checksum(hmac_key, entry, acc.previous_checksum)
+      })
+    end
+  end
+
+  defp verify_checkpoint(acc, %Log{} = entry, checkpoints_by_sequence) do
+    case Map.get(checkpoints_by_sequence, entry.sequence_number) do
+      nil ->
+        acc
+
+      %Checkpoint{} = checkpoint ->
+        valid? = checkpoint.checksum == entry.checksum
+        result = %{sequence_number: entry.sequence_number, valid: valid?}
+        next_acc = Map.update!(acc, :checkpoint_results, &[result | &1])
+
+        if valid? do
+          next_acc
+        else
+          mark_invalid(next_acc, %{
+            type: :checkpoint_mismatch,
+            sequence_number: entry.sequence_number,
+            checkpoint_checksum: checkpoint.checksum,
+            entry_checksum: entry.checksum
+          })
+        end
+    end
+  end
+
+  defp advance_state(acc, %Log{} = entry) do
+    %{
+      acc
+      | previous_checksum: entry.checksum,
+        last_sequence: entry.sequence_number,
+        total_entries: acc.total_entries + 1,
+        verified_entries: acc.verified_entries + 1
+    }
+  end
+
+  defp expected_checksum(hmac_key, entry, previous_checksum) do
+    Chain.compute_checksum(hmac_key, Map.from_struct(entry), previous_checksum)
+  rescue
+    _ -> nil
+  end
+
+  defp load_batch(account_id, after_sequence) do
+    from(log in Log,
+      where: log.account_id == ^account_id and log.sequence_number > ^after_sequence,
+      order_by: [asc: log.sequence_number],
+      limit: @batch_size
+    )
+    |> Repo.all()
+  end
+
+  defp load_checkpoints(account_id) do
+    from(checkpoint in Checkpoint, where: checkpoint.account_id == ^account_id)
+    |> Repo.all()
+    |> Map.new(fn checkpoint -> {checkpoint.sequence_number, checkpoint} end)
+  end
+
+  defp new_accumulator do
+    %{
+      account_id: nil,
+      valid: true,
+      total_entries: 0,
+      verified_entries: 0,
+      expected_sequence: 1,
+      previous_checksum: nil,
+      last_sequence: 0,
+      first_failure: nil,
+      sequence_gaps: [],
+      checkpoint_results: []
+    }
+  end
+
+  defp finalize_report(acc) do
+    %{
+      valid: acc.valid,
+      total_entries: acc.total_entries,
+      verified_entries: acc.verified_entries,
+      first_failure: acc.first_failure,
+      sequence_gaps: Enum.reverse(acc.sequence_gaps),
+      checkpoint_results: Enum.reverse(acc.checkpoint_results),
+      duration_ms: 0
+    }
+  end
+
+  defp mark_invalid(acc, failure) do
+    case acc.first_failure do
+      nil -> %{acc | valid: false, first_failure: failure}
+      _existing -> %{acc | valid: false}
+    end
+  end
+
+  defp elapsed_ms(started_at) do
+    System.monotonic_time(:millisecond)
+    |> Kernel.-(started_at)
+    |> max(0)
+  end
+end
diff --git a/app/test/app/audit/checkpoint_worker_test.exs b/app/test/app/audit/checkpoint_worker_test.exs
new file mode 100644
index 0000000000000000000000000000000000000000..ec9ea5e45a2b2d84cd0e7872a7bf7288d8e3ea11
--- /dev/null
+++ b/app/test/app/audit/checkpoint_worker_test.exs
@@ -0,0 +1,111 @@
+defmodule GA.Audit.CheckpointWorkerTest do
+  use GA.DataCase, async: false
+
+  import ExUnit.CaptureLog
+
+  alias GA.Accounts
+  alias GA.Audit
+  alias GA.Audit.CheckpointWorker
+
+  describe "checkpoint worker ticks" do
+    test "creates checkpoints for all active accounts" do
+      account_a = account_fixture()
+      account_b = account_fixture()
+      add_log(account_a.id, "active-a")
+      add_log(account_b.id, "active-b")
+
+      worker = start_worker()
+      send(worker, :create_checkpoints)
+
+      assert_eventually(fn ->
+        length(Audit.list_checkpoints(account_a.id)) == 1 and
+          length(Audit.list_checkpoints(account_b.id)) == 1
+      end)
+    end
+
+    test "skips suspended accounts" do
+      active_account = account_fixture()
+      suspended_account = account_fixture(%{status: :suspended})
+      add_log(active_account.id, "active")
+      add_log(suspended_account.id, "suspended")
+
+      worker = start_worker()
+      send(worker, :create_checkpoints)
+
+      assert_eventually(fn ->
+        length(Audit.list_checkpoints(active_account.id)) == 1
+      end)
+
+      assert Audit.list_checkpoints(suspended_account.id) == []
+    end
+
+    test "logs no-entry accounts and continues processing" do
+      empty_account = account_fixture()
+      populated_account = account_fixture()
+      add_log(populated_account.id, "populated")
+
+      worker = start_worker()
+
+      log =
+        capture_log([level: :debug], fn ->
+          send(worker, :create_checkpoints)
+
+          assert_eventually(fn ->
+            length(Audit.list_checkpoints(populated_account.id)) == 1
+          end)
+        end)
+
+      assert log =~ "checkpoint_worker skipping"
+      assert log =~ empty_account.id
+      assert Audit.list_checkpoints(empty_account.id) == []
+    end
+  end
+
+  defp start_worker do
+    start_supervised!(
+      {CheckpointWorker,
+       [
+         name: {:global, {:checkpoint_worker_test, System.unique_integer([:positive])}},
+         interval_ms: 60_000
+       ]}
+    )
+  end
+
+  defp account_fixture(attrs \\ %{}) do
+    defaults = %{name: "Worker Account #{System.unique_integer([:positive])}"}
+    {:ok, account} = Accounts.create_account(Map.merge(defaults, attrs))
+    account
+  end
+
+  defp add_log(account_id, resource_id) do
+    attrs = %{
+      user_id: Ecto.UUID.generate(),
+      user_role: "admin",
+      session_id: "session-#{System.unique_integer([:positive])}",
+      action: "read",
+      resource_type: "patient",
+      resource_id: resource_id,
+      timestamp: ~U[2026-03-03 16:30:00Z],
+      source_ip: "127.0.0.1",
+      user_agent: "ExUnit",
+      outcome: "success",
+      failure_reason: nil,
+      phi_accessed: false,
+      metadata: %{"source" => "checkpoint_worker_test"}
+    }
+
+    assert {:ok, _} = Audit.create_log_entry(account_id, attrs)
+  end
+
+  defp assert_eventually(fun, attempts \\ 50)
+  defp assert_eventually(fun, 0), do: assert(fun.())
+
+  defp assert_eventually(fun, attempts) do
+    if fun.() do
+      :ok
+    else
+      Process.sleep(20)
+      assert_eventually(fun, attempts - 1)
+    end
+  end
+end
diff --git a/app/test/app/audit/verifier_test.exs b/app/test/app/audit/verifier_test.exs
new file mode 100644
index 0000000000000000000000000000000000000000..430e2450186c77bb9e816cfa26910a774995b717
--- /dev/null
+++ b/app/test/app/audit/verifier_test.exs
@@ -0,0 +1,189 @@
+defmodule GA.Audit.VerifierTest do
+  use GA.DataCase, async: false
+
+  alias GA.Accounts
+  alias GA.Audit
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  describe "verify_chain/1" do
+    test "returns a valid report for an intact account chain" do
+      account = account_fixture()
+
+      assert {:ok, _first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "r-1"}))
+      assert {:ok, _second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "r-2"}))
+      assert {:ok, checkpoint} = Audit.create_checkpoint(account.id)
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == true
+      assert report.total_entries == 2
+      assert report.verified_entries == 2
+      assert report.first_failure == nil
+      assert report.sequence_gaps == []
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == checkpoint.sequence_number and &1.valid))
+      assert is_integer(report.duration_ms)
+      assert report.duration_ms >= 0
+    end
+
+    test "detects tampered checksum entries" do
+      account = account_fixture()
+
+      assert {:ok, first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "seed"}))
+
+      insert_raw_log(account.id, %{
+        sequence_number: 2,
+        previous_checksum: first.checksum,
+        checksum: String.duplicate("0", 64),
+        resource_id: "tampered"
+      })
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert report.first_failure.type == :checksum_mismatch
+      assert report.first_failure.sequence_number == 2
+      assert report.first_failure.stored_checksum == String.duplicate("0", 64)
+      refute is_nil(report.first_failure.expected_checksum)
+    end
+
+    test "detects sequence gaps within an account" do
+      account = account_fixture()
+      assert {:ok, _first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "gap-1"}))
+      assert {:ok, second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "gap-2"}))
+      assert {:ok, hmac_key} = Accounts.get_hmac_key(account.id)
+
+      checksum =
+        checksum_for_sequence(
+          account.id,
+          hmac_key,
+          4,
+          second.checksum,
+          %{resource_id: "gap-4"}
+        )
+
+      insert_raw_log(account.id, %{
+        sequence_number: 4,
+        previous_checksum: second.checksum,
+        checksum: checksum,
+        resource_id: "gap-4"
+      })
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert report.first_failure.type == :sequence_gap
+      assert [%{expected: 3, found: 4, missing: [3]}] = report.sequence_gaps
+    end
+
+    test "validates checkpoint anchors and reports invalid anchors" do
+      account = account_fixture()
+      assert {:ok, first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "cp-1"}))
+      assert {:ok, second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "cp-2"}))
+
+      insert_checkpoint(account.id, first.sequence_number, first.checksum)
+      insert_checkpoint(account.id, second.sequence_number, String.duplicate("f", 64))
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == first.sequence_number and &1.valid))
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == second.sequence_number and not &1.valid))
+    end
+
+    test "includes non-negative duration tracking" do
+      account = account_fixture()
+      assert {:ok, _} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "duration"}))
+
+      report = Audit.verify_chain(account.id)
+
+      assert is_integer(report.duration_ms)
+      assert report.duration_ms >= 0
+    end
+
+    test "verification remains account-scoped" do
+      account_a = account_fixture()
+      account_b = account_fixture()
+
+      assert {:ok, _} = Audit.create_log_entry(account_a.id, valid_attrs(%{resource_id: "a-1"}))
+      assert {:ok, first_b} = Audit.create_log_entry(account_b.id, valid_attrs(%{resource_id: "b-1"}))
+
+      insert_raw_log(account_b.id, %{
+        sequence_number: 2,
+        previous_checksum: first_b.checksum,
+        checksum: String.duplicate("a", 64),
+        resource_id: "b-2-tampered"
+      })
+
+      report = Audit.verify_chain(account_a.id)
+
+      assert report.valid == true
+      assert report.total_entries == 1
+      assert report.first_failure == nil
+      assert report.sequence_gaps == []
+    end
+  end
+
+  defp account_fixture(attrs \\ %{}) do
+    defaults = %{name: "Verifier Account #{System.unique_integer([:positive])}"}
+    {:ok, account} = Accounts.create_account(Map.merge(defaults, attrs))
+    account
+  end
+
+  defp valid_attrs(overrides \\ %{}) do
+    defaults = %{
+      user_id: Ecto.UUID.generate(),
+      user_role: "admin",
+      session_id: "session-#{System.unique_integer([:positive])}",
+      action: "read",
+      resource_type: "patient",
+      resource_id: Ecto.UUID.generate(),
+      timestamp: ~U[2026-03-03 16:30:00Z],
+      source_ip: "127.0.0.1",
+      user_agent: "ExUnit",
+      outcome: "success",
+      failure_reason: nil,
+      phi_accessed: false,
+      metadata: %{"source" => "verifier_test"}
+    }
+
+    Map.merge(defaults, overrides)
+  end
+
+  defp checksum_for_sequence(account_id, hmac_key, sequence_number, previous_checksum, overrides \\ %{}) do
+    attrs =
+      valid_attrs(overrides)
+      |> Map.put(:account_id, account_id)
+      |> Map.put(:sequence_number, sequence_number)
+
+    Chain.compute_checksum(hmac_key, attrs, previous_checksum)
+  end
+
+  defp insert_raw_log(account_id, attrs) do
+    now = DateTime.utc_now() |> DateTime.truncate(:microsecond)
+    id = Ecto.UUID.generate()
+
+    row =
+      valid_attrs()
+      |> Map.merge(attrs)
+      |> Map.merge(%{
+        id: id,
+        account_id: account_id,
+        inserted_at: now,
+        updated_at: now
+      })
+
+    {1, _} = Repo.insert_all(Log, [row])
+    Repo.get!(Log, id)
+  end
+
+  defp insert_checkpoint(account_id, sequence_number, checksum) do
+    %Checkpoint{}
+    |> Checkpoint.changeset(%{
+      account_id: account_id,
+      sequence_number: sequence_number,
+      checksum: checksum
+    })
+    |> Repo.insert!()
+  end
+end

exec
/bin/zsh -lc 'td tree td-9db2d6' in /Users/tbrewer/projects/goodway/goodaudit succeeded in 52ms:
epic td-9db2d6: Chain Verification
├── task td-1d7eca: Create GA.Audit.Verifier module [in_review]  ⧗
├── task td-46c84b: Add GA.Audit.verify_chain(account_id) delegation [in_review]  ⧗
├── task td-8a9954: Implement verify(account_id) streaming entrypoint [in_review]  ⧗
├── task td-486ab1: Implement do_verify_batches/4 recursive batch loader [in_review]  ⧗
├── task td-5806a7: Implement verify_entry/4 chain and checkpoint checks [in_review]  ⧗
├── task td-7ae373: Create GA.Audit.CheckpointWorker GenServer module [in_review]  ⧗
├── task td-e596e4: Implement CheckpointWorker init/1 scheduling [in_review]  ⧗
├── task td-39f158: Implement checkpoint tick over active accounts [in_review]  ⧗
├── task td-cb684b: Add CheckpointWorker to GA.Application children [in_review]  ⧗
├── task td-bb90d7: Test valid chain verification report [in_review]  ⧗
├── task td-c5e429: Test tampered checksum detection [in_review]  ⧗
├── task td-166913: Test sequence gap detection within account [in_review]  ⧗
├── task td-dbf670: Test checkpoint anchor validation outcomes [in_review]  ⧗
├── task td-c4e66e: Test verification duration tracking [in_review]  ⧗
├── task td-f96191: Test account-scoped verification isolation [in_review]  ⧗
├── task td-54c8dc: Test worker checkpoints all active accounts [in_review]  ⧗
├── task td-e39b5f: Test worker skips suspended accounts [in_review]  ⧗
├── task td-ad7419: Test worker handles accounts with no entries [in_review]  ⧗
└── task td-833971: Review all work for Chain Verification [open] 
file update:
diff --git a/app/lib/app/application.ex b/app/lib/app/application.ex
index d865bfe6a48ed688366f7d85220490621cd809a7..08bab073bf607f5bb04aae846094b0b08ec40fe4
--- a/app/lib/app/application.ex
+++ b/app/lib/app/application.ex
@@ -14,6 +14,7 @@
       {Phoenix.PubSub, name: GA.PubSub},
       # Start a worker by calling: GA.Worker.start_link(arg)
       # {GA.Worker, arg},
+      {GA.Audit.CheckpointWorker, checkpoint_worker_opts()},
       # Start to serve requests, typically the last entry
       GAWeb.Endpoint
     ]
@@ -31,4 +32,8 @@
     GAWeb.Endpoint.config_change(changed, removed)
     :ok
   end
+
+  defp checkpoint_worker_opts do
+    Application.get_env(:app, GA.Audit.CheckpointWorker, [])
+  end
 end
diff --git a/app/lib/app/audit.ex b/app/lib/app/audit.ex
index a48830aacf4da8275b40f15016dcd16bbc87411f..2c7fbce41d873e721533728bcc1c965168addd3d
--- a/app/lib/app/audit.ex
+++ b/app/lib/app/audit.ex
@@ -7,7 +7,7 @@
   import Ecto.Query, warn: false
 
   alias GA.Accounts
-  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Audit.{Chain, Checkpoint, Log, Verifier}
   alias GA.Repo
 
   @default_limit 50
@@ -114,6 +114,12 @@
 
   def list_checkpoints(_), do: []
 
+  @doc """
+  Verifies chain integrity for a single account.
+  """
+  def verify_chain(account_id) when is_binary(account_id), do: Verifier.verify(account_id)
+  def verify_chain(_), do: {:error, :invalid_id}
+
   defp lock_account(account_id) do
     {key_a, key_b} = account_lock_keys(account_id)
 
diff --git a/app/lib/app/audit/checkpoint_worker.ex b/app/lib/app/audit/checkpoint_worker.ex
new file mode 100644
index 0000000000000000000000000000000000000000..f54908642ad7c854eae895b9605661f5beb0246f
--- /dev/null
+++ b/app/lib/app/audit/checkpoint_worker.ex
@@ -0,0 +1,82 @@
+defmodule GA.Audit.CheckpointWorker do
+  @moduledoc """
+  Periodically creates audit checkpoints for all active accounts.
+  """
+
+  use GenServer
+
+  import Ecto.Query, warn: false
+
+  require Logger
+
+  alias GA.Accounts.Account
+  alias GA.Audit
+  alias GA.Repo
+
+  @default_interval_ms :timer.hours(1)
+
+  def start_link(opts \\ []) do
+    name = Keyword.get(opts, :name, __MODULE__)
+    GenServer.start_link(__MODULE__, opts, name: name)
+  end
+
+  @impl true
+  def init(opts) do
+    interval_ms =
+      opts
+      |> Keyword.get(:interval_ms, configured_interval_ms())
+      |> normalize_interval()
+
+    schedule_tick(interval_ms)
+
+    {:ok, %{interval_ms: interval_ms}}
+  end
+
+  @impl true
+  def handle_info(:create_checkpoints, state) do
+    active_account_ids()
+    |> Enum.each(&create_checkpoint_for_account/1)
+
+    schedule_tick(state.interval_ms)
+    {:noreply, state}
+  end
+
+  defp active_account_ids do
+    from(account in Account,
+      where: account.status == :active,
+      select: account.id
+    )
+    |> Repo.all()
+  end
+
+  defp create_checkpoint_for_account(account_id) do
+    case Audit.create_checkpoint(account_id) do
+      {:ok, _checkpoint} ->
+        Logger.debug("checkpoint_worker created checkpoint for account=#{account_id}")
+
+      {:error, :no_entries} ->
+        Logger.debug("checkpoint_worker skipping account=#{account_id}: no audit entries")
+
+      {:error, reason} ->
+        Logger.error("checkpoint_worker failed for account=#{account_id}: #{inspect(reason)}")
+    end
+  rescue
+    error ->
+      Logger.error(
+        "checkpoint_worker crashed for account=#{account_id}: #{Exception.message(error)}"
+      )
+  end
+
+  defp schedule_tick(interval_ms) do
+    Process.send_after(self(), :create_checkpoints, interval_ms)
+  end
+
+  defp configured_interval_ms do
+    :app
+    |> Application.get_env(__MODULE__, [])
+    |> Keyword.get(:interval_ms, @default_interval_ms)
+  end
+
+  defp normalize_interval(interval_ms) when is_integer(interval_ms) and interval_ms > 0, do: interval_ms
+  defp normalize_interval(_), do: @default_interval_ms
+end
diff --git a/app/lib/app/audit/verifier.ex b/app/lib/app/audit/verifier.ex
new file mode 100644
index 0000000000000000000000000000000000000000..2a4bd77516a2c76ccc9b05de1778dd8781ded096
--- /dev/null
+++ b/app/lib/app/audit/verifier.ex
@@ -0,0 +1,203 @@
+defmodule GA.Audit.Verifier do
+  @moduledoc """
+  Streams and verifies account-scoped audit chains.
+  """
+
+  import Ecto.Query, warn: false
+
+  alias GA.Accounts
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  @batch_size 1000
+
+  @doc """
+  Verifies an account's audit chain and returns a report map.
+  """
+  @spec verify(binary()) :: map() | {:error, :invalid_id | :not_found}
+  def verify(account_id) when is_binary(account_id) do
+    started_at = System.monotonic_time(:millisecond)
+
+    with {:ok, hmac_key} <- Accounts.get_hmac_key(account_id) do
+      checkpoints_by_sequence = load_checkpoints(account_id)
+      report = stream_and_verify(account_id, hmac_key, checkpoints_by_sequence)
+
+      Map.put(report, :duration_ms, elapsed_ms(started_at))
+    end
+  end
+
+  def verify(_), do: {:error, :invalid_id}
+
+  defp stream_and_verify(account_id, hmac_key, checkpoints_by_sequence) do
+    initial =
+      new_accumulator()
+      |> Map.put(:account_id, account_id)
+
+    account_id
+    |> do_verify_batches(hmac_key, checkpoints_by_sequence, initial)
+    |> finalize_report()
+  end
+
+  defp do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, acc) do
+    batch = load_batch(account_id, acc.last_sequence)
+
+    case batch do
+      [] ->
+        acc
+
+      _entries ->
+        next_acc =
+          Enum.reduce(batch, acc, fn entry, state ->
+            verify_entry(entry, hmac_key, checkpoints_by_sequence, state)
+          end)
+
+        do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, next_acc)
+    end
+  end
+
+  defp verify_entry(%Log{} = entry, hmac_key, checkpoints_by_sequence, acc) do
+    acc
+    |> verify_sequence(entry)
+    |> verify_checksum(entry, hmac_key)
+    |> verify_checkpoint(entry, checkpoints_by_sequence)
+    |> advance_state(entry)
+  end
+
+  defp verify_sequence(acc, %Log{sequence_number: found_sequence}) do
+    expected_sequence = acc.expected_sequence
+
+    if found_sequence == expected_sequence do
+      %{acc | expected_sequence: expected_sequence + 1}
+    else
+      missing =
+        if found_sequence > expected_sequence do
+          Enum.to_list(expected_sequence..(found_sequence - 1))
+        else
+          []
+        end
+
+      gap = %{
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      }
+
+      acc
+      |> mark_invalid(%{
+        type: :sequence_gap,
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      })
+      |> Map.update!(:sequence_gaps, &[gap | &1])
+      |> Map.put(:expected_sequence, max(found_sequence, expected_sequence) + 1)
+    end
+  end
+
+  defp verify_checksum(acc, %Log{} = entry, hmac_key) do
+    if Chain.verify_checksum(hmac_key, entry, acc.previous_checksum) do
+      acc
+    else
+      mark_invalid(acc, %{
+        type: :checksum_mismatch,
+        sequence_number: entry.sequence_number,
+        stored_checksum: entry.checksum,
+        expected_checksum: expected_checksum(hmac_key, entry, acc.previous_checksum)
+      })
+    end
+  end
+
+  defp verify_checkpoint(acc, %Log{} = entry, checkpoints_by_sequence) do
+    case Map.get(checkpoints_by_sequence, entry.sequence_number) do
+      nil ->
+        acc
+
+      %Checkpoint{} = checkpoint ->
+        valid? = checkpoint.checksum == entry.checksum
+        result = %{sequence_number: entry.sequence_number, valid: valid?}
+        next_acc = Map.update!(acc, :checkpoint_results, &[result | &1])
+
+        if valid? do
+          next_acc
+        else
+          mark_invalid(next_acc, %{
+            type: :checkpoint_mismatch,
+            sequence_number: entry.sequence_number,
+            checkpoint_checksum: checkpoint.checksum,
+            entry_checksum: entry.checksum
+          })
+        end
+    end
+  end
+
+  defp advance_state(acc, %Log{} = entry) do
+    %{
+      acc
+      | previous_checksum: entry.checksum,
+        last_sequence: entry.sequence_number,
+        total_entries: acc.total_entries + 1,
+        verified_entries: acc.verified_entries + 1
+    }
+  end
+
+  defp expected_checksum(hmac_key, entry, previous_checksum) do
+    Chain.compute_checksum(hmac_key, Map.from_struct(entry), previous_checksum)
+  rescue
+    _ -> nil
+  end
+
+  defp load_batch(account_id, after_sequence) do
+    from(log in Log,
+      where: log.account_id == ^account_id and log.sequence_number > ^after_sequence,
+      order_by: [asc: log.sequence_number],
+      limit: @batch_size
+    )
+    |> Repo.all()
+  end
+
+  defp load_checkpoints(account_id) do
+    from(checkpoint in Checkpoint, where: checkpoint.account_id == ^account_id)
+    |> Repo.all()
+    |> Map.new(fn checkpoint -> {checkpoint.sequence_number, checkpoint} end)
+  end
+
+  defp new_accumulator do
+    %{
+      account_id: nil,
+      valid: true,
+      total_entries: 0,
+      verified_entries: 0,
+      expected_sequence: 1,
+      previous_checksum: nil,
+      last_sequence: 0,
+      first_failure: nil,
+      sequence_gaps: [],
+      checkpoint_results: []
+    }
+  end
+
+  defp finalize_report(acc) do
+    %{
+      valid: acc.valid,
+      total_entries: acc.total_entries,
+      verified_entries: acc.verified_entries,
+      first_failure: acc.first_failure,
+      sequence_gaps: Enum.reverse(acc.sequence_gaps),
+      checkpoint_results: Enum.reverse(acc.checkpoint_results),
+      duration_ms: 0
+    }
+  end
+
+  defp mark_invalid(acc, failure) do
+    case acc.first_failure do
+      nil -> %{acc | valid: false, first_failure: failure}
+      _existing -> %{acc | valid: false}
+    end
+  end
+
+  defp elapsed_ms(started_at) do
+    System.monotonic_time(:millisecond)
+    |> Kernel.-(started_at)
+    |> max(0)
+  end
+end
diff --git a/app/test/app/audit/checkpoint_worker_test.exs b/app/test/app/audit/checkpoint_worker_test.exs
new file mode 100644
index 0000000000000000000000000000000000000000..ec9ea5e45a2b2d84cd0e7872a7bf7288d8e3ea11
--- /dev/null
+++ b/app/test/app/audit/checkpoint_worker_test.exs
@@ -0,0 +1,111 @@
+defmodule GA.Audit.CheckpointWorkerTest do
+  use GA.DataCase, async: false
+
+  import ExUnit.CaptureLog
+
+  alias GA.Accounts
+  alias GA.Audit
+  alias GA.Audit.CheckpointWorker
+
+  describe "checkpoint worker ticks" do
+    test "creates checkpoints for all active accounts" do
+      account_a = account_fixture()
+      account_b = account_fixture()
+      add_log(account_a.id, "active-a")
+      add_log(account_b.id, "active-b")
+
+      worker = start_worker()
+      send(worker, :create_checkpoints)
+
+      assert_eventually(fn ->
+        length(Audit.list_checkpoints(account_a.id)) == 1 and
+          length(Audit.list_checkpoints(account_b.id)) == 1
+      end)
+    end
+
+    test "skips suspended accounts" do
+      active_account = account_fixture()
+      suspended_account = account_fixture(%{status: :suspended})
+      add_log(active_account.id, "active")
+      add_log(suspended_account.id, "suspended")
+
+      worker = start_worker()
+      send(worker, :create_checkpoints)
+
+      assert_eventually(fn ->
+        length(Audit.list_checkpoints(active_account.id)) == 1
+      end)
+
+      assert Audit.list_checkpoints(suspended_account.id) == []
+    end
+
+    test "logs no-entry accounts and continues processing" do
+      empty_account = account_fixture()
+      populated_account = account_fixture()
+      add_log(populated_account.id, "populated")
+
+      worker = start_worker()
+
+      log =
+        capture_log([level: :debug], fn ->
+          send(worker, :create_checkpoints)
+
+          assert_eventually(fn ->
+            length(Audit.list_checkpoints(populated_account.id)) == 1
+          end)
+        end)
+
+      assert log =~ "checkpoint_worker skipping"
+      assert log =~ empty_account.id
+      assert Audit.list_checkpoints(empty_account.id) == []
+    end
+  end
+
+  defp start_worker do
+    start_supervised!(
+      {CheckpointWorker,
+       [
+         name: {:global, {:checkpoint_worker_test, System.unique_integer([:positive])}},
+         interval_ms: 60_000
+       ]}
+    )
+  end
+
+  defp account_fixture(attrs \\ %{}) do
+    defaults = %{name: "Worker Account #{System.unique_integer([:positive])}"}
+    {:ok, account} = Accounts.create_account(Map.merge(defaults, attrs))
+    account
+  end
+
+  defp add_log(account_id, resource_id) do
+    attrs = %{
+      user_id: Ecto.UUID.generate(),
+      user_role: "admin",
+      session_id: "session-#{System.unique_integer([:positive])}",
+      action: "read",
+      resource_type: "patient",
+      resource_id: resource_id,
+      timestamp: ~U[2026-03-03 16:30:00Z],
+      source_ip: "127.0.0.1",
+      user_agent: "ExUnit",
+      outcome: "success",
+      failure_reason: nil,
+      phi_accessed: false,
+      metadata: %{"source" => "checkpoint_worker_test"}
+    }
+
+    assert {:ok, _} = Audit.create_log_entry(account_id, attrs)
+  end
+
+  defp assert_eventually(fun, attempts \\ 50)
+  defp assert_eventually(fun, 0), do: assert(fun.())
+
+  defp assert_eventually(fun, attempts) do
+    if fun.() do
+      :ok
+    else
+      Process.sleep(20)
+      assert_eventually(fun, attempts - 1)
+    end
+  end
+end
diff --git a/app/test/app/audit/verifier_test.exs b/app/test/app/audit/verifier_test.exs
new file mode 100644
index 0000000000000000000000000000000000000000..430e2450186c77bb9e816cfa26910a774995b717
--- /dev/null
+++ b/app/test/app/audit/verifier_test.exs
@@ -0,0 +1,189 @@
+defmodule GA.Audit.VerifierTest do
+  use GA.DataCase, async: false
+
+  alias GA.Accounts
+  alias GA.Audit
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  describe "verify_chain/1" do
+    test "returns a valid report for an intact account chain" do
+      account = account_fixture()
+
+      assert {:ok, _first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "r-1"}))
+      assert {:ok, _second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "r-2"}))
+      assert {:ok, checkpoint} = Audit.create_checkpoint(account.id)
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == true
+      assert report.total_entries == 2
+      assert report.verified_entries == 2
+      assert report.first_failure == nil
+      assert report.sequence_gaps == []
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == checkpoint.sequence_number and &1.valid))
+      assert is_integer(report.duration_ms)
+      assert report.duration_ms >= 0
+    end
+
+    test "detects tampered checksum entries" do
+      account = account_fixture()
+
+      assert {:ok, first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "seed"}))
+
+      insert_raw_log(account.id, %{
+        sequence_number: 2,
+        previous_checksum: first.checksum,
+        checksum: String.duplicate("0", 64),
+        resource_id: "tampered"
+      })
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert report.first_failure.type == :checksum_mismatch
+      assert report.first_failure.sequence_number == 2
+      assert report.first_failure.stored_checksum == String.duplicate("0", 64)
+      refute is_nil(report.first_failure.expected_checksum)
+    end
+
+    test "detects sequence gaps within an account" do
+      account = account_fixture()
+      assert {:ok, _first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "gap-1"}))
+      assert {:ok, second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "gap-2"}))
+      assert {:ok, hmac_key} = Accounts.get_hmac_key(account.id)
+
+      checksum =
+        checksum_for_sequence(
+          account.id,
+          hmac_key,
+          4,
+          second.checksum,
+          %{resource_id: "gap-4"}
+        )
+
+      insert_raw_log(account.id, %{
+        sequence_number: 4,
+        previous_checksum: second.checksum,
+        checksum: checksum,
+        resource_id: "gap-4"
+      })
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert report.first_failure.type == :sequence_gap
+      assert [%{expected: 3, found: 4, missing: [3]}] = report.sequence_gaps
+    end
+
+    test "validates checkpoint anchors and reports invalid anchors" do
+      account = account_fixture()
+      assert {:ok, first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "cp-1"}))
+      assert {:ok, second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "cp-2"}))
+
+      insert_checkpoint(account.id, first.sequence_number, first.checksum)
+      insert_checkpoint(account.id, second.sequence_number, String.duplicate("f", 64))
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == first.sequence_number and &1.valid))
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == second.sequence_number and not &1.valid))
+    end
+
+    test "includes non-negative duration tracking" do
+      account = account_fixture()
+      assert {:ok, _} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "duration"}))
+
+      report = Audit.verify_chain(account.id)
+
+      assert is_integer(report.duration_ms)
+      assert report.duration_ms >= 0
+    end
+
+    test "verification remains account-scoped" do
+      account_a = account_fixture()
+      account_b = account_fixture()
+
+      assert {:ok, _} = Audit.create_log_entry(account_a.id, valid_attrs(%{resource_id: "a-1"}))
+      assert {:ok, first_b} = Audit.create_log_entry(account_b.id, valid_attrs(%{resource_id: "b-1"}))
+
+      insert_raw_log(account_b.id, %{
+        sequence_number: 2,
+        previous_checksum: first_b.checksum,
+        checksum: String.duplicate("a", 64),
+        resource_id: "b-2-tampered"
+      })
+
+      report = Audit.verify_chain(account_a.id)
+
+      assert report.valid == true
+      assert report.total_entries == 1
+      assert report.first_failure == nil
+      assert report.sequence_gaps == []
+    end
+  end
+
+  defp account_fixture(attrs \\ %{}) do
+    defaults = %{name: "Verifier Account #{System.unique_integer([:positive])}"}
+    {:ok, account} = Accounts.create_account(Map.merge(defaults, attrs))
+    account
+  end
+
+  defp valid_attrs(overrides \\ %{}) do
+    defaults = %{
+      user_id: Ecto.UUID.generate(),
+      user_role: "admin",
+      session_id: "session-#{System.unique_integer([:positive])}",
+      action: "read",
+      resource_type: "patient",
+      resource_id: Ecto.UUID.generate(),
+      timestamp: ~U[2026-03-03 16:30:00Z],
+      source_ip: "127.0.0.1",
+      user_agent: "ExUnit",
+      outcome: "success",
+      failure_reason: nil,
+      phi_accessed: false,
+      metadata: %{"source" => "verifier_test"}
+    }
+
+    Map.merge(defaults, overrides)
+  end
+
+  defp checksum_for_sequence(account_id, hmac_key, sequence_number, previous_checksum, overrides \\ %{}) do
+    attrs =
+      valid_attrs(overrides)
+      |> Map.put(:account_id, account_id)
+      |> Map.put(:sequence_number, sequence_number)
+
+    Chain.compute_checksum(hmac_key, attrs, previous_checksum)
+  end
+
+  defp insert_raw_log(account_id, attrs) do
+    now = DateTime.utc_now() |> DateTime.truncate(:microsecond)
+    id = Ecto.UUID.generate()
+
+    row =
+      valid_attrs()
+      |> Map.merge(attrs)
+      |> Map.merge(%{
+        id: id,
+        account_id: account_id,
+        inserted_at: now,
+        updated_at: now
+      })
+
+    {1, _} = Repo.insert_all(Log, [row])
+    Repo.get!(Log, id)
+  end
+
+  defp insert_checkpoint(account_id, sequence_number, checksum) do
+    %Checkpoint{}
+    |> Checkpoint.changeset(%{
+      account_id: account_id,
+      sequence_number: sequence_number,
+      checksum: checksum
+    })
+    |> Repo.insert!()
+  end
+end

exec
/bin/zsh -lc 'td ws current' in /Users/tbrewer/projects/goodway/goodaudit succeeded in 52ms:
WORK SESSION: ws-e303 "Epic: Chain Verification"
Started: 5m ago
Git: 0c29eaa → 0c29eaa (+0 commits)

TAGGED ISSUES:
  td-1d7eca  Create GA.Audit.Verifier module  [in_review]  P2
  td-46c84b  Add GA.Audit.verify_chain(account_id) delegation  [in_review]  P2
  td-8a9954  Implement verify(account_id) streaming entrypoint  [in_review]  P2
  td-486ab1  Implement do_verify_batches/4 recursive batch loader  [in_review]  P2
  td-5806a7  Implement verify_entry/4 chain and checkpoint checks  [in_review]  P2
  td-7ae373  Create GA.Audit.CheckpointWorker GenServer module  [in_review]  P2
  td-e596e4  Implement CheckpointWorker init/1 scheduling  [in_review]  P2
  td-39f158  Implement checkpoint tick over active accounts  [in_review]  P2
  td-cb684b  Add CheckpointWorker to GA.Application children  [in_review]  P2
  td-bb90d7  Test valid chain verification report  [in_review]  P2
  td-c5e429  Test tampered checksum detection  [in_review]  P2
  td-166913  Test sequence gap detection within account  [in_review]  P2
  td-dbf670  Test checkpoint anchor validation outcomes  [in_review]  P2
  td-c4e66e  Test verification duration tracking  [in_review]  P2
  td-f96191  Test account-scoped verification isolation  [in_review]  P2
file update:
diff --git a/app/lib/app/application.ex b/app/lib/app/application.ex
index d865bfe6a48ed688366f7d85220490621cd809a7..08bab073bf607f5bb04aae846094b0b08ec40fe4
--- a/app/lib/app/application.ex
+++ b/app/lib/app/application.ex
@@ -14,6 +14,7 @@
       {Phoenix.PubSub, name: GA.PubSub},
       # Start a worker by calling: GA.Worker.start_link(arg)
       # {GA.Worker, arg},
+      {GA.Audit.CheckpointWorker, checkpoint_worker_opts()},
       # Start to serve requests, typically the last entry
       GAWeb.Endpoint
     ]
@@ -31,4 +32,8 @@
     GAWeb.Endpoint.config_change(changed, removed)
     :ok
   end
+
+  defp checkpoint_worker_opts do
+    Application.get_env(:app, GA.Audit.CheckpointWorker, [])
+  end
 end
diff --git a/app/lib/app/audit.ex b/app/lib/app/audit.ex
index a48830aacf4da8275b40f15016dcd16bbc87411f..2c7fbce41d873e721533728bcc1c965168addd3d
--- a/app/lib/app/audit.ex
+++ b/app/lib/app/audit.ex
@@ -7,7 +7,7 @@
   import Ecto.Query, warn: false
 
   alias GA.Accounts
-  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Audit.{Chain, Checkpoint, Log, Verifier}
   alias GA.Repo
 
   @default_limit 50
@@ -114,6 +114,12 @@
 
   def list_checkpoints(_), do: []
 
+  @doc """
+  Verifies chain integrity for a single account.
+  """
+  def verify_chain(account_id) when is_binary(account_id), do: Verifier.verify(account_id)
+  def verify_chain(_), do: {:error, :invalid_id}
+
   defp lock_account(account_id) do
     {key_a, key_b} = account_lock_keys(account_id)
 
diff --git a/app/lib/app/audit/checkpoint_worker.ex b/app/lib/app/audit/checkpoint_worker.ex
new file mode 100644
index 0000000000000000000000000000000000000000..f54908642ad7c854eae895b9605661f5beb0246f
--- /dev/null
+++ b/app/lib/app/audit/checkpoint_worker.ex
@@ -0,0 +1,82 @@
+defmodule GA.Audit.CheckpointWorker do
+  @moduledoc """
+  Periodically creates audit checkpoints for all active accounts.
+  """
+
+  use GenServer
+
+  import Ecto.Query, warn: false
+
+  require Logger
+
+  alias GA.Accounts.Account
+  alias GA.Audit
+  alias GA.Repo
+
+  @default_interval_ms :timer.hours(1)
+
+  def start_link(opts \\ []) do
+    name = Keyword.get(opts, :name, __MODULE__)
+    GenServer.start_link(__MODULE__, opts, name: name)
+  end
+
+  @impl true
+  def init(opts) do
+    interval_ms =
+      opts
+      |> Keyword.get(:interval_ms, configured_interval_ms())
+      |> normalize_interval()
+
+    schedule_tick(interval_ms)
+
+    {:ok, %{interval_ms: interval_ms}}
+  end
+
+  @impl true
+  def handle_info(:create_checkpoints, state) do
+    active_account_ids()
+    |> Enum.each(&create_checkpoint_for_account/1)
+
+    schedule_tick(state.interval_ms)
+    {:noreply, state}
+  end
+
+  defp active_account_ids do
+    from(account in Account,
+      where: account.status == :active,
+      select: account.id
+    )
+    |> Repo.all()
+  end
+
+  defp create_checkpoint_for_account(account_id) do
+    case Audit.create_checkpoint(account_id) do
+      {:ok, _checkpoint} ->
+        Logger.debug("checkpoint_worker created checkpoint for account=#{account_id}")
+
+      {:error, :no_entries} ->
+        Logger.debug("checkpoint_worker skipping account=#{account_id}: no audit entries")
+
+      {:error, reason} ->
+        Logger.error("checkpoint_worker failed for account=#{account_id}: #{inspect(reason)}")
+    end
+  rescue
+    error ->
+      Logger.error(
+        "checkpoint_worker crashed for account=#{account_id}: #{Exception.message(error)}"
+      )
+  end
+
+  defp schedule_tick(interval_ms) do
+    Process.send_after(self(), :create_checkpoints, interval_ms)
+  end
+
+  defp configured_interval_ms do
+    :app
+    |> Application.get_env(__MODULE__, [])
+    |> Keyword.get(:interval_ms, @default_interval_ms)
+  end
+
+  defp normalize_interval(interval_ms) when is_integer(interval_ms) and interval_ms > 0, do: interval_ms
+  defp normalize_interval(_), do: @default_interval_ms
+end
diff --git a/app/lib/app/audit/verifier.ex b/app/lib/app/audit/verifier.ex
new file mode 100644
index 0000000000000000000000000000000000000000..2a4bd77516a2c76ccc9b05de1778dd8781ded096
--- /dev/null
+++ b/app/lib/app/audit/verifier.ex
@@ -0,0 +1,203 @@
+defmodule GA.Audit.Verifier do
+  @moduledoc """
+  Streams and verifies account-scoped audit chains.
+  """
+
+  import Ecto.Query, warn: false
+
+  alias GA.Accounts
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  @batch_size 1000
+
+  @doc """
+  Verifies an account's audit chain and returns a report map.
+  """
+  @spec verify(binary()) :: map() | {:error, :invalid_id | :not_found}
+  def verify(account_id) when is_binary(account_id) do
+    started_at = System.monotonic_time(:millisecond)
+
+    with {:ok, hmac_key} <- Accounts.get_hmac_key(account_id) do
+      checkpoints_by_sequence = load_checkpoints(account_id)
+      report = stream_and_verify(account_id, hmac_key, checkpoints_by_sequence)
+
+      Map.put(report, :duration_ms, elapsed_ms(started_at))
+    end
+  end
+
+  def verify(_), do: {:error, :invalid_id}
+
+  defp stream_and_verify(account_id, hmac_key, checkpoints_by_sequence) do
+    initial =
+      new_accumulator()
+      |> Map.put(:account_id, account_id)
+
+    account_id
+    |> do_verify_batches(hmac_key, checkpoints_by_sequence, initial)
+    |> finalize_report()
+  end
+
+  defp do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, acc) do
+    batch = load_batch(account_id, acc.last_sequence)
+
+    case batch do
+      [] ->
+        acc
+
+      _entries ->
+        next_acc =
+          Enum.reduce(batch, acc, fn entry, state ->
+            verify_entry(entry, hmac_key, checkpoints_by_sequence, state)
+          end)
+
+        do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, next_acc)
+    end
+  end
+
+  defp verify_entry(%Log{} = entry, hmac_key, checkpoints_by_sequence, acc) do
+    acc
+    |> verify_sequence(entry)
+    |> verify_checksum(entry, hmac_key)
+    |> verify_checkpoint(entry, checkpoints_by_sequence)
+    |> advance_state(entry)
+  end
+
+  defp verify_sequence(acc, %Log{sequence_number: found_sequence}) do
+    expected_sequence = acc.expected_sequence
+
+    if found_sequence == expected_sequence do
+      %{acc | expected_sequence: expected_sequence + 1}
+    else
+      missing =
+        if found_sequence > expected_sequence do
+          Enum.to_list(expected_sequence..(found_sequence - 1))
+        else
+          []
+        end
+
+      gap = %{
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      }
+
+      acc
+      |> mark_invalid(%{
+        type: :sequence_gap,
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      })
+      |> Map.update!(:sequence_gaps, &[gap | &1])
+      |> Map.put(:expected_sequence, max(found_sequence, expected_sequence) + 1)
+    end
+  end
+
+  defp verify_checksum(acc, %Log{} = entry, hmac_key) do
+    if Chain.verify_checksum(hmac_key, entry, acc.previous_checksum) do
+      acc
+    else
+      mark_invalid(acc, %{
+        type: :checksum_mismatch,
+        sequence_number: entry.sequence_number,
+        stored_checksum: entry.checksum,
+        expected_checksum: expected_checksum(hmac_key, entry, acc.previous_checksum)
+      })
+    end
+  end
+
+  defp verify_checkpoint(acc, %Log{} = entry, checkpoints_by_sequence) do
+    case Map.get(checkpoints_by_sequence, entry.sequence_number) do
+      nil ->
+        acc
+
+      %Checkpoint{} = checkpoint ->
+        valid? = checkpoint.checksum == entry.checksum
+        result = %{sequence_number: entry.sequence_number, valid: valid?}
+        next_acc = Map.update!(acc, :checkpoint_results, &[result | &1])
+
+        if valid? do
+          next_acc
+        else
+          mark_invalid(next_acc, %{
+            type: :checkpoint_mismatch,
+            sequence_number: entry.sequence_number,
+            checkpoint_checksum: checkpoint.checksum,
+            entry_checksum: entry.checksum
+          })
+        end
+    end
+  end
+
+  defp advance_state(acc, %Log{} = entry) do
+    %{
+      acc
+      | previous_checksum: entry.checksum,
+        last_sequence: entry.sequence_number,
+        total_entries: acc.total_entries + 1,
+        verified_entries: acc.verified_entries + 1
+    }
+  end
+
+  defp expected_checksum(hmac_key, entry, previous_checksum) do
+    Chain.compute_checksum(hmac_key, Map.from_struct(entry), previous_checksum)
+  rescue
+    _ -> nil
+  end
+
+  defp load_batch(account_id, after_sequence) do
+    from(log in Log,
+      where: log.account_id == ^account_id and log.sequence_number > ^after_sequence,
+      order_by: [asc: log.sequence_number],
+      limit: @batch_size
+    )
+    |> Repo.all()
+  end
+
+  defp load_checkpoints(account_id) do
+    from(checkpoint in Checkpoint, where: checkpoint.account_id == ^account_id)
+    |> Repo.all()
+    |> Map.new(fn checkpoint -> {checkpoint.sequence_number, checkpoint} end)
+  end
+
+  defp new_accumulator do
+    %{
+      account_id: nil,
+      valid: true,
+      total_entries: 0,
+      verified_entries: 0,
+      expected_sequence: 1,
+      previous_checksum: nil,
+      last_sequence: 0,
+      first_failure: nil,
+      sequence_gaps: [],
+      checkpoint_results: []
+    }
+  end
+
+  defp finalize_report(acc) do
+    %{
+      valid: acc.valid,
+      total_entries: acc.total_entries,
+      verified_entries: acc.verified_entries,
+      first_failure: acc.first_failure,
+      sequence_gaps: Enum.reverse(acc.sequence_gaps),
+      checkpoint_results: Enum.reverse(acc.checkpoint_results),
+      duration_ms: 0
+    }
+  end
+
+  defp mark_invalid(acc, failure) do
+    case acc.first_failure do
+      nil -> %{acc | valid: false, first_failure: failure}
+      _existing -> %{acc | valid: false}
+    end
+  end
+
+  defp elapsed_ms(started_at) do
+    System.monotonic_time(:millisecond)
+    |> Kernel.-(started_at)
+    |> max(0)
+  end
+end
diff --git a/app/test/app/audit/checkpoint_worker_test.exs b/app/test/app/audit/checkpoint_worker_test.exs
new file mode 100644
index 0000000000000000000000000000000000000000..ec9ea5e45a2b2d84cd0e7872a7bf7288d8e3ea11
--- /dev/null
+++ b/app/test/app/audit/checkpoint_worker_test.exs
@@ -0,0 +1,111 @@
+defmodule GA.Audit.CheckpointWorkerTest do
+  use GA.DataCase, async: false
+
+  import ExUnit.CaptureLog
+
+  alias GA.Accounts
+  alias GA.Audit
+  alias GA.Audit.CheckpointWorker
+
+  describe "checkpoint worker ticks" do
+    test "creates checkpoints for all active accounts" do
+      account_a = account_fixture()
+      account_b = account_fixture()
+      add_log(account_a.id, "active-a")
+      add_log(account_b.id, "active-b")
+
+      worker = start_worker()
+      send(worker, :create_checkpoints)
+
+      assert_eventually(fn ->
+        length(Audit.list_checkpoints(account_a.id)) == 1 and
+          length(Audit.list_checkpoints(account_b.id)) == 1
+      end)
+    end
+
+    test "skips suspended accounts" do
+      active_account = account_fixture()
+      suspended_account = account_fixture(%{status: :suspended})
+      add_log(active_account.id, "active")
+      add_log(suspended_account.id, "suspended")
+
+      worker = start_worker()
+      send(worker, :create_checkpoints)
+
+      assert_eventually(fn ->
+        length(Audit.list_checkpoints(active_account.id)) == 1
+      end)
+
+      assert Audit.list_checkpoints(suspended_account.id) == []
+    end
+
+    test "logs no-entry accounts and continues processing" do
+      empty_account = account_fixture()
+      populated_account = account_fixture()
+      add_log(populated_account.id, "populated")
+
+      worker = start_worker()
+
+      log =
+        capture_log([level: :debug], fn ->
+          send(worker, :create_checkpoints)
+
+          assert_eventually(fn ->
+            length(Audit.list_checkpoints(populated_account.id)) == 1
+          end)
+        end)
+
+      assert log =~ "checkpoint_worker skipping"
+      assert log =~ empty_account.id
+      assert Audit.list_checkpoints(empty_account.id) == []
+    end
+  end
+
+  defp start_worker do
+    start_supervised!(
+      {CheckpointWorker,
+       [
+         name: {:global, {:checkpoint_worker_test, System.unique_integer([:positive])}},
+         interval_ms: 60_000
+       ]}
+    )
+  end
+
+  defp account_fixture(attrs \\ %{}) do
+    defaults = %{name: "Worker Account #{System.unique_integer([:positive])}"}
+    {:ok, account} = Accounts.create_account(Map.merge(defaults, attrs))
+    account
+  end
+
+  defp add_log(account_id, resource_id) do
+    attrs = %{
+      user_id: Ecto.UUID.generate(),
+      user_role: "admin",
+      session_id: "session-#{System.unique_integer([:positive])}",
+      action: "read",
+      resource_type: "patient",
+      resource_id: resource_id,
+      timestamp: ~U[2026-03-03 16:30:00Z],
+      source_ip: "127.0.0.1",
+      user_agent: "ExUnit",
+      outcome: "success",
+      failure_reason: nil,
+      phi_accessed: false,
+      metadata: %{"source" => "checkpoint_worker_test"}
+    }
+
+    assert {:ok, _} = Audit.create_log_entry(account_id, attrs)
+  end
+
+  defp assert_eventually(fun, attempts \\ 50)
+  defp assert_eventually(fun, 0), do: assert(fun.())
+
+  defp assert_eventually(fun, attempts) do
+    if fun.() do
+      :ok
+    else
+      Process.sleep(20)
+      assert_eventually(fun, attempts - 1)
+    end
+  end
+end
diff --git a/app/test/app/audit/verifier_test.exs b/app/test/app/audit/verifier_test.exs
new file mode 100644
index 0000000000000000000000000000000000000000..430e2450186c77bb9e816cfa26910a774995b717
--- /dev/null
+++ b/app/test/app/audit/verifier_test.exs
@@ -0,0 +1,189 @@
+defmodule GA.Audit.VerifierTest do
+  use GA.DataCase, async: false
+
+  alias GA.Accounts
+  alias GA.Audit
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  describe "verify_chain/1" do
+    test "returns a valid report for an intact account chain" do
+      account = account_fixture()
+
+      assert {:ok, _first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "r-1"}))
+      assert {:ok, _second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "r-2"}))
+      assert {:ok, checkpoint} = Audit.create_checkpoint(account.id)
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == true
+      assert report.total_entries == 2
+      assert report.verified_entries == 2
+      assert report.first_failure == nil
+      assert report.sequence_gaps == []
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == checkpoint.sequence_number and &1.valid))
+      assert is_integer(report.duration_ms)
+      assert report.duration_ms >= 0
+    end
+
+    test "detects tampered checksum entries" do
+      account = account_fixture()
+
+      assert {:ok, first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "seed"}))
+
+      insert_raw_log(account.id, %{
+        sequence_number: 2,
+        previous_checksum: first.checksum,
+        checksum: String.duplicate("0", 64),
+        resource_id: "tampered"
+      })
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert report.first_failure.type == :checksum_mismatch
+      assert report.first_failure.sequence_number == 2
+      assert report.first_failure.stored_checksum == String.duplicate("0", 64)
+      refute is_nil(report.first_failure.expected_checksum)
+    end
+
+    test "detects sequence gaps within an account" do
+      account = account_fixture()
+      assert {:ok, _first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "gap-1"}))
+      assert {:ok, second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "gap-2"}))
+      assert {:ok, hmac_key} = Accounts.get_hmac_key(account.id)
+
+      checksum =
+        checksum_for_sequence(
+          account.id,
+          hmac_key,
+          4,
+          second.checksum,
+          %{resource_id: "gap-4"}
+        )
+
+      insert_raw_log(account.id, %{
+        sequence_number: 4,
+        previous_checksum: second.checksum,
+        checksum: checksum,
+        resource_id: "gap-4"
+      })
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert report.first_failure.type == :sequence_gap
+      assert [%{expected: 3, found: 4, missing: [3]}] = report.sequence_gaps
+    end
+
+    test "validates checkpoint anchors and reports invalid anchors" do
+      account = account_fixture()
+      assert {:ok, first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "cp-1"}))
+      assert {:ok, second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "cp-2"}))
+
+      insert_checkpoint(account.id, first.sequence_number, first.checksum)
+      insert_checkpoint(account.id, second.sequence_number, String.duplicate("f", 64))
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == first.sequence_number and &1.valid))
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == second.sequence_number and not &1.valid))
+    end
+
+    test "includes non-negative duration tracking" do
+      account = account_fixture()
+      assert {:ok, _} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "duration"}))
+
+      report = Audit.verify_chain(account.id)
+
+      assert is_integer(report.duration_ms)
+      assert report.duration_ms >= 0
+    end
+
+    test "verification remains account-scoped" do
+      account_a = account_fixture()
+      account_b = account_fixture()
+
+      assert {:ok, _} = Audit.create_log_entry(account_a.id, valid_attrs(%{resource_id: "a-1"}))
+      assert {:ok, first_b} = Audit.create_log_entry(account_b.id, valid_attrs(%{resource_id: "b-1"}))
+
+      insert_raw_log(account_b.id, %{
+        sequence_number: 2,
+        previous_checksum: first_b.checksum,
+        checksum: String.duplicate("a", 64),
+        resource_id: "b-2-tampered"
+      })
+
+      report = Audit.verify_chain(account_a.id)
+
+      assert report.valid == true
+      assert report.total_entries == 1
+      assert report.first_failure == nil
+      assert report.sequence_gaps == []
+    end
+  end
+
+  defp account_fixture(attrs \\ %{}) do
+    defaults = %{name: "Verifier Account #{System.unique_integer([:positive])}"}
+    {:ok, account} = Accounts.create_account(Map.merge(defaults, attrs))
+    account
+  end
+
+  defp valid_attrs(overrides \\ %{}) do
+    defaults = %{
+      user_id: Ecto.UUID.generate(),
+      user_role: "admin",
+      session_id: "session-#{System.unique_integer([:positive])}",
+      action: "read",
+      resource_type: "patient",
+      resource_id: Ecto.UUID.generate(),
+      timestamp: ~U[2026-03-03 16:30:00Z],
+      source_ip: "127.0.0.1",
+      user_agent: "ExUnit",
+      outcome: "success",
+      failure_reason: nil,
+      phi_accessed: false,
+      metadata: %{"source" => "verifier_test"}
+    }
+
+    Map.merge(defaults, overrides)
+  end
+
+  defp checksum_for_sequence(account_id, hmac_key, sequence_number, previous_checksum, overrides \\ %{}) do
+    attrs =
+      valid_attrs(overrides)
+      |> Map.put(:account_id, account_id)
+      |> Map.put(:sequence_number, sequence_number)
+
+    Chain.compute_checksum(hmac_key, attrs, previous_checksum)
+  end
+
+  defp insert_raw_log(account_id, attrs) do
+    now = DateTime.utc_now() |> DateTime.truncate(:microsecond)
+    id = Ecto.UUID.generate()
+
+    row =
+      valid_attrs()
+      |> Map.merge(attrs)
+      |> Map.merge(%{
+        id: id,
+        account_id: account_id,
+        inserted_at: now,
+        updated_at: now
+      })
+
+    {1, _} = Repo.insert_all(Log, [row])
+    Repo.get!(Log, id)
+  end
+
+  defp insert_checkpoint(account_id, sequence_number, checksum) do
+    %Checkpoint{}
+    |> Checkpoint.changeset(%{
+      account_id: account_id,
+      sequence_number: sequence_number,
+      checksum: checksum
+    })
+    |> Repo.insert!()
+  end
+end

exec
/bin/zsh -lc 'git diff --stat' in /Users/tbrewer/projects/goodway/goodaudit succeeded in 51ms:
 .autopilot-logs/audit-context/phase-5-commit.log | 47 ++++++++++++++++++++++++
 .autopilot-logs/pipeline.log                     | 30 +++++++++++++++
 app/lib/app/application.ex                       |  5 +++
 app/lib/app/audit.ex                             |  8 +++-
 4 files changed, 89 insertions(+), 1 deletion(-)
file update:
diff --git a/app/lib/app/application.ex b/app/lib/app/application.ex
index d865bfe6a48ed688366f7d85220490621cd809a7..08bab073bf607f5bb04aae846094b0b08ec40fe4
--- a/app/lib/app/application.ex
+++ b/app/lib/app/application.ex
@@ -14,6 +14,7 @@
       {Phoenix.PubSub, name: GA.PubSub},
       # Start a worker by calling: GA.Worker.start_link(arg)
       # {GA.Worker, arg},
+      {GA.Audit.CheckpointWorker, checkpoint_worker_opts()},
       # Start to serve requests, typically the last entry
       GAWeb.Endpoint
     ]
@@ -31,4 +32,8 @@
     GAWeb.Endpoint.config_change(changed, removed)
     :ok
   end
+
+  defp checkpoint_worker_opts do
+    Application.get_env(:app, GA.Audit.CheckpointWorker, [])
+  end
 end
diff --git a/app/lib/app/audit.ex b/app/lib/app/audit.ex
index a48830aacf4da8275b40f15016dcd16bbc87411f..2c7fbce41d873e721533728bcc1c965168addd3d
--- a/app/lib/app/audit.ex
+++ b/app/lib/app/audit.ex
@@ -7,7 +7,7 @@
   import Ecto.Query, warn: false
 
   alias GA.Accounts
-  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Audit.{Chain, Checkpoint, Log, Verifier}
   alias GA.Repo
 
   @default_limit 50
@@ -114,6 +114,12 @@
 
   def list_checkpoints(_), do: []
 
+  @doc """
+  Verifies chain integrity for a single account.
+  """
+  def verify_chain(account_id) when is_binary(account_id), do: Verifier.verify(account_id)
+  def verify_chain(_), do: {:error, :invalid_id}
+
   defp lock_account(account_id) do
     {key_a, key_b} = account_lock_keys(account_id)
 
diff --git a/app/lib/app/audit/checkpoint_worker.ex b/app/lib/app/audit/checkpoint_worker.ex
new file mode 100644
index 0000000000000000000000000000000000000000..f54908642ad7c854eae895b9605661f5beb0246f
--- /dev/null
+++ b/app/lib/app/audit/checkpoint_worker.ex
@@ -0,0 +1,82 @@
+defmodule GA.Audit.CheckpointWorker do
+  @moduledoc """
+  Periodically creates audit checkpoints for all active accounts.
+  """
+
+  use GenServer
+
+  import Ecto.Query, warn: false
+
+  require Logger
+
+  alias GA.Accounts.Account
+  alias GA.Audit
+  alias GA.Repo
+
+  @default_interval_ms :timer.hours(1)
+
+  def start_link(opts \\ []) do
+    name = Keyword.get(opts, :name, __MODULE__)
+    GenServer.start_link(__MODULE__, opts, name: name)
+  end
+
+  @impl true
+  def init(opts) do
+    interval_ms =
+      opts
+      |> Keyword.get(:interval_ms, configured_interval_ms())
+      |> normalize_interval()
+
+    schedule_tick(interval_ms)
+
+    {:ok, %{interval_ms: interval_ms}}
+  end
+
+  @impl true
+  def handle_info(:create_checkpoints, state) do
+    active_account_ids()
+    |> Enum.each(&create_checkpoint_for_account/1)
+
+    schedule_tick(state.interval_ms)
+    {:noreply, state}
+  end
+
+  defp active_account_ids do
+    from(account in Account,
+      where: account.status == :active,
+      select: account.id
+    )
+    |> Repo.all()
+  end
+
+  defp create_checkpoint_for_account(account_id) do
+    case Audit.create_checkpoint(account_id) do
+      {:ok, _checkpoint} ->
+        Logger.debug("checkpoint_worker created checkpoint for account=#{account_id}")
+
+      {:error, :no_entries} ->
+        Logger.debug("checkpoint_worker skipping account=#{account_id}: no audit entries")
+
+      {:error, reason} ->
+        Logger.error("checkpoint_worker failed for account=#{account_id}: #{inspect(reason)}")
+    end
+  rescue
+    error ->
+      Logger.error(
+        "checkpoint_worker crashed for account=#{account_id}: #{Exception.message(error)}"
+      )
+  end
+
+  defp schedule_tick(interval_ms) do
+    Process.send_after(self(), :create_checkpoints, interval_ms)
+  end
+
+  defp configured_interval_ms do
+    :app
+    |> Application.get_env(__MODULE__, [])
+    |> Keyword.get(:interval_ms, @default_interval_ms)
+  end
+
+  defp normalize_interval(interval_ms) when is_integer(interval_ms) and interval_ms > 0, do: interval_ms
+  defp normalize_interval(_), do: @default_interval_ms
+end
diff --git a/app/lib/app/audit/verifier.ex b/app/lib/app/audit/verifier.ex
new file mode 100644
index 0000000000000000000000000000000000000000..2a4bd77516a2c76ccc9b05de1778dd8781ded096
--- /dev/null
+++ b/app/lib/app/audit/verifier.ex
@@ -0,0 +1,203 @@
+defmodule GA.Audit.Verifier do
+  @moduledoc """
+  Streams and verifies account-scoped audit chains.
+  """
+
+  import Ecto.Query, warn: false
+
+  alias GA.Accounts
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  @batch_size 1000
+
+  @doc """
+  Verifies an account's audit chain and returns a report map.
+  """
+  @spec verify(binary()) :: map() | {:error, :invalid_id | :not_found}
+  def verify(account_id) when is_binary(account_id) do
+    started_at = System.monotonic_time(:millisecond)
+
+    with {:ok, hmac_key} <- Accounts.get_hmac_key(account_id) do
+      checkpoints_by_sequence = load_checkpoints(account_id)
+      report = stream_and_verify(account_id, hmac_key, checkpoints_by_sequence)
+
+      Map.put(report, :duration_ms, elapsed_ms(started_at))
+    end
+  end
+
+  def verify(_), do: {:error, :invalid_id}
+
+  defp stream_and_verify(account_id, hmac_key, checkpoints_by_sequence) do
+    initial =
+      new_accumulator()
+      |> Map.put(:account_id, account_id)
+
+    account_id
+    |> do_verify_batches(hmac_key, checkpoints_by_sequence, initial)
+    |> finalize_report()
+  end
+
+  defp do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, acc) do
+    batch = load_batch(account_id, acc.last_sequence)
+
+    case batch do
+      [] ->
+        acc
+
+      _entries ->
+        next_acc =
+          Enum.reduce(batch, acc, fn entry, state ->
+            verify_entry(entry, hmac_key, checkpoints_by_sequence, state)
+          end)
+
+        do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, next_acc)
+    end
+  end
+
+  defp verify_entry(%Log{} = entry, hmac_key, checkpoints_by_sequence, acc) do
+    acc
+    |> verify_sequence(entry)
+    |> verify_checksum(entry, hmac_key)
+    |> verify_checkpoint(entry, checkpoints_by_sequence)
+    |> advance_state(entry)
+  end
+
+  defp verify_sequence(acc, %Log{sequence_number: found_sequence}) do
+    expected_sequence = acc.expected_sequence
+
+    if found_sequence == expected_sequence do
+      %{acc | expected_sequence: expected_sequence + 1}
+    else
+      missing =
+        if found_sequence > expected_sequence do
+          Enum.to_list(expected_sequence..(found_sequence - 1))
+        else
+          []
+        end
+
+      gap = %{
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      }
+
+      acc
+      |> mark_invalid(%{
+        type: :sequence_gap,
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      })
+      |> Map.update!(:sequence_gaps, &[gap | &1])
+      |> Map.put(:expected_sequence, max(found_sequence, expected_sequence) + 1)
+    end
+  end
+
+  defp verify_checksum(acc, %Log{} = entry, hmac_key) do
+    if Chain.verify_checksum(hmac_key, entry, acc.previous_checksum) do
+      acc
+    else
+      mark_invalid(acc, %{
+        type: :checksum_mismatch,
+        sequence_number: entry.sequence_number,
+        stored_checksum: entry.checksum,
+        expected_checksum: expected_checksum(hmac_key, entry, acc.previous_checksum)
+      })
+    end
+  end
+
+  defp verify_checkpoint(acc, %Log{} = entry, checkpoints_by_sequence) do
+    case Map.get(checkpoints_by_sequence, entry.sequence_number) do
+      nil ->
+        acc
+
+      %Checkpoint{} = checkpoint ->
+        valid? = checkpoint.checksum == entry.checksum
+        result = %{sequence_number: entry.sequence_number, valid: valid?}
+        next_acc = Map.update!(acc, :checkpoint_results, &[result | &1])
+
+        if valid? do
+          next_acc
+        else
+          mark_invalid(next_acc, %{
+            type: :checkpoint_mismatch,
+            sequence_number: entry.sequence_number,
+            checkpoint_checksum: checkpoint.checksum,
+            entry_checksum: entry.checksum
+          })
+        end
+    end
+  end
+
+  defp advance_state(acc, %Log{} = entry) do
+    %{
+      acc
+      | previous_checksum: entry.checksum,
+        last_sequence: entry.sequence_number,
+        total_entries: acc.total_entries + 1,
+        verified_entries: acc.verified_entries + 1
+    }
+  end
+
+  defp expected_checksum(hmac_key, entry, previous_checksum) do
+    Chain.compute_checksum(hmac_key, Map.from_struct(entry), previous_checksum)
+  rescue
+    _ -> nil
+  end
+
+  defp load_batch(account_id, after_sequence) do
+    from(log in Log,
+      where: log.account_id == ^account_id and log.sequence_number > ^after_sequence,
+      order_by: [asc: log.sequence_number],
+      limit: @batch_size
+    )
+    |> Repo.all()
+  end
+
+  defp load_checkpoints(account_id) do
+    from(checkpoint in Checkpoint, where: checkpoint.account_id == ^account_id)
+    |> Repo.all()
+    |> Map.new(fn checkpoint -> {checkpoint.sequence_number, checkpoint} end)
+  end
+
+  defp new_accumulator do
+    %{
+      account_id: nil,
+      valid: true,
+      total_entries: 0,
+      verified_entries: 0,
+      expected_sequence: 1,
+      previous_checksum: nil,
+      last_sequence: 0,
+      first_failure: nil,
+      sequence_gaps: [],
+      checkpoint_results: []
+    }
+  end
+
+  defp finalize_report(acc) do
+    %{
+      valid: acc.valid,
+      total_entries: acc.total_entries,
+      verified_entries: acc.verified_entries,
+      first_failure: acc.first_failure,
+      sequence_gaps: Enum.reverse(acc.sequence_gaps),
+      checkpoint_results: Enum.reverse(acc.checkpoint_results),
+      duration_ms: 0
+    }
+  end
+
+  defp mark_invalid(acc, failure) do
+    case acc.first_failure do
+      nil -> %{acc | valid: false, first_failure: failure}
+      _existing -> %{acc | valid: false}
+    end
+  end
+
+  defp elapsed_ms(started_at) do
+    System.monotonic_time(:millisecond)
+    |> Kernel.-(started_at)
+    |> max(0)
+  end
+end
diff --git a/app/test/app/audit/checkpoint_worker_test.exs b/app/test/app/audit/checkpoint_worker_test.exs
new file mode 100644
index 0000000000000000000000000000000000000000..ec9ea5e45a2b2d84cd0e7872a7bf7288d8e3ea11
--- /dev/null
+++ b/app/test/app/audit/checkpoint_worker_test.exs
@@ -0,0 +1,111 @@
+defmodule GA.Audit.CheckpointWorkerTest do
+  use GA.DataCase, async: false
+
+  import ExUnit.CaptureLog
+
+  alias GA.Accounts
+  alias GA.Audit
+  alias GA.Audit.CheckpointWorker
+
+  describe "checkpoint worker ticks" do
+    test "creates checkpoints for all active accounts" do
+      account_a = account_fixture()
+      account_b = account_fixture()
+      add_log(account_a.id, "active-a")
+      add_log(account_b.id, "active-b")
+
+      worker = start_worker()
+      send(worker, :create_checkpoints)
+
+      assert_eventually(fn ->
+        length(Audit.list_checkpoints(account_a.id)) == 1 and
+          length(Audit.list_checkpoints(account_b.id)) == 1
+      end)
+    end
+
+    test "skips suspended accounts" do
+      active_account = account_fixture()
+      suspended_account = account_fixture(%{status: :suspended})
+      add_log(active_account.id, "active")
+      add_log(suspended_account.id, "suspended")
+
+      worker = start_worker()
+      send(worker, :create_checkpoints)
+
+      assert_eventually(fn ->
+        length(Audit.list_checkpoints(active_account.id)) == 1
+      end)
+
+      assert Audit.list_checkpoints(suspended_account.id) == []
+    end
+
+    test "logs no-entry accounts and continues processing" do
+      empty_account = account_fixture()
+      populated_account = account_fixture()
+      add_log(populated_account.id, "populated")
+
+      worker = start_worker()
+
+      log =
+        capture_log([level: :debug], fn ->
+          send(worker, :create_checkpoints)
+
+          assert_eventually(fn ->
+            length(Audit.list_checkpoints(populated_account.id)) == 1
+          end)
+        end)
+
+      assert log =~ "checkpoint_worker skipping"
+      assert log =~ empty_account.id
+      assert Audit.list_checkpoints(empty_account.id) == []
+    end
+  end
+
+  defp start_worker do
+    start_supervised!(
+      {CheckpointWorker,
+       [
+         name: {:global, {:checkpoint_worker_test, System.unique_integer([:positive])}},
+         interval_ms: 60_000
+       ]}
+    )
+  end
+
+  defp account_fixture(attrs \\ %{}) do
+    defaults = %{name: "Worker Account #{System.unique_integer([:positive])}"}
+    {:ok, account} = Accounts.create_account(Map.merge(defaults, attrs))
+    account
+  end
+
+  defp add_log(account_id, resource_id) do
+    attrs = %{
+      user_id: Ecto.UUID.generate(),
+      user_role: "admin",
+      session_id: "session-#{System.unique_integer([:positive])}",
+      action: "read",
+      resource_type: "patient",
+      resource_id: resource_id,
+      timestamp: ~U[2026-03-03 16:30:00Z],
+      source_ip: "127.0.0.1",
+      user_agent: "ExUnit",
+      outcome: "success",
+      failure_reason: nil,
+      phi_accessed: false,
+      metadata: %{"source" => "checkpoint_worker_test"}
+    }
+
+    assert {:ok, _} = Audit.create_log_entry(account_id, attrs)
+  end
+
+  defp assert_eventually(fun, attempts \\ 50)
+  defp assert_eventually(fun, 0), do: assert(fun.())
+
+  defp assert_eventually(fun, attempts) do
+    if fun.() do
+      :ok
+    else
+      Process.sleep(20)
+      assert_eventually(fun, attempts - 1)
+    end
+  end
+end
diff --git a/app/test/app/audit/verifier_test.exs b/app/test/app/audit/verifier_test.exs
new file mode 100644
index 0000000000000000000000000000000000000000..430e2450186c77bb9e816cfa26910a774995b717
--- /dev/null
+++ b/app/test/app/audit/verifier_test.exs
@@ -0,0 +1,189 @@
+defmodule GA.Audit.VerifierTest do
+  use GA.DataCase, async: false
+
+  alias GA.Accounts
+  alias GA.Audit
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  describe "verify_chain/1" do
+    test "returns a valid report for an intact account chain" do
+      account = account_fixture()
+
+      assert {:ok, _first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "r-1"}))
+      assert {:ok, _second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "r-2"}))
+      assert {:ok, checkpoint} = Audit.create_checkpoint(account.id)
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == true
+      assert report.total_entries == 2
+      assert report.verified_entries == 2
+      assert report.first_failure == nil
+      assert report.sequence_gaps == []
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == checkpoint.sequence_number and &1.valid))
+      assert is_integer(report.duration_ms)
+      assert report.duration_ms >= 0
+    end
+
+    test "detects tampered checksum entries" do
+      account = account_fixture()
+
+      assert {:ok, first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "seed"}))
+
+      insert_raw_log(account.id, %{
+        sequence_number: 2,
+        previous_checksum: first.checksum,
+        checksum: String.duplicate("0", 64),
+        resource_id: "tampered"
+      })
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert report.first_failure.type == :checksum_mismatch
+      assert report.first_failure.sequence_number == 2
+      assert report.first_failure.stored_checksum == String.duplicate("0", 64)
+      refute is_nil(report.first_failure.expected_checksum)
+    end
+
+    test "detects sequence gaps within an account" do
+      account = account_fixture()
+      assert {:ok, _first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "gap-1"}))
+      assert {:ok, second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "gap-2"}))
+      assert {:ok, hmac_key} = Accounts.get_hmac_key(account.id)
+
+      checksum =
+        checksum_for_sequence(
+          account.id,
+          hmac_key,
+          4,
+          second.checksum,
+          %{resource_id: "gap-4"}
+        )
+
+      insert_raw_log(account.id, %{
+        sequence_number: 4,
+        previous_checksum: second.checksum,
+        checksum: checksum,
+        resource_id: "gap-4"
+      })
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert report.first_failure.type == :sequence_gap
+      assert [%{expected: 3, found: 4, missing: [3]}] = report.sequence_gaps
+    end
+
+    test "validates checkpoint anchors and reports invalid anchors" do
+      account = account_fixture()
+      assert {:ok, first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "cp-1"}))
+      assert {:ok, second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "cp-2"}))
+
+      insert_checkpoint(account.id, first.sequence_number, first.checksum)
+      insert_checkpoint(account.id, second.sequence_number, String.duplicate("f", 64))
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == first.sequence_number and &1.valid))
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == second.sequence_number and not &1.valid))
+    end
+
+    test "includes non-negative duration tracking" do
+      account = account_fixture()
+      assert {:ok, _} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "duration"}))
+
+      report = Audit.verify_chain(account.id)
+
+      assert is_integer(report.duration_ms)
+      assert report.duration_ms >= 0
+    end
+
+    test "verification remains account-scoped" do
+      account_a = account_fixture()
+      account_b = account_fixture()
+
+      assert {:ok, _} = Audit.create_log_entry(account_a.id, valid_attrs(%{resource_id: "a-1"}))
+      assert {:ok, first_b} = Audit.create_log_entry(account_b.id, valid_attrs(%{resource_id: "b-1"}))
+
+      insert_raw_log(account_b.id, %{
+        sequence_number: 2,
+        previous_checksum: first_b.checksum,
+        checksum: String.duplicate("a", 64),
+        resource_id: "b-2-tampered"
+      })
+
+      report = Audit.verify_chain(account_a.id)
+
+      assert report.valid == true
+      assert report.total_entries == 1
+      assert report.first_failure == nil
+      assert report.sequence_gaps == []
+    end
+  end
+
+  defp account_fixture(attrs \\ %{}) do
+    defaults = %{name: "Verifier Account #{System.unique_integer([:positive])}"}
+    {:ok, account} = Accounts.create_account(Map.merge(defaults, attrs))
+    account
+  end
+
+  defp valid_attrs(overrides \\ %{}) do
+    defaults = %{
+      user_id: Ecto.UUID.generate(),
+      user_role: "admin",
+      session_id: "session-#{System.unique_integer([:positive])}",
+      action: "read",
+      resource_type: "patient",
+      resource_id: Ecto.UUID.generate(),
+      timestamp: ~U[2026-03-03 16:30:00Z],
+      source_ip: "127.0.0.1",
+      user_agent: "ExUnit",
+      outcome: "success",
+      failure_reason: nil,
+      phi_accessed: false,
+      metadata: %{"source" => "verifier_test"}
+    }
+
+    Map.merge(defaults, overrides)
+  end
+
+  defp checksum_for_sequence(account_id, hmac_key, sequence_number, previous_checksum, overrides \\ %{}) do
+    attrs =
+      valid_attrs(overrides)
+      |> Map.put(:account_id, account_id)
+      |> Map.put(:sequence_number, sequence_number)
+
+    Chain.compute_checksum(hmac_key, attrs, previous_checksum)
+  end
+
+  defp insert_raw_log(account_id, attrs) do
+    now = DateTime.utc_now() |> DateTime.truncate(:microsecond)
+    id = Ecto.UUID.generate()
+
+    row =
+      valid_attrs()
+      |> Map.merge(attrs)
+      |> Map.merge(%{
+        id: id,
+        account_id: account_id,
+        inserted_at: now,
+        updated_at: now
+      })
+
+    {1, _} = Repo.insert_all(Log, [row])
+    Repo.get!(Log, id)
+  end
+
+  defp insert_checkpoint(account_id, sequence_number, checksum) do
+    %Checkpoint{}
+    |> Checkpoint.changeset(%{
+      account_id: account_id,
+      sequence_number: sequence_number,
+      checksum: checksum
+    })
+    |> Repo.insert!()
+  end
+end

codex
Takeoff executed for `openspec/changes/chain-verification/` on the current branch (`main`) without creating a new branch.

- Epic found: `td-9db2d6` (`Chain Verification`)
- Work session: `ws-e303` (`Epic: Chain Verification`)
- Tagged/started: 18 child issues
- Skipped: review gate `td-833971` (left `open` as required)

Implementation started and completed in this pass:

- Added verifier module: [app/lib/app/audit/verifier.ex](/Users/tbrewer/projects/goodway/goodaudit/app/lib/app/audit/verifier.ex)
- Added audit context delegation: [app/lib/app/audit.ex](/Users/tbrewer/projects/goodway/goodaudit/app/lib/app/audit.ex)
- Added checkpoint worker: [app/lib/app/audit/checkpoint_worker.ex](/Users/tbrewer/projects/goodway/goodaudit/app/lib/app/audit/checkpoint_worker.ex)
- Wired worker into supervision tree before endpoint: [app/lib/app/application.ex](/Users/tbrewer/projects/goodway/goodaudit/app/lib/app/application.ex)
- Added verifier tests: [app/test/app/audit/verifier_test.exs](/Users/tbrewer/projects/goodway/goodaudit/app/test/app/audit/verifier_test.exs)
- Added checkpoint worker tests: [app/test/app/audit/checkpoint_worker_test.exs](/Users/tbrewer/projects/goodway/goodaudit/app/test/app/audit/checkpoint_worker_test.exs)

`td` status updates done:

- Logged implementation/decision/result notes to `ws-e303`
- Moved all 18 tagged child issues to `in_review`
- Review gate `td-833971` remains `open`

Validation attempt:

- `mix format` failed due environment toolchain mismatch:
  - Elixir requires newer Erlang/OTP, current runtime is OTP 27
- Because of that, tests could not be executed here.
file update:
diff --git a/app/lib/app/application.ex b/app/lib/app/application.ex
index d865bfe6a48ed688366f7d85220490621cd809a7..08bab073bf607f5bb04aae846094b0b08ec40fe4
--- a/app/lib/app/application.ex
+++ b/app/lib/app/application.ex
@@ -14,6 +14,7 @@
       {Phoenix.PubSub, name: GA.PubSub},
       # Start a worker by calling: GA.Worker.start_link(arg)
       # {GA.Worker, arg},
+      {GA.Audit.CheckpointWorker, checkpoint_worker_opts()},
       # Start to serve requests, typically the last entry
       GAWeb.Endpoint
     ]
@@ -31,4 +32,8 @@
     GAWeb.Endpoint.config_change(changed, removed)
     :ok
   end
+
+  defp checkpoint_worker_opts do
+    Application.get_env(:app, GA.Audit.CheckpointWorker, [])
+  end
 end
diff --git a/app/lib/app/audit.ex b/app/lib/app/audit.ex
index a48830aacf4da8275b40f15016dcd16bbc87411f..2c7fbce41d873e721533728bcc1c965168addd3d
--- a/app/lib/app/audit.ex
+++ b/app/lib/app/audit.ex
@@ -7,7 +7,7 @@
   import Ecto.Query, warn: false
 
   alias GA.Accounts
-  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Audit.{Chain, Checkpoint, Log, Verifier}
   alias GA.Repo
 
   @default_limit 50
@@ -114,6 +114,12 @@
 
   def list_checkpoints(_), do: []
 
+  @doc """
+  Verifies chain integrity for a single account.
+  """
+  def verify_chain(account_id) when is_binary(account_id), do: Verifier.verify(account_id)
+  def verify_chain(_), do: {:error, :invalid_id}
+
   defp lock_account(account_id) do
     {key_a, key_b} = account_lock_keys(account_id)
 
diff --git a/app/lib/app/audit/checkpoint_worker.ex b/app/lib/app/audit/checkpoint_worker.ex
new file mode 100644
index 0000000000000000000000000000000000000000..f54908642ad7c854eae895b9605661f5beb0246f
--- /dev/null
+++ b/app/lib/app/audit/checkpoint_worker.ex
@@ -0,0 +1,82 @@
+defmodule GA.Audit.CheckpointWorker do
+  @moduledoc """
+  Periodically creates audit checkpoints for all active accounts.
+  """
+
+  use GenServer
+
+  import Ecto.Query, warn: false
+
+  require Logger
+
+  alias GA.Accounts.Account
+  alias GA.Audit
+  alias GA.Repo
+
+  @default_interval_ms :timer.hours(1)
+
+  def start_link(opts \\ []) do
+    name = Keyword.get(opts, :name, __MODULE__)
+    GenServer.start_link(__MODULE__, opts, name: name)
+  end
+
+  @impl true
+  def init(opts) do
+    interval_ms =
+      opts
+      |> Keyword.get(:interval_ms, configured_interval_ms())
+      |> normalize_interval()
+
+    schedule_tick(interval_ms)
+
+    {:ok, %{interval_ms: interval_ms}}
+  end
+
+  @impl true
+  def handle_info(:create_checkpoints, state) do
+    active_account_ids()
+    |> Enum.each(&create_checkpoint_for_account/1)
+
+    schedule_tick(state.interval_ms)
+    {:noreply, state}
+  end
+
+  defp active_account_ids do
+    from(account in Account,
+      where: account.status == :active,
+      select: account.id
+    )
+    |> Repo.all()
+  end
+
+  defp create_checkpoint_for_account(account_id) do
+    case Audit.create_checkpoint(account_id) do
+      {:ok, _checkpoint} ->
+        Logger.debug("checkpoint_worker created checkpoint for account=#{account_id}")
+
+      {:error, :no_entries} ->
+        Logger.debug("checkpoint_worker skipping account=#{account_id}: no audit entries")
+
+      {:error, reason} ->
+        Logger.error("checkpoint_worker failed for account=#{account_id}: #{inspect(reason)}")
+    end
+  rescue
+    error ->
+      Logger.error(
+        "checkpoint_worker crashed for account=#{account_id}: #{Exception.message(error)}"
+      )
+  end
+
+  defp schedule_tick(interval_ms) do
+    Process.send_after(self(), :create_checkpoints, interval_ms)
+  end
+
+  defp configured_interval_ms do
+    :app
+    |> Application.get_env(__MODULE__, [])
+    |> Keyword.get(:interval_ms, @default_interval_ms)
+  end
+
+  defp normalize_interval(interval_ms) when is_integer(interval_ms) and interval_ms > 0, do: interval_ms
+  defp normalize_interval(_), do: @default_interval_ms
+end
diff --git a/app/lib/app/audit/verifier.ex b/app/lib/app/audit/verifier.ex
new file mode 100644
index 0000000000000000000000000000000000000000..2a4bd77516a2c76ccc9b05de1778dd8781ded096
--- /dev/null
+++ b/app/lib/app/audit/verifier.ex
@@ -0,0 +1,203 @@
+defmodule GA.Audit.Verifier do
+  @moduledoc """
+  Streams and verifies account-scoped audit chains.
+  """
+
+  import Ecto.Query, warn: false
+
+  alias GA.Accounts
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  @batch_size 1000
+
+  @doc """
+  Verifies an account's audit chain and returns a report map.
+  """
+  @spec verify(binary()) :: map() | {:error, :invalid_id | :not_found}
+  def verify(account_id) when is_binary(account_id) do
+    started_at = System.monotonic_time(:millisecond)
+
+    with {:ok, hmac_key} <- Accounts.get_hmac_key(account_id) do
+      checkpoints_by_sequence = load_checkpoints(account_id)
+      report = stream_and_verify(account_id, hmac_key, checkpoints_by_sequence)
+
+      Map.put(report, :duration_ms, elapsed_ms(started_at))
+    end
+  end
+
+  def verify(_), do: {:error, :invalid_id}
+
+  defp stream_and_verify(account_id, hmac_key, checkpoints_by_sequence) do
+    initial =
+      new_accumulator()
+      |> Map.put(:account_id, account_id)
+
+    account_id
+    |> do_verify_batches(hmac_key, checkpoints_by_sequence, initial)
+    |> finalize_report()
+  end
+
+  defp do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, acc) do
+    batch = load_batch(account_id, acc.last_sequence)
+
+    case batch do
+      [] ->
+        acc
+
+      _entries ->
+        next_acc =
+          Enum.reduce(batch, acc, fn entry, state ->
+            verify_entry(entry, hmac_key, checkpoints_by_sequence, state)
+          end)
+
+        do_verify_batches(account_id, hmac_key, checkpoints_by_sequence, next_acc)
+    end
+  end
+
+  defp verify_entry(%Log{} = entry, hmac_key, checkpoints_by_sequence, acc) do
+    acc
+    |> verify_sequence(entry)
+    |> verify_checksum(entry, hmac_key)
+    |> verify_checkpoint(entry, checkpoints_by_sequence)
+    |> advance_state(entry)
+  end
+
+  defp verify_sequence(acc, %Log{sequence_number: found_sequence}) do
+    expected_sequence = acc.expected_sequence
+
+    if found_sequence == expected_sequence do
+      %{acc | expected_sequence: expected_sequence + 1}
+    else
+      missing =
+        if found_sequence > expected_sequence do
+          Enum.to_list(expected_sequence..(found_sequence - 1))
+        else
+          []
+        end
+
+      gap = %{
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      }
+
+      acc
+      |> mark_invalid(%{
+        type: :sequence_gap,
+        expected: expected_sequence,
+        found: found_sequence,
+        missing: missing
+      })
+      |> Map.update!(:sequence_gaps, &[gap | &1])
+      |> Map.put(:expected_sequence, max(found_sequence, expected_sequence) + 1)
+    end
+  end
+
+  defp verify_checksum(acc, %Log{} = entry, hmac_key) do
+    if Chain.verify_checksum(hmac_key, entry, acc.previous_checksum) do
+      acc
+    else
+      mark_invalid(acc, %{
+        type: :checksum_mismatch,
+        sequence_number: entry.sequence_number,
+        stored_checksum: entry.checksum,
+        expected_checksum: expected_checksum(hmac_key, entry, acc.previous_checksum)
+      })
+    end
+  end
+
+  defp verify_checkpoint(acc, %Log{} = entry, checkpoints_by_sequence) do
+    case Map.get(checkpoints_by_sequence, entry.sequence_number) do
+      nil ->
+        acc
+
+      %Checkpoint{} = checkpoint ->
+        valid? = checkpoint.checksum == entry.checksum
+        result = %{sequence_number: entry.sequence_number, valid: valid?}
+        next_acc = Map.update!(acc, :checkpoint_results, &[result | &1])
+
+        if valid? do
+          next_acc
+        else
+          mark_invalid(next_acc, %{
+            type: :checkpoint_mismatch,
+            sequence_number: entry.sequence_number,
+            checkpoint_checksum: checkpoint.checksum,
+            entry_checksum: entry.checksum
+          })
+        end
+    end
+  end
+
+  defp advance_state(acc, %Log{} = entry) do
+    %{
+      acc
+      | previous_checksum: entry.checksum,
+        last_sequence: entry.sequence_number,
+        total_entries: acc.total_entries + 1,
+        verified_entries: acc.verified_entries + 1
+    }
+  end
+
+  defp expected_checksum(hmac_key, entry, previous_checksum) do
+    Chain.compute_checksum(hmac_key, Map.from_struct(entry), previous_checksum)
+  rescue
+    _ -> nil
+  end
+
+  defp load_batch(account_id, after_sequence) do
+    from(log in Log,
+      where: log.account_id == ^account_id and log.sequence_number > ^after_sequence,
+      order_by: [asc: log.sequence_number],
+      limit: @batch_size
+    )
+    |> Repo.all()
+  end
+
+  defp load_checkpoints(account_id) do
+    from(checkpoint in Checkpoint, where: checkpoint.account_id == ^account_id)
+    |> Repo.all()
+    |> Map.new(fn checkpoint -> {checkpoint.sequence_number, checkpoint} end)
+  end
+
+  defp new_accumulator do
+    %{
+      account_id: nil,
+      valid: true,
+      total_entries: 0,
+      verified_entries: 0,
+      expected_sequence: 1,
+      previous_checksum: nil,
+      last_sequence: 0,
+      first_failure: nil,
+      sequence_gaps: [],
+      checkpoint_results: []
+    }
+  end
+
+  defp finalize_report(acc) do
+    %{
+      valid: acc.valid,
+      total_entries: acc.total_entries,
+      verified_entries: acc.verified_entries,
+      first_failure: acc.first_failure,
+      sequence_gaps: Enum.reverse(acc.sequence_gaps),
+      checkpoint_results: Enum.reverse(acc.checkpoint_results),
+      duration_ms: 0
+    }
+  end
+
+  defp mark_invalid(acc, failure) do
+    case acc.first_failure do
+      nil -> %{acc | valid: false, first_failure: failure}
+      _existing -> %{acc | valid: false}
+    end
+  end
+
+  defp elapsed_ms(started_at) do
+    System.monotonic_time(:millisecond)
+    |> Kernel.-(started_at)
+    |> max(0)
+  end
+end
diff --git a/app/test/app/audit/checkpoint_worker_test.exs b/app/test/app/audit/checkpoint_worker_test.exs
new file mode 100644
index 0000000000000000000000000000000000000000..ec9ea5e45a2b2d84cd0e7872a7bf7288d8e3ea11
--- /dev/null
+++ b/app/test/app/audit/checkpoint_worker_test.exs
@@ -0,0 +1,111 @@
+defmodule GA.Audit.CheckpointWorkerTest do
+  use GA.DataCase, async: false
+
+  import ExUnit.CaptureLog
+
+  alias GA.Accounts
+  alias GA.Audit
+  alias GA.Audit.CheckpointWorker
+
+  describe "checkpoint worker ticks" do
+    test "creates checkpoints for all active accounts" do
+      account_a = account_fixture()
+      account_b = account_fixture()
+      add_log(account_a.id, "active-a")
+      add_log(account_b.id, "active-b")
+
+      worker = start_worker()
+      send(worker, :create_checkpoints)
+
+      assert_eventually(fn ->
+        length(Audit.list_checkpoints(account_a.id)) == 1 and
+          length(Audit.list_checkpoints(account_b.id)) == 1
+      end)
+    end
+
+    test "skips suspended accounts" do
+      active_account = account_fixture()
+      suspended_account = account_fixture(%{status: :suspended})
+      add_log(active_account.id, "active")
+      add_log(suspended_account.id, "suspended")
+
+      worker = start_worker()
+      send(worker, :create_checkpoints)
+
+      assert_eventually(fn ->
+        length(Audit.list_checkpoints(active_account.id)) == 1
+      end)
+
+      assert Audit.list_checkpoints(suspended_account.id) == []
+    end
+
+    test "logs no-entry accounts and continues processing" do
+      empty_account = account_fixture()
+      populated_account = account_fixture()
+      add_log(populated_account.id, "populated")
+
+      worker = start_worker()
+
+      log =
+        capture_log([level: :debug], fn ->
+          send(worker, :create_checkpoints)
+
+          assert_eventually(fn ->
+            length(Audit.list_checkpoints(populated_account.id)) == 1
+          end)
+        end)
+
+      assert log =~ "checkpoint_worker skipping"
+      assert log =~ empty_account.id
+      assert Audit.list_checkpoints(empty_account.id) == []
+    end
+  end
+
+  defp start_worker do
+    start_supervised!(
+      {CheckpointWorker,
+       [
+         name: {:global, {:checkpoint_worker_test, System.unique_integer([:positive])}},
+         interval_ms: 60_000
+       ]}
+    )
+  end
+
+  defp account_fixture(attrs \\ %{}) do
+    defaults = %{name: "Worker Account #{System.unique_integer([:positive])}"}
+    {:ok, account} = Accounts.create_account(Map.merge(defaults, attrs))
+    account
+  end
+
+  defp add_log(account_id, resource_id) do
+    attrs = %{
+      user_id: Ecto.UUID.generate(),
+      user_role: "admin",
+      session_id: "session-#{System.unique_integer([:positive])}",
+      action: "read",
+      resource_type: "patient",
+      resource_id: resource_id,
+      timestamp: ~U[2026-03-03 16:30:00Z],
+      source_ip: "127.0.0.1",
+      user_agent: "ExUnit",
+      outcome: "success",
+      failure_reason: nil,
+      phi_accessed: false,
+      metadata: %{"source" => "checkpoint_worker_test"}
+    }
+
+    assert {:ok, _} = Audit.create_log_entry(account_id, attrs)
+  end
+
+  defp assert_eventually(fun, attempts \\ 50)
+  defp assert_eventually(fun, 0), do: assert(fun.())
+
+  defp assert_eventually(fun, attempts) do
+    if fun.() do
+      :ok
+    else
+      Process.sleep(20)
+      assert_eventually(fun, attempts - 1)
+    end
+  end
+end
diff --git a/app/test/app/audit/verifier_test.exs b/app/test/app/audit/verifier_test.exs
new file mode 100644
index 0000000000000000000000000000000000000000..430e2450186c77bb9e816cfa26910a774995b717
--- /dev/null
+++ b/app/test/app/audit/verifier_test.exs
@@ -0,0 +1,189 @@
+defmodule GA.Audit.VerifierTest do
+  use GA.DataCase, async: false
+
+  alias GA.Accounts
+  alias GA.Audit
+  alias GA.Audit.{Chain, Checkpoint, Log}
+  alias GA.Repo
+
+  describe "verify_chain/1" do
+    test "returns a valid report for an intact account chain" do
+      account = account_fixture()
+
+      assert {:ok, _first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "r-1"}))
+      assert {:ok, _second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "r-2"}))
+      assert {:ok, checkpoint} = Audit.create_checkpoint(account.id)
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == true
+      assert report.total_entries == 2
+      assert report.verified_entries == 2
+      assert report.first_failure == nil
+      assert report.sequence_gaps == []
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == checkpoint.sequence_number and &1.valid))
+      assert is_integer(report.duration_ms)
+      assert report.duration_ms >= 0
+    end
+
+    test "detects tampered checksum entries" do
+      account = account_fixture()
+
+      assert {:ok, first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "seed"}))
+
+      insert_raw_log(account.id, %{
+        sequence_number: 2,
+        previous_checksum: first.checksum,
+        checksum: String.duplicate("0", 64),
+        resource_id: "tampered"
+      })
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert report.first_failure.type == :checksum_mismatch
+      assert report.first_failure.sequence_number == 2
+      assert report.first_failure.stored_checksum == String.duplicate("0", 64)
+      refute is_nil(report.first_failure.expected_checksum)
+    end
+
+    test "detects sequence gaps within an account" do
+      account = account_fixture()
+      assert {:ok, _first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "gap-1"}))
+      assert {:ok, second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "gap-2"}))
+      assert {:ok, hmac_key} = Accounts.get_hmac_key(account.id)
+
+      checksum =
+        checksum_for_sequence(
+          account.id,
+          hmac_key,
+          4,
+          second.checksum,
+          %{resource_id: "gap-4"}
+        )
+
+      insert_raw_log(account.id, %{
+        sequence_number: 4,
+        previous_checksum: second.checksum,
+        checksum: checksum,
+        resource_id: "gap-4"
+      })
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert report.first_failure.type == :sequence_gap
+      assert [%{expected: 3, found: 4, missing: [3]}] = report.sequence_gaps
+    end
+
+    test "validates checkpoint anchors and reports invalid anchors" do
+      account = account_fixture()
+      assert {:ok, first} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "cp-1"}))
+      assert {:ok, second} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "cp-2"}))
+
+      insert_checkpoint(account.id, first.sequence_number, first.checksum)
+      insert_checkpoint(account.id, second.sequence_number, String.duplicate("f", 64))
+
+      report = Audit.verify_chain(account.id)
+
+      assert report.valid == false
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == first.sequence_number and &1.valid))
+      assert Enum.any?(report.checkpoint_results, &(&1.sequence_number == second.sequence_number and not &1.valid))
+    end
+
+    test "includes non-negative duration tracking" do
+      account = account_fixture()
+      assert {:ok, _} = Audit.create_log_entry(account.id, valid_attrs(%{resource_id: "duration"}))
+
+      report = Audit.verify_chain(account.id)
+
+      assert is_integer(report.duration_ms)
+      assert report.duration_ms >= 0
+    end
+
+    test "verification remains account-scoped" do
+      account_a = account_fixture()
+      account_b = account_fixture()
+
+      assert {:ok, _} = Audit.create_log_entry(account_a.id, valid_attrs(%{resource_id: "a-1"}))
+      assert {:ok, first_b} = Audit.create_log_entry(account_b.id, valid_attrs(%{resource_id: "b-1"}))
+
+      insert_raw_log(account_b.id, %{
+        sequence_number: 2,
+        previous_checksum: first_b.checksum,
+        checksum: String.duplicate("a", 64),
+        resource_id: "b-2-tampered"
+      })
+
+      report = Audit.verify_chain(account_a.id)
+
+      assert report.valid == true
+      assert report.total_entries == 1
+      assert report.first_failure == nil
+      assert report.sequence_gaps == []
+    end
+  end
+
+  defp account_fixture(attrs \\ %{}) do
+    defaults = %{name: "Verifier Account #{System.unique_integer([:positive])}"}
+    {:ok, account} = Accounts.create_account(Map.merge(defaults, attrs))
+    account
+  end
+
+  defp valid_attrs(overrides \\ %{}) do
+    defaults = %{
+      user_id: Ecto.UUID.generate(),
+      user_role: "admin",
+      session_id: "session-#{System.unique_integer([:positive])}",
+      action: "read",
+      resource_type: "patient",
+      resource_id: Ecto.UUID.generate(),
+      timestamp: ~U[2026-03-03 16:30:00Z],
+      source_ip: "127.0.0.1",
+      user_agent: "ExUnit",
+      outcome: "success",
+      failure_reason: nil,
+      phi_accessed: false,
+      metadata: %{"source" => "verifier_test"}
+    }
+
+    Map.merge(defaults, overrides)
+  end
+
+  defp checksum_for_sequence(account_id, hmac_key, sequence_number, previous_checksum, overrides \\ %{}) do
+    attrs =
+      valid_attrs(overrides)
+      |> Map.put(:account_id, account_id)
+      |> Map.put(:sequence_number, sequence_number)
+
+    Chain.compute_checksum(hmac_key, attrs, previous_checksum)
+  end
+
+  defp insert_raw_log(account_id, attrs) do
+    now = DateTime.utc_now() |> DateTime.truncate(:microsecond)
+    id = Ecto.UUID.generate()
+
+    row =
+      valid_attrs()
+      |> Map.merge(attrs)
+      |> Map.merge(%{
+        id: id,
+        account_id: account_id,
+        inserted_at: now,
+        updated_at: now
+      })
+
+    {1, _} = Repo.insert_all(Log, [row])
+    Repo.get!(Log, id)
+  end
+
+  defp insert_checkpoint(account_id, sequence_number, checksum) do
+    %Checkpoint{}
+    |> Checkpoint.changeset(%{
+      account_id: account_id,
+      sequence_number: sequence_number,
+      checksum: checksum
+    })
+    |> Repo.insert!()
+  end
+end

tokens used
92,777
Takeoff executed for `openspec/changes/chain-verification/` on the current branch (`main`) without creating a new branch.

- Epic found: `td-9db2d6` (`Chain Verification`)
- Work session: `ws-e303` (`Epic: Chain Verification`)
- Tagged/started: 18 child issues
- Skipped: review gate `td-833971` (left `open` as required)

Implementation started and completed in this pass:

- Added verifier module: [app/lib/app/audit/verifier.ex](/Users/tbrewer/projects/goodway/goodaudit/app/lib/app/audit/verifier.ex)
- Added audit context delegation: [app/lib/app/audit.ex](/Users/tbrewer/projects/goodway/goodaudit/app/lib/app/audit.ex)
- Added checkpoint worker: [app/lib/app/audit/checkpoint_worker.ex](/Users/tbrewer/projects/goodway/goodaudit/app/lib/app/audit/checkpoint_worker.ex)
- Wired worker into supervision tree before endpoint: [app/lib/app/application.ex](/Users/tbrewer/projects/goodway/goodaudit/app/lib/app/application.ex)
- Added verifier tests: [app/test/app/audit/verifier_test.exs](/Users/tbrewer/projects/goodway/goodaudit/app/test/app/audit/verifier_test.exs)
- Added checkpoint worker tests: [app/test/app/audit/checkpoint_worker_test.exs](/Users/tbrewer/projects/goodway/goodaudit/app/test/app/audit/checkpoint_worker_test.exs)

`td` status updates done:

- Logged implementation/decision/result notes to `ws-e303`
- Moved all 18 tagged child issues to `in_review`
- Review gate `td-833971` remains `open`

Validation attempt:

- `mix format` failed due environment toolchain mismatch:
  - Elixir requires newer Erlang/OTP, current runtime is OTP 27
- Because of that, tests could not be executed here.
